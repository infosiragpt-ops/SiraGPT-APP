{
  "id": "task_flow_get",
  "name": "Inspect durable task flow",
  "version": "1.0.0",
  "description": "Read one owner-scoped durable flow, including resumable state, wait metadata, linked child tasks, and recent lifecycle events.",
  "capabilities": ["agent:read"],
  "timeoutMs": 5000,
  "params": {
    "type": "object",
    "properties": {
      "flowId": { "type": "string", "minLength": 1, "maxLength": 120, "description": "Flow id returned by task_flow_create or task_flow_list." },
      "eventLimit": { "type": "integer", "minimum": 0, "maximum": 100, "description": "Recent lifecycle events to return. Default 20." }
    },
    "required": ["flowId"],
    "additionalProperties": false
  }
}
