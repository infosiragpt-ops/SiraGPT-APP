ALTER TABLE "saved_searches"
  ADD COLUMN IF NOT EXISTS "kind" TEXT NOT NULL DEFAULT 'general',
  ADD COLUMN IF NOT EXISTS "schedule" TEXT NOT NULL DEFAULT 'manual',
  ADD COLUMN IF NOT EXISTS "active" BOOLEAN NOT NULL DEFAULT true,
  ADD COLUMN IF NOT EXISTS "notifyInApp" BOOLEAN NOT NULL DEFAULT true,
  ADD COLUMN IF NOT EXISTS "lastRunAt" TIMESTAMP(3),
  ADD COLUMN IF NOT EXISTS "nextRunAt" TIMESTAMP(3),
  ADD COLUMN IF NOT EXISTS "lastResultCount" INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "lastNewCount" INTEGER NOT NULL DEFAULT 0,
  ADD COLUMN IF NOT EXISTS "resultIdentities" JSONB,
  ADD COLUMN IF NOT EXISTS "lastError" TEXT,
  ADD COLUMN IF NOT EXISTS "updatedAt" TIMESTAMP(3) NOT NULL DEFAULT CURRENT_TIMESTAMP;

CREATE INDEX IF NOT EXISTS "saved_searches_kind_active_nextRunAt_idx"
  ON "saved_searches"("kind", "active", "nextRunAt");
