# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage
/.test-dist/
/.playwright-cli/
/output/
/playwright-report/
/test-results/
/artifacts/
/uploads/
/tmp-smoke/
/tmp-smoke-tel/
# Codex Agent V2 hybrid "export to disk" mirror (runner bind-mount target)
/.codex-workspaces/
/.next.broken*/
/.next.stale*/
backend/prisma/dev.db
backend/prisma/*.db-journal
# scheduler state (generated at runtime by services/scheduler/scheduler.js)
backend/data/
# generated eval reports (corpus is checked in; only timestamped reports are ignored)
backend/eval-results/

# next.js
/.next/
/out/


# production
/build

# misc
.DS_Store
*.pem
*.jks
*.keystore
android/keystore.properties
android/keystores/

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# MCP config (contains secret tokens — keep local)
.mcp.json

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE
.vscode/*
!.vscode/.gitkeep
.idea/
*.swp
*.swo

# Claude Code agent workspace (local worktrees, personal settings)
.claude/

# Tesseract trained-data (downloaded at install time, see scripts/download-tesseract-data.sh)
*.traineddata

# OS
Thumbs.db

# Logs (runtime log directories only — the bare "logs" pattern used to
# also swallow source dirs like app/admin/logs/)
/logs
backend/logs
*.log

# Runtime data
pids
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# nyc test coverage
.nyc_output

# Dependency directories
node_modules/
jspm_packages/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Microbundle cache
.rpt2_cache/
.rts2_cache_cjs/
.rts2_cache_es/
.rts2_cache_umd/

# Optional REPL history
.node_repl_history

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# parcel-bundler cache (https://parceljs.org/)
.cache
.parcel-cache

# Next.js build output
.next

# Nuxt.js build / generate output
.nuxt
dist

# Storybook build outputs
.out
.storybook-out

# Temporary folders
tmp/
temp/

# Editor directories and files
.vscode/*
!.vscode/.gitkeep
!.vscode/extensions.json
.idea
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# husky 9 keeps bootstrap scripts under .husky/_/ — regenerated by `npm run prepare`
.husky/_/

# Stale build artifacts
.next.broken*/
.next.stale*/

# Test artifacts
tmp-smoke/
tmp-smoke-tel/

# Package manager conflict
pnpm-lock.yaml

# TypeScript build info
tsconfig.tsbuildinfo

# Android build outputs
android/.gradle/
android/app/build/
android/capacitor-cordova-android-plugins/

# iOS build outputs
ios/Pods/
ios/build/
ios/.build/

# Capacitor
capacitor-cordova-ios-plugins/
capacitor-cordova-android-plugins/

# Bundle size baseline / report (regenerated on demand)
bundle-size-report.json
