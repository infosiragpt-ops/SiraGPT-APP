Actúa como ingeniero principal full-stack a cargo de una integración interna profunda en el repositorio de SirAGPT respetando como regla absoluta e inviolable la inmutabilidad total de la interfaz de usuario existente, lo que implica que ningún componente React, ningún botón, ningún ícono, ninguna ruta visible de Next.js App Router, ningún color del sistema de diseño, espaciado, radio, sombra, animación Framer Motion, tipografía, microcopy ni posición de elemento puede alterarse bajo ninguna circunstancia ni siquiera para “mejorar” detalles visuales, ya que el objetivo declarado es que el usuario perciba una experiencia notablemente más profesional, fluida, inteligente, rápida y confiable sin poder señalar un solo cambio visual concreto, viniendo toda la mejora desde la capa interna de orquestación, servicios, adaptadores, memoria y pipelines, comenzando obligatoriamente por crear la rama feat/siragpt-internal-orchestration-upgrade desde main, ejecutar git status y git log -5 --oneline para confirmar partida limpia, generar un snapshot del árbol actual de client/src/components, client/src/pages, client/src/app y archivos de estilos con tree -L 4 -I 'node_modules|dist|.next|.turbo' guardándolo en docs/UI_LOCK_SNAPSHOT.md junto con el hash SHA-256 de cada archivo .tsx, .css y tailwind.config.* listado en docs/UI_LOCK_HASHES.txt como contrato visual congelado verificable por el script scripts/verify-ui-lock.sh que cualquier cambio futuro deberá respetar debiendo arrojar cero diferencias al finalizar; introduce a continuación una capa de abstracción interna llamada server/orchestration que reorganiza el flujo LLM detrás de las rutas Express existentes manteniendo intactos los endpoints públicos, sus contratos JSON, sus nombres de eventos SSE y sus códigos de estado HTTP, integrando un LLM Gateway multi-proveedor exclusivamente de APIs de pago que envuelve OpenRouter (github.com/OpenRouterTeam/openrouter-runner, ~3k★) como proveedor primario y añade fallback automático en cascada hacia Anthropic directa (claude-opus-4-7, claude-sonnet-4-6, claude-haiku-4-5), OpenAI (gpt-4o, gpt-4o-mini), Google AI Studio API de pago (gemini-2.5-pro, gemini-2.5-flash), Groq Cloud (llama-3.3-70b-versatile, deepseek-r1-distill), Cerebras Inference, Mistral La Plateforme y DeepSeek API directa (deepseek-chat, deepseek-reasoner) implementando el patrón circuit breaker con la librería opossum (github.com/nodeshift/opossum, ~2k★), reintentos exponenciales con jitter, detección automática de rate limit por proveedor mediante parsing de headers x-ratelimit-* y retry-after, balanceo por costo-latencia-calidad usando una tabla de scoring configurable en server/orchestration/llm-routing.config.ts, y selección dinámica del modelo óptimo según tipo de tarea detectada (razonamiento profundo y matemáticas → claude-opus-4-7 o deepseek-reasoner, velocidad → gemini-2.5-flash o Groq, multimodal → gemini-2.5-pro o gpt-4o, generación de código → claude-sonnet-4-6, embeddings → Voyage AI con Jina como respaldo), todo configurable vía variables de entorno sin tocar absolutamente nada del frontend; incorpora LangGraph (github.com/langchain-ai/langgraph, ~13k★) como motor de orquestación de agentes en el backend reemplazando cualquier lógica imperativa de chains por grafos de estado tipados con nodos especializados (planner, retriever, tool-executor, critic, synthesizer, finalizer), persistencia de checkpoints en PostgreSQL vía Drizzle ORM creando tabla agent_checkpoints con columnas thread_id, checkpoint_id, parent_checkpoint_id, state jsonb, metadata jsonb, created_at, e índice GIN sobre state, complementado opcionalmente con LangChain.js (github.com/langchain-ai/langchainjs, ~14k★) para abstracciones de prompts y output parsers; integra Mem0 (github.com/mem0ai/mem0, ~32k★) como sistema de memoria episódica y semántica a largo plazo por usuario almacenado en PostgreSQL con pgvector (github.com/pgvector/pgvector, ~14k★) activado mediante migración Drizzle (CREATE EXTENSION IF NOT EXISTS vector) creando tabla user_memories con columnas id uuid, user_id uuid, content text, embedding vector(1024), category text, importance_score real, last_accessed_at, access_count int, created_at, updated_at, índice HNSW para búsqueda vectorial rápida, sin exponer ninguna UI nueva sino enriqueciendo automáticamente el contexto del system prompt en cada turno mediante recuperación top-k de memorias relevantes y consolidación periódica en background job, opcionalmente complementado con Letta (ex-MemGPT, github.com/letta-ai/letta, ~15k★) si se requiere memoria jerárquica tipo sistema operativo; instala LlamaIndex.TS (github.com/run-llama/LlamaIndexTS, ~2k★, con su contraparte Python github.com/run-llama/llama_index de ~40k★) junto a Marker (github.com/VikParuchuri/marker, ~22k★) como parser principal de PDFs académicos con preservación de tablas y fórmulas LaTeX, Docling (github.com/docling-project/docling, ~28k★) de IBM como alternativa configurable para documentos técnicos complejos, y MarkItDown (github.com/microsoft/markitdown, ~60k★) de Microsoft para conversión rápida de DOCX, XLSX y PPTX a Markdown limpio, integrándolos en el pipeline de ingestión para que cuando un usuario suba un archivo a través del botón existente el backend lo procese con calidad superior chunkeándolo de forma semántica con overlap inteligente e indexándolo en pgvector usando embeddings de Voyage AI voyage-3-large (oficialmente recomendado por Anthropic) o Jina AI v3 multilingüe como respaldo, complementado con Unstructured (github.com/Unstructured-IO/unstructured, ~10k★) para casos edge y Surya (github.com/VikParuchuri/surya, ~16k★) cuando se requiera OCR multilingüe en español sobre escaneos; agrega Tavily API como herramienta primaria de búsqueda web con Exa AI como respaldo para búsqueda semántica académica, instalando opcionalmente Firecrawl (github.com/mendableai/firecrawl, ~25k★) en modo self-hosted para scraping profundo de fuentes académicas y SearXNG (github.com/searxng/searxng, ~17k★) como metabuscador privado sin API keys, todos invocables transparentemente por el agente LangGraph cuando detecta consultas que requieren información actual sin añadir ningún toggle visible al usuario; despliega Langfuse (github.com/langfuse/langfuse, ~9k★) en modo Cloud Hobby (50,000 observaciones/mes gratis) con opción de migrar a self-hosted vía Docker Compose, conectándolo mediante los SDK langfuse y @langfuse/langchain para que cada llamada LLM, cada paso de LangGraph, cada recuperación RAG y cada herramienta ejecutada quede trazada con costo en dólares, latencia P50/P95/P99, tokens input/output, modelo usado y user feedback implícito, complementado con Helicone (github.com/Helicone/helicone, ~3k★) como proxy de observabilidad si se desea capa adicional; añade Upstash Redis como caché semántico de respuestas LLM con clave por hash SHA-256 del prompt normalizado + contexto + modelo + temperatura, TTL configurable por tipo de consulta y bypass automático para queries con timestamp o información volátil, reduciendo latencia percibida en 30-60% para consultas repetidas; refuerza el streaming SSE existente con backpressure handling, reconexión automática con replay de eventos perdidos vía Last-Event-ID, heartbeats cada 15 segundos, compresión gzip y manejo robusto de timeouts, conservando exactamente los mismos nombres de eventos y estructura JSON que el frontend actual consume; añade middleware Express en orden estricto compuesto por helmet, CORS estricto con allowlist desde env, rate limiting con Upstash Ratelimit aplicado por IP y por usuario autenticado, validación Zod (github.com/colinhacks/zod, ~37k★) en todos los endpoints con esquemas en server/schemas/, sanitización contra XSS y prompt injection con detección heurística, logging estructurado con pino (github.com/pinojs/pino, ~15k★) enviando a Better Stack o Sentry (github.com/getsentry/sentry-javascript, ~8k★) con sampling configurable; integra Cloudflare R2 vía SDK @aws-sdk/client-s3 apuntando al endpoint R2 para mover el almacenamiento de artifacts pesados (Word, Excel, PowerPoint, PDFs de tesis, anexos académicos) fuera del servidor Express, generando presigned URLs con expiración corta que el frontend ya existente consumirá sin cambios; integra adicionalmente OpenClaw (github.com/openclaw/openclaw, ~295k★, el proyecto open-source de IA de más rápido crecimiento en la historia de GitHub creado por Peter Steinberger) como capa opcional de canales externos de mensajería que permite a usuarios power de SirAGPT recibir respuestas, ejecutar tareas y consultar sus tesis desde WhatsApp, Telegram, Slack, Discord, Signal e iMessage conectándose al mismo backend de orquestación mediante el gateway interno LLM y compartiendo la misma memoria Mem0 y el mismo índice RAG por usuario, sin exponer ninguna ruta nueva en la UI web actual sino dejando OpenClaw como servicio paralelo desplegable opcionalmente vía Docker Compose en infra/openclaw/docker-compose.yml apuntando al endpoint interno de SirAGPT vía API key dedicada, de manera que la web siga viéndose idéntica pero el ecosistema gane multicanalidad profesional silenciosa; opcionalmente incorpora CrewAI (github.com/crewAIInc/crewAI, ~30k★) o AutoGen (github.com/microsoft/autogen, ~38k★) en server/orchestration/multi-agent/ para escenarios específicos donde se requieran equipos de agentes especializados (por ejemplo, redactor + revisor + verificador APA en flujos de tesis); crea suite de tests con Vitest (github.com/vitest-dev/vitest, ~14k★) para unitarios e integración, Playwright (github.com/microsoft/playwright, ~70k★) para E2E sobre flujos críticos y visual regression comparando snapshots pixel-perfect de rutas clave (/, /chat, /chat/[id], /settings, /projects, /projects/[id]) antes y después del cambio debiendo arrojar diff visual cero fallando el build si detecta cualquier alteración; configura GitHub Actions en .github/workflows/ci.yml con jobs paralelos lint (eslint + prettier check), typecheck (tsc --noEmit), test-unit, test-integration (con DB de test vía testcontainers), test-e2e-playwright, visual-regression-check, build, drizzle-migrations-dry-run, secret-scan con gitleaks (github.com/gitleaks/gitleaks, ~19k★), dependency-audit (npm audit --production) y un job final ui-lock-verify que ejecuta scripts/verify-ui-lock.sh fallando si detecta cualquier modificación en archivos de UI, asegurando que el pipeline termine en CI green check verificable con gh run list --limit 1 --json status,conclusion mostrando {"status": "completed", "conclusion": "success"}; documenta toda la arquitectura interna en docs/INTERNAL_ARCHITECTURE.md con diagramas Mermaid del flujo de orquestación multi-LLM, capas de fallback, pipeline RAG, ciclo de vida de memoria Mem0, integración OpenClaw multicanal y trazas Langfuse, más un docs/ENV_VARIABLES.md con cada variable, su propósito, formato esperado y proveedor de origen; finalmente ejecuta npm install, aplica migraciones Drizzle con npm run db:migrate, ejecuta npm run build && npm run start localmente, valida manualmente que cada botón, ruta, animación y flujo de usuario funciona idéntico al estado previo, ejecuta git diff --stat client/ && bash scripts/verify-ui-lock.sh para confirmar cero cambios en frontend con exit code 0, haz commit semántico con mensaje exacto feat(orchestration): integrate LangGraph, Mem0, LlamaIndex, Marker, Docling, Voyage, Tavily, Langfuse, Upstash, R2, OpenClaw multichannel and multi-provider paid LLM gateway preserving UI lock y push a la rama, abre Pull Request titulado [SirAGPT] Internal orchestration upgrade — zero UI changes con descripción detallada del cambio, checklist de verificación visual marcado, listado completo de archivos modificados (todos en server/, db/, docs/, scripts/, .github/, infra/, ninguno en client/), espera la confirmación de los diez jobs verdes del CI antes de mergear a main, y reporta al final el listado completo de variables de entorno nuevas requeridas (ANTHROPIC_API_KEY, OPENROUTER_API_KEY, OPENAI_API_KEY, GOOGLE_AI_API_KEY, GROQ_API_KEY, CEREBRAS_API_KEY, MISTRAL_API_KEY, DEEPSEEK_API_KEY, VOYAGE_API_KEY, JINA_API_KEY, TAVILY_API_KEY, EXA_API_KEY, FIRECRAWL_API_KEY, LANGFUSE_PUBLIC_KEY, LANGFUSE_SECRET_KEY, LANGFUSE_HOST, UPSTASH_REDIS_REST_URL, UPSTASH_REDIS_REST_TOKEN, R2_ACCOUNT_ID, R2_ACCESS_KEY_ID, R2_SECRET_ACCESS_KEY, R2_BUCKET_NAME, R2_ENDPOINT, SENTRY_DSN, OPENCLAW_API_KEY, OPENCLAW_GATEWAY_URL) junto con la captura final del workflow de GitHub Actions en estado green check como prueba definitiva de que la integración profunda de SirAGPT quedó completa con UI intacta verificada por hash, orquestación profesional multi-proveedor de pago activa, memoria persistente por usuario, RAG académico de calidad superior con los mejores parsers de documentos del ecosistema, observabilidad total, caché semántico, almacenamiento externo en R2 y opción de multicanalidad vía OpenClaw lista para activarse analiza el sofware y trabaja de manera autónoma por que necesito mejoras en el sofware sin camabiar nada de la interfaz solo mejoras en el funcionamiento del sofware y el codigo esta en la
  carpeta de escritorio siragpt y el repcotirio que vas a enviar a main es https://github.com/SiraGPT-ORg/siraGPT cuando acabes cada tarea sube a GitHub los cambios de manera profesional y vigila hasta que
  quede en main y en estatus verde cuando este en estatus verde me avisas