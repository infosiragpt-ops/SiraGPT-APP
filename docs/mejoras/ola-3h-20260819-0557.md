# Ola 3H26 — 2026-08-19 00:57 America/Lima (05:57Z)  [ship ~01:22 Lima]

Numero 3H26. Caps 7 sandbox / 8 SSE / 9 queue estaban **sin cambio** en 3H25 (wire Codex). Esta ola ataca esos huecos en el motor vivo `/chat` + `/code`. Frontend untouched. Backend-only. No OpenRouter. DeepSeek Flash/Pro. No down -v.

## Por que

3H25 dejo el interpreter `local`, el SSE sin timeout de drain, y la cola con lease por sesion (no por job) mas un catch path que saltaba cancel/lease. Claude Code/Cowork parity exige stdout en streaming, reap garantizado, heartbeat que no empuja contra backpressure, replay por Last-Event-ID, cancel-all pending y fairness round-robin.

## Live files rewritten (not just new helpers)

- `backend/src/services/doc-agent/sandbox.js` — `runProcess` emite `onChunk` en stdout/stderr; SIGKILL de process group via engine-runtime; force-settle si `close` no llega (reap grace); `exec` (local y docker) reenvia `onChunk`; `destroy` con `guaranteedDestroy`.
- `backend/src/services/sandbox/local-sandbox.js` — `detached` process group, env scrub (sin secretos), `onChunk` streaming, force-settle + settled flag.
- `backend/src/services/agent-runner/tools.js` — `execute_python` / `execute_bash` pasan `onChunk` a `sandbox.exec` (ya no dump post-hoc).
- `backend/src/utils/sse-writer.js` — drain con timeout, orphan hub (abort producer al close), replay Last-Event-ID, heartbeat skip si backpressure, first-byte p50/p95 sin DeepSeek.
- `backend/src/services/agent-gateway/queue.js` — job record por enqueue (lease por job, no overwrite de sesion); `assertJobRunnable` en then Y catch; cancel-all pending + cancel por jobId; `pickFairReady`.
- `backend/src/services/error_codes.js`, `observability/public-stream-error.js`, `observability/health-check.js`.
- `docker-compose.production.override.yml` — bind-mounts 3H26.

## New first-party module

- `backend/src/services/agent-runner/engine-runtime.js` — streaming chunks, kill-tree, force-settle, env scrub, guaranteed destroy, SSE drain timeout / orphan / resume / heartbeat gap / ring, per-job lease, cancel pending, fairness, first-byte snapshot. Rewrite, no vendor copy.

## Proof live imports them

Container: `engine_loop.runtime.sandboxStream/sseDrainTimeout/queueJobLease === true`.
`generate_path` flash/pro `openrouter: false`.
`sandbox_backends.local: true` (interpreter sigue local; gVisor `runsc` es el driver docker del agent-runner).
`chat_run_worker` skipped `dormant_ok: true` (no encender `CHAT_RUN_QUEUE_ENABLED` — cambiaria generate in-process SSE).

## Shipped (metric deltas)

1. Streaming stdout/stderr `onChunk` en `runProcess` vivo (cap 7)
2. tools.js reenvia `onChunk` a `sandbox.exec` (cap 7)
3. Process-group SIGKILL via `killProcessTree` (cap 7)
4. Force-settle si el child no emite `close` tras kill (cap 7)
5. `reapAfterKill` vivo vs zombie (cap 7)
6. `scrubSandboxEnv` no copia API keys (cap 7)
7. `guaranteedDestroy` con timeout (cap 7)
8. local-sandbox detached + stream + scrub (cap 7)
9. docker `exec` y local `exec` reenvian `onChunk` (cap 7)
10. SSE `waitDrainWithTimeout` — no hang eterno (cap 8)
11. Orphan hub: drain frames + abort producer al close (cap 8)
12. `replayFromSeq` Last-Event-ID (cap 8)
13. Heartbeat skip si backpressure/closed (cap 8)
14. Ring `pushRing` drop-oldest `sse_backpressure` (cap 8)
15. first-byte observado en el writer SSE (cap 8/12)
16. Per-job `enqueuedAt` (lease ya no se pisa entre jobs) (cap 9)
17. `jobMayRun` lease TTL (cap 9)
18. Cancel-all pending (no solo el siguiente) (cap 9)
19. Cancel por `jobId` (cap 9)
20. Catch path de la cola usa los mismos guards (cap 9)
21. `pickFairReady` round-robin (cap 9)
22. p50/p95 first-byte scripted sin quemar DeepSeek (cap 12)
23. Codigos ES: sandbox_stream / sandbox_reap / sandbox_cleanup / sse_drain_timeout / sse_heartbeat / queue_fairness (cap 11)
24. Health flags 3H26 `runtime.*`
25. Tests 32/32 3H26
26. 28/28 3H25 siguen verdes
27. DeepSeek lock intacto (`openrouterGenerate: false`)
28. Interpreter declarado `local` (no inventar gVisor en el snippet runner)

Tests: 32/32 3H26, 28/28 3H25.

## Live

backend recreated 1x (profile docker-backend, bind-mounts, no image rebuild, no down -v).
live/ready 200. chat/code 200 local+https. engine_loop healthy. generate_path flash/pro openrouter:false.
ttfbMs count=39 p50/p95 hidratados (scripted/overhead, no Flash cobrado).
chat_run_worker dormant_ok.

## Leftovers (skipped a proposito)

- HMAC secret missing (`SIRAGPT_WEBHOOK_HMAC_SECRET`) — no inventar secreto de webhook.
- `mcp_policy` deny-all (`SIRAGPT_MCP_ALLOWED_HOSTS` vacio).
- `chat_run_worker` dormant (`dormant_ok:true`) — encenderlo cambiaria generate a cola async y romperia SSE in-process. Sin UI change no es seguro.
- `SIRAGPT_REFRESH_TOKEN_ROTATION` unset (no encender sin dual-read).
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto.
- Sandbox interpreter sigue `local` (gVisor `runsc` es el agent-runner docker).
- Live Flash TTFB no se midio con DeepSeek (no quemar creditos). firstByte n del writer arranca en 0 post-recreate; P² ttfbMs viene de Redis hidratado.
