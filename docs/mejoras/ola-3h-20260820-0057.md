# Ola 3H34 — 2026-08-20 00:57 America/Lima (05:57Z)

Numero 3H34. Caps 1–12 tenian huecos reales tras 3H33 (line-numbers, last-N bodies, secret redact, jail, glob ignore, Flash/Pro lock, SSE ring/gap, rate, stop_reason, bg bash). Esta ola ataca ranking pgvector fail-closed, rollback de last-N edits, audit de tokens, generate lock FIFO, cache GET-like, timeout modelo vs tool, strip additionalProperties, jail NFC, escape symlink, SSE comment vs event, refund 0-token, compact por presupuesto restante, herencia de pasos del subagente, file lock cross-process. Frontend untouched. Backend-only. No OpenRouter. DeepSeek Flash/Pro. No down -v.

## Por que

3H33 cerro read con numeros de linea, last-N bodies, redact de secretos, jail, glob ignore, allowlist Flash/Pro, anillo SSE, rate, bg bash. Leftovers de alto valor vs Claude Code/Cowork: retrieve sin ranking si hay hits; checkpoint solo revertia 1 edit; sin audit de tokens; generate lock drop-newest; GET identicos se reejecutaban; timeout de modelo y tool mezclados; extras de schema no se recortaban; jail sin NFC (puntos fullwidth); symlink podia salir del root; comentarios SSE inflaban el anillo; error 0-token podia cobrar; compact ignoraba el presupuesto restante; subagente no partia pasos entre hermanos; lock de path solo in-memory.

## Live files rewritten (not just new helpers)

- `backend/src/services/agent-runner/engine-adapter.js` — 3H33 intacto + 14 helpers 3H34.
- `backend/src/services/agent-runner/loop.js` — rank, compact budget, NFC/symlink, schema strip, GET cache, file lock, 0-token refund, inherit steps.
- `backend/src/services/react-agent.js` + `agentic-chat-stream.js` — strip schema + model TTFB split + refund 0-token en /chat.
- `backend/src/utils/sse-writer.js` — comentarios SSE no entran al anillo de replay.
- `backend/src/services/agent-gateway/queue.js` — fair generate lock FIFO (cap 4) en vez de solo drop-newest.
- `backend/src/services/agent-runner/engine-gateway.js` — strip additionalProperties + token audit.
- `backend/src/services/error_codes.js`, `observability/public-stream-error.js`.
- `docker-compose.production.override.yml` — bind-mount ola-3h34-invariants.test.js.

## Proof live imports them

Container: `engine_loop.adapter.pgvectorRankHits/rollbackLastNEdits/tokenAuditLog/fairGenerateLock/getLikeToolCache/modelVsToolTimeout/additionalPropertiesStrip/nfcPathJail/symlinkEscapeReject/sseCommentVsEvent/zeroTokenRefund/compactTokenBudget/subagentStepInherit/crossProcessFileLock === true`.
`generate_path` flash/pro `openrouter: false`.
`sandbox_backends.local: true` (`usesRunsc: false`).
`chat_run_worker` skipped `dormant_ok: true`.

## Shipped (metric deltas)

1. rankPgvectorHits score*recency + dedupe (cap 4)
2. rollbackLastNFileEdits stack 8 (cap 5)
3. appendTokenAuditLog + sweep TTL (cap 10)
4. acquireFairGenerateLock FIFO cap 4 (cap 9)
5. GET-like tool cache + invalidate on write (cap 2)
6. splitModelVsToolTimeout 45s/12s vs overlay tool (cap 12)
7. stripAdditionalProperties additionalProperties:false (cap 2)
8. nfcPath jail fullwidth dots (cap 6)
9. rejectSymlinkEscape realpath (cap 6)
10. classifySseFrame comment vs event (cap 8)
11. refundZeroTokenError (cap 10)
12. compactUntilTokenBudget (cap 4)
13. inheritSubagentSteps fair siblings (cap 1)
14. acquireCrossProcessFileLock wx+stale (cap 6)
15. Tests 22/22 3H34
16. DeepSeek lock intacto
17. Interpreter `local`
18. chat_run_worker dormant

Tests: 22/22 3H34 + 26/26 3H33 + 37/37 3H32 + 34/34 3H31 (unit, stub model, 0 DeepSeek credits).

## Live

backend recreated 1x (compose.prod + production.override, bind-mounts, no image rebuild, no down -v, no frontend rebuild).
live/ready 200. chat/code 200. engine_loop healthy. generate_path flash/pro openrouter:false.
computer/health 200. Frontend untouched.

## Leftovers (skipped a proposito)

- HMAC secret missing (`SIRAGPT_WEBHOOK_HMAC_SECRET`) — no inventar secreto de webhook.
- `mcp_policy` deny-all para hosts nuevos (`SIRAGPT_MCP_ALLOWED_HOSTS` vacio). Already-connected reuse no abre todo.
- `chat_run_worker` dormant (`dormant_ok:true`) — encenderlo cambiaria generate a cola async y romperia SSE in-process.
- `SIRAGPT_REFRESH_TOKEN_ROTATION` unset (no encender sin dual-read).
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto.
- Sandbox interpreter sigue `local` (gVisor `runsc` es el agent-runner docker driver).
- `create_file` / `delete_file` / `move_file` / `notebook_edit` no existen live.
- Live Flash TTFB no se midio con DeepSeek (no quemar creditos). p50/p95 siguen scripted.
