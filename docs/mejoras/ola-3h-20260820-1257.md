# Ola 3H38 — 2026-08-20 12:57 America/Lima (17:57Z)

Numero 3H38. Caps 1-12 tenian huecos reales tras 3H37 (observation cut, abort siblings, enum, truncate, same-turn cache, DAG, step reminder, pair compact, min-score, write ckpt, binary edit, CRLF, same-volume move, RSS+CPU, scrub env, tmpdir finally, SSE buffer, heartbeat jitter, generate overloaded, completion refund, net taxonomy, skip compact). Esta ola ataca cap paralelo de tools (max 4 + deferred), cap de resultado de subagente 8KiB, repair de required desde el turno previo, shape del tool result, timeout vs presupuesto restante, dead-letter tool+error x3, linea de progreso del plan, compact que conserva los ultimos 3 errores, facts `pin:true`, CAS seq del checkpoint, checksum sha256 post-write, syntax check js/py, reject C0 en paths, create exclusive, tmpfs 64MB, redact `$HOME`, ping SSE si tool >5s, gap Last-Event-ID, starvation bound 15s en cola, audit de creditos en error de tool, taxonomy FS publica, skip retrieve si el turno ya va >2500ms. Frontend untouched. Backend-only. No OpenRouter. DeepSeek Flash/Pro. No down -v. Computer persistente intacto (no se toco agentic-chat-stream.js).

## Por que

3H37 cerro observation cut, abort siblings, enum, truncate, same-turn cache, DAG, step reminder, pair compact, min-score, write ckpt, binary edit, CRLF, same-volume, RSS/CPU, scrub env, tmpdir, SSE buffer, jitter, generate overloaded, completion refund, net taxonomy, skip compact. Leftovers de alto valor vs Claude Code/Cowork: el modelo disparaba >4 tools en paralelo; subagentes devolvián blobs enormes; required faltantes no se rellenaban del turno previo; resultados no-JSON/primitivos pasaban crudos; un tool timeout mayor que el remainingMs seguia lanzandose; el mismo tool+error se reintentaba sin tope; el plan no mostraba paso i/n; compact tiraba mensajes de error; facts criticos sin pin; checkpoint seq no era CAS; write sin verificar sha256; .js/.py invalidos se conservaban; paths con NUL; create pisa archivos; tmpfs sin hint 64MB; /home y /root en stdout; SSE mudo durante tools largos; Last-Event-ID saltaba sin replay; waiters viejos se morian de hambre; error de tool no auditaba tokens; ENOENT crudo; retrieve de memoria con el turno ya tarde.

## Live files rewritten (not just new helpers)

- `backend/src/services/agent-runner/engine-adapter.js` — 3H37 intacto + 22 helpers 3H38.
- `backend/src/services/agent-runner/loop.js` — concurrent cap, subagent cap, repair required, shape, timeout budget, dead-letter, plan progress, compact errors, pin:true, CAS, checksum, syntax, C0 path, exclusive create, credit audit, FS errors, skip memory if busy.
- `backend/src/services/react-agent.js` — maxConcurrentToolsPerTurn en /chat.
- `backend/src/utils/sse-writer.js` — classifySseGap + ssePingOnIdleTool.
- `backend/src/services/agent-gateway/queue.js` — fairQueueStarvationBound (no roba in-flight).
- `backend/src/services/agent-runner/engine-gateway.js` — missing_required, FS, shape, CAS, credit audit.
- `backend/src/services/sandbox/local-sandbox.js` — tmpfs 64MB hint + redact $HOME.
- `backend/src/services/error_codes.js`, `observability/public-stream-error.js`.
- `docker-compose.production.override.yml` — bind-mount ola-3h38-invariants.test.js.
- NO se toco `agentic-chat-stream.js` (persistentComputerTools intacto).

## Proof live imports them

Container: `engine_loop.adapter.* 3H38 flags === true` y `wave: 3H38`.
`generate_path` flash/pro `openrouter: false`.
`sandbox_backends.local: true` (`usesRunsc: false`).
`chat_run_worker` skipped `dormant_ok: true`.

## Shipped (metric deltas)

1. maxConcurrentToolsPerTurn (cap 1)
2. subagentResultSizeCap (cap 1)
3. repairMissingRequiredFromPriorTurn (cap 2)
4. validateToolResultShape (cap 2)
5. toolTimeoutFitsRemainingBudget (cap 2)
6. deadLetterSameToolAfterN (cap 3)
7. injectPlanProgressLine (cap 3)
8. compactPreserveLastErrors (cap 4)
9. pinCriticalFacts (cap 4)
10. checkpointCasSeq (cap 5)
11. checksumVerifyAfterWrite (cap 6)
12. syntaxCheckJsPyAfterWrite (cap 6)
13. rejectControlCharsInPaths (cap 6)
14. createFileExclusive (cap 6)
15. sandboxTmpfsHint (cap 7)
16. redactHomePathsInResults (cap 7)
17. ssePingOnIdleTool (cap 8)
18. classifySseGap (cap 8)
19. fairQueueStarvationBound (cap 9)
20. creditAuditOnToolError (cap 10)
21. classifyFsErrors (cap 11)
22. skipMemoryRetrieveIfBusy (cap 12)
23. Tests 3H38 + 3H31–3H37 green
24. DeepSeek lock intacto
25. Interpreter `local`
26. Health flags 3H37+3H38 (`wave: 3H38`)

## Live

backend recreated 1x (compose.prod + production.override, bind-mounts, no image rebuild, no down -v, no frontend rebuild).
live/ready 200. chat/code 200. engine_loop healthy. generate_path flash/pro openrouter:false.
computer/health 200. persistentComputerTools intact. Frontend untouched.

## Leftovers (skipped a proposito)

- HMAC secret missing (`SIRAGPT_WEBHOOK_HMAC_SECRET`) — no inventar secreto de webhook.
- `mcp_policy` deny-all para hosts nuevos (`SIRAGPT_MCP_ALLOWED_HOSTS` vacio). Already-connected reuse no abre todo.
- `chat_run_worker` dormant (`dormant_ok:true`) — encenderlo cambiaria generate a cola async y romperia SSE in-process.
- `SIRAGPT_REFRESH_TOKEN_ROTATION` unset (no encender sin dual-read).
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto. 3H36 lo fail-closea.
- Sandbox interpreter sigue `local` (gVisor `runsc` es el agent-runner docker driver).
- `create_file` / `delete_file` / `notebook_edit` no existen live.
- Live Flash TTFB no se midio con DeepSeek (no quemar creditos). p50/p95 siguen scripted.
