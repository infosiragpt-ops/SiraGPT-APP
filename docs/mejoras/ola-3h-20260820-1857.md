# Ola 3H40 — 2026-08-20 18:57 America/Lima (23:57Z)

Numero 3H40. Caps 1-12 tenian huecos reales tras 3H39 (call-order parallel results, cancel in-flight, trailing-comma JSON, aliases, nested depth, subagent depth, wall-clock, merge dup users, hash dedupe, stale checksum, hunk context, atomic write, UNC/Windows, no-new-privs, LD_PRELOAD, drop buffered tokens, SSE id monotonic, coalesced call id, settle if gone, json_parse/cancelled, skip dup web_fetch). Esta ola ataca hard cap 32 tools/paso, abort de subagentes anidados (depth>=1) al halt del padre, repair de keys sin comillas, strip de NUL en args, coerce integer (`"3"` ok, `"3.2"` rejected), circuit breaker de dos respuestas vacias, hint de budget cada 5 pasos si remaining<=10, drop de image/pdf mas viejos que las ultimas 2 user turns, skip de facts >30d, rollback si syntaxCheck falla, refuse write por symlink, strip UTF-8 BOM, SIGTERM+SIGKILL a 1500ms, cap stdout 64KiB (helper exportado), pad SSE para proxies, destroy SSE on close, max 2 generate inflight por user, steal lock si heartbeat >45s, never charge 401/403, redact IPv4, EPIPE/ECONNRESET del stream de respuesta → cancelled, glob cap 500, TTFB watchdog 8s (scripted). Frontend untouched. Backend-only. No OpenRouter. DeepSeek Flash/Pro. No down -v. Computer persistente intacto (no se toco agentic-chat-stream.js).

## Por que

3H39 cerro orden paralelo, cancel in-flight, trailing comma, aliases, depth, subagent, wall-clock, merge users, hash dedupe, checksum, hunk, atomic write, UNC, no-new-privs, LD_PRELOAD, drop tokens, SSE id, coalesced id, settle gone, json/abort, skip dup fetch. Leftovers de alto valor vs Claude Code/Cowork: un paso podia lanzar 33+ tools; halt del padre no abortaba children depth>=1; JSON `{path: "a"}` fallaba; args con `\0`; `"3"` no coercía a integer y `"3.2"` pasaba; dos vacios seguian el loop; remaining<=10 no se inyectaba cada 5 pasos; compact dejaba image/pdf viejos; retrieve traia facts de hace meses; syntax fail no restauraba bytes; write por symlink; BOM rompia parsers; sandbox SIGKILL inmediato; stdout cap no era el helper 3H40; CF/nginx bufferizaba SSE idle>10s; close del cliente no destroy del writer; 3er generate del mismo user; lock zombie >45s; 401/403 cobraba; IPv4 en errores publicos; EPIPE del response iba a net_reset; glob/grep sin tope 500; TTFB sin watchdog 8s scripted.

## Live files rewritten (not just new helpers)

- `backend/src/services/agent-runner/engine-adapter.js` — 3H39 intacto + 23 helpers 3H40.
- `backend/src/services/agent-runner/loop.js` — hard cap 32, nested abort, unquoted JSON, NUL, coerce int, empty-model circuit, budget cada 5, stale images, facts 30d, rollback syntax, symlink write, BOM, glob cap, TTFB watchdog.
- `backend/src/services/react-agent.js` — repairUnquotedKeysInToolJson + maxToolsPerTurnHardCap en /chat.
- `backend/src/utils/sse-writer.js` — ssePadForProxyBuffering + destroySseOnClientClose.
- `backend/src/services/sandbox/local-sandbox.js` — sandboxKillAfterGraceMs + stdoutByteCapPerCommand.
- `backend/src/services/agent-gateway/queue.js` — maxPendingGeneratePerUser + stealLockIfHeartbeatExpired.
- `backend/src/services/agent-runner/engine-gateway.js` — unauthorized no-charge, EPIPE cancelled, IPv4 redact, hard cap, TTFB.
- `backend/src/services/error_codes.js`, `observability/public-stream-error.js`.
- `docker-compose.production.override.yml` — bind-mount ola-3h40-invariants.test.js.
- NO se toco `agentic-chat-stream.js` (persistentComputerTools intacto).

## Proof live imports them

Container: `engine_loop.adapter.* 3H40 flags === true` y `wave: 3H40`.
`generate_path` flash/pro `openrouter: false`.
`sandbox_backends.local: true` (`usesRunsc: false`).
`chat_run_worker` skipped `dormant_ok: true`.

## Shipped (metric deltas)

1. maxToolsPerTurnHardCap (cap 1)
2. abortNestedSubagentsOnParentHalt (cap 1)
3. repairUnquotedKeysInToolJson (cap 2)
4. dropNullBytesInToolArgs (cap 2)
5. coerceIntegerFromNumericString (cap 2)
6. circuitBreakerEmptyModelTwice (cap 3)
7. budgetHintEveryFiveSteps (cap 3)
8. compactDropStaleImageBlocks (cap 4)
9. memorySkipFactsOlderThanDays (cap 4)
10. rollbackFileOnSyntaxFail (cap 5)
11. refuseWriteThroughSymlink (cap 6)
12. stripUtf8BomOnRead (cap 6)
13. sandboxKillAfterGraceMs (cap 7)
14. stdoutByteCapPerCommand (cap 7)
15. ssePadForProxyBuffering (cap 8)
16. destroySseOnClientClose (cap 8)
17. maxPendingGeneratePerUser (cap 9)
18. stealLockIfHeartbeatExpired (cap 9)
19. neverChargeOnUnauthorized (cap 10)
20. redactIpv4InPublicErrors (cap 11)
21. classifyEpipeAsCancelled (cap 11)
22. skipGlobIfMatchCap (cap 12)
23. firstTokenWatchdogMs (cap 12)
24. Tests 3H40 + 3H31–3H39 green
25. DeepSeek lock intacto
26. Interpreter `local`
27. Health flags 3H39+3H40 (`wave: 3H40`)

## Live

backend recreated 1x (compose.prod + production.override, bind-mounts, no image rebuild, no down -v, no frontend rebuild).
live/ready 200. chat/code 200. engine_loop healthy. generate_path flash/pro openrouter:false.
computer/health 200. persistentComputerTools intact. Frontend untouched.

## Leftovers (skipped a proposito)

- HMAC secret missing (`SIRAGPT_WEBHOOK_HMAC_SECRET`) — no inventar secreto de webhook.
- `mcp_policy` deny-all para hosts nuevos (`SIRAGPT_MCP_ALLOWED_HOSTS` vacio). Already-connected reuse no abre todo.
- `chat_run_worker` dormant (`dormant_ok:true`) — encenderlo cambiaria generate a cola async y romperia SSE in-process.
- `SIRAGPT_REFRESH_TOKEN_ROTATION` unset (no encender sin dual-read).
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto. 3H36 lo fail-closea.
- Sandbox interpreter sigue `local` (gVisor `runsc` es el agent-runner docker driver).
- `create_file` / `delete_file` / `notebook_edit` no existen live.
- Live Flash TTFB no se midio con DeepSeek (no quemar creditos). p50/p95 siguen scripted.
