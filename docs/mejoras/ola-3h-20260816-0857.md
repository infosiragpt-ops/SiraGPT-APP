# Ola 3h3 — 2026-08-16-0857 ship log (America/Lima)

Backup: `/opt/siragpt/backups/ola-3h3-20260816T0857Z` (override snapshot + edited FE/BE trees)
UI lock: no layout moves, no Configuración avanzada, no composer/nav/office/Proactivo placement. No compose down. No `down -v`. No repo clone. No OpenRouter. No invented keys.
DeepSeek V4 Flash/Pro only. `SIRAGPT_REFRESH_TOKEN_ROTATION` stays unset (BE-013 still skipped).
Ola-200 A–G, 3H-BE-001..023 / 3H-FE-001..008, and 3H2-BE-001..026 / 3H2-FE-001..006 not redone. Numbered 3H3-BE/FE so it does not collide.

## Shipped this run (30 items)

### Backend (recreated `backend` only; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H3-BE-001** remaining SSE helper — `createSSEWriter` stamps `id: N` and honors Last-Event-ID (`options.req`).
- **3H3-BE-002** remaining streams — `attachSseIds` on doc-agent, answer, math, research, research-agent.
- **3H3-BE-003** remaining streams — `attachSseIds` on agent-batch, plan, apps-ai, goals.
- **3H3-BE-004** remaining streams — `attachSseIds` on design, artifact, voice-grok.
- **3H3-BE-005** remaining streams — `attachSseIds` on thesis-engine, marco-teorico, viz.
- **3H3-BE-006** remaining streams — `attachSseIds` on hosting, deployments, opencode.
- **3H3-BE-007** turn-events leftover — `createSSEEvents` writes `id: ${sseSeq}` (not just `event:`/`data:`).
- **3H3-BE-008** rate-limit leftover — shared generate budget adds `/api/doc-agent`, `/api/answer`, `/api/math`, `/api/research`, `/api/plan`, `/api/apps-ai`, `/api/goals`, `/api/agent/batch`, `/api/design`, `/api/artifact`, `/api/voice`.
- **3H3-BE-009** rate-limit leftover — `/api/ai/stop-stream` and `/api/gateway/abort` skip the catch-all API limiter (stop ≠ generate).
- **3H3-BE-010** PII leftover — ai.js PPT/Gmail/Excel/Web/Google logs use `promptLen`, never raw prompt.
- **3H3-BE-011** PII leftover — impersonation end log uses `superAdmin.id`, not email.
- **3H3-BE-012** OAuth leftover — GitHub maps `user_denied`/`consent_required`/`login_required`/`temporarily_unavailable`/`server_error`.
- **3H3-BE-013** OAuth leftover — Gmail maps `user_denied`/`consent_required`/`insufficient_scope`/`invalid_client`.
- **3H3-BE-014** gateway session queue honesty — `queue.snapshot()` reports lanes/writers/aborted counts (no sessionKey leak).
- **3H3-BE-015** gateway status leftover — `/api/gateway/status` includes `queueHonesty` + `abortedRuns`.
- **3H3-BE-016** sandbox timeout leftover — idle timer resets on exec/put/write/read; hard max `SIRAGPT_DOC_SANDBOX_MAX_MS` (default 30 min). Never touches webtops.
- **3H3-BE-017** memory search leftover — per-user episode cap `MAX_ITEMS_PER_USER=200` (TTL prune already shipped).
- **3H3-BE-018** skill persist leftover — list/load/delete no-op and persist throws without userId.
- **3H3-BE-019** health secrets leftover — `stripHealthSecrets` redacts secret-shaped strings (`Bearer`, `sk-`, `DEEPSEEK_API_KEY`).
- **3H3-BE-020** computer-use audit leftover — `/chat-integration`, `/acknowledge-safety`, `/generate-html` now audit (start/stop/resume already shipped).
- **3H3-BE-021** Word/PPT verify leftover — EDIT_TOOLS adds `patch_file`, `delete_slide`, `duplicate_slide`, `set_cell`, `merge_docx`, `update_paragraph`, `insert_table`, `set_chart_title`.
- **3H3-BE-022** PII leftover — structured-logger keys include `id_token` / `client_secret` / `private_key`.
- **3H3-BE-023** payments leftover — `redactPaymentPii` helper (email/customer/receipt keys).
- **3H3-BE-024** credit/idempotency leftover — `attachAbortHoldOnClose` so client abort before first token never captures; wired on generate-document.
- **3H3-BE-025** PII leftover — telegram `redactTelegramPii` (emails stripped).

### Frontend (ONE rebuild only; no UI moves; /chat and /code layout unchanged)
- **3H3-FE-001** public/sw.js NEVER_CACHE_EXACT leftover generate/stop paths (`/api/ai/stop-stream`, `/api/doc-agent`, `/api/answer`, `/api/math`, `/api/research`, `/api/research-agent`, `/api/plan`, `/api/apps-ai`, `/api/goals`, `/api/design`, `/api/artifact`, `/api/voice`, `/api/agent/batch`). SCHEMA_VERSION `sira-v4`.
- **3H3-FE-002** sse-client leftover resume helpers: `answerStreamResumeHeaders`, `mathStreamResumeHeaders`, `researchStreamResumeHeaders`, `goalsStreamResumeHeaders`, `planStreamResumeHeaders`, `artifactStreamResumeHeaders`, `voiceStreamResumeHeaders`, `docAgentStreamResumeHeaders`.
- **3H3-FE-003** client-logs leftover — SENSITIVE_KEY_RE adds `id_token` / `client_secret` / `mailto`.
- **3H3-FE-004** stop-stream leftover — `stopStreamHeaders()` is `Cache-Control: no-store`.
- **3H3-FE-005** cowork leftover — `coworkControlHeaders()` no-store on control/steer mutations.

## Live proof (backend) — 04:12 PT
- `docker compose ... up -d --no-deps --force-recreate backend` — Recreated + Started
- Container created **04:12 PT** (2026-08-16T09:12:30Z), healthy
- `/health/live` 200, `/health/ready` 200
- Full `/health`: deepseek healthy (dummy: false, api.deepseek.com), r2_artifacts healthy (init: ok), generate_path healthy (path: F2, models deepseek-v4-flash / deepseek-v4-pro, openrouter: false), gvisor_runsc healthy (runtime: runsc)
- `stripHealthSecrets` live: no `DEEPSEEK_API_KEY` in health JSON
- GET `/api/gateway/status` → ok, models flash/pro only, `queueHonesty: {lanes:0,writers:0,aborted:0,abortedRuns:0}`, deadLetterCount: 0, no openrouter
- Rotation flag unset (BE-013 skip)
- Existing webtops still up: sira-dpc-cms2hkubp0013oz01mzf0m49e-ceo-office (13h), sira-dpc-testproj-sales (16h)
- POST `/api/gateway/abort` without sessionKey → 400 `{code:bad_request}`
- Invariants inside live backend: **47/47 passed** (`node --test tests/ola-3h3-invariants.test.js tests/ola-3h2-invariants.test.js tests/ola-3h-invariants.test.js`)

## Live proof (frontend) — 04:20 PT
- One rebuild only; previous 3h2 image stayed up until the new image was ready
- Compiled successfully (~5.1 min next build + postbuild-slim); image `siragpt-frontend:latest` Built
- Frontend container created **04:20 PT** (2026-08-16T09:20:25Z), healthy, BUILD_ID `wwKZj5KcjVcrjiUfZlicRSW`
- http://127.0.0.1:3000/ 200
- https://siragpt.com/ 200
- https://siragpt.com/chat 200
- https://siragpt.com/code 200
- https://siragpt.com/api/health/live 200
- SW live SCHEMA_VERSION `sira-v4` + `/api/ai/stop-stream` + `/api/doc-agent` + `/api/answer` (3H3-FE-001)
- Bundle markers: Last-Event-ID (chunk 7806), `REDACTED:email` + `id_token` (chunk 37621, 3H3-FE-003)

## Services
- Recreated: backend, frontend (frontend via the single 3h3 rebuild)
- Untouched: db, redis, caddy, runner
- Never ran `down` or `down -v`

## Honest partials
- **3H3-BE-016** idle reset + hard max apply to sandboxes created after this recreate; existing long-running doc containers keep previous lifetime.
- **3H3-BE-023/025** helpers exported (`redactPaymentPii`, `redactTelegramPii`); no remaining `console.log(...email/prompt...)` sites were found to replace on those two files.
- **3H3-FE-002/004/005** extra resume/no-store helpers are exported; tree-shaking may drop unused exports. SW deny-list (3H3-FE-001) and client-logs keys (3H3-FE-003) are in the live bundle.
- Health overall `degraded` remains the pre-existing non-critical `mcp_policy` (MCP_ALLOWED_HOSTS_REQUIRED). `/health/ready` stays 200.

## Skipped (with reason)
- **BE-013** — do not enable SIRAGPT_REFRESH_TOKEN_ROTATION until a dual-read window exists. Flag remains unset.
- Did not wire BE-081/BE-082 into live handlers.
- Did not rename live webtop containers.

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
