# Ola 3H42 — 2026-08-21 06:57 America/Lima (11:57Z) — SHIPPED

Numero 3H42 **aplicado**. Motor only. UI / OpenRouter / companies / iliagpt DNS no tocados. DeepSeek only (`openrouterGenerate=false`).

## STEP 0 — 402

- StartedAt previo 3H41: `2026-08-21T06:33:46Z`.
- `docker logs --since 06:33Z` con `Insufficient Balance` / `Error from DeepSeek` / `quota_exhausted`: **0**.
- No se llamo live DeepSeek en smoke largo de esta oleada (regla: no reventar creditos). TTFB live **unmeasured**.
- Health 200 (status=degraded, MCP deny-all no critico). generate_path F2 DeepSeek Flash/Pro, openrouter=false.

## Helpers nuevos (27)

ensureUniqueToolCallIdsAcrossResume, clampSchemaIntegerNumberToMinMax, repairMissingClosingBracesWithBudget, refundHoldIfNoTokensUsed, pinLastToolErrorOnCompact, replayLastNSseEventsFromCursor, rejectIdenticalPromptInflightSameSession, refuseWriteOver2MiB, skipEmptyEmbeddingUpsert, neverChargeToolOnlyObservationLoop, enforceTotalTurnWall120s, repairEnumCaseInsensitive, stripZeroWidthCharsFromArgs, clampJsonArrayLength256, retryAfterJitter50to150ms, tombstoneDeletedCheckpoint, stderrByteCapPerCommand, dropToolResultsOlderThan6Steps, rejectToolNameWithWhitespace, keepIdNumericStringsAsStrings, requireSessionEventSeqIncrease, abortSiblingToolsOnParentCancelToken, redactEmailsInLogs, maxHeartbeatsPerMinute, refuseReadThroughSymlink, markPlanStepFailedIfToolErrorTwice, restoreLastSseIdOnResume.

3H41 se conserva (ckpt prune N=8, SSE cursor, single-quote JSON, max tokens 8192, etc.).

Hotfix chat 02:56Z intacto: `persistentComputerTools`, `computerOnly` gated a intent, CDP observe cap 8KiB, MAX_CONTROL_STEPS=25, 402 clasificado ES, wrapExecutors no-throw. No se toco ACS/ai.js.

No se reabrio `SIRAGPT_SANDBOX_NET_ALLOW` (ya leftover fail-closed; no romper computer). glob ignore node_modules/.git y ulimit nproc ya existian.

## Verificacion

- `tests/ola-3h42-invariants.test.js` + `ola-3h41` + `ola-3h40` (wave 3H40|3H41|3H42).
- `/health` wave=3H42, openrouterGenerate=false.
- Frontend no rebuild. Recreate solo `siragpt-backend-1 --no-deps`.

## Fuera de alcance

- UI, iliagpt DNS, companies/data, OpenRouter, compose down -v, clone de repos, push a main: no tocados.
