# Ola 3H17 — 2026-08-17 22:26 America/Lima (2026-08-18T03:26Z)

Motor-only: paridad Claude Code / Cowork en el **engine**, no en la UI.
Numero 3H17 (3H16 = `ola-3h-20260818-0210.md`).

**Frontend skipped / untouched.** Container id `e2d0a0c2c064be49f88d9bb2c22de9598556f1acac8dddd08ea5b8f518fd4755` created `2026-08-18T02:36:50Z` (21:36 Lima) — Luis /chat expand-corner. Zero frontend rebuilds. Did not touch chat Video, duration chips, fal, `ai.js` generate catalog, webtop/computer/noVNC.

This batch is the next layer of the 12-capability rubric: durable checkpoints (Redis + Postgres `agent_checkpoints`) with real message rollback and `resumeFrom`, pgvector/keyword retrieve inside `runAgentLoop`, data-frame SSE heartbeats + Last-Event-ID replay that survives backend recreate, usage persist on cancel, apply_patch unique hunk, stdout/write caps, classified ES errors, latency hydrate across recreate from scripted turns (no Fal, no paid Flash). Did not invent HMAC secret, MCP allowlist, or rotation flag.

## Shipped this run (35 items)

### Backend (recreated `backend` only, three times; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H17-BE-001** (cap 5) `createDurableCheckpointStore` memory+redis+PG. `engine-durability.js`. Verified: invariant + live redis put/get.
- **3H17-BE-002** (cap 5) parent checkpoint chain. Verified: invariant c2.parent=c1.
- **3H17-BE-003** (cap 5) `restoreMessagesFromCheckpoint` splices live array. Verified: invariant.
- **3H17-BE-004** (cap 4) `retrieveMemoryForLoop` injectable store, never throws. Verified: invariant.
- **3H17-BE-005** (cap 4) `memoryHitsToPins` cap 400. Verified: invariant.
- **3H17-BE-006** (cap 3) `planSubtasks` numbered / luego. Verified: invariant.
- **3H17-BE-007** (cap 1) `canRunToolsInParallel` readonly-only. Verified: invariant.
- **3H17-BE-008** (cap 7) `capStdout` 64KiB. Verified: invariant.
- **3H17-BE-009** (cap 6) `assertWriteSize` 512KiB. Verified: invariant.
- **3H17-BE-010** (cap 6) `applyUnifiedDiff` unique hunk / not_unique / miss. Verified: invariant.
- **3H17-BE-011** (cap 8) `createSseHeartbeat` data-frame + guaranteed stop. Verified: invariant.
- **3H17-BE-012** (cap 8) durable SSE Last-Event-ID replay kv. Verified: invariant + live redis replay `["b"]`.
- **3H17-BE-013** (cap 10) persist/load usage on cancel key. Verified: invariant.
- **3H17-BE-014** (cap 12) latency observation p50/p95 helper. Verified: invariant 10/20/30/40/100 → p50=30 p95=100.
- **3H17-BE-015** (cap 7) sandbox cleanup registry runs remaining fns if one throws. Verified: 2/2.
- **3H17-BE-016** (cap 11) classifyLoopError `checkpoint_missing`/`tool_timeout`/`sandbox_killed`. Verified: ES copy, no paths.
- **3H17-BE-017** (cap 6) syntaxValidate json ok / py skip. Verified: invariant.
- **3H17-BE-018** (cap 3) fuzzy fingerprint whitespace-equal args. Verified: cut @3.
- **3H17-BE-019** (cap 4) live loop injects memory pins + `memory_retrieved`. Verified: scripted loop.
- **3H17-BE-020** (cap 5) `resumeFrom` restores messages + `session_resumed`. Verified: scripted loop.
- **3H17-BE-021** (cap 5/11) missing ckpt classified, loop continues. Verified: scripted loop.
- **3H17-BE-022** (cap 5) invalid tool args **restore** messages (3H16 only emitted the event). Verified: `checkpoint_rollback.restored>0`.
- **3H17-BE-023** (cap 5) durable store receives ckpt after tool turn. Verified: scripted loop.
- **3H17-BE-024** (cap 7/10) cancel runs `onCleanup` + `err.usage`. Verified: scripted abort.
- **3H17-BE-025** (cap 1/9) parallel readonly tools keep SSE event order. Verified: [read_file, list_files].
- **3H17-BE-026** (cap 4) compact re-pins PINNED FACTS. Verified: scripted 20-turn compact.
- **3H17-BE-027** (cap 12) five scripted turns produce ttfb+turnEnd. Verified: n=5; live `/health` hydrated `{ttfbMs.count:5, p50:0, p95:0}` / `{turnEndMs.count:5, p50:1, p95:1}` after recreate.
- **3H17-BE-028** (cap 11) error_codes durable taxonomy. Verified: invariant.
- **3H17-BE-029** (cap 11) public-stream-error classifies durable codes. Verified: invariant.
- **3H17-BE-030** (cap 12) `/health` `engine_loop` durableCheckpoint/memoryRetrieve/heartbeat. Verified: live JSON.
- **3H17-BE-031** (cap 4/6) tools `retrieve_memory` + `apply_patch` + file_too_large. Verified: source markers + write cap.
- **3H17-BE-032** (cap 5) loop.js wires resume/rollback/memory/cleanup. Verified: source markers.
- **3H17-BE-033** (cap 8) event-log persistEventFrame hook. Verified: source markers.
- **3H17-BE-034** (cap 8) sse-writer data heartbeat. Verified: source markers.
- **3H17-BE-035** (cap 4/11) failing retrieve_memory does not kill loop. Verified: scripted recover.

## Live proof (backend) — 22:26 Lima / 03:26 UTC
- Recreated backend only (3×: first ship, redis wait-ready, redis connect-once). Frontend id unchanged.
- Container created **22:24 Lima** (2026-08-18T03:24:53Z), healthy, pid 1
- `/health/live` 200, `/health/ready` 200
- Full `/health` status degraded remains pre-existing non-critical `mcp_policy`. No `openrouter.ai`
- `engine_loop` healthy `{ttfbMs:{p50:0,p95:0,count:5}, turnEndMs:{p50:1,p95:1,count:5}, repair:true, compact:true, checkpoint:true, durableCheckpoint:true, memoryRetrieve:true, heartbeat:true}`
- `generate_path` healthy (F2, deepseek-v4-flash / deepseek-v4-pro, openrouter: false)
- `gateway_queue` healthy `{lanes:0,pending:0,maxPending:8,order:fifo}`
- `GET /api/gateway/status` queueHonesty `{order:fifo, maxPending:8}` models flash/pro only, surfaces chat+code
- POST `/api/webhooks/inbound` → 503 `{code:hmac_secret_missing, retryable:false}`
- POST `/api/ai/generate` unauthenticated → 401 `access_token_required`
- POST `/api/ai/stop-stream` unauthenticated → 401 `access_token_required`
- http://127.0.0.1:3000/chat 200, /code 200; https://siragpt.com/chat 200, /code 200
- Invariants inside live backend: **35/35** `ola-3h17` + **26/26** `ola-3h16`
- Redis smoke (new node process, same modules): `{redisResume:"resume", replay:["b"], setOk:"OK"}` — durable resume after process restart
- Gateway 9th enqueue → `session_queue_full` pending=8
- Existing webtops still up. No Fal video jobs.

## Services
- Recreated: backend only
- Untouched: frontend, db, redis, caddy, runner, webtops
- Never ran `down` or `down -v`
- No frontend rebuild (count = 0)

## Honest partials
- Inbound HMAC secret still unset (503 fail-closed). Do not invent a key.
- MCP deny-all until Luis sets `SIRAGPT_MCP_ALLOWED_HOSTS`.
- `chat_run_worker` dormant skipped, `dormant_ok:true`.
- Refresh-token rotation flag unset.
- Latency n=5 is **scripted engine overhead** (mock LLM, first-token ~0ms). Not DeepSeek Flash TTFB. Hydrated via Redis so recreate does not zero the counter. Do not report as Flash p50.
- PG put is fail-open; Redis path verified for post-recreate resume.
- Sandbox code-interpreter backend still `local`.
- RAG embeddings still OpenAI vector space (retrieve in-loop uses injectable/keyword/LTM; generate path unchanged).

## Skipped (with reason)
- **All FE** — Luis owns UI /chat expand-corner (image 02:36 UTC).
- Did not set HMAC / MCP hosts / rotation.
- Did not touch `ai.js` / video.js / fal / duration chips.
- Did not enable AGENT_RUNNER_ASYNC.
- Did not burn DeepSeek or Fal to measure live Flash TTFB.

## Rubric moved
See `docs/mejoras/benchmark-capacidades.md` v3H17. 12/12 capabilities moved (durable ckpt + real rollback, retrieve in-loop, data heartbeat, n=5 latency, apply_patch, new ES codes).

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
Do not put a dummy value in SIRAGPT_MCP_ALLOWED_HOSTS or SIRAGPT_WEBHOOK_HMAC_SECRET.
