# 3H64 — latencia persistida, reconnect SSE e2e, sandbox grace, compact/crc32 (no UI)

Ola engine-only posterior a 3H63. **No inventa nombres overlay** que choquen con exports vivos de 3H59–3H63. Compone **31 helpers #388 vivos** de `engine-adapter.js` y los aplica fail-closed en el hot path (`loop.js`, `ai.js` generate, `sse-writer.js`, `local-sandbox.js`).

## Live #388 names cableados (31)

### A. Latencia bajo carga — ring persistido + GET interno

1. `observeAdapterLatency`
2. `adapterLatencySnapshot`
3. `cancelIfThreeStreamStalls`
4. `enforceTotalTurnWall120s`
5. `remainingWallClockCut`
6. `resetStallCountOnToken`

`persistLatencyRingClosed` (nombre 3H64) llama los dos primeros y escribe JSONL en `/tmp/siragpt-latency` (o `SIRAGPT_LATENCY_DIR`). `GET /api/version/latency` (sin UI) devuelve p50/p95 del snapshot + del ring.

### B. SSE reconnect / client-gone — generate + sse-writer

7. `destroySseOnClientClose`
8. `closeIfClientGone30s`
9. `flushLastSseEventBeforeClose`
10. `endSseWithErrorEventOnAbort`
11. `detectSseGap`

Test Node (sin browser): abre un SSE generate-semantics, guarda Last-Event-ID, aborta, reconecta con inclusive replay y rechaza cursor hacia atrás.

### C. Sandbox leftover — `local-sandbox.js`

12. `sandboxKillAfterGraceMs`
13. `sandboxNetFailClosed`
14. `sandboxNoNewPrivs`
15. `wrapSandboxSpawnWithRssCpu`
16. `tmpCleanupOnCancel`
17. `reapBackgroundBashOnAbort`
18. `pollBackgroundBash`

`setpriv --no-new-privs` solo se aplica si el binario existe. `sandboxNetFailClosed` se llama (allowlist vacía = fail-closed) sin abortar el intérprete local.

### D. Compact + resume leftover — `loop.js`

19. `compactKeepPinnedFactsAndLast3UserTurns`
20. `compactNeverDropSystemPrompt`
21. `compactNeverDropLastAssistantToolCalls`
22. `skipCompactIfUnderBudget`
23. `pinLastToolErrorOnCompact`
24. `replayToolResultsOnResume`
25. `boundStepsOnCheckpointResume`
26. `crc32CheckOnCheckpointLoad`
27. `gzipCheckpointIfOver64KiB`
28. `pruneCheckpointsKeepLastN`
29. `crc32StampOnCheckpointSave`

### E. Errores públicos ES

30. `classifyToolFailure`
31. `sanitizeClientError`

En el path público de generate/loop: español, 0 stacks, 0 `sk-`. `classifyPublicGenerateErrorClosed` no pisa códigos de olas previas (`subtask_no_progress`, `credit_ledger_settle`, ...): solo clasifica fallos de herramienta **sin** `code` de loop.

## Apply layer (nombres 3H64, no colisionan)

`persistLatencyRingClosed` / `readLatencyRingClosed` / `applyTurnWallAndStallsClosed` / `guardSseClientGoneClosed` / `applySandboxSpawnGuardsClosed` / `applyCompactKeepPinsClosed` / `applyCheckpointResumeClosed` / `classifyPublicGenerateErrorClosed` viven en `engine-3h64.js`. El hot path también nombra los helpers #388 vivos.

## Wiring fail-closed

- `loop.js` — wall 120s + 3 stalls; compact keep system/pins/tool_calls; crc32/gzip/prune/bound/replay en checkpoint; latency ring; classify/sanitize en error público
- `ai.js` generate — persist first-token/turn-end; detectSseGap; destroy/close/flush/abort SSE; classify público
- `sse-writer.js` — gap + client-gone + flush + abort error event
- `local-sandbox.js` — RSS/CPU wrap + kill-after-grace + reap/tmp on abort
- `version.js` — `GET /api/version/latency`
- `engine-adapter.js` — snapshot `wave: 3H64`, bind si el nombre está libre (`engine-3h64` en `loadOptionalEngineWave`)

## Tests

`backend/tests/ola-3h64-invariants.test.js` + `engine-hotpath-wire.test.js`. Snapshots 3H59–3H63 aceptan **3H64**.

```bash
cd backend && node --test tests/ola-3h64-invariants.test.js tests/ola-3h63-invariants.test.js tests/engine-hotpath-wire.test.js
```

## Fuera de alcance

Sin UI. Sin OpenRouter. DeepSeek Flash/Pro only. EventSource real en browser sigue pendiente (este ship cubre reconnect Node + EventSource-semantics: Last-Event-ID, replay inclusivo, reject-backwards). p50/p95 ahora salen de un ring persistido (JSONL + snapshot); carga VPS real sigue midiendo el mismo instrumento, no números inventados.
