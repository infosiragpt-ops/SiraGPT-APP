# Ola 3h9 — 2026-08-17-0357 ship log (America/Lima)

Backup: `/opt/siragpt/backups/ola-3h9-20260817T0357Z` (override snapshot + edited FE/BE trees)
UI lock: no layout moves, no Configuración avanzada, no composer/nav/office/Proactivo placement. No compose down. No `down -v`. No repo clone. No OpenRouter. No invented keys.
DeepSeek V4 Flash/Pro only. `SIRAGPT_REFRESH_TOKEN_ROTATION` stays unset (BE-013 still skipped).
Ola-200 A–G, 3H / 3H2 / 3H3 / 3H4 / 3H5 / 3H6 / 3H7 / 3H8 batches not redone. Numbered 3H9-BE/FE so it does not collide.

This batch wires leftover generate candado into live handlers that still constructed OpenAI clients or defaulted to gpt-4o / gpt-4o-mini (se-agents specialists, agent planner/executor, task-tools self_rag, boot-recovery, OpenCode, Hermes), leftover OpenClaw gateway timeouts/abort propagation/user-scoping fail-closed, leftover rate-limit generate/stop paths, leftover PII/health secret shapes, leftover Word credit-cancel, leftover FE generate clamps/no-store. Not helper-only unused functions.

## Shipped this run (26 items)

### Backend (recreated `backend` only; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H9-BE-001** se-agents leftover candado — `requireOpenAI()` now returns native DeepSeek (live specialists no longer 503 on missing `OPENAI_API_KEY`).
- **3H9-BE-002** se-agents leftover generate — `/agent-coder`, `/prompting`, `/mbpp`, `/humaneval`, `/repo-retrieve` use `requireDeepSeek` + `resolveNativeDeepSeekModel` (were `rag.getOpenAI()` + unclamped `req.body.model`).
- **3H9-BE-003** se-agents leftover align — `maybeAlign` LLM is DeepSeek; embeddings stay `rag.embed` (honest partial).
- **3H9-BE-004** agent-core leftover — `DEFAULT_MODEL` is Flash (was `gpt-4o-mini`) for live SE specialists.
- **3H9-BE-005** planner leftover — `DEFAULT_MODEL` is Flash (was `gpt-4o-mini`).
- **3H9-BE-006** executor leftover — `plannerModel` / `executorModel` default Flash (were `gpt-4o-mini` / `gpt-4o`).
- **3H9-BE-007** agent.js leftover — live `/api/agent/run` thinking=medium/high passes `plannerModel: lockedModel`.
- **3H9-BE-008** task-tools leftover — live `self_rag_answer` model is Flash (was `gpt-4o-mini`); used by agent-task + agentic /chat.
- **3H9-BE-009** boot-recovery leftover — resume model is `resolveNativeDeepSeekModel(snapshot.model)` (was `gpt-4o`).
- **3H9-BE-010** intent-clarifier leftover — `DEFAULT_MODEL` is Flash.
- **3H9-BE-011** opencode leftover — default provider/model is `deepseek` / `deepseek-v4-flash` (was `openai` / `gpt-4o-mini`).
- **3H9-BE-012** Hermes leftover rewritten — agent/cli/delegate default model is Flash (was `gpt-4o`).
- **3H9-BE-013** gateway leftover user scoping — HTTP `POST /api/gateway/agent` fail-closed 401 `user_required` without `req.user` (never unscoped generate). Internal `startAgent` still accepts cron `userId`.
- **3H9-BE-014** gateway leftover timeout/abort — per-run `AbortController`, `Promise.race` vs abort, default 180s `SIRAGPT_GATEWAY_TURN_TIMEOUT_MS`, timeout/abort push DLQ; status exposes `turnTimeoutMs`.
- **3H9-BE-015** rate-limit leftover — shared generate budget adds `/api/hermes`, `/api/library`, `/api/compute`, `/api/sandbox`, `/api/document-ai`, `/api/elevenlabs`, `/api/memory`, `/api/se-agents`.
- **3H9-BE-016** rate-limit leftover — stop/cancel skips for sandbox stop/destroy, compute cancel, elevenlabs stop, hermes abort/stop, document-ai abort (stop ≠ generate).
- **3H9-BE-017** PII leftover — structured-logger keys include `bearer` / `jwt` / `csrf` / `sessionid` / `auth_header`.
- **3H9-BE-018** health secrets leftover — `stripHealthSecrets` / `stripSecretShaped` also redact bearer/csrf/jwt/session shapes.
- **3H9-BE-019** credit-race leftover — live `/document-ai/generate-word` calls `attachAbortHoldOnClose` (not only the forbidden-model branch).
- **3H9-BE-020** specialist leftover defaults — live se-agent modules (code-review, orchestrator, align/judge/truth/safety/calibrator, prompting, agent-coder, selective-rag, repo-retriever, debug/test-gen/codegen/static/requirements/log/maintenance) default Flash.
- **3H9-BE-021** ragas leftover — live RAGAS metrics `DEFAULT_MODEL` is Flash.
- **3H9-BE-022** se-agents leftover health — `/health`-style openaiReady is `DEEPSEEK_API_KEY` (not OpenAI).

### Frontend (ONE rebuild only; no UI moves; /chat and /code layout unchanged)
- **3H9-FE-001** generateWordStream leftover — live POST `/document-ai/generate-word` sends `freshGenerateHeaders()` + `clampDeepSeekModel` + provider DeepSeek.
- **3H9-FE-002** client-logs leftover — SENSITIVE_KEY_RE adds `jwt` / `sessionid` / `set-cookie` / `x-csrf` / `auth_header`.
- **3H9-FE-003** generateSpeech leftover — live POST `/ai/generate-speech` sends `freshGenerateHeaders()` (no-store). Not a text-generate model clamp (TTS).
- **3H9-FE-004** generateMusic leftover — live POST `/ai/generate-music` sends `freshGenerateHeaders()` (no-store). Provider unchanged (DeepSeek cannot emit audio).

## Live proof (backend) — 23:16 PT
- `docker compose -f docker-compose.prod.yml -f docker-compose.production.override.yml --env-file .env up -d --no-deps --force-recreate backend` — Recreated + Started
- Container created **23:16 PT** (2026-08-17T04:16:17Z), healthy
- `/health/live` 200, `/health/ready` 200
- Full `/health`: deepseek healthy (dummy: false, api.deepseek.com), r2_artifacts healthy (init: ok), generate_path healthy (path: F2, models deepseek-v4-flash / deepseek-v4-pro, openrouter: false), gvisor_runsc healthy (runtime: runsc)
- `stripHealthSecrets` live: no `DEEPSEEK_API_KEY` literal, no `openrouter.ai`
- GET `/api/gateway/status` → ok, models flash/pro only, `queueHonesty: {lanes:0,writers:0,aborted:0,abortedRuns:0}`, `scoped: {memory:true,skills:true,cron:true}`, deadLetterCount: 0, `turnTimeoutMs: 180000`, no openrouter
- POST `/api/gateway/abort` without sessionKey → `{code:bad_request}`
- POST `/api/gateway/agent` unauthenticated → `{code:user_required}`
- Rotation flag unset (BE-013 skip)
- Existing webtops still up: sira-dpc-cms2hkubp0013oz01mzf0m49e-ceo-office (32h), sira-dpc-testproj-sales (35h)
- Invariants inside live backend: **150/150 passed** (`node --test tests/ola-3h9-invariants.test.js tests/ola-3h8-invariants.test.js tests/ola-3h7-invariants.test.js tests/ola-3h6-invariants.test.js tests/ola-3h5-invariants.test.js tests/ola-3h4-invariants.test.js tests/ola-3h3-invariants.test.js tests/ola-3h2-invariants.test.js tests/ola-3h-invariants.test.js`)

## Live proof (frontend) — 23:25 PT
- One rebuild only; previous 3h8 image stayed up until the new image was ready
- Image `siragpt-frontend:latest` Built (id d50a0306ebcc, 2026-08-17 04:24:38Z)
- Frontend container created **23:25 PT** (2026-08-17T04:25:11Z), healthy, BUILD_ID `_ui3gjUEBvxvVzNmUvqYO`
- http://127.0.0.1:3000/ 200
- https://siragpt.com/ 200
- https://siragpt.com/chat 200
- https://siragpt.com/code 200
- https://siragpt.com/api/health/live 200
- Bundle markers: `sessionid`/`jwt` (chunk 37621, 3H9-FE-002), `generate-word` + `deepseek-v4-flash` (chunk 37621, 3H9-FE-001), `generate-speech` (chunk 37621, 3H9-FE-003), `no-store` + `Last-Event-ID` (chunk 7806)

## Services
- Recreated: backend, frontend (frontend via the single 3h9 rebuild)
- Untouched: db, redis, caddy, runner
- Never ran `down` or `down -v`

## Honest partials
- **Gmail MCP** (`/generate-gmail`) still uses OpenAI Responses API `connector_gmail` — DeepSeek has no MCP connector.
- **generate-chart** still uses OpenAI containers/code-interpreter (not text generate).
- RAG embeddings still use OpenAI (vector space; `maybeAlign` embedder unchanged). Search-preview still OpenAI (search, not generate).
- **apps-ai** remains Cerebras free-tier by design (generated apps, not /chat generate).
- **3H9-FE-003/004** no-store only; speech/music providers unchanged (DeepSeek cannot emit audio).
- **3H9-BE-011** OpenCode is fail-soft when `OPENCODE_SERVER_URL` is unset; default model id is DeepSeek Flash if the engine is enabled.
- **3H9-BE-013** HTTP fail-closed; internal `startAgent` (cron-as-turn) still accepts a job `userId` (unscoped jobs already skipped in 3H5).
- Health overall `degraded` remains the pre-existing non-critical `mcp_policy` (MCP_ALLOWED_HOSTS_REQUIRED). `/health/ready` stays 200.

## Skipped (with reason)
- **BE-013** — do not enable SIRAGPT_REFRESH_TOKEN_ROTATION until a dual-read window exists. Flag remains unset.
- Did not wire BE-081/BE-082 into live handlers (empty prod allowlist would 503; free-shell would break).
- Did not replace OpenAI Gmail MCP connector, code-interpreter charts, search-preview, embeddings, or image/music providers.
- Did not rename live webtop containers.
- Did not touch mobile/stores.

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
