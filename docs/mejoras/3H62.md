# 3H62 — read-after-write, SSE resume persist, ledger (no UI)

Ola engine-only posterior a 3H61. **No inventa nombres overlay** que choquen con exports vivos de 3H59/3H60/3H61. Compone esos helpers y los aplica fail-closed en el hot path: RAW + syntax revert, Last-Event-ID persistido, checkpoint de sesión en disco, pins pgvector, ledger Prisma, latencia p50/p95.

## Helpers únicos (nombres 3H62)

1. `validateWriteThenRevertClosed` — hash post-write + syntax validate; restaura el checkpoint si falla
2. `requireExactDiffMarkersClosed` — `apply_patch` / `edit_file` exigen `---` / `+++`
3. `persistLastEventIdClosed` — cursor Last-Event-ID en disco (nunca hacia atrás)
4. `resumeGenerateFromPersistedIdClosed` — EventSource resume inclusivo; suelta listeners previos
5. `persistSessionCheckpointClosed` — checkpoint de sesión file-backed (sobrevive restart)
6. `hydrateSessionCheckpointClosed` — rehidrata tras restart (no solo Map)
7. `recoverPgvectorPinsClosed` — pins `pin`/`score≥0.85` vía `searchableMemoryHook` / hits
8. `settleLedgerOnErrorClosed` — settle 3H60/3H61 + `failLedgerTransaction` Prisma
9. `observeTurnLatencyClosed` — first-token / end-of-turn p50/p95 (clock inyectable)
10. `classifyEngine3h62Error` — ES, sin stacks / `sk-`
11. `refuseOpenRouterInWave3h62`

`str_replace` uniqueness (`old_str occurs more than once` / `not found`) **no** se reescribe como timeout ni syntax revert.

## Wiring fail-closed

- `loop.js` — RAW/syntax tras el rollback 3H61; diff markers antes de `apply_patch`; pins en compact; persist/hydrate de sesión; ledger en cancel/error; latencia first-token + turn-end
- `ai.js` generate — `resumeGenerateFromPersistedIdClosed` + persist del cursor; ledger + turn-end en `finally`
- `sse-writer.js` — resume persistido + drop de listeners
- `doc-agent/tools.js` — RAW/syntax sobre el checkpoint 3H61
- `engine-gateway.js` — `settleLedgerOnErrorClosed`
- `engine-adapter.js` — snapshot `wave: 3H62`, bind si el nombre está libre

## Tests

`backend/tests/ola-3h62-invariants.test.js` + `engine-hotpath-wire.test.js`.

## Fuera de alcance

Sin UI. Sin OpenRouter. Sin PRs 381–385. DeepSeek Flash/Pro only.
