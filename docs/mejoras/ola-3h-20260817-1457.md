# Ola 3h12 — 2026-08-17-1457 ship log (America/Lima)

Backup: `/opt/siragpt/backups/ola-3h12-20260817T1457Z` (override snapshot + edited BE trees). Never copied `.env`.
UI lock: no layout moves, no Configuración avanzada, no composer/nav/office/Proactivo placement. No compose down. No `down -v`. No repo clone. No OpenRouter. No invented keys.
DeepSeek V4 Flash/Pro only. `SIRAGPT_REFRESH_TOKEN_ROTATION` stays unset (BE-013 still skipped).
Ola-200 A–G, 3H through 3H11 batches not redone. Numbered 3H12-BE so it does not collide.

**Frontend skipped.** 3H11/video frontend image stayed up. This wave is **BACKEND ONLY**. No UI leftovers shipped.

This batch wires leftover reliability/agentic into live handlers: cron-as-turn fail-closed (user required, timeout, DLQ, prompt cap), searchable skill registry + persisted skills (never cross-user, admin clearance hidden), memory-search persist fail-closed + term-frequency scoring, leftover generate/stop rate-limit paths, leftover PII/health/log-redaction keys, pii-mask aadhaar/nino/passport, OAuth missing-state fail-closed, live `/api/webhooks/inbound` HMAC + delivery idempotency, `/api/memory/search` query cap. Not helper-only unused functions. Did not re-close OpenAI generate leaks from 3H11.

## Shipped this run (20 items)

### Backend (recreated `backend` only; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H12-BE-001** cron leftover user required — `dispatchCronJobAsAgentTurn` fail-closed `cron_user_required` without `userId` (never unscoped generate). `backend/src/services/cron-as-turn.js`. Verified: invariant + live file marker `cron_user_required`.
- **3H12-BE-002** cron leftover timeout — per-tick `Promise.race` vs `SIRAGPT_CRON_TURN_TIMEOUT_MS` (default 180s) and abortSession on timeout. Same file. Verified: invariant + live marker `SIRAGPT_CRON_TURN_TIMEOUT_MS`.
- **3H12-BE-003** cron leftover DLQ — timeout/error push to gateway `pushDeadLetter`. Same file. Verified: invariant (mock gateway received 1 letter, `deadLettered: true`).
- **3H12-BE-004** cron leftover prompt cap — ticks >8000 chars fail-closed `prompt_too_long` (no generate). Same file. Verified: invariant.
- **3H12-BE-005** skills-registry leftover search — `searchSkills(query)` searchable registry. `backend/src/services/skills-registry.js`. Verified: invariant (query `invoices` hits demo skill).
- **3H12-BE-006** skills-persist leftover search — `searchPersistedSkills` user-scoped, never cross-user (empty without userId). `backend/src/services/skills-persist.js`. Verified: invariant.
- **3H12-BE-007** memory-search leftover persist fail-closed — `persistEpisode` without userId returns `{ indexed: 0, error: 'user_required' }`. `backend/src/services/memory-search-persist.js`. Verified: invariant.
- **3H12-BE-008** memory-search leftover scoring — term-frequency (`indexOf` loop) instead of boolean include. Same file. Verified: invariant + live marker.
- **3H12-BE-009** rate-limit leftover generate — shared budget adds `/api/github-codex`, `/api/github-search`, `/api/thesis-engine`, `/api/search-brain-universal`, `/api/research-agent`, `/api/research-library`, `/api/cowork-platform`, `/api/publishing`, `/api/voice-grok`, `/api/integrations`, `/api/hooks`, `/api/platform-improvements`. `backend/src/middleware/rate-limit-policy.js`. Verified: invariant `isSharedGenerateAgentPath`.
- **3H12-BE-010** rate-limit leftover stop — skip generate budget for scheduler/attribution/document-collections/project-documents/cowork-ai/deployments/video/voice/github-codex/research-agent/thesis-engine/publishing/integrations/voice-grok/search-brain/agent-batch/hooks abort-stop-cancel. Same file. Verified: invariant `isStopStreamPath` (generate remains false).
- **3H12-BE-011** PII leftover — structured-logger keys include aadhaar / nino / ein / pkce / code_verifier / cookie_secret / encryption_key / signing_secret / bot_token / slack_token / discord_token / telegram_token / huggingface / smtp_pass / mailgun / paypal_secret / jwt_secret / session_secret / vault_token / passphrase / master_key / db_password / redis_password / connection_string / sas_token / account_number / sort_code / ifsc. `backend/src/services/observability/structured-logger.js`. Verified: invariant `PII_KEY_RE.test('aadhaar'|'bot_token'|'jwt_secret')`.
- **3H12-BE-012** health secrets leftover — `stripHealthSecrets` / `stripSecretShaped` redact the same leftover shapes. `backend/src/services/observability/health-check.js`. Verified: invariant + live `/health` has no `DEEPSEEK_API_KEY` literal and no `openrouter.ai`.
- **3H12-BE-013** log-redaction leftover — `SENSITIVE_KEYS` adds mfa/2fa/otp/totp/jwt/csrf/bearer/fal/stripe/encryption_key/signing_secret/bot_token/cookie_secret/pkce/code_verifier/aadhaar/session_secret/vault_token/passphrase/master_key/db_password/redis_password/connection_string/sas_token/account_number/sort_code/ifsc/slack_token/discord_token/telegram_token/smtp_pass/mailgun/paypal_secret/jwt_secret. `backend/src/utils/log-redaction.js`. Verified: invariant `isSensitiveKey`.
- **3H12-BE-014** pii-mask leftover types — aadhaar (Verhoeff), UK NINO, labeled passport. `backend/src/utils/pii-mask.js`. Verified: invariant `mask('passport: A1234567')` / `mask('id AB123456C here')`.
- **3H12-BE-015** oauth-state leftover missing state — `verifyOAuthState` empty/null throws `invalid_state` 401 `missing_state` (never skip CSRF). `backend/src/services/oauth-state.js`. Verified: invariant rejects `''` and `null`.
- **3H12-BE-016** webhooks leftover inbound HMAC fail-closed — live `POST /api/webhooks/inbound` 401/503 `webhook_hmac_failed` without valid HMAC. `backend/src/routes/webhooks.js`. Verified: curl POST without signature → 503 `{code:hmac_secret_missing, retryable:false}` (fail-closed).
- **3H12-BE-017** webhooks leftover delivery idempotency — `rememberInboundDelivery` + `x-siragpt-delivery-id` replay window 15m; replay returns 200 `{replay:true}`. Same file. Verified: invariant + live file marker; unsigned curl never reaches replay (HMAC first).
- **3H12-BE-018** memory route leftover search cap — `GET /api/memory/search` q sliced to 200; empty q returns `{results:[]}`. `backend/src/routes/memory.js`. Verified: invariant + live unauthenticated curl 401 `access_token_required`.
- **3H12-BE-019** skills-registry leftover clearance — `searchSkills` hides admin/system skills from unscoped callers. `backend/src/services/skills-registry.js`. Verified: invariant (wipe/admin-only hidden unless `clearance:'admin'`).
- **3H12-BE-020** cron leftover empty prompt — blank prompt fail-closed `empty_prompt` (no generate). `backend/src/services/cron-as-turn.js`. Verified: invariant.

## Live proof (backend) — 10:12 PT
- `docker compose -f docker-compose.prod.yml -f docker-compose.production.override.yml --env-file .env up -d --no-deps --force-recreate backend` — Recreated + Started
- Container created **10:12 PT** (2026-08-17T15:12:32Z), healthy, id `d9d4e4cd…`
- `/health/live` 200 (pid 1 in this VPS backend; public https://siragpt.com/health/live same pid/uptime), `/health/ready` 200
- Full `/health`: status degraded remains pre-existing non-critical `mcp_policy`; no `DEEPSEEK_API_KEY` literal, no `openrouter.ai`
- GET `/api/gateway/status` → ok, models flash/pro only, `queueHonesty: {lanes:0,writers:0,aborted:0,abortedRuns:0}`, `scoped: {memory:true,skills:true,cron:true}`, deadLetterCount: 0, no openrouter
- POST `/api/webhooks/inbound` without signature → 503 `{error:webhook_hmac_failed, code:hmac_secret_missing, retryable:false}`
- GET `/api/memory/search?q=test` unauthenticated → 401 `access_token_required`
- Rotation flag unset (BE-013 skip)
- Existing webtops still up: sira-dpc-cms2hkubp0013oz01mzf0m49e-ceo-office (43h), sira-dpc-testproj-sales (46h)
- Frontend container unchanged (Up 8 hours, healthy)
- http://127.0.0.1:3000/chat 200, /code 200; https://siragpt.com/chat 200, /code 200
- Invariants inside live backend: **46/46 passed** (`node --test tests/ola-3h11-invariants.test.js tests/ola-3h12-invariants.test.js`); 3H12 suite alone **21/21 passed**

## Services
- Recreated: backend only
- Untouched: frontend, db, redis, caddy, runner, webtops
- Never ran `down` or `down -v`
- No frontend rebuild

## Honest partials
- Inbound webhook HMAC secret (`SIRAGPT_WEBHOOK_HMAC_SECRET`) is unset in this VPS, so live inbound fail-closes 503 `hmac_secret_missing` (correct fail-closed; replay path is code-complete but unsigned requests never reach it).
- RAG embeddings / rerank / graph still OpenAI (vector space). Generate path unchanged from 3H11.
- Gmail MCP / generate-chart / search-preview / computer-use / apps-ai (Cerebras) unchanged honest partials from 3H11.
- Health overall `degraded` remains pre-existing `mcp_policy`. `/health/ready` stays 200.

## Skipped (with reason)
- **All 3H12-FE** — backend-only; UI lock.
- **BE-013** — do not enable SIRAGPT_REFRESH_TOKEN_ROTATION until a dual-read window exists. Flag remains unset.
- Did not wire BE-081/BE-082 into live handlers (empty prod allowlist would 503).
- Did not re-close OpenAI generate leaks already closed in 3H11.
- Did not rename live webtop containers.
- Did not touch mobile/stores.
- Did not start a frontend rebuild.

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
