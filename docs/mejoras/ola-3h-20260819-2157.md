# Ola 3H33 — 2026-08-19 21:57 America/Lima (02:57Z)

Numero 3H33. Caps 1–12 tenian huecos reales tras 3H32 (retry/jitter, consecutive cut, compact names-only, Last-Event-ID, heartbeat 15s, deny-list, abort cascade). Esta ola ataca read con offset/limit y numeros de linea, compact que conserva last-N cuerpos, redact de secretos en tool results, refuse binario, clamp base64, glob ignore, path jail, allowlist Flash/Pro, anillo SSE acotado + sse_gap, spoof de rol, rate limit de generate, tope de args, max tool_calls/mensaje, taxonomia stop_reason, web_fetch content-type, bash en background + reap, SIRAGPT.md una vez, pin TTL sweep, write_noop por hash, todos canonicos, pre-tool hook, persist parcial en abort. Frontend untouched. Backend-only. No OpenRouter. DeepSeek Flash/Pro. No down -v.

## Por que

3H32 cerro backoff+jitter, consecutive cut, empty retry, deny dangerous, SSE :ping 15s, sandbox env/stdout. Leftovers de alto valor vs Claude Code/Cowork: `read_file` sin numeros de linea ni ventana; compact tiraba tambien los last-N cuerpos; secretos podian volver al modelo en el resultado (no solo en el error); glob no ignoraba node_modules/.git; sin jail de workspace; generate no rechazaba un model id ajeno; el anillo SSE crecia; Last-Event-ID no detectaba hueco; un user spoofeaba `assistant:`; sin rate limit de generate por sesion; 12 tool_calls en un turno; finish_reason length no se clasificaba; web_fetch tragaba PDF; bash no tenia handle background; pins expirados no se barrían; write identico no era noop.

## Live files rewritten (not just new helpers)

- `backend/src/services/agent-runner/engine-adapter.js` — 3H32 intacto + 26 helpers 3H33.
- `backend/src/services/agent-runner/loop.js` — keep last-N bodies, line-numbers, jail, redact, storm cap, stop_reason, rate, model lock, pre-hook, partial abort.
- `backend/src/services/react-agent.js` + `agentic-chat-stream.js` — same wire on /chat (spoof, model lock, rate, heartbeat abort, partial).
- `backend/src/services/codex/agent-loop.js` — glob ignore, read line-numbers, todos, background reap.
- `backend/src/services/sandbox/local-sandbox.js` — register background bash + reap on abort.
- `backend/src/utils/sse-writer.js` — bound ring 64 + detectSseGap + heartbeat respeta abort.
- `backend/src/services/agent-gateway/queue.js` — sessionGenerateRateLimit.
- `backend/src/services/agent-runner/engine-gateway.js` — pre-tool hook before run.
- `backend/src/services/agent-runner/index.js` — Flash/Pro model lock.
- `backend/src/services/error_codes.js`, `observability/public-stream-error.js`.
- `docker-compose.production.override.yml` — bind-mount ola-3h33-invariants.test.js.

## Proof live imports them

Container: `engine_loop.adapter.readLineNumbers/compactKeepLastNBodies/secretRedactToolResult/workspacePathJail/deepseekModelAllow/sseRingBound/backgroundBash === true`.
`generate_path` flash/pro `openrouter: false`.
`sandbox_backends.local: true` (`usesRunsc: false`).
`chat_run_worker` skipped `dormant_ok: true`.

## Shipped (metric deltas)

1. formatReadWithLineNumbers + sliceReadWindow (cap 6)
2. compactKeepLastNBodies last-N intactos (cap 4)
3. redactSecretsInToolResult (cap 11)
4. refuseBinaryRead (cap 6)
5. clampBase64InToolResult (cap 2/4)
6. filterGlobHits node_modules/.git (cap 6)
7. workspacePathJail (cap 6/11)
8. allowDeepSeekGenerateModel (cap 11)
9. boundSseRing 64 (cap 8)
10. detectSseGap (cap 5/8)
11. guardUserRoleSpoof (cap 11)
12. sessionGenerateRateLimit 30/min (cap 9)
13. capToolArgBytes 32KB (cap 2)
14. maxToolCallsPerMessage 8 (cap 2/3)
15. classifyStopReason length/stop/tools (cap 1/12)
16. webFetchGuard type+size (cap 7)
17. start/poll/reap background bash (cap 1/7)
18. injectProjectInstructions once (cap 4)
19. expireAndSweepPins (cap 4)
20. skipUnchangedWrite hash (cap 6)
21. canonicalizeTodoList (cap 3)
22. runPreToolHook (cap 2)
23. snapshotPartialOnAbort (cap 5/10)
24. clampToolResultWithHash (cap 2/4)
25. heartbeat abort signal (cap 8)
26. abortCascade backgroundReap (cap 1)
27. Tests 26/26 3H33
28. DeepSeek lock intacto
29. Interpreter `local`
30. chat_run_worker dormant

Tests: 26/26 3H33 + 37/37 3H32 + 34/34 3H31 (unit, stub model, 0 DeepSeek credits).

## Live

backend recreated 1x (compose.prod + production.override, bind-mounts, no image rebuild, no down -v, no frontend rebuild).
live/ready 200. chat/code 200. engine_loop healthy. generate_path flash/pro openrouter:false.
computer/health 200. Frontend untouched.

## Leftovers (skipped a proposito)

- HMAC secret missing (`SIRAGPT_WEBHOOK_HMAC_SECRET`) — no inventar secreto de webhook.
- `mcp_policy` deny-all para hosts nuevos (`SIRAGPT_MCP_ALLOWED_HOSTS` vacio). Already-connected reuse no abre todo.
- `chat_run_worker` dormant (`dormant_ok:true`) — encenderlo cambiaria generate a cola async y romperia SSE in-process.
- `SIRAGPT_REFRESH_TOKEN_ROTATION` unset (no encender sin dual-read).
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto.
- Sandbox interpreter sigue `local` (gVisor `runsc` es el agent-runner docker driver).
- `create_file` / `delete_file` / `move_file` / `notebook_edit` no existen live.
- Live Flash TTFB no se midio con DeepSeek (no quemar creditos). p50/p95 siguen scripted.
