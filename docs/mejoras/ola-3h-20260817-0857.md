# Ola 3h11 — 2026-08-17-0857 ship log (America/Lima)

Backup: `/opt/siragpt/backups/ola-3h11-20260817T0857Z` (override snapshot + edited BE trees). Never copied `.env`.
UI lock: no layout moves, no Configuración avanzada, no composer/nav/office/Proactivo placement. No Video composer / duration chips / fal client files. No compose down. No `down -v`. No repo clone. No OpenRouter. No invented keys.
DeepSeek V4 Flash/Pro only. `SIRAGPT_REFRESH_TOKEN_ROTATION` stays unset (BE-013 still skipped).
Ola-200 A–G, 3H through 3H10 batches not redone. Numbered 3H11-BE so it does not collide.

**Frontend skipped.** Video/3H9/3H10 frontend image stayed up (`68608171301e…`, created 2026-08-17T06:47:17Z). This wave is **BACKEND ONLY**. No FE leftovers shipped. Did not touch chat Video composer, duration chips, or fal client files. Did not overwrite `FAL_*` keys (`.env` mtime still 05:53Z).

This batch wires leftover generate candado into live `/api/rag` handlers that still constructed OpenAI clients / 503 on missing `OPENAI_API_KEY` and defaulted to gpt-4o-mini (query-transform, chain-of-note, route, propositions, compress-abstractive, ares, raptor-build, genread, rewrite, self-rag-engine/critic, citation, factscore, gear, ingest-triples), leftover RAG module defaults, leftover se-agents remaining benchmarks, leftover gateway skills session_spawn/summarize, leftover rate-limit generate/stop paths, leftover PII/health secret shapes. Embeddings/rerank/graph stay `rag.getOpenAI()` (honest partial). Not helper-only unused functions (model-router still unwired — skipped).

## Shipped this run (25 items)

### Backend (recreated `backend` only; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H11-BE-001** rag leftover candado — live `/api/rag` generate handlers use `createNativeDeepSeekClient` + `resolveNativeDeepSeekModel` (were `rag.getOpenAI()` + 503 `OPENAI_API_KEY not configured`). Embeddings/rerank/graph stay OpenAI.
- **3H11-BE-002** query-transforms leftover — live default model is Flash (`/api/rag/query-transform`).
- **3H11-BE-003** rewrite-retrieve-read leftover — `DEFAULT_MODEL` is Flash (live `/api/rag/rewrite`).
- **3H11-BE-004** generate-then-read leftover — `DEFAULT_MODEL` is Flash (live `/api/rag/genread`).
- **3H11-BE-005** abstractive-compressor leftover — `DEFAULT_MODEL` is Flash (live `/api/rag/compress-abstractive`).
- **3H11-BE-006** raptor-tree leftover — default model is Flash (live `/api/rag/raptor-build`; embed stays `rag.embed`).
- **3H11-BE-007** factscore leftover — default model is Flash (live `/api/rag/factscore`).
- **3H11-BE-008** ares-eval leftover — `DEFAULT_MODEL` is Flash (live `/api/rag/ares`).
- **3H11-BE-009** metadata-router leftover — default model is Flash (live `/api/rag/route`).
- **3H11-BE-010** proposition-indexer leftover — default model is Flash (live `/api/rag/propositions`).
- **3H11-BE-011** citation-metrics leftover — default model is Flash (live `/api/rag/citation-*`).
- **3H11-BE-012** self-rag-engine leftover — default model is Flash (live `/api/rag/self-rag-engine` + task-tools).
- **3H11-BE-013** self-rag-critic leftover — default model is Flash (live `/api/rag/self-rag-critic`).
- **3H11-BE-014** iterative-retgen leftover — default model is Flash (wired from `/api/rag`).
- **3H11-BE-015** advanced-patterns leftover — default model is Flash (wired from `/api/rag`).
- **3H11-BE-016** context-curation leftover — `DEFAULT_NOTE_MODEL` is Flash (live `/api/rag/chain-of-note`).
- **3H11-BE-017** nli-faithfulness leftover — default LLM is Flash (RAG faithfulness generate).
- **3H11-BE-018** gear-agent leftover — live `/api/rag/gear` + agent-tools GEAR loop default model is Flash.
- **3H11-BE-019** se-agents leftover remaining benchmarks — closed-domain-hallucination / bias-eval / real-toxicity / alignment-tax / truthful-qa `DEFAULT_MODEL` is Flash.
- **3H11-BE-020** session_spawn leftover — live gateway skill default model is Flash (was `gpt-4o`).
- **3H11-BE-021** summarize leftover — live gateway skill generate default is Flash (was `gpt-4o-mini`).
- **3H11-BE-022** rate-limit leftover generate — shared budget adds `/api/chats`, `/api/social-posts`, `/api/dept-computer`, `/api/skills`, `/api/document-collections`, `/api/project-documents`, `/api/cowork-ai`, `/api/attribution`, `/api/scheduler`.
- **3H11-BE-023** rate-limit leftover stop — skip generate budget for social-posts cancel, se-agents/memory/research/orchestration/builder/design/gpts/figma/rag/search/math/viz/artifact/intent/library/enterprise/free-ia/paraphrase/images/agent-run/appshots/files/projects/marco-teorico/gmail/telegram/x-search/context-intelligence/scientific-search/dept-computer/skills abort-stop.
- **3H11-BE-024** PII leftover — structured-logger keys include `privatekey` / `apikey` / `setcookie` / `mfa` / `2fa` / recovery+backup codes / oauth token-secret / stripe-secret / fal-key / aws-secret / r2-access / proxy-authorization.
- **3H11-BE-025** health secrets leftover — `stripHealthSecrets` / `stripSecretShaped` also redact privatekey/apikey/setcookie/mfa/2fa/oauth/fal-key/aws-secret/r2-access/proxy-authorization.

## Live proof (backend) — 04:19 PT
- `docker compose -f docker-compose.prod.yml -f docker-compose.production.override.yml --env-file .env up -d --no-deps --force-recreate backend` — Recreated + Started
- Container created **04:19 PT** (2026-08-17T09:19:21Z), healthy, id `1b6db316…`
- `/health/live` 200 (pid 1 in this VPS backend; public https://siragpt.com/health/live same pid/uptime), `/health/ready` 200
- Full `/health`: deepseek healthy (dummy: false, api.deepseek.com), r2_artifacts healthy (init: ok), generate_path healthy (path: F2, models deepseek-v4-flash / deepseek-v4-pro, openrouter: false), gvisor_runsc healthy (runtime: runsc)
- `stripHealthSecrets` live: no `DEEPSEEK_API_KEY` literal, no `openrouter.ai`
- GET `/api/gateway/status` → ok, models flash/pro only, `queueHonesty: {lanes:0,writers:0,aborted:0,abortedRuns:0}`, `scoped: {memory:true,skills:true,cron:true}`, deadLetterCount: 0, `turnTimeoutMs: 180000`, no openrouter
- POST `/api/gateway/abort` without sessionKey → `{code:bad_request}`
- POST `/api/gateway/agent` unauthenticated → `{code:user_required}`
- POST `/api/rag/query-transform` unauthenticated → 401
- Rotation flag unset (BE-013 skip)
- Existing webtops still up: sira-dpc-cms2hkubp0013oz01mzf0m49e-ceo-office (37h), sira-dpc-testproj-sales (40h)
- Frontend container id unchanged (`68608171301e…` created 2026-08-17T06:47:17Z)
- `.env` mtime still 2026-08-17 05:53Z; FAL_* names present, not rewritten
- http://127.0.0.1:3000/chat 200, /code 200; https://siragpt.com/chat 200, /code 200
- Live code: rag.js has `requireDeepSeek` / no `OPENAI_API_KEY not configured`; self-rag-engine / gear-agent / session_spawn / truthful-qa default Flash; rate-limit chats/skills generate true, social cancel skip true, generate skip false
- Invariants inside live backend: **198/198 passed** (`node --test tests/ola-3h11-invariants.test.js tests/ola-3h10-invariants.test.js tests/ola-3h9-invariants.test.js tests/ola-3h8-invariants.test.js tests/ola-3h7-invariants.test.js tests/ola-3h6-invariants.test.js tests/ola-3h5-invariants.test.js tests/ola-3h4-invariants.test.js tests/ola-3h3-invariants.test.js tests/ola-3h2-invariants.test.js tests/ola-3h-invariants.test.js`)

## Services
- Recreated: backend only
- Untouched: frontend, db, redis, caddy, runner, webtops
- Never ran `down` or `down -v`
- No frontend rebuild

## Honest partials
- **RAG embeddings / rerank / graph** still use `rag.getOpenAI()` (vector space). Generate on `/api/rag/*` is DeepSeek.
- **Gmail MCP** (`/generate-gmail`) still uses OpenAI Responses API `connector_gmail` — DeepSeek has no MCP connector.
- **generate-chart** still uses OpenAI containers/code-interpreter (not text generate).
- Search-preview still OpenAI (search, not generate).
- **apps-ai** remains Cerebras free-tier by design (generated apps, not /chat generate).
- **computer-use** remains OpenAI CUA (not text generate).
- Health overall `degraded` remains the pre-existing non-critical `mcp_policy` (MCP_ALLOWED_HOSTS_REQUIRED). `/health/ready` stays 200.
- Provider inventory still reports `openrouter: true` because `OPENROUTER_API_KEY` exists in env; generate_path `openrouter: false` (not used for text generate).
- model-router.js still unwired — skipped (unused helper).

## Skipped (with reason)
- **All 3H11-FE** — frontend recently rebuilt; backend-only per conflict rules.
- **BE-013** — do not enable SIRAGPT_REFRESH_TOKEN_ROTATION until a dual-read window exists. Flag remains unset.
- Did not wire BE-081/BE-082 into live handlers (empty prod allowlist would 503; free-shell would break).
- Did not replace OpenAI Gmail MCP connector, code-interpreter charts, search-preview, embeddings, computer-use, or image/music providers.
- Did not rename live webtop containers.
- Did not touch mobile/stores.
- Did not overwrite `/opt/siragpt/.env` FAL_* keys.
- Did not start a frontend rebuild.

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
