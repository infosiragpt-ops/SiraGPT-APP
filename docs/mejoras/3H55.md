# Wave 3H55 — agent-loop robustness (engine-only)

Engine helpers that close holes after 3H46 without touching chat/code layout,
buttons, mobile chrome, or admin UI. DeepSeek V4 Flash/Pro only — no OpenRouter
generate client. Continuity mounts from PR #380 (Activos passthrough,
`tryDocEngineAfterSelection`, SDIE, ACS, DeepSeek lock) are not modified.

Module: `backend/src/services/agent-runner/engine-3h55.js` (re-exported from
`engine-adapter.js`). Wired into `loop.js`, `sse-writer.js`,
`agent-gateway/queue.js`, `engine-gateway.js`, and `local-sandbox.js`.

## Helpers (39)

### Agent loop completion
1. `completeLoopOnlyAfterToolResultsSettle` — refuse final while tool_call ids lack results
2. `retryIfStopReasonLength` — truncated `finish_reason=length` retries once
3. `cutLoopIfRepairDidNotChangeArgs` — stop infinite schema-repair loops

### Tool-call schema + retry/backoff + repair
4. `rejectNullToolArguments` — drop `arguments: null`
5. `repairPartialToolCallName` — strip streamed leftovers (`read_file(`)
6. `backoffOn408RequestTimeout` — retry 408 with exponential backoff
7. `refuseUnknownToolAgainstManifest` — name must be in the turn manifest
8. `repairJsonUnquotedKeysOnce` — `{foo:1}` → `{"foo":1}` once

### Plan / subagent budget + infinite-loop cut
9. `cutPlanIfStepsExceedSessionBudget` — plan steps vs remaining session budget
10. `detectToolPingPongABAB` — cut A-B-A-B tool oscillation
11. `refuseSubagentDepthOver2` — no nested subagents beyond depth 2
12. `inheritSubagentWallClockMs` — child wall clock ≤ parent remainder

### Long-context compact / prune / pin + pgvector
13. `compactPinSelectedMessageIds` — keep caller-pinned message ids
14. `dropImagesBeforeTextWhenOverBudget` — drop image blocks before text
15. `rejectPgvectorDimMismatch` — refuse retrieve when embedding dims differ
16. `dedupeMemoryHitsByContentHash` — SHA-256 content dedupe

### Session checkpoints + real rollback
17. `refuseRollbackOnCrcMismatch` — no rollback if CRC32 does not match
18. `rollbackRestoreSizeAndHash` — after rollback, size + hash must match before

### Exact diff file edits
19. `validateUnifiedDiffHunkHeaders` — require `@@ -a,b +c,d @@`
20. `syntaxValidateJsOrJsonAfterWrite` — JSON.parse / `vm.Script` compile-only
21. `revertWriteOnSyntaxFail` — apply previous bytes when syntax fails
22. `readAfterWriteByteEqual` — byte-identical expected vs actual
23. `refuseOverlappingDiffHunks` — overlapping old-line ranges rejected

### Sandbox stdout/stderr + limits + cleanup
24. `splitUtf8SafeStreamChunk` — never emit a split UTF-8 codepoint
25. `capConcurrentSandboxPerSession` — default 2 children / session
26. `cleanupTmpEvenIfSpawnNeverStarted` — reap tmp dirs on spawn throw

### SSE heartbeats + resume + cancel
27. `heartbeatIncludesLastEventId` — `: hb id=N`
28. `resumeSkipIdsLteLastEventId` — replay only events after Last-Event-ID
29. `rejectHeartbeatFromOtherSession` — ignore hb from a foreign session
30. `cancelDrainThenClose` — drain then close on abort

### Per-session queue + strict event order
31. `rejectOutOfOrderEnqueueSeq` — next seq must be > last
32. `requireToolResultFollowsToolCall` — drop orphan tool_result
33. `dropEventsFromSupersededWriter` — discard events from an old writer

### Exact credit / token accounting
34. `chargeOnlyBilledTokensOnError` — never charge the estimate
35. `refundHoldRemainderIfUnderReserved` — refund reserved − used
36. `neverDoubleChargeCachedPromptTokens` — subtract already-charged cache
37. `recordCancelPartialUsage` — audit partial tokens on cancel

### Classified errors + latency hints
38. `classifyEpipeAsClientGone` / `neverRetry422Unprocessable` / `actionableErrorHint`
39. `ttfbHintWhenOverP95` — first-token slower than p95

## Tests

`backend/tests/ola-3h55-invariants.test.js` — helper contracts, live source
wiring, error taxonomy, public-stream Spanish codes, DeepSeek lock.
