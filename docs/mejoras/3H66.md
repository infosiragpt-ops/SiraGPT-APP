# 3H66 — tool JSON coerce, path jail, memory retrieve, empty/caps, read/bash, SSE lock (no UI)

Ola engine-only posterior a 3H65. **No inventa nombres overlay** que choquen con exports vivos de 3H59–3H65. Compone **36 helpers #388 vivos** de `engine-adapter.js` y los aplica fail-closed en el hot path (`loop.js`, `ai.js` generate, `sse-writer.js`, `engine-durability.js`, sandbox session + local).

## Live #388 names cableados (36)

### A. Tool JSON coerce leftover

1. `coerceTrueFalseStringsToBool`
2. `coerceIntegerFromNumericString`
3. `repairEnumCaseInsensitive`
4. `repairMissingRequiredFromPriorTurn`
5. `repairSingleQuotesAndCommentsInToolJson`
6. `repairUnquotedKeysInToolJson`

### B. Path jail leftover

7. `refuseWriteOver2MiB`
8. `refuseWriteThroughSymlink`
9. `refuseReadThroughSymlink`
10. `rejectSymlinkEscape`
11. `nfcPath`
12. `rejectNulInPath`
13. `rejectControlCharsInPaths`
14. `rejectUncAndWindowsPaths`

Uniqueness de `str_replace` **no** se trata como timeout ni como fallo de jail.

`rejectSymlinkEscape` llama `workspacePathJail` y fail-cierra sin `root`. En el hot path **no** se invoca sin workspace root: `list_files path:'.'` sigue ejecutándose. NFC / NUL / control / UNC sí aplican siempre.

### C. Memory retrieve leftover

15. `memoryRetrieveDedupeByHash`
16. `sortMemoryHitsByScoreDesc`
17. `skipEmptyEmbeddingUpsert`
18. `skipMemoryIfVectorAllZeros`
19. `skipEmptyWhitespaceMemoryFacts`
20. `capMemoryHitsReturned8`

### D. Empty-model / parallel caps

21. `emptyResponseRetryOnce`
22. `circuitBreakerEmptyModelTwice`
23. `allowParallelReads`
24. `maxToolsPerTurnHardCap`
25. `maxUniqueToolsPerTurn16`
26. `maxToolCallsPerMessage`

Dos respuestas vacías consecutivas empujan el stall existente a `loop_stall` (el contrato de `agent-runner-native-llm` se conserva). El hard-cap de 33 tools corta el turno.

### E. Read hygiene + bash + idempotency + SSE lock

27. `formatReadWithLineNumbers` (solo sustituye la observación si hay `offset`/`limit`)
28. `stripUtf8BomOnRead`
29. `sliceReadWindow`
30. `startBackgroundBash`
31. `resetBackgroundBash`
32. `idempotentSameCallIdInflight`
33. `rememberCallResult`
34. `closeSseThenSettleCredits`
35. `sessionLockTtl90s`
36. `stealLockIfHeartbeatExpired`

## Apply layer (nombres 3H66, no colisionan)

`applyToolJsonCoerceClosed` / `applyPathJailClosed` / `applyMemoryRetrieveClosed` / `applyEmptyModelAndParallelCapsClosed` / `applyReadHygieneClosed` / `applyCallIdempotencyClosed` / `applySseCreditLockClosed` viven en `engine-3h66.js`. El hot path también nombra los helpers #388 vivos.

## Wiring fail-closed

- `loop.js` — coerce/repair JSON; jail de rutas; caps empty/parallel; BOM + ventana de lectura; bash background; idempotencia por call-id; filtros de memoria
- `engine-durability.js` — mismos filtros en `retrieveMemoryForLoop`
- `session-manager.js` + `local-sandbox.js` — jail de path + BOM en read; track/reset bash
- `ai.js` generate — lock TTL 90s / steal heartbeat + close-SSE-then-settle
- `sse-writer.js` — close-then-settle al `done()`
- `version.js` — `GET /api/version/latency` marca ola 3H66 (mismo ring persistido)
- `engine-adapter.js` — snapshot `wave: 3H66`, bind si el nombre está libre

HTTP 4xx distinto de 429 no se reintenta. HTTP 402 sigue: `'DeepSeek sin crédito (402). No reintenté.'` y `stoppedReason: llm_402`.

## Tests

`backend/tests/ola-3h66-invariants.test.js` + `engine-hotpath-wire.test.js`. Snapshots 3H59–3H65 aceptan **3H66**.

```bash
cd backend && node --test tests/ola-3h66-invariants.test.js tests/ola-3h65-invariants.test.js tests/engine-hotpath-wire.test.js tests/agent-runner-routing.test.js
```

## Fuera de alcance

Sin UI. Sin OpenRouter. DeepSeek Flash/Pro only. EventSource real en browser sigue pendiente. p50/p95 bajo carga VPS real (≥200 muestras de producción) sigue sin corpus: el instrumento ya persiste; esta ola no inventa números Flash.
