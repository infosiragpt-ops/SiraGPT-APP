# Ola 3h14 — 2026-08-17-2057 ship log (America/Lima)

Backup: `/opt/siragpt/backups/ola-3h14-20260817T2057Z` (override snapshot + edited BE trees). Never copied `.env`.
UI lock: no layout moves, no Configuración avanzada, no composer/nav/office/Proactivo placement. No compose down. No `down -v`. No repo clone. No OpenRouter. No invented keys.
DeepSeek V4 Flash/Pro only. `SIRAGPT_REFRESH_TOKEN_ROTATION` stays unset (BE-013 still skipped).
Ola-200 A–G, 3H through 3H13 batches not redone. Numbered 3H14-BE so it does not collide.

**Frontend skipped.** Video/3H frontend image stayed up (`created=2026-08-17T20:45:09Z`, 15:45 Lima). This wave is **BACKEND ONLY**. No UI leftovers shipped. Did not touch chat Video composer, duration chips, fal client, or `ai.js` generate-video. Did not overwrite `FAL_*` keys. Did not edit `backend/src/routes/video.js` / `ai.js` / `backend/src/services/fal/**`.

This batch wires leftover reliability/agentic into live handlers: gateway `queueHonesty.pending`, public status DLQ PII redact, startAgent empty/cap/idempotency, HTTP fail-closed taxonomy (agent/abort/skills/memory/cron/dlq), inbound webhook oversized delivery-id, memory-search chatId + duplicate skip + route persist merge, skill recommend hide admin, cron job-id cap + inflight snapshot + tick unique, health gateway_queue/webhook_hmac/cron_inflight, leftover generate/stop rate-limit paths, scheduled-agent-tasks payload cap. Not helper-only unused functions. Did not re-close OpenAI generate leaks from 3H11. Did not retouch video composer.

## Shipped this run (28 items)

### Backend (recreated `backend` only; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H14-BE-001** gateway leftover queueHonesty.pending — live `GET /api/gateway/status` now includes `pending` from session-queue snapshot (3H13 leftover). `backend/src/services/agent-gateway/index.js`. Verified: live JSON `queueHonesty.pending: 0`.
- **3H14-BE-002** gateway leftover public DLQ PII — status `deadLetter` redacts sessionKey/userId; adds `retryableDeadLetterCount`. Same file. Verified: invariant + live JSON has no sessionKey.
- **3H14-BE-003** startAgent leftover empty prompt — fail-closed `empty_prompt` (no generate). Same file. Verified: invariant.
- **3H14-BE-004** startAgent leftover prompt cap — fail-closed `prompt_too_long` >8000. Same file. Verified: invariant.
- **3H14-BE-005** startAgent leftover idempotency — `idempotencyKey` uses `claimTurnIdentityUnique` (`duplicate_turn`, keys >256 fail-closed). Same file + HTTP agent. Verified: invariant.
- **3H14-BE-006** HTTP agent leftover taxonomy — `failHttp` + `httpStatusFor` (429 `session_queue_full`, 401 `user_required`). `backend/src/services/agent-gateway/http.js`. Verified: live unauthenticated POST `/api/gateway/agent` 401 `user_required` retryable:false.
- **3H14-BE-007** HTTP abort leftover user_required — abort without actor 401. Same file. Verified: live POST `/api/gateway/abort` 401.
- **3H14-BE-008** HTTP skills leftover search — unauthenticated 401; `?q=` searches persist + registry (admin still hidden). Same file. Verified: invariant + live file markers.
- **3H14-BE-009** HTTP memory leftover user + query cap — GET `/api/gateway/memory` 401 without user; q sliced to 200. Same file + gateway dispatch. Verified: invariant.
- **3H14-BE-010** HTTP cron leftover user/prompt — create fail-closed without user + empty/oversized prompt in dispatch. `http.js` + `agent-gateway/index.js`. Verified: invariant.
- **3H14-BE-011** HTTP DLQ leftover retryable list — live `GET /api/gateway/dlq` user-scoped, no foreign letters. Mounted in `backend/src/routes/gateway.js`. Verified: live unauthenticated 401 `user_required`.
- **3H14-BE-012** error taxonomy leftover — `turn_timeout` retryable, `duplicate_turn`/`delivery_id_too_long`/`httpStatusFor`. `backend/src/services/error_codes.js`. Verified: invariant.
- **3H14-BE-013** webhooks leftover oversized delivery-id — `rememberInboundDelivery` fail-closed >128 (never identity). HMAC still runs first. `backend/src/routes/webhooks.js`. Verified: invariant + live unsigned POST 503 `hmac_secret_missing` retryable:false.
- **3H14-BE-014** memory-search leftover chatId filter — `searchUserEpisodes({chatId})` never cross-chat. `backend/src/services/memory-search-persist.js`. Verified: invariant.
- **3H14-BE-015** memory-search leftover duplicate skip — same text+chatId retry returns `{duplicate:true, indexed:0}`. Same file. Verified: invariant.
- **3H14-BE-016** memory route leftover search merge — `GET /api/memory/search` also returns persist `episodes`. `backend/src/routes/memory.js`. Verified: invariant + live unauthenticated 401 `access_token_required`.
- **3H14-BE-017** memory route leftover persist on POST — `persistEpisode` on add. Same file. Verified: invariant.
- **3H14-BE-018** memory route leftover forget-me persist — DELETE `/` also `deleteUserEpisodes` (`persistCleared`). Same file. Verified: invariant.
- **3H14-BE-019** skills leftover recommend hide admin — `recommendSkills` skips admin/system unless `clearance:"admin"`. `backend/src/services/skills-registry.js`. Verified: invariant.
- **3H14-BE-020** cron leftover oversized job id — fail-closed `cron_job_id_too_long` >128. `backend/src/services/cron-as-turn.js`. Verified: invariant.
- **3H14-BE-021** cron leftover inflight snapshot — `inflightSnapshot()` count-only for health (never jobId). Same file. Verified: live `/health` `cron_inflight {inflight:0,max:8}`.
- **3H14-BE-022** cron leftover tick unique — per-minute `claimTurnIdentityUnique` so two workers cannot double-dispatch. Same file. Verified: invariant.
- **3H14-BE-023** health leftover gateway_queue — live `/health` non-critical `gateway_queue` (pending/lanes/dlq, never sessionKey). `backend/src/services/observability/health-check.js`. Verified: live check healthy.
- **3H14-BE-024** health leftover webhook_hmac — boolean `configured` only (never secret). Same file. Verified: live `{configured:false}`.
- **3H14-BE-025** health leftover cron_inflight — live `/health` inflight/max. Same file. Verified: live check healthy.
- **3H14-BE-026** rate-limit leftover generate — shared budget adds `/api/webhooks`, `/api/gateway/memory|skills|cron|dlq`, `/api/agent-keys`, `/api/accounting`, `/api/push`. `backend/src/middleware/rate-limit-policy.js`. Verified: invariant.
- **3H14-BE-027** rate-limit leftover stop — skip generate budget for those abort/stop/revoke paths (`/api/gateway/status` remains false). Same file. Verified: invariant.
- **3H14-BE-028** scheduled-agent-tasks leftover payload cap — `assertTaskPayload` empty/oversized prompt fail-closed on `createTask`. `backend/src/services/scheduled-agent-tasks.js`. Verified: invariant.

## Live proof (backend) — 16:15 PT
- `docker compose -f docker-compose.prod.yml -f docker-compose.production.override.yml --env-file .env up -d --no-deps --force-recreate backend` — Recreated + Started
- Container created **16:15 PT** (2026-08-17T21:15:36Z), healthy, pid 1
- `/health/live` 200 (pid 1 in this VPS backend; public https://siragpt.com/health/live same pid), `/health/ready` 200
- Full `/health`: status degraded remains pre-existing non-critical `mcp_policy`; no `DEEPSEEK_API_KEY` literal, no `openrouter.ai`
- generate_path healthy (path: F2, models deepseek-v4-flash / deepseek-v4-pro, openrouter: false)
- gvisor_runsc healthy (runtime: runsc)
- sandbox_backends healthy ({remote:false, e2b:false, local:true, preference:[remote,e2b,local]})
- gateway_queue healthy ({lanes:0, writers:0, aborted:0, pending:0, deadLetterCount:0, retryableDeadLetterCount:0})
- webhook_hmac healthy ({configured:false}) — inbound still fail-closed
- cron_inflight healthy ({inflight:0, max:8})
- GET `/api/gateway/status` → ok, models flash/pro only, `queueHonesty: {lanes:0,writers:0,aborted:0,abortedRuns:0,pending:0}`, `scoped: {memory:true,skills:true,cron:true}`, deadLetterCount: 0, retryableDeadLetterCount: 0, no openrouter, no sessionKey
- POST `/api/webhooks/inbound` without signature → 503 `{code:hmac_secret_missing, retryable:false}`
- GET `/api/memory/search?q=test` unauthenticated → 401 `access_token_required`
- GET `/api/gateway/dlq` unauthenticated → 401 `user_required` retryable:false
- POST `/api/gateway/agent` unauthenticated → 401 `user_required`
- POST `/api/gateway/abort` unauthenticated → 401 `user_required`
- Rotation flag unset (BE-013 skip)
- Existing webtops still up: sira-dpc-cms2hkubp0013oz01mzf0m49e-ceo-office (2d), sira-dpc-testproj-sales (2d)
- Frontend container unchanged (created 2026-08-17T20:45:09Z / 15:45 Lima, healthy)
- http://127.0.0.1:3000/chat 200, /code 200; https://siragpt.com/chat 200, /code 200
- Video generate path unchanged: live `ai.js` still has `Bearer ${req.token}`
- Invariants inside live backend: **38/38 passed** (`node --test tests/ola-3h14-invariants.test.js tests/ola-3h13-invariants.test.js`)

## Services
- Recreated: backend only
- Untouched: frontend, db, redis, caddy, runner, webtops
- Never ran `down` or `down -v`
- No frontend rebuild

## Honest partials
- Inbound webhook HMAC secret (`SIRAGPT_WEBHOOK_HMAC_SECRET`) is still unset, so live inbound fail-closes 503 `hmac_secret_missing`. Oversized delivery-id is code-complete but unsigned requests never reach it (HMAC first).
- Sandbox code-interpreter backend in this VPS is still `local` (no SANDBOX_SERVICE_URL / E2B). gVisor remains the agent-runner runtime (`runsc`).
- RAG embeddings / rerank / graph still OpenAI (vector space). Generate path unchanged from 3H11.
- Gmail MCP / generate-chart / search-preview / computer-use / apps-ai (Cerebras) unchanged honest partials from 3H11.
- Health overall `degraded` remains pre-existing `mcp_policy`. `/health/ready` stays 200.
- `webhook_hmac.configured` is false (honest); check stays non-critical healthy so ready is not 503.

## Skipped (with reason)
- **All 3H14-FE** — backend-only; video FE image still up from 15:45 Lima.
- **BE-013** — do not enable SIRAGPT_REFRESH_TOKEN_ROTATION until a dual-read window exists. Flag remains unset.
- Did not wire BE-081/BE-082 into live handlers (empty prod allowlist would 503).
- Did not re-close OpenAI generate leaks already closed in 3H11.
- Did not touch `ai.js` / video.js / fal client / duration chips.
- Did not rename live webtop containers.
- Did not touch mobile/stores.
- Did not start a frontend rebuild.

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
