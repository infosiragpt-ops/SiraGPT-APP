# Ola 3H41 — 2026-08-21 00:57 America/Lima (05:57Z) — SHIPPED

Numero 3H41 **aplicado**. Motor only. UI / OpenRouter / companies / iliagpt DNS no tocados. DeepSeek only (`openrouterGenerate=false`).

## Por que ahora si (vs 21:57 diferida)

- La corrida 21:57 Lima se diferio por DeepSeek 402 Insufficient Balance.
- Logs desde StartedAt `2026-08-21T02:56:29Z`: 402 reales a las 03:04, 03:06 y 03:33Z.
- Generates 200 posteriores **sin** `Error from DeepSeek API: 402` (03:39:41Z y 04:32:04Z, responseChars 175). Evidencia de que el bloqueo de pago no esta activo en cada turno.
- No se llamo live DeepSeek en smoke largo de esta oleada (regla: no reventar creditos). TTFB live **unmeasured**.

## Helpers nuevos (26)

pruneCheckpointsKeepLastN, persistSseLastEventIdCursor, repairSingleQuotesAndCommentsInToolJson, clampMaxOutputTokens, dropDuplicateConsecutiveToolCalls, classifyHttpFamily, compactKeepLastUserAssistantPair, redactKeyLikeToolArgsFromLogs, boundStepsOnCheckpointResume, rejectEmptyToolName, rejectNulInPath, skipHeartbeatIfWriteWouldBlock, waitInflightToolThenDropOnCancel, recordTokenUsageOnErrorPath, pgvectorMemoryQueryTimeout, refuseComputerToolsIfFlagOff, coerceTrueFalseStringsToBool, maxConcurrentSubagents, dropEmptyAssistantTurn, sseRetryMsInPad, sandboxTmpCleanupOnTimeout, subagentInheritAbortSignal, truncateToolResultWithMarker, isolateParallelToolTimeout, holdSettleNeverDoubleCharge, enforceAdditionalPropertiesFalse.

3H40 se conserva (hard cap 32, nested abort, unquoted JSON, NUL args, integer coerce, empty-model circuit, budget /5, stale images, facts 30d, syntax rollback, symlink write, BOM, SIGTERM+SIGKILL, stdout 64KiB, SSE pad, destroy on close, generate/user cap, steal lock, never charge 401/403, IPv4 redact, EPIPE cancelled, glob cap 500, TTFB 8s).

Hotfix chat 02:56Z intacto: `persistentComputerTools`, `computerOnly` gated a intent, CDP observe cap 8KiB, MAX_CONTROL_STEPS=25, 402 clasificado ES, wrapExecutors no-throw.

## Verificacion

- `tests/ola-3h41-invariants.test.js` + `tests/ola-3h40-invariants.test.js` (wave 3H40|3H41).
- `/health` wave=3H41, openrouterGenerate=false.
- Frontend no rebuild.

## Fuera de alcance

- UI, iliagpt DNS, companies/data, OpenRouter, compose down -v, clone de repos, push a main: no tocados.
