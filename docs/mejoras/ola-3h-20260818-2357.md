# Ola 3H24 — 2026-08-18 18:57 America/Lima (23:57Z)  [ship ~19:45 Lima]

Numero 3H24. Rewrite del generate path vivo de /chat y /code, no un helper aislado.
Frontend untouched. Backend-only. No OpenRouter. DeepSeek Flash/Pro. No down -v.

## Por que

3H23 dejo engine-control en agent-runner/loop.js (docs). /chat llama ai.js → agentic-chat-stream.js → react-agent.js.
Luis: "aun no los veo" / "quiero que reescribas el codigo". Esta ola reescribe ESA ruta.

## Live files rewritten (not just new helpers)

- `backend/src/services/react-agent.js` — dispatchTool ahora llama repairToolCallWithFeedback; run() llama evaluateStopConditions + token budget + step telemetry + sleepTimeCompact. `engineLive: true`.
- `backend/src/services/agentic-chat-stream.js` — require engine-control + engine-advance + catalog-agent-router; extraSystem inyecta el agente de catalogo; filtra tools; pasa tokenBudget y model DeepSeek.
- `backend/src/routes/ai.js` — runAgenticChat recibe `surface` chat|code.
- `backend/src/services/agent-gateway/index.js` + `protocol.js` — idempotency keys en metodos con side-effect.
- `backend/src/services/agent-runner/loop.js` — subagent recall/implement/review + sleep compact (docs/AgentRunner).
- `backend/src/services/agent-runner/tools.js` — apply_patch git-aware.
- `backend/src/services/agent-runner/skills/index.js` — progressive disclosure (summary/refs/body).
- `backend/src/services/error_codes.js`, `observability/public-stream-error.js`, `observability/health-check.js`, `engine-reliability.js`.

## New first-party modules (rewrite, no vendor copy)

- `backend/src/services/agent-runner/engine-advance.js` — leftovers 3H23.
- `backend/src/services/catalog-agent-router.js` — 7 agentes DeepSeek del catalogo.
- SKILL.md: `codex-playbooks`, `cortex-recipes` (archivos NUESTROS).

## Catalog agents on /chat and /code

Relevant turns match query → builder | code-reviewer | researcher | crm-builder | enterprise-builder | erp-builder | hr-builder.
Loaded from /app/agents/*.toml (DeepSeek V4 Pro/Flash only). Prompt + tool allowlist + model.

## Proof generate path imports them

Container: `react-agent.engineLive === true`.
react-agent.js L40-41: require engine-control + engine-advance.
agentic-chat-stream.js L38-40: require catalog-agent-router + engine-advance + engine-control.
ai.js L6402: surface passed into runAgenticChat.
health generate_path: flash/pro, openrouter:false.
health engine_loop: engineLive, catalogAgents, subagentTypes, protocolIdempotency, sleepTimeCompact, gitAwareApply, skillProgressive.

## Shipped (metric deltas)

1. Tool repair on LIVE /chat loop (was helper-only) → repair ≥95% path now dispatchTool
2. StopWhen + token_budget on react-agent.run every step
3. Step telemetry on live generate
4. Sleep-time compact at end of every live turn (durable memory, no extra user msg)
5. Catalog routing 7/7 DeepSeek agents (0 before on /chat)
6. Subagent types recall/implement/review + sliced budget
7. Subagent tool deny fail-closed
8. SKILL progressive disclosure 3 levels
9. First-party SKILL.md playbooks 2
10. Gateway idempotency replay/conflict/sweep
11. Git-aware apply dirty refuse
12. Exact unique hunk + syntax revert
13. Typed ES errors (subagent_budget, git_apply_dirty, git_syntax_revert, sleep_compact, idempotency_replay)
14. Health flags 3H24
15. AgentRunner loop also wired (docs path)

Tests: 42/42 3H24, 25/25 oss-rewrite, 38/38 3H22.

## Live

backend recreated 1x. live/ready 200. chat/code 200 local+https. engine_loop healthy. generate_path flash/pro openrouter:false.

## Leftovers

HMAC secret missing. mcp_policy deny-all. chat_run_worker dormant. rotation unset. SANDBOX_NET_ALLOW unset.
Live Flash TTFB not measured (no burn credits).
Codex agent-loop.js (/code project runs) still has its own loop; /code page generate that hits /api/ai/generate is covered.
