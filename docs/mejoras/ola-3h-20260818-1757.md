# Ola 3H22 — 2026-08-18 13:18 America/Lima (2026-08-18T18:18Z)

Motor-only: paridad Claude Code / Cowork en el **engine**, no en la UI.
Numero 3H22 (3H21 = `ola-3h-20260818-1457.md`, 3H20 = `ola-3h-20260818-1157.md`, 3H19 = `ola-3h-20260818-0857.md`, 3H18 = `ola-3h-20260818-0557.md`, 3H17 = `ola-3h-20260818-0325.md`, 3H16 = `ola-3h-20260818-0210.md`).
Run programado 12:57 Lima 8/18/2026 («Mejora constante chat y code»).

**Frontend skipped / untouched.** Container id `fc1137d6b8da7b37b87ea5e7cae71b4ee17f9f3829163b4ec384497ee4fb80de` created `2026-08-18T16:02:36Z` (11:02 Lima) — last user-facing frontend is DeepSeek catalog remap. Zero frontend rebuilds this wave. Did not touch chat Video, duration chips, fal, `ai.js` generate catalog, webtop/computer/noVNC, document extractors, RAG attach-to-prompt, or upload pipelines.

This batch is the next layer of the 12-capability rubric after 3H21: wall-clock turn deadline that actually stops generate, tool alias map, parallel tool isolation, tool-result size cap, per-tool circuit breaker, error budget, DAG wait + plan revise, token-budget compact, pin LRU, memory dedup, checkpoint CAS, N-deep rollback, atomic write + write hash, process-group kill + zombie reap + stderr rate, SSE Last-Event-ID watermark + drain + backpressure, queue cancel-of-pending + lease TTL + round-robin fairness, credit hold/settle/release, classified ES codes + retry-after, first-byte latency snapshot. Did not invent HMAC secret, MCP allowlist, rotation flag, or `SIRAGPT_SANDBOX_NET_ALLOW`.

## Shipped this run (38 items)

### Backend (recreated `backend` only, once at end; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H22-BE-001** (cap 1) `assertTurnWallClock` under deadline ok. `engine-parity.js`. Verified: invariant.
- **3H22-BE-002** (cap 1) over deadline → `turn_deadline` stop. Verified: invariant.
- **3H22-BE-003** (cap 2) `mapToolAlias` shell/run/cmd/bash → execute_bash; read/write/memory. Verified: invariant.
- **3H22-BE-004** (cap 2) unknown tool fail-closed `unknown_tool`. Verified: invariant.
- **3H22-BE-005** (cap 2) `capToolResult` under cap keeps text. Verified: invariant.
- **3H22-BE-006** (cap 2) over cap → `tool_result_capped`. Verified: invariant.
- **3H22-BE-007** (cap 1/2) `isolateParallelTools` one reject does not kill siblings. Verified: a+c ok, b isolated.
- **3H22-BE-008** (cap 5) CAS checkpoint put/get version 1. Verified: invariant.
- **3H22-BE-009** (cap 5) stale CAS → `ckpt_cas`. Verified: invariant.
- **3H22-BE-010** (cap 4) `evictPinsLru` keeps newest max. Verified: 5→3.
- **3H22-BE-011** (cap 7) process group `killOnCancel` `pg_killed`. Verified: killed=2 leftover=0.
- **3H22-BE-012** (cap 8) Last-Event-ID watermark persist/read. Verified: seq=9.
- **3H22-BE-013** (cap 9) `cancelQueuedJob` pending vs running. Verified: j1 cancelled, j2 running refused.
- **3H22-BE-014** (cap 10) credit hold reserve then settle. Verified: remaining 60 → leftover 15.
- **3H22-BE-015** (cap 10) release on cancel + over-reserve `credit_hold`. Verified: reserved=0.
- **3H22-BE-016** (cap 1/11) error budget stops after N. Verified: `error_budget` at 2.
- **3H22-BE-017** (cap 6) `atomicWriteFile` tmp+rename. Verified: dest content.
- **3H22-BE-018** (cap 3) `waitSubtaskDag` blocked until deps done. Verified: a then b.
- **3H22-BE-019** (cap 2/3) tool circuit opens after threshold. Verified: `circuit_open`.
- **3H22-BE-020** (cap 4) `compactByTokenBudget` drops oldest, keeps system. Verified: removed≥1.
- **3H22-BE-021** (cap 7) `reapZombies` dead pids. Verified: alive=[2].
- **3H22-BE-022** (cap 8) `drainSseThenClose`. Verified: emitted=2 closed.
- **3H22-BE-023** (cap 9) `expireQueueLease`. Verified: `queue_lease`.
- **3H22-BE-024** (cap 5) rollback stack N-deep max 3. Verified: depth 3 then rollback 2.
- **3H22-BE-025** (cap 11) `retryAfterMs` retryable vs not. Verified: tool_timeout>0, error_budget=0.
- **3H22-BE-026** (cap 12) first-byte snapshot mean. Verified: count=2 mean=16.
- **3H22-BE-027** (cap 4) `dedupMemoryHits`. Verified: dropped=1.
- **3H22-BE-028** (cap 8) `dropOldestSse` `sse_backpressure`. Verified: 8→5.
- **3H22-BE-029** (cap 9) `pickFairSession` round-robin. Verified: s1→s2→s1.
- **3H22-BE-030** (cap 7) `capStderrRate` over cap. Verified: `stderr_rate`.
- **3H22-BE-031** (cap 3) `revisePlanOnFailure` inserts retry. Verified: `a#retry`.
- **3H22-BE-032** (cap 6) `verifyWriteHash` mismatch `write_hash`. Verified: invariant.
- **3H22-BE-033** (cap 1/12) live loop `turn_deadline` stops generate. Verified: calls=0, stoppedReason=turn_deadline.
- **3H22-BE-034** (cap 2) live loop alias `memory` → retrieve_memory. Verified: called=1, ok.
- **3H22-BE-035** (cap 11) live loop error budget stops. Verified: stoppedReason=error_budget.
- **3H22-BE-036** (cap 2) live loop circuit_open skips executor. Verified: called=1 after threshold 1.
- **3H22-BE-037** (cap 11) error_codes + classifyLoopError + public-stream ES, 0 traces. Verified: invariant.
- **3H22-BE-038** (cap 1–12) health flags + loop/parity/queue/event-log wired. Verified: source markers. No openrouter.ai.

## Live proof (backend) — 13:18 Lima / 18:18 UTC
- Recreated backend only (1× at end). Frontend id unchanged (`fc1137d6…` 11:02 Lima).
- Container created **13:17 Lima** (2026-08-18T18:17:01Z), healthy, pid 1
- `/health/live` 200, `/health/ready` 200
- Full `/health` status degraded remains pre-existing non-critical `mcp_policy`. No `openrouter.ai`
- `engine_loop` healthy `{turnWallClock, toolAlias, toolResultCap, parallelIsolate, ckptCas, pinLru, processGroupKill, sseWatermark, queueCancel, creditHold, errorBudget, toolCircuit, sseBackpressure, fairnessRr, stderrRateCap, firstByte, parity}`
- `generate_path` healthy (F2, deepseek-v4-flash / deepseek-v4-pro, openrouter: false)
- `gateway_queue` healthy `{lanes:0,pending:0,maxPending:8,order:fifo}`
- `GET /api/gateway/status` queueHonesty `{order:fifo, maxPending:8}` models flash/pro only, surfaces chat+code
- POST `/api/webhooks/inbound` → 503 `{code:hmac_secret_missing, retryable:false}`
- POST `/api/ai/generate` unauthenticated → 401 `access_token_required`
- POST `/api/ai/stop-stream` unauthenticated → 401 `access_token_required`
- http://127.0.0.1:3000/chat 200, /code 200; https://siragpt.com/chat 200, /code 200
- Invariants inside live backend: **38/38** `ola-3h22` + **210/210** `ola-3h16`…`ola-3h21`
- Existing webtops still up. No Fal video jobs.
- Did not edit document extractors / RAG / upload pipelines (other executor).

## Services
- Recreated: backend only (1× at end)
- Untouched: frontend, db, redis, caddy, runner, webtops
- Never ran `down` or `down -v`
- No frontend rebuild (count = 0)

## Honest partials
- Inbound HMAC secret still unset (503 fail-closed). Do not invent a key.
- MCP deny-all until Luis sets `SIRAGPT_MCP_ALLOWED_HOSTS`.
- `chat_run_worker` dormant skipped, `dormant_ok:true`.
- Refresh-token rotation flag unset.
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto. No inventar hosts.
- Latency hydrate is **scripted engine overhead** (mock LLM). firstByte count=0 on live health after recreate. Not DeepSeek Flash TTFB.
- PG put remains fail-open; CAS is an injectable store on top of durable put.
- Sandbox code-interpreter backend still `local`. Process-group kill is a pid registry + injectable killer.
- `execute_bash` remains an EDIT_TOOL (verification gate). Alias tests used `retrieve_memory` to avoid that gate.

## Skipped (with reason)
- **All FE** — last user-facing frontend is DeepSeek catalog 11:02 Lima. Constraint: do not rebuild.
- Did not set HMAC / MCP hosts / rotation / SANDBOX_NET_ALLOW.
- Did not touch `ai.js` / video.js / fal / duration chips / document extractors / RAG attach.
- Did not enable AGENT_RUNNER_ASYNC.
- Did not burn DeepSeek or Fal to measure live Flash TTFB.

## Rubric moved
See `docs/mejoras/benchmark-capacidades.md` v3H22. 12/12 capabilities moved (turn wall-clock, alias+isolate+cap+circuit, DAG+plan revise, token compact+pin LRU+dedup, CAS+rollback stack, atomic write+hash, pg kill+zombies+stderr, SSE watermark+drain+backpressure, queue cancel+lease+rr, credit hold, new ES codes, first-byte + health flags).

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
Do not put a dummy value in SIRAGPT_MCP_ALLOWED_HOSTS or SIRAGPT_WEBHOOK_HMAC_SECRET or SIRAGPT_SANDBOX_NET_ALLOW.
