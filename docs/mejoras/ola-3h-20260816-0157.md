# Ola 3h — 2026-08-16-0157 ship log (America/Lima)

Backup: `/opt/siragpt/backups/ola-3h-20260816T0157Z` (override snapshot + edited FE/BE trees)
UI lock: no layout moves, no Configuración avanzada, no composer/nav/office/Proactivo placement. No compose down. No `down -v`. No repo clone. No OpenRouter. No invented keys.
DeepSeek V4 Flash/Pro only. `SIRAGPT_REFRESH_TOKEN_ROTATION` stays unset (BE-013 still skipped).
Ola-200 A–G not redone. Numbered 200 list was exhausted; this batch is leftover gaps + unused high-impact index items (IDX / OpenClaw-rewrite / Hermes-rewrite).

## Shipped this run (31 items)

### Backend (recreated `backend` only; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H-BE-001** agent-task SSE — `id: N` via createSseEventCounter + Last-Event-ID resume (both inline and queued streamTaskEvents).
- **3H-BE-002** cowork-progress-stream — SSE `id:` + replay stageHistory from Last-Event-ID.
- **3H-BE-003** codex run stream — writes `id: ${envelope.seq}`.
- **3H-BE-004 / 3H-BE-019** charge-credits — `completeGenerateHold` uses shouldCaptureGenerateHold; abort/no-token never captures (IDX-BE-186/100 leftover race).
- **3H-BE-005** doc-agent sandbox.destroy — 3× `docker rm -f` independent of parent abort; isDocSandboxContainerName (never touches webtops).
- **3H-BE-006** spotify.js — mapSpotifyOAuthError (`access_denied`, `reconnect_required`, `invalid_state`); callback no longer generic `error`.
- **3H-BE-007** health-check — checkChatRunWorkerHeartbeat (non-critical; dormant worker skipped, never 503).
- **3H-BE-008** /health queue details — `deadLetterTotal` + `deadLetterByQueue` (live: 0 across 5 queues).
- **3H-BE-009** gateway event ring + Last-Event-ID replay on GET /api/gateway/events (OpenClaw idea rewritten; no clone).
- **3H-BE-010** gateway per-session DLQ — status.deadLetterCount (live: 0).
- **3H-BE-011** per-session queue — claimWriter aborts superseded writer (`queue.isAborted`).
- **3H-BE-012** searchable memory persist (`memory-search-persist.js` under /app/data) + userId scope on searchSessions.
- **3H-BE-013** skills that persist (`skills-persist.js` user SKILL.md store; name regex; no host-shell).
- **3H-BE-014** cron as agent turns — tick dispatches via gateway.startAgent when present (`cron-as-turn.js`, DeepSeek flash, allowCronTools:false).
- **3H-BE-015** structured-logger redactPiiFields (email/prompt/authorization/cookie).
- **3H-BE-016** computer-use auditComputerUseAction wired on POST /start and /stop (BE-079 leftover; no extra UI).
- **3H-BE-017** BE-099 leftover — backend/.npmignore `*.bak*` / `.bak-*`.
- **3H-BE-018** agent-runner/tools escapeToolText + assertNoRawScript (XSS).
- **3H-BE-020** stripHealthSecrets — env/api_key stripped from health JSON helper.
- **3H-BE-021** fetch-instrument EXTRA_REDACT_HEADERS (cookie, x-api-key, authorization).
- **3H-BE-022** telemetry rejectPromptText — prompt/completion/email dropped before sanitizeClientEvent.
- **3H-BE-023** gateway liveMemory.search now passes userId (was searching with no user → empty).

### Frontend (ONE rebuild only; no UI moves; /chat and /code layout unchanged)
- **3H-FE-001** agentic-search-service runIterator uses agenticSearchFetchInit (abort + cache:no-store). Wave G leftover wire.
- **3H-FE-002** public/sw.js NEVER_CACHE_EXACT + `/api/search/agentic`, `/api/gateway/events`, `/api/cowork/analyze-stream`.
- **3H-FE-003** client-logs SENSITIVE_KEY_RE + email/prompt/completion/access_token/refresh_token.
- **3H-FE-004** escape.ts assertNoRawScript + safeToolPreview (XSS in /code tool preview helper).
- **3H-FE-005** sse-client agentTaskResumeHeaders (Last-Event-ID leftover streams).
- **3H-FE-006** sse-client codeStreamResumeHeaders.
- **3H-FE-007** safe-uuid generateIdempotencyKey (remaining generate/code retries).
- **3H-FE-008** cowork-api coworkAnalyzeStreamResumeHeaders.

## Live proof (backend) — 22:13 PT
- `docker compose ... up -d --no-deps --force-recreate backend` — Recreated + Started
- Container created **22:13 PT** (2026-08-16T03:13:42Z), healthy
- `/health/live` 200, `/health/ready` 200
- Full `/health`: deepseek healthy (dummy: false, api.deepseek.com), r2_artifacts healthy (init: ok), generate_path healthy (path: F2, models deepseek-v4-flash / deepseek-v4-pro, openrouter: false), gvisor_runsc healthy (runtime: runsc)
- Queue DLQ visible: `deadLetterTotal: 0` for agent-tasks, chat-runs, codex-runs, document-collections, goal-runs
- GET `/api/gateway/status` → ok, models flash/pro only, deadLetterCount: 0, no openrouter
- Rotation flag unset (BE-013 skip)
- Existing webtops still up: sira-dpc-cms2hkubp0013oz01mzf0m49e-ceo-office (7h), sira-dpc-testproj-sales (10h)
- Invariants inside live backend: **16/16 passed** (`node --test tests/ola-3h-invariants.test.js`)

## Live proof (frontend) — 22:22 PT
- One rebuild only; Wave G image stayed up until the new image was ready
- Compiled successfully (~5.3 min next build + postbuild-slim); image `siragpt-frontend:latest` Built
- Frontend container created **22:22 PT** (2026-08-16T03:22:15Z), healthy, BUILD_ID `tPKCsYPm_fiXyFD1Obq-h`
- http://127.0.0.1:3000/ 200
- https://siragpt.com/ 200
- https://siragpt.com/chat 200
- https://siragpt.com/code 200
- https://siragpt.com/api/health/live 200
- Bundle markers: `search/agentic` (3H-FE-001, chat page chunk), SW live `/api/search/agentic` + `/api/gateway/events` (3H-FE-002), `refresh_token` in client-logs regex (3H-FE-003, error chunk), Last-Event-ID still in chunk 7806

## Services
- Recreated: backend, frontend (frontend via the single 3h rebuild)
- Untouched: db, redis, caddy, runner
- Never ran `down` or `down -v`

## Honest partials
- **3H-BE-007** heartbeat helper exported; chat_run_worker is not a critical /ready probe (worker still dormant — would 503 generate if we failed closed).
- **3H-BE-016** audit on start/stop only, not every computer-use route.
- **3H-BE-020** stripHealthSecrets helper + sendHealthJson exported; original /health handlers still compose the report (no env leak observed in live JSON).
- **3H-FE-005/006/007/008** helpers in sse-client / safe-uuid / cowork-api; tree-shaking may drop unused exports. 3H-FE-001 fetch-site wire is in the chat bundle.

## Skipped (with reason)
- **BE-013** — do not enable SIRAGPT_REFRESH_TOKEN_ROTATION until a dual-read window exists. Flag remains unset.
- Did not wire BE-081/BE-082 into live handlers.
- Did not rename live webtop containers.

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
