# 3H63 — cola por sesión, abort de subagentes, repair de tool-calls, checksum exact-diff (no UI)

Ola engine-only posterior a 3H62. **No inventa nombres overlay** que choquen con exports vivos de 3H59–3H62. Compone **35 helpers #388 vivos** de `engine-adapter.js` y los aplica fail-closed en el hot path (`loop.js`, `ai.js` generate, `sse-writer.js`).

## Live #388 names cableados (35)

### A. Session generate queue / fairness — `ai.js` generate

1. `acquireFairGenerateLock`
2. `releaseFairGenerateLock`
3. `queueMaxWait60sThen503`
4. `dropDuplicateInFlightGenerate`
5. `idempotentGenerateByRequestId`
6. `sessionGenerateRateLimit`

### B. Subagent abort cascade + inherited budget — `loop.js`

7. `abortNestedSubagentsOnParentHalt`
8. `abortSiblingToolsOnParentCancelToken`
9. `refuseSubagentIfParentCancelled`
10. `subagentInheritAbortSignal`
11. `subagentInheritRemainingStepBudget`
12. `abortCascade`
13. `inheritSubagentSteps`
14. `minRemainingSubagentBudget1`

### C. Streaming / partial tool-call repair — `loop.js` before execute

15. `concatenateSplitToolCallFragments`
16. `dropIncompleteTrailingToolCall`
17. `repairStreamingJsonAcrossChunks`
18. `repairUnescapedNewlinesInJsonStrings`
19. `dropOrphanToolResults`
20. `requireToolCallId`
21. `aliasCommonToolNames`
22. `joinParallelToolResultsStableOrder`
23. `isolateParallelToolTimeout`
24. `cacheIdenticalToolCallSameTurn`

### D. Exact-diff leftover — mutating writes after 3H62 RAW/syntax

25. `applyUnifiedDiff`
26. `refuseEditIfChecksumChangedSinceRead`
27. `checksumVerifyAfterWrite`
28. `atomicWriteViaTempRename`
29. `refuseBinaryFileEdit`
30. `workspacePathJail`
31. `skipUnchangedWrite`
32. `normalizeLineEndingsBeforeDiff`

### E. SSE / credits / TTFB leftovers

33. `rejectLastEventIdGoingBackwards` — generate resume + `sse-writer.js` (`replayLastNSseEventsFromCursor` / `sseEventIdMonotonic` / `dropDuplicateSseEventIds` en el writer)
34. `refundPartialTokensOnCancel` — loop bail + generate finally (`holdThenSettleCredits` en cancel)
35. `abortIfFirstByteOver45s` — loop callModel + generate watchdog

`str_replace` uniqueness (`old_str occurs more than once` / `not found` / empty) **no** se reescribe como timeout ni syntax revert.

## Ledger success (3H62 leftover)

`completeLedgerTransaction` **ya existía** en `credit-ledger.js` (no se inventó). 3H63 lo llama en generate success y cancel-after-tokens vía `completeLedgerOnSuccessClosed`. `failLedgerTransaction` de 3H62 se conserva para error.

## Apply layer (nombres 3H63, no colisionan)

`applyFairGenerateQueueClosed` / `abortSubagentCascadeClosed` / `inheritSubagentBudgetClosed` / `repairPartialToolCallsClosed` / `applyExactDiffChecksumClosed` / `guardSseLastIdRefundClosed` / `completeLedgerOnSuccessClosed` viven en `engine-3h63.js`. El hot path también nombra los helpers #388 vivos.

## Wiring fail-closed

- `loop.js` — repair de fragments antes de execute; abort cascade + budget parent−1; checksum/jail/diff; refund + TTFB
- `ai.js` generate — cola/fair lock + 503 a los 60 s; Last-Event-ID hacia atrás; refund; `completeLedgerTransaction`; watchdog 45 s
- `sse-writer.js` — `rejectLastEventIdGoingBackwards` + replay last-N
- `engine-gateway.js` — refund + complete-on-success
- `engine-adapter.js` — snapshot `wave: 3H63`, bind si el nombre está libre

## Tests

`backend/tests/ola-3h63-invariants.test.js` + `engine-hotpath-wire.test.js`.

## Fuera de alcance

Sin UI. Sin OpenRouter. Sin PRs 381–385. DeepSeek Flash/Pro only. p50/p95 bajo carga VPS y EventSource e2e en browser siguen pendientes.
