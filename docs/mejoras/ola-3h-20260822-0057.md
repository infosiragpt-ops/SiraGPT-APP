# Ola 3H47 — 2026-08-22 00:57 America/Lima (05:57Z) — SHIPPED

Numero 3H47 **aplicado**. Motor only. UI / OpenRouter / companies / iliagpt DNS no tocados. DeepSeek only (`openrouterGenerate=false`).

## STEP 0 — 402

- StartedAt previo (post-3H46 stack recreate): se reemplaza con recreate --no-deps de esta ola. Bind-mounts PAGE WIDTH Word (doc-engine/ai.js/documents.js) no se tocan.
- No se llamo live DeepSeek en smoke largo de esta oleada. TTFB live **unmeasured**.
- Health 200 (status=degraded, MCP deny-all no critico). generate_path F2 DeepSeek Flash/Pro, openrouter=false.

## Helpers nuevos (32)

rejectToolNameEndingWithDot, capToolArgString4096, refusePlanIfStepsNotArray, capPlanStepTitle80Chars, compactNeverDropLastUserMessage, skipMemoryIfScoreBelowFloor, capMemoryFactChars512, dropDuplicateMemoryIds, refuseCheckpointIfCrcMissing, sessionLockRefuseIfTtlExpired, refuseWriteToVarLogRun, skipNodeModulesGlobFiles, dropSseRetryFramesFromReplay, capSseDataBytes32KiB, endSseWithCommentOnIdle, ignoreNegativeTotalTokens, neverChargeIfNoModelCall, neverRetry408Timeout, classifyEtimedoutAsTimeout, mapMysqlEconnresetRetryable, abortIfIdleOver30sMidTool, rejectIdempotencyKeyOver128Chars, capUserMessage32KiB, redactAwsAccessKeysInResults, classifyEconnrefusedAsUnavailable, refuseSubagentIfNameEmpty, sortPlanStepsByOrderAsc, rejectToolCallIfNameIsObject, capSandboxArgv24, refuseSandboxIfCwdIsRoot, capSandboxCode256KiB, refuseSandboxIfTimeoutMissing.

3H46 se conserva (29 helpers previos). 3H45 se conserva.

Hotfix chat 02:56Z intacto: `persistentComputerTools`, `computerOnly` gated a intent, CDP observe cap 8KiB, MAX_CONTROL_STEPS=25. wrapExecutors no-throw. 402 ES. No se toco ACS/ai.js. FEATURE_DOC_ENGINE=1. PAGE WIDTH Word fix no revertido.

Skipped (ya existian / 3H45+3H46 names): no se rehicieron nombres 3H45 ni 3H46.

## Verificacion

- `tests/ola-3h47-invariants.test.js` (one-off container, not live backend).
- `/health` wave=3H47, openrouterGenerate=false.
- Frontend no rebuild. Recreate solo `siragpt-backend-1 --no-deps`.

## Fuera de alcance

- UI, iliagpt DNS, companies/data, OpenRouter, compose down -v, clone de repos, push a main: no tocados.
