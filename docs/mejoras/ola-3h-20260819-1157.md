# Ola 3H28 — 2026-08-19 06:57 America/Lima (11:57Z)  [ship ~07:20 Lima]

Numero 3H28. Caps 1/2/3/4/5/6/8/9/11/12 tenian huecos reales tras 3H27 (retrieve-before/CAS/syntax/credit). Esta ola ataca tool-storm completion, partial tool-calls, DAG wait, compact fidelity, rollback N-deep, git dirty en mas writers, SSE drop-under-load, gateway event order, latencia concurrente. Frontend untouched. Backend-only. No OpenRouter. DeepSeek Flash/Pro. No down -v.

## Por que

3H27 cerro retrieve-before, CAS durable, syntax revert, credit cancel, first-byte real. Leftovers de alto valor vs Claude Code/Cowork: un storm de N tool_calls disparaba todas en paralelo y el loop no completaba leftovers; ReAct ```tool y formas parciales no se reparaban; el orquestador hacia shift() sin esperar deps; compact partia pares assistant/tool; rollback no hacia pop N-deep; write_file/str_replace/edit_file no rehusaban git dirty; SSE esperaba drain bajo carga; seq del gateway era global; p50/p95 no se etiquetaba por inflight.

## Live files rewritten (not just new helpers)

- `backend/src/services/agent-runner/loop.js` — capToolStorm + runToolStorm; compactPreservingPairs; restoreN; begin/end concurrent; normalizePartialToolCall.
- `backend/src/services/agent-runner/tools.js` — git dirty refuse en write_file/str_replace/edit_file.
- `backend/src/services/agent-runner/react.js` — repairReactFence en fences ```tool.
- `backend/src/services/agent-runner/orchestrator/planner.js` — pickReadyNode.
- `backend/src/services/agent-runner/orchestrator/index.js` — DAG wait; dag_blocked si no hay ready.
- `backend/src/services/agent-gateway/protocol.js` — nextSessionSeq; decodeGatewayFrame.
- `backend/src/services/agent-gateway/event-log.js` — stampMonotonicSeq (seq por sesion, reordena global).
- `backend/src/utils/sse-writer.js` — sseDropUnderLoad (no stall del productor).
- `backend/src/services/codex/tool-scheduler.js` — maxParallel=4 parte batches.
- `backend/src/services/agent-runner/engine-reliability.js` — rollbackN/restoreN + classify 3H28.
- `backend/src/services/error_codes.js`, `observability/public-stream-error.js`, `observability/health-check.js`.
- `docker-compose.production.override.yml` — bind-mounts 3H28.

## New first-party module

- `backend/src/services/agent-runner/engine-completion.js` — tool-storm cap+complete, partial tool-call, DAG wait, compact fidelity, rollback N-deep, git dirty writers, SSE drop-under-load, seq per-session, concurrent p50/p95. Rewrite, no vendor copy.

## Proof live imports them

Container: `engine_loop.completion.toolStormCap/dagWait/compactFidelity/rollbackNDeep/gitDirtyWriters/sseDropUnderLoad/eventOrderMonotonic === true`.
`generate_path` flash/pro `openrouter: false`.
`sandbox_backends.local: true` (interpreter sigue local).
`chat_run_worker` skipped `dormant_ok: true`.

## Shipped (metric deltas)

1. capToolStorm max=8 (cap 1)
2. leftovers del storm se completan como ERROR tool_storm (cap 1)
3. runToolStorm maxParallel=4 (cap 1/2)
4. isolate throw no mata el storm (cap 2)
5. normalizePartialToolCall name/input + delta (cap 2)
6. repairReactFence trailing comma / fences (cap 2)
7. decodeGatewayFrame JSON parcial (cap 2/9)
8. waitDagReady nunca corre deps pendientes (cap 3)
9. pickReadyNode en planner (cap 3)
10. orchestrator espera DAG; dag_blocked honesto (cap 3)
11. compactPreservingPairs no parte assistant/tool (cap 4)
12. createNDeepCheckpoint rollback N (cap 5)
13. restoreN pop+restore previous (cap 5)
14. loop restoreN en tool_args_invalid (cap 5)
15. git dirty refuse write_file (cap 6)
16. git dirty refuse str_replace (cap 6)
17. git dirty refuse edit_file (cap 6)
18. sseDropUnderLoad no stall (cap 8)
19. stampMonotonicSeq per-session (cap 9)
20. nextSessionSeq en protocol (cap 9)
21. tool-scheduler maxParallel split (cap 1)
22. concurrent-turn p50/p95 scripted (cap 12)
23. begin/endConcurrentTurn en loop (cap 12)
24. Codigos ES: tool_storm / dag_blocked / dag_wait / compact_fidelity / event_order / concurrent_turn (cap 11)
25. Health flags 3H28 completion.*
26. Tests 36/36 3H28
27. DeepSeek lock intacto (`openrouterGenerate: false`)
28. Interpreter declarado `local`
29. chat_run_worker sigue dormant

Tests: 36/36 3H28 (+ 3H27/26/25 se corren en el container).

## Live

backend recreated 1x (compose.prod + production.override, bind-mounts, no image rebuild, no down -v).
live/ready 200. chat/code 200 local+https. engine_loop healthy. generate_path flash/pro openrouter:false.

## Leftovers (skipped a proposito)

- HMAC secret missing (`SIRAGPT_WEBHOOK_HMAC_SECRET`) — no inventar secreto de webhook.
- `mcp_policy` deny-all (`SIRAGPT_MCP_ALLOWED_HOSTS` vacio).
- `chat_run_worker` dormant (`dormant_ok:true`) — encenderlo cambiaria generate a cola async y romperia SSE in-process.
- `SIRAGPT_REFRESH_TOKEN_ROTATION` unset (no encender sin dual-read).
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto.
- Sandbox interpreter sigue `local` (gVisor `runsc` es el agent-runner).
- Live Flash TTFB no se midio con DeepSeek (no quemar creditos). p50/p95 concurrentes son scripted.
