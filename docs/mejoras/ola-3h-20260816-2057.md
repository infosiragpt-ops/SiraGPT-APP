# Ola 3h7 — 2026-08-16-2057 ship log (America/Lima)

Backup: `/opt/siragpt/backups/ola-3h7-20260816T2057Z` (override snapshot + edited FE/BE trees)
UI lock: no layout moves, no Configuración avanzada, no composer/nav/office/Proactivo placement. No compose down. No `down -v`. No repo clone. No OpenRouter. No invented keys.
DeepSeek V4 Flash/Pro only. `SIRAGPT_REFRESH_TOKEN_ROTATION` stays unset (BE-013 still skipped).
Ola-200 A–G, 3H / 3H2 / 3H3 / 3H4 / 3H5 / 3H6 batches not redone. Numbered 3H7-BE/FE so it does not collide.

This batch wires leftover generate candado into live handlers that still constructed OpenAI/OpenRouter/Gemini clients (SearchBrain, grok-voice, project-memory, /chat LTM+lexicon, paraphrase, figma, google-mcp, RAG rerank/triples, file/OCR vision, vector-PPT), leftover rate-limit/stop-skip/PII/health, leftover intent LLM on file upload, leftover FE generate clamps and fresh-POST Last-Event-ID. Not helper-only unused functions.

## Shipped this run (32 items)

### Backend (recreated `backend` only; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H7-BE-001** searchBrain leftover candado — `llmClient.js` uses `createNativeDeepSeekClient` + Flash; never OpenRouter/OpenAI.
- **3H7-BE-002** grok-voice leftover candado — OpenRouter generate hop removed; native DeepSeek Flash is the fallback (xAI native kept if `XAI_API_KEY` is set).
- **3H7-BE-003** project-memory leftover candado — `extractAndSave` uses native DeepSeek; model Flash (was `gpt-4o-mini` + `OPENAI_API_KEY`).
- **3H7-BE-004** /chat leftover candado — live LTM/lexicon extraction after generate uses DeepSeek, not `new OpenAI({ OPENAI_API_KEY })`.
- **3H7-BE-005** long-term-memory leftover — extractFacts model is Flash.
- **3H7-BE-006** personal-lexicon leftover — extractTermsLLM model is Flash.
- **3H7-BE-007** paraphrase leftover candado — charged path is native DeepSeek (Cerebras remains forceFreeIa only).
- **3H7-BE-008** figma leftover candado — mermaid generate is DeepSeek Flash (lazy client).
- **3H7-BE-009** google-mcp leftover candado — live `chat.completions` use Flash/Pro (lazy client; Google OAuth unchanged).
- **3H7-BE-010** llm-reranker leftover — default Flash; live rerank ignores leftover OpenAI client and uses native DeepSeek.
- **3H7-BE-011** triple-extractor leftover — extractTriples / extractProximalTriples default Flash.
- **3H7-BE-012** files leftover — live `scheduleCrossDocumentAnalysis` + auto-file-bridge pass a DeepSeek intent LLM (heuristic fallback if unconfigured).
- **3H7-BE-013** vector-ppt leftover candado — `getClient` is DeepSeek only; generate clamps via `resolveNativeDeepSeekModel` (no Gemini/OpenAI).
- **3H7-BE-014** fileProcessor leftover vision — fallback gate + client are DeepSeek (label `deepseek-vision`).
- **3H7-BE-015** ocr-engine leftover vision — vision model Flash; client native DeepSeek.
- **3H7-BE-016** rate-limit leftover — shared generate budget adds `/api/projects`, `/api/video`, `/api/free-ia`, `/api/builder`.
- **3H7-BE-017** rate-limit leftover — `/api/video/cancel` skips the generate limiter (stop ≠ generate).
- **3H7-BE-018** PII leftover — structured-logger keys include `ruc` / `dni` / `cpf` / `curp` / `rfc` / `pin` (pin is word-bounded so `ping` is not redacted).
- **3H7-BE-019** health secrets leftover — `stripHealthSecrets` also redacts AWS/DATABASE/REDIS/AKIA shapes.
- **3H7-BE-020** PII leftover — impersonate_granted console.warn uses `user.id`, never email.
- **3H7-BE-021** PII leftover — instant_subscription console.warn uses ids, never email.

### Frontend (ONE rebuild only; no UI moves; /chat and /code layout unchanged)
- **3H7-FE-001** client-logs leftover — SENSITIVE_KEY_RE adds `ruc` / `dni` / `cpf` / `curp` / `rfc` / `pin`.
- **3H7-FE-002** paraphrase leftover — live POST `/paraphrase` sends `Cache-Control: no-store`.
- **3H7-FE-003** remaining stream leftover — `generateAIStream` does **not** send Last-Event-ID on a fresh POST (attempt 1 uses `freshGenerateHeaders()`); reconnect still resumes.
- **3H7-FE-004** gmail leftover candado — `generateGmailResponse` clamps model via `clampDeepSeekModel` + no-store.
- **3H7-FE-005** excel leftover candado — `generateExcel` clamps model/provider to DeepSeek + no-store.
- **3H7-FE-006** vector-ppt leftover candado — `generateVectorPPT` clamps model/provider to DeepSeek + no-store.
- **3H7-FE-007** ppt leftover candado — `generatePPT` clamps model/provider to DeepSeek + no-store.
- **3H7-FE-008** figma leftover — `generateFigmaFlowchart` sends `freshGenerateHeaders()`.
- **3H7-FE-009** google-services leftover candado — `generateGoogleServicesResponse` clamps model + no-store.
- **3H7-FE-010** thesis leftover — `generateThesis` sends `freshGenerateHeaders()`.
- **3H7-FE-011** webdev leftover candado — `generate-webdev` clamps model/provider to DeepSeek + no-store.

## Live proof (backend) — 16:14 PT
- `docker compose ... up -d --no-deps --force-recreate backend` — Recreated + Started
- Container created **16:14 PT** (2026-08-16T21:14:27Z), healthy
- `/health/live` 200, `/health/ready` 200
- Full `/health`: deepseek healthy (dummy: false, api.deepseek.com), r2_artifacts healthy (init: ok), generate_path healthy (path: F2, models deepseek-v4-flash / deepseek-v4-pro, openrouter: false), gvisor_runsc healthy (runtime: runsc)
- `stripHealthSecrets` live: no `DEEPSEEK_API_KEY` literal and no `openrouter.ai` in health JSON
- GET `/api/gateway/status` → ok, models flash/pro only, `queueHonesty: {lanes:0,writers:0,aborted:0,abortedRuns:0}`, `scoped: {memory:true,skills:true,cron:true}`, deadLetterCount: 0, no openrouter
- POST `/api/gateway/abort` without sessionKey → `{code:bad_request}`
- Rotation flag unset (BE-013 skip)
- Existing webtops still up: sira-dpc-cms2hkubp0013oz01mzf0m49e-ceo-office (25h), sira-dpc-testproj-sales (28h)
- Invariants inside live backend: **113/113 passed** (`node --test tests/ola-3h7-invariants.test.js tests/ola-3h6-invariants.test.js tests/ola-3h5-invariants.test.js tests/ola-3h4-invariants.test.js tests/ola-3h3-invariants.test.js tests/ola-3h2-invariants.test.js tests/ola-3h-invariants.test.js`)

## Live proof (frontend) — 16:23 PT
- One rebuild only; previous 3h6 image stayed up until the new image was ready
- Compiled successfully (~4.5 min next build + postbuild-slim); image `siragpt-frontend:latest` Built
- Frontend container created **16:23 PT** (2026-08-16T21:23:00Z), healthy, BUILD_ID `4nCK5NWs9PR460BJvul8q`
- http://127.0.0.1:3000/ 200
- https://siragpt.com/ 200
- https://siragpt.com/chat 200
- https://siragpt.com/code 200
- https://siragpt.com/api/health/live 200
- SW live SCHEMA_VERSION `sira-v4` + `/api/` never-cache prefix (not padded)
- Bundle markers: `no-store` (parafraseo page `79e7f52f`, chunk 7806, 3H7-FE-002/003), `ruc`/`dni`/`cpf`/`curp`/`rfc` (chunk 37621, 3H7-FE-001), Last-Event-ID (chunk 7806 + chat page `d25df406`, 3H7-FE-003 reconnect), `deepseek-v4-flash` + `generate-vector-ppt`/`generate-gmail`/`generate-excel` (chunk 37621, 3H7-FE-004/005/006)

## Services
- Recreated: backend, frontend (frontend via the single 3h7 rebuild)
- Untouched: db, redis, caddy, runner
- Never ran `down` or `down -v`

## Honest partials
- **3H7-BE-002** native xAI remains first if `XAI_API_KEY` is set (Grok voice STT/TTS sibling). OpenRouter generate is gone; DeepSeek is the leftover fallback.
- **3H7-BE-007** Cerebras remains forceFreeIa only; charged paraphrase is DeepSeek.
- **3H7-BE-010** Cohere rerank path unchanged (not a chat.completions generate).
- **3H7-BE-014/015** Tesseract stays primary OCR; DeepSeek vision is the leftover fallback when local OCR is weak.
- RAG embeddings still use OpenAI (vector space; changing it would break existing indexes). Search-preview in `search.js` still OpenAI (search, not generate). Lyria music still needs an audio provider (DeepSeek cannot emit mp3).
- Health overall `degraded` remains the pre-existing non-critical `mcp_policy` (MCP_ALLOWED_HOSTS_REQUIRED). `/health/ready` stays 200.

## Skipped (with reason)
- **BE-013** — do not enable SIRAGPT_REFRESH_TOKEN_ROTATION until a dual-read window exists. Flag remains unset.
- Did not wire BE-081/BE-082 into live handlers (empty prod allowlist would 503; free-shell would break).
- Did not replace OpenAI search-preview or embeddings with DeepSeek.
- Did not replace Lyria/OpenRouter music or image-engine providers (not text generate).
- Did not rename live webtop containers.

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
