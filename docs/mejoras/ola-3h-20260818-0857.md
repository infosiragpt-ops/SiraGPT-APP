# Ola 3H19 — 2026-08-18 04:18 America/Lima (2026-08-18T09:18Z)

Motor-only: paridad Claude Code / Cowork en el **engine**, no en la UI.
Numero 3H19 (3H18 = `ola-3h-20260818-0557.md`, 3H17 = `ola-3h-20260818-0325.md`, 3H16 = `ola-3h-20260818-0210.md`).

**Frontend skipped / untouched.** Container id `e2d0a0c2c064be49f88d9bb2c22de9598556f1acac8dddd08ea5b8f518fd4755` created `2026-08-18T02:36:50Z` (21:36 Lima) — last user-facing frontend expand-corner. Zero frontend rebuilds. Did not touch chat Video, duration chips, fal, `ai.js` generate catalog, webtop/computer/noVNC.

This batch is the next layer of the 12-capability rubric after 3H18: tool-call argument coercion bounds (maxLength/maxItems/min/max fail-closed), dead-letter after 3 tool failures, session fence SET NX across two backend processes, memory retrieve score threshold, checkpoint blob compaction, sandbox network deny-by-default if allowlist missing, SSE replay window cap, gateway drop-duplicate by event hash, credit audit log (never double-charge), path traversal reject, classified ES codes, health probes for the new flags (scripted, no Fal, no paid Flash). Did not invent HMAC secret, MCP allowlist, or rotation flag.

## Shipped this run (38 items)

### Backend (recreated `backend` only, twice; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H19-BE-001** (cap 2) `coerceToolArgs` string maxLength fail-closed. `engine-next.js`. Verified: 100 chars vs maxLength 64 → `coercion_rejected`.
- **3H19-BE-002** (cap 2) numeric string coerced to integer. Verified: `"7"` → `7`.
- **3H19-BE-003** (cap 2) array maxItems fail-closed. Verified: 3 items vs maxItems 2.
- **3H19-BE-004** (cap 2) depth overflow fail-closed. Verified: maxDepth 3.
- **3H19-BE-005** (cap 2) integer minimum fail-closed. Verified: n=-4.
- **3H19-BE-006** (cap 3/9) retry tracker exhausts at 3. Verified: `exhausted:true`.
- **3H19-BE-007** (cap 3) retry tracker reset after success. Verified: count 0.
- **3H19-BE-008** (cap 1/5) session fence SET NX first-wins. `engine-next.js`. Verified: second `fence_conflict`.
- **3H19-BE-009** (cap 5) fence release then ok. Verified: invariant.
- **3H19-BE-010** (cap 5) `assertFenceSafe` conflict vs skipped. Verified: invariant.
- **3H19-BE-011** (cap 4) `filterMemoryByScore` drops low hits. Verified: 0.01 stripped at 0.15.
- **3H19-BE-012** (cap 4) unscore keyword hits kept. Verified: invariant.
- **3H19-BE-013** (cap 5) `compactCheckpointBlobs` strips large content. Verified: 20k → marker.
- **3H19-BE-014** (cap 5) small blobs preserved. Verified: invariant.
- **3H19-BE-015** (cap 7) sandbox network deny-all when allowlist unset. Verified: `deniedDefault:true`.
- **3H19-BE-016** (cap 7) `assertSandboxNetwork` deny-default. Verified: `network_denied`.
- **3H19-BE-017** (cap 7) allowlist host ok / unknown denied. Verified: invariant.
- **3H19-BE-018** (cap 8) SSE replay window drops stale frames. Verified: 10min-old gone.
- **3H19-BE-019** (cap 8) replay window max keeps newest. Verified: 12→5.
- **3H19-BE-020** (cap 8/9) `eventContentHash` stable ignoring `at`. Verified: invariant.
- **3H19-BE-021** (cap 9) `dropDuplicateByHash` → `duplicate_event`. Verified: invariant.
- **3H19-BE-022** (cap 10) credit audit skip duplicate charge. Verified: `audit_skip`.
- **3H19-BE-023** (cap 10) distinct streams recorded. Verified: count 2.
- **3H19-BE-024** (cap 6) `rejectPathTraversal` `..` / NUL / absolute. Verified: invariant.
- **3H19-BE-025** (cap 6) workspace relative ok. Verified: invariant.
- **3H19-BE-026** (cap 2) live loop coercion_rejected does **not** call executor. Verified: called=0.
- **3H19-BE-027** (cap 5/11) live loop `fence_conflict` continues. Verified: event + final.
- **3H19-BE-028** (cap 4) live loop score threshold filters memory. Verified: memoryHits=1.
- **3H19-BE-029** (cap 3/9) live loop `dlq_exhausted` after 3 tool failures. Verified: event.
- **3H19-BE-030** (cap 11) error_codes next taxonomy. Verified: coercion/dlq/fence/network/path/dup/replay/audit.
- **3H19-BE-031** (cap 11) classifyLoopError + public-stream-error ES, no traces. Verified: invariant.
- **3H19-BE-032** (cap 12) `/health` `engine_loop` next flags. Verified: live JSON turnFence/coercionBounds/scoreThreshold/blobCompact/networkDeny/replayWindow/creditAudit/eventHash/deadLetterRetry.
- **3H19-BE-033** (cap 7) session-manager snapshot `networkPolicy` deny-all. Verified: invariant.
- **3H19-BE-034** (cap 8) persistEventFrame hash-drop + window cap. Verified: replay length 1.
- **3H19-BE-035** (cap 4) `retrieveMemoryForLoop` applies score threshold. Verified: 1 hit.
- **3H19-BE-036** (cap 8/9) event-log remember drops duplicate by hash. Verified: length 1.
- **3H19-BE-037** (cap 5) durable put compactes large checkpoint blobs. Verified: `blobCompacted`.
- **3H19-BE-038** (cap 2/5) loop.js / tools.js wire engine-next. Verified: source markers. No openrouter.ai.

## Live proof (backend) — 04:18 Lima / 09:18 UTC
- Recreated backend only (2×). Frontend id unchanged (`e2d0a0c2c064…` 21:36 Lima).
- Container created **04:18 Lima** (2026-08-18T09:18:42Z), healthy, pid 1
- `/health/live` 200, `/health/ready` 200
- Full `/health` status degraded remains pre-existing non-critical `mcp_policy`. No `openrouter.ai`
- `engine_loop` healthy `{hardening:true, turnFence:true, coercionBounds:true, scoreThreshold:true, blobCompact:true, networkDeny:true, replayWindow:true, creditAudit:true, eventHash:true, deadLetterRetry:true, durableCheckpoint:true, memoryRetrieve:true, heartbeat:true}` — ttfb/turnEnd hydrated from Redis (scripted overhead, not Flash)
- `generate_path` healthy (F2, deepseek-v4-flash / deepseek-v4-pro, openrouter: false)
- `gateway_queue` healthy `{lanes:0,pending:0,maxPending:8,order:fifo}`
- `GET /api/gateway/status` queueHonesty `{order:fifo, maxPending:8}` models flash/pro only, surfaces chat+code
- POST `/api/webhooks/inbound` → 503 `{code:hmac_secret_missing, retryable:false}`
- POST `/api/ai/generate` unauthenticated → 401 `access_token_required`
- POST `/api/ai/stop-stream` unauthenticated → 401 `access_token_required`
- http://127.0.0.1:3000/chat 200, /code 200; https://siragpt.com/chat 200, /code 200
- Invariants inside live backend: **38/38** `ola-3h19` + **35/35** `ola-3h18` + **35/35** `ola-3h17` + **26/26** `ola-3h16`
- Redis smoke (live kv fence): `{lockA:true, lockB:false, codeB:"fence_conflict", released:true, lockC:true}`
- Existing webtops still up. No Fal video jobs.

## Services
- Recreated: backend only (2×)
- Untouched: frontend, db, redis, caddy, runner, webtops
- Never ran `down` or `down -v`
- No frontend rebuild (count = 0)

## Honest partials
- Inbound HMAC secret still unset (503 fail-closed). Do not invent a key.
- MCP deny-all until Luis sets `SIRAGPT_MCP_ALLOWED_HOSTS`.
- `chat_run_worker` dormant skipped, `dormant_ok:true`.
- Refresh-token rotation flag unset.
- Latency hydrate is **scripted engine overhead** (mock LLM). Not DeepSeek Flash TTFB. Do not report as Flash p50.
- PG put remains fail-open; Redis path verified for post-recreate resume + fence NX.
- Sandbox code-interpreter backend still `local`. Network policy is deny-all until Luis sets `SIRAGPT_SANDBOX_NET_ALLOW` (not invented).
- RAG embeddings still OpenAI vector space (retrieve in-loop uses injectable/keyword/LTM + ACL + score; generate path unchanged).
- Public `/health` strips keys matching `/session/` so the probe is named `turnFence` (not `sessionFence`).

## Skipped (with reason)
- **All FE** — last user-facing frontend is expand-corner 21:36 Lima. Constraint: do not rebuild.
- Did not set HMAC / MCP hosts / rotation / SANDBOX_NET_ALLOW.
- Did not touch `ai.js` / video.js / fal / duration chips.
- Did not enable AGENT_RUNNER_ASYNC.
- Did not burn DeepSeek or Fal to measure live Flash TTFB.

## Rubric moved
See `docs/mejoras/benchmark-capacidades.md` v3H19. 12/12 capabilities moved (coercion bounds, session fence, score threshold, blob compact, network deny-default, replay window, event hash, credit audit, dlq after 3, path traversal, new ES codes, health flags).

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
Do not put a dummy value in SIRAGPT_MCP_ALLOWED_HOSTS or SIRAGPT_WEBHOOK_HMAC_SECRET or SIRAGPT_SANDBOX_NET_ALLOW.
