# Ola 3H46 — 2026-08-21 21:57 America/Lima (02:57Z) — SHIPPED

Numero 3H46 **aplicado**. Motor only. UI / OpenRouter / companies / iliagpt DNS no tocados. DeepSeek only (`openrouterGenerate=false`).

## STEP 0 — 402

- StartedAt previo (PAGE WIDTH Word fix): `2026-08-22T02:24:03Z` (se conserva en bind-mounts doc-engine; este recreate --no-deps no los toca).
- No se llamo live DeepSeek en smoke largo de esta oleada. TTFB live **unmeasured**.
- Health 200 (status=degraded, MCP deny-all no critico). generate_path F2 DeepSeek Flash/Pro, openrouter=false.

## Helpers nuevos (29)

rejectPrototypePollutionKeys, dropDuplicateToolCallIds, rejectToolNameStartingWithHyphen, capToolArgKeys32, capPlanTitle128Chars, refuseDuplicatePlanStepIds, compactNeverDropSystemPrompt, skipMemoryIfTtlExpired, capMemoryHitsReturned8, crc32StampOnCheckpointSave, sessionLockStealIfHeartbeatStale, refuseWriteToRootMnt, skipVendorDirGlobFiles, dropSseCommentFramesFromReplay, capReplayFrames64, endSseWithErrorEventOnAbort, ignoreNegativePromptTokens, neverChargeIfCancelledBeforeFirstToken, neverRetry410Gone, classifyEhostunreachAsTimeout, mapPostgresEconnresetRetryable, abortIfFirstByteOver45s, rejectStaleIdempotencyKeyOver1h, capAssistantMessage64KiB, redactPemPrivateKeysInResults, classifyEconnabortedAsCancelled, refuseSubagentIfSameToolAsParent, sortMemoryHitsByScoreDesc, rejectToolCallIfArgsIsArray.

3H45 se conserva (29 helpers previos).

Hotfix chat 02:56Z intacto: `persistentComputerTools`, `computerOnly` gated a intent, CDP observe cap 8KiB, MAX_CONTROL_STEPS=25. No se toco ACS/ai.js. FEATURE_DOC_ENGINE=1. PAGE WIDTH Word fix no revertido.

Skipped (ya existian): `jsonRepairTrailingComma`, `stripUtf8BomOnRead`, `stderrByteCapPerCommand`, `maxSubagentDepth`, `classifyEpipeAsCancelled`, `rejectEmptyToolName`, `rejectToolNameWithWhitespace`.

## Verificacion

- `tests/ola-3h46-invariants.test.js` (34 pass local + one-off container).
- `/health` wave=3H46, openrouterGenerate=false.
- Frontend no rebuild. Recreate solo `siragpt-backend-1 --no-deps`.

## Fuera de alcance

- UI, iliagpt DNS, companies/data, OpenRouter, compose down -v, clone de repos, push a main: no tocados.
