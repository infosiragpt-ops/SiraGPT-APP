# Ola 3h15 — 2026-08-17-2357 ship log (America/Lima)

Backup: `/opt/siragpt/backups/ola-3h15-20260817T2357Z` (override snapshot + edited BE trees). Never copied `.env`.
UI lock: no layout moves, no Configuración avanzada, no composer/nav/office/Proactivo placement. No compose down. No `down -v`. No repo clone. No OpenRouter. No invented keys.
DeepSeek V4 Flash/Pro only. `SIRAGPT_REFRESH_TOKEN_ROTATION` stays unset (BE-013 still skipped).
Ola-200 A–G, 3H through 3H14 batches not redone. Numbered 3H15-BE so it does not collide.

**Frontend skipped.** Video/3H frontend image stayed up (`created=2026-08-17T20:45:09Z`, 15:45 Lima). This wave is **BACKEND ONLY**. No UI leftovers shipped. Did not touch chat Video composer, duration chips, fal client, or `ai.js` generate-video. Did not overwrite `FAL_*` keys. Did not edit `backend/src/routes/video.js` / `ai.js` / `backend/src/services/fal/**`. Did not touch webtop/computer/noVNC/dept-pc files.

This batch wires leftover reliability/agentic into live handlers: startAgent `user_required` (never unscoped generate), HTTP fail-closed on remaining gateway handlers (connect/skills load-delete/memory delete/cron list), dispatch list/load unscoped fail-closed, persist empty/cap (skills name + body, memory text + chatId), HTTP persist catch uses shared taxonomy, error codes `empty_text`/`invalid_skill_name`/`payload_too_long`/`chat_id_too_long`/`retry_unavailable`, health leftover `chat_run_worker` (was helper-only) + `skills_persist` unscoped-empty, leftover generate/stop rate-limit paths, inbound webhook replay-map cap, HTTP readBody 256KiB cap, DLQ POST ack user-scoped (retry stays `retry_unavailable` — no original prompt), cron job-id cap on create, liveSkills never dumps f8 without user. Not helper-only unused functions. Did not re-close OpenAI generate leaks from 3H11. Did not retouch video composer.

## Shipped this run (28 items)

### Backend (recreated `backend` only; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H15-BE-001** startAgent leftover user_required — never unscoped generate. `backend/src/services/agent-gateway/index.js`. Verified: invariant throws `user_required`.
- **3H15-BE-002** HTTP skillsDelete leftover user_required. `backend/src/services/agent-gateway/http.js`. Verified: live DELETE `/api/gateway/skills` 401 `user_required`.
- **3H15-BE-003** HTTP skillsLoad leftover user_required. Same file. Verified: live GET `/api/gateway/skills/load` 401.
- **3H15-BE-004** HTTP memoryDelete leftover user_required. Same file. Verified: live DELETE `/api/gateway/memory` 401.
- **3H15-BE-005** HTTP cronGet leftover user_required. Same file. Verified: live GET `/api/gateway/cron` 401.
- **3H15-BE-006** HTTP connect leftover user_required. Same file. Verified: live POST `/api/gateway/connect` 401.
- **3H15-BE-007** gateway SKILLS_LIST leftover user_required — never unscoped list. `agent-gateway/index.js`. Verified: invariant.
- **3H15-BE-008** gateway SKILLS_LOAD leftover user_required. Same file. Verified: invariant.
- **3H15-BE-009** gateway CRON_LIST leftover user_required. Same file. Verified: invariant.
- **3H15-BE-010** gateway SKILLS_PERSIST leftover empty/cap — empty name `invalid_skill_name`, body >16000 `payload_too_long`. Same file. Verified: invariant.
- **3H15-BE-011** gateway MEMORY_PERSIST leftover empty/cap — empty `empty_text`, >4000 `payload_too_long`. Same file. Verified: invariant.
- **3H15-BE-012** HTTP skills/memory persist leftover empty/cap failHttp. `http.js`. Verified: invariant file markers.
- **3H15-BE-013** HTTP persist leftover failHttp on catch + payload.error (was unstructured 400). Same file. Verified: invariant.
- **3H15-BE-014** error taxonomy leftover — `empty_text`/`invalid_skill_name`/`chat_id_too_long`/`retry_unavailable`; 409 retry_unavailable; 413 payload/chatId. `backend/src/services/error_codes.js`. Verified: invariant.
- **3H15-BE-015** health leftover chat_run_worker — unused 3H-BE-007 wired into live `/health` (non-critical; dormant skipped not 503). `backend/src/services/observability/health-check.js`. Verified: live check `chat_run_worker skipped {healthy:false, dormant_ok:true}`.
- **3H15-BE-016** health leftover skills_persist — unscoped list must be empty. Same file. Verified: live `{unscopedEmpty:true}` healthy.
- **3H15-BE-017** rate-limit leftover generate — shared budget adds `/api/artifacts`, `/api/admin-queues`, `/api/admin-connections`, `/api/admin-software-errors`, `/api/admin-user-context`, `/api/document-index-internal`, `/api/scheduler-internal`, `/api/ai-failover-health`, `/api/dev-sentry`. `backend/src/middleware/rate-limit-policy.js`. Verified: invariant.
- **3H15-BE-018** rate-limit leftover stop — skip generate budget for `/api/gateway/dlq` ack/retry, `/api/admin-queues` abort/cancel/retry, `/api/artifacts` abort, `/api/credits` abort/refund. Same file. Verified: invariant (`/api/ai/generate` remains false).
- **3H15-BE-019** webhooks leftover inbound replay map cap — 10k max, drop oldest (never unbounded identity). `backend/src/routes/webhooks.js`. Verified: invariant `INBOUND_IDEM_MAX === 10000`.
- **3H15-BE-020** HTTP leftover readBody size cap — 256KiB fail-closed `payload_too_long`. `http.js`. Verified: invariant.
- **3H15-BE-021** DLQ leftover POST ack user-scoped — never acks another user's letters. `session-dlq.js` + HTTP `dlqPost` + `gateway.js` POST `/dlq`. Verified: invariant + live unauthenticated POST 401 `user_required`.
- **3H15-BE-022** DLQ leftover retry_unavailable — POST action=retry does not re-generate (no original prompt stored). Same files. Verified: invariant.
- **3H15-BE-023** memory leftover chatId cap + oversized text fail-closed (never silent slice). `backend/src/services/memory-search-persist.js`. Verified: invariant.
- **3H15-BE-024** skills leftover oversized body fail-closed (never silent slice). `backend/src/services/skills-persist.js`. Verified: invariant.
- **3H15-BE-025** cron leftover job-id cap on create (tick already capped in 3H14). `backend/src/services/agent-cron/index.js`. Verified: invariant.
- **3H15-BE-026** liveSkills leftover never unscoped list/load (no f8 dump without user). `backend/src/routes/gateway.js`. Verified: invariant.
- **3H15-BE-027** memory route leftover empty_text / payload_too_long / chat_id_too_long taxonomy. `backend/src/routes/memory.js`. Verified: invariant.
- **3H15-BE-028** default cron stub leftover never lists/creates unscoped jobs. `agent-gateway/index.js`. Verified: invariant.

## Live proof (backend) — 19:16 PT
- `docker compose -f docker-compose.prod.yml -f docker-compose.production.override.yml --env-file .env up -d --no-deps --force-recreate backend` — Recreated + Started
- Container created **19:15 PT** (2026-08-18T00:15:35Z), healthy, pid 1
- `/health/live` 200 (pid 1 in this VPS backend; public https://siragpt.com/health/live same pid), `/health/ready` 200
- Full `/health`: status degraded remains pre-existing non-critical `mcp_policy`; no `DEEPSEEK_API_KEY` literal, no `openrouter.ai`
- generate_path healthy (path: F2, models deepseek-v4-flash / deepseek-v4-pro, openrouter: false)
- gvisor_runsc healthy (runtime: runsc)
- sandbox_backends healthy ({remote:false, e2b:false, local:true, preference:[remote,e2b,local]})
- gateway_queue healthy ({lanes:0, writers:0, aborted:0, pending:0, deadLetterCount:0, retryableDeadLetterCount:0})
- webhook_hmac healthy ({configured:false}) — inbound still fail-closed
- cron_inflight healthy ({inflight:0, max:8})
- chat_run_worker skipped ({healthy:false, dormant_ok:true}) — non-critical, ready stays 200
- skills_persist healthy ({unscopedEmpty:true})
- GET `/api/gateway/status` → ok, models flash/pro only, `queueHonesty: {lanes:0,writers:0,aborted:0,abortedRuns:0,pending:0}`, `scoped: {memory:true,skills:true,cron:true}`, deadLetterCount: 0, retryableDeadLetterCount: 0, no openrouter, no sessionKey
- POST `/api/webhooks/inbound` without signature → 503 `{code:hmac_secret_missing, retryable:false}`
- GET `/api/memory/search?q=test` unauthenticated → 401 `access_token_required`
- POST `/api/gateway/connect` unauthenticated → 401 `user_required`
- GET `/api/gateway/skills` / `/skills/load` / DELETE `/skills` unauthenticated → 401 `user_required`
- GET `/api/gateway/cron` / DELETE `/api/gateway/memory` unauthenticated → 401 `user_required`
- GET+POST `/api/gateway/dlq` unauthenticated → 401 `user_required`
- POST `/api/gateway/agent` and `/abort` unauthenticated → 401 `user_required`
- Rotation flag unset (BE-013 skip)
- Existing webtops still up: sira-dpc-cms2hkubp0013oz01mzf0m49e-ceo-office (2d), sira-dpc-testproj-sales (2d)
- Frontend container unchanged (created 2026-08-17T20:45:09Z / 15:45 Lima, healthy)
- http://127.0.0.1:3000/chat 200, /code 200; https://siragpt.com/chat 200, /code 200
- Video generate path unchanged: live `ai.js` still has `Bearer ${req.token}`
- Invariants inside live backend: **38/38 passed** (`node --test tests/ola-3h15-invariants.test.js tests/ola-3h14-invariants.test.js`)

## Services
- Recreated: backend only
- Untouched: frontend, db, redis, caddy, runner, webtops
- Never ran `down` or `down -v`
- No frontend rebuild

## Honest partials
- Inbound webhook HMAC secret (`SIRAGPT_WEBHOOK_HMAC_SECRET`) is still unset, so live inbound fail-closes 503 `hmac_secret_missing`. Replay-map cap is code-complete but unsigned requests never reach it (HMAC first).
- Sandbox code-interpreter backend in this VPS is still `local` (no SANDBOX_SERVICE_URL / E2B). gVisor remains the agent-runner runtime (`runsc`).
- `chat_run_worker` is skipped (dormant, `dormant_ok:true`) — honest, non-critical.
- RAG embeddings / rerank / graph still OpenAI (vector space). Generate path unchanged from 3H11.
- Gmail MCP / generate-chart / search-preview / computer-use / apps-ai (Cerebras) unchanged honest partials from 3H11.
- Health overall `degraded` remains pre-existing `mcp_policy`. `/health/ready` stays 200.
- `webhook_hmac.configured` is false (honest); check stays non-critical healthy so ready is not 503.
- DLQ POST `retry` is fail-closed `retry_unavailable` (no original prompt stored; does not re-dispatch generate). Ack is live.

## Skipped (with reason)
- **All 3H15-FE** — backend-only; video FE image still up from 15:45 Lima. Other executor owns UI (computer, composer expand, video duration, frontend rebuild).
- **BE-013** — do not enable SIRAGPT_REFRESH_TOKEN_ROTATION until a dual-read window exists. Flag remains unset.
- Did not wire BE-081/BE-082 into live handlers (empty prod allowlist would 503).
- Did not re-close OpenAI generate leaks already closed in 3H11.
- Did not touch `ai.js` / video.js / fal client / duration chips.
- Did not rename live webtop containers.
- Did not touch mobile/stores.
- Did not start a frontend rebuild.

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
