# Ola 3H45 — 2026-08-21 15:57 America/Lima (20:57Z) — SHIPPED

Numero 3H45 **aplicado**. Motor only. UI / OpenRouter / companies / iliagpt DNS no tocados. DeepSeek only (`openrouterGenerate=false`).

## STEP 0 — 402

- StartedAt previo 3H44: `2026-08-21T18:18:47Z` (se conserva hasta el recreate de esta ola).
- `docker logs --since 18:18Z` con `Insufficient Balance` / `Error from DeepSeek` / `quota_exhausted`: **0**.
- No se llamo live DeepSeek en smoke largo de esta oleada (regla: no reventar creditos). TTFB live **unmeasured**.
- Health 200 (status=degraded, MCP deny-all no critico). generate_path F2 DeepSeek Flash/Pro, openrouter=false.

## Helpers nuevos (29)

capToolResultJson128KiB, repairSingleQuotedKeysLeftover, rejectNegativeZeroNumbers, dropDuplicateSseEventIds, compactNeverDropLastAssistantToolCalls, refuseWriteToDevBoot, ignoreNegativeCompletionTokens, classifyEnetunreachAsTimeout, maxQueuedGenerate16, skipMemoryIfVectorAllZeros, resetStallCountOnToken, stripTagCharsUPlusE0000, rejectToolNameStartingWithDigit, maxUniqueToolsPerTurn16, refuseEmptyPlanTitle, crc32CheckOnCheckpointLoad, skipHiddenGlobFiles, redactSkPrefixesInResults, refuseComputerToolsIfSessionMissing, refuseSubagentIfParentCancelled, requireToolCallId, neverRetry451, stripAnsiFromSandboxOut, sessionLockHeartbeatEvery20s, mapRedisEaiAgainRetryable, perToolRemainingWallClock, endSseWithEventDone, sortToolsByNameForCache, observeOnlyNoCharge.

3H44 se conserva (inflight 8, leftover // comments, NaN/Infinity, SSE >2min, compact 2KiB, /etc /proc /sys, never negative usage, queue fair-share, skip NaN score, 3 stalls cancel, bidi, charset, A→B→A, plan 24, ckpt 1MiB, Last-Event-ID backwards, glob 32, JWT, computer no userId, min remaining 1, drop trailing incomplete, never 413, line 8KiB, client gone 30s, lock ttl 90s, Redis ECONNREFUSED, tool timeout 120s, flush last SSE, tool list 8KB, screenshot-only no-charge).

Hotfix chat 02:56Z intacto: `persistentComputerTools`, `computerOnly` gated a intent, CDP observe cap 8KiB, MAX_CONTROL_STEPS=25. No se toco ACS/ai.js.

Skipped (ya existian): `parseLastEventIdIntOnly` (Last-Event-ID no-digito), `destroySseOnClientClose` (destroy on req.close).

## Verificacion

- `tests/ola-3h45-invariants.test.js` + `ola-3h44` (wave 3H44|3H45).
- `/health` wave=3H45, openrouterGenerate=false.
- Frontend no rebuild. Recreate solo `siragpt-backend-1 --no-deps`.

## Fuera de alcance

- UI, iliagpt DNS, companies/data, OpenRouter, compose down -v, clone de repos, push a main: no tocados.
