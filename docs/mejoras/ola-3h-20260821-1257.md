# Ola 3H44 — 2026-08-21 12:57 America/Lima (17:57Z) — SHIPPED

Numero 3H44 **aplicado**. Motor only. UI / OpenRouter / companies / iliagpt DNS no tocados. DeepSeek only (`openrouterGenerate=false`).

## STEP 0 — 402

- StartedAt previo 3H43: `2026-08-21T15:25:29Z`.
- `docker logs --since 15:25Z` con `Insufficient Balance` / `Error from DeepSeek` / `quota_exhausted`: **0**.
- No se llamo live DeepSeek en smoke largo de esta oleada (regla: no reventar creditos). TTFB live **unmeasured**.
- Health 200 (status=degraded, MCP deny-all no critico). generate_path F2 DeepSeek Flash/Pro, openrouter=false.

## Helpers nuevos (30)

maxInflightToolsPerSession8, stripLeftoverLineCommentsInJson, rejectNaNInfinityNumbers, dropSseEventsOlderThan2min, capCompactSummary2KiB, refuseWriteToEtcProcSys, neverNegativeUsage, queueFairShareExtraSlotIfWaitOver20s, skipMemoryIfScoreNaN, cancelIfThreeStreamStalls, stripBidiOverrideChars, rejectToolNameOutsideCharset, rejectToolCallCycleAtoBtoA, capPlanSteps24, refuseCheckpointOver1MiBUncompressed, rejectLastEventIdGoingBackwards, capGlobMatchesReturned32, redactJwtShapedStrings, refuseComputerToolsIfNoUserId, minRemainingSubagentBudget1, dropIncompleteTrailingToolCall, neverRetry413, capStdoutLine8KiB, closeIfClientGone30s, sessionLockTtl90s, mapRedisEconnrefusedRetryable, hardCapToolTimeout120s, flushLastSseEventBeforeClose, capSerializedToolList8KB, screenshotOnlyNoCharge.

3H43 se conserva (32KiB tool args, SSE 16 buffers, stall 20s, never retry 402, dest dir missing, ceil tokens, ECONNRESET cancelled, queue 60s, embedding dim, UTF-16 NUL, tool name 64, same-name >8, skip completed plan, gzip ckpt 64KiB, Last-Event-ID int, glob 1MiB, Bearer redact, host_bash computer-only, subagent inherit remaining, concat fragments, combined 96KiB, ping >15s, homoglyph, lock pid, Prisma disconnect, default tool 30s, close SSE then settle).

Hotfix chat 02:56Z intacto: `persistentComputerTools`, `computerOnly` gated a intent, CDP observe cap 8KiB, MAX_CONTROL_STEPS=25, 402 clasificado ES, wrapExecutors no-throw. No se toco ACS/ai.js.

No se reabrio `SIRAGPT_SANDBOX_NET_ALLOW`. ETIMEDOUT / NUL path / empty-model twice / additionalProperties strip / subagent depth 2 ya existian — no se rehicieron.

## Verificacion

- `tests/ola-3h44-invariants.test.js` + `ola-3h43` + `ola-3h42` (wave 3H42|3H43|3H44).
- `/health` wave=3H44, openrouterGenerate=false.
- Frontend no rebuild. Recreate solo `siragpt-backend-1 --no-deps`.

## Fuera de alcance

- UI, iliagpt DNS, companies/data, OpenRouter, compose down -v, clone de repos, push a main: no tocados.
