# Ola 3H41 — 2026-08-20 21:57 America/Lima (02:57Z) — DEFERRED

Numero 3H41 **no se aplico**. La rutina "Mejora constante" de 21:57 Lima se difiere: DeepSeek no tiene credito (402 Insufficient Balance / quota_exhausted). Un smoke live de `/chat` `/code` que necesita el modelo no puede pasar. Extreme block = pago. 3H40 permanece live.

## Por que se difiere

- Generate path sigue F2 DeepSeek Flash/Pro (`openrouterGenerate=false`). Sin saldo, cada turno live falla 402.
- No se midio TTFB live: **unmeasured because 402**. No se inventaron metricas p50/p95.
- No se escribio codigo 3H41 (ni helpers nuevos de engine-adapter/loop/ACS/ai.js computer).
- No se recreo `siragpt-backend-1`. StartedAt sigue `2026-08-21T02:56:29Z` (hotfix chat).

## Hotfix chat ya en live (02:56Z, no es 3H41)

Parches confirmados en VPS (backups `*.bak-fixchat-20260821T0247Z`):

- `hasComputerOrWebIntent` + `currentUserUtterance`: `computerOnly` **no** es cada turno `/code` `disableAgentic` (un "hola" usa el stream normal).
- `persistentComputerTools` presente; `webtopComputerTools` ausente; `MAX_CONTROL_STEPS=25`.
- Cap CDP observe (`persist.capObserveText` 8192).
- 402 clasificado (ES) en vez de sanitize generico "Hubo un problema".
- `engine-gateway` `wrapExecutors` try/catch no-throw.

## Health (esta corrida)

- HTTP `/health` 200 (cuerpo `status: degraded` no-critico: MCP deny-all).
- Docker Health=healthy.
- `engine_loop.adapter.wave=3H40`.
- `openrouterGenerate=false`.
- Logs desde StartedAt: 0 TypeError; 0 402 reales de DeepSeek (no hubo smoke live). El 402 de saldo sigue siendo el bloqueo si se intentara un turno.

## Tests (docker exec, sin recreate)

- `tests/ola-3h40-invariants.test.js`: 29 pass / 0 fail.
- `tests/ola-3h-invariants.test.js`: 16 pass / 0 fail.

## Fuera de alcance

- UI, iliagpt DNS, companies/data, OpenRouter, compose down -v, clone de repos: no tocados.
- ACS/ai.js computer loop/cdp/persistent: no smash. 3H41 helpers: no.
