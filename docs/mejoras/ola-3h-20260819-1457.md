# Ola 3H29 — 2026-08-19 09:57 America/Lima (14:57Z)  [ship ~10:20 Lima]

Numero 3H29. Caps 7/1/2/4/5/6/8/9/10/11/12 tenian huecos reales tras 3H28 (tool-storm/DAG/compact pairs/rollback N/git dirty write+str_replace+edit). Esta ola ataca aislamiento del sandbox local (sin inventar secretos ni gVisor), corte de loop stall, formas malformadas extra, pins criticos bajo LRU, exactly-once de tools al resume, git dirty en writers restantes, heartbeat durante exec largo, cola por sesion, hold de creditos una vez por turn_id. Frontend untouched. Backend-only. No OpenRouter. DeepSeek Flash/Pro. No down -v.

## Por que

3H28 cerro tool-storm, partial tool-calls, DAG wait, compact fidelity, rollback N-deep, git dirty en write/str_replace/edit, SSE drop-under-load, seq por sesion. Leftovers de alto valor vs Claude Code/Cowork: el interpreter sigue `local` y no mataba nietos ni recortaba stdout con drop-oldest; un generate colgado no cortaba con `loop_stall`; `sliceSubagentBudget` podia pedir mas que el padre; LRU de pins tiraba hechos `critical`; resume podia re-ejecutar un tool que ya produjo resultado; create_presentation/add_slide/set_slide_background no rehusaban git dirty; no habia heartbeat mientras corria el sandbox; un segundo turno de la misma sesion podia intercalarse; retry del mismo turn_id podia double-hold.

## Live files rewritten (not just new helpers)

- `backend/src/services/agent-runner/loop.js` — detectLoopStall + withIdleCut; inheritParentRemaining; repairMalformedToolTurn; evictPinsKeepingCritical; compactKeepingSystemAndPins; exactly-once tool store; holdCreditsOnce; settleStormCancel; heartbeat generate/sandbox.
- `backend/src/services/agent-runner/tools.js` — git dirty refuse en create_presentation/add_slide/set_slide_background.
- `backend/src/services/sandbox/local-sandbox.js` — byte ring drop-oldest, killProcessGroup, idle-timeout, rlimit CPU+RSS (prlimit o fallback honesto), tmp markers, heartbeat sandbox, usesRunsc=false.
- `backend/src/services/codex/agent-loop.js` — stall, malformed, exactly-once, hold once, settle on cancel.
- `backend/src/services/agent-runner/index.js` — per-session turn gate (/chat espera, no intercala).
- `backend/src/services/agent-gateway/queue.js` — sessionTurnGate alrededor de enqueue.
- `backend/src/utils/sse-writer.js` — heartbeat tag inflight sandbox|generate.
- `backend/src/services/agent-runner/engine-reliability.js` — classify 3H29.
- `backend/src/services/error_codes.js`, `observability/public-stream-error.js`, `observability/health-check.js`.
- `docker-compose.production.override.yml` — bind-mounts 3H29.

## New first-party module

- `backend/src/services/agent-runner/engine-resilience.js` — stall/idle-cut, inherit parent remaining, malformed shapes, pin keep-critical, compact keep pins, exactly-once tool, extra git writers, sandbox isolation (pg kill, ring, rlimit fallback, nnp/umask hints, tmp sweep, idle), heartbeat inflight, session wait, credit hold-once, storm cancel settle. Rewrite, no vendor copy.

## Proof live imports them

Container: `engine_loop.resilience.loopStall/pinKeepCritical/exactlyOnceTool/sandboxIdleTimeout/sessionTurnWait/creditHoldOnce === true`.
`generate_path` flash/pro `openrouter: false`.
`sandbox_backends.local: true` (interpreter sigue local; `usesRunsc: false` en este path).
`chat_run_worker` skipped `dormant_ok: true`.

## Shipped (metric deltas)

1. detectLoopStall corta generate sin token ni tool result (cap 1)
2. withIdleCut race sobre callModel (cap 1)
3. inheritParentRemaining: hijo no excede padre (cap 1/3)
4. repairMalformedToolTurn arguments JSON stringificado (cap 2)
5. empty name → tool_name_empty, no hang (cap 2)
6. duplicate id mismo turno → tool_id_duplicate rename (cap 2)
7. tool result huerfano → tool_result_orphan (cap 2)
8. evictPinsKeepingCritical conserva critical (cap 4)
9. score-tie-break recency+pin (cap 4)
10. compactKeepingSystemAndPins no tira system ni PINNED FACTS (cap 4)
11. exactly-once tool id skip al resume (cap 5)
12. in-flight marker no re-ejecuta (cap 5)
13. git dirty create_presentation (cap 6)
14. git dirty add_slide (cap 6)
15. git dirty set_slide_background (cap 6)
16. stdout/stderr ring drop-oldest (cap 7)
17. killProcessGroup nietos SIGTERM+SIGKILL (cap 7)
18. rlimit CPU+RSS o fallback honesto (cap 7)
19. idle-timeout sin bytes → sandbox_timeout (cap 7)
20. tmp markers + sweep aunque el padre muera (cap 7)
21. usesRunsc=false en este interpreter (cap 7)
22. heartbeat durante exec sandbox (cap 8)
23. heartbeat tag inflight sandbox vs generate (cap 8/12)
24. session turn gate: segundo turno espera (cap 9)
25. eventos no intercalan (cap 9)
26. credit hold once por turn_id (cap 10)
27. storm cancel settle used + release leftover (cap 10)
28. codigos ES: loop_stall / sandbox_timeout / tool_id_duplicate / session_busy / pin_evict / exactly_once_tool (cap 11)
29. p50/p95 scripted por inflight (cap 12)
30. Health flags 3H29 resilience.*
31. Tests 35/35 3H29
32. DeepSeek lock intacto (`openrouterGenerate: false`)
33. Interpreter declarado `local`
34. chat_run_worker sigue dormant

Tests: 35/35 3H29 (+ 3H28/27/26/25 se corren en el container).

## Live

backend recreated 1x (compose.prod + production.override, bind-mounts, no image rebuild, no down -v).
live/ready 200. chat/code 200 local+https. engine_loop healthy. generate_path flash/pro openrouter:false.

## Leftovers (skipped a proposito)

- HMAC secret missing (`SIRAGPT_WEBHOOK_HMAC_SECRET`) — no inventar secreto de webhook.
- `mcp_policy` deny-all (`SIRAGPT_MCP_ALLOWED_HOSTS` vacio).
- `chat_run_worker` dormant (`dormant_ok:true`) — encenderlo cambiaria generate a cola async y romperia SSE in-process.
- `SIRAGPT_REFRESH_TOKEN_ROTATION` unset (no encender sin dual-read).
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto.
- Sandbox interpreter sigue `local` (gVisor `runsc` es el agent-runner docker driver, no este path). No se invento allowlist de red.
- `create_file` / `delete_file` / `move_file` / `notebook_edit` no existen live — no se fabricaron tools. Git dirty cubre los writers que si existen.
- Live Flash TTFB no se midio con DeepSeek (no quemar creditos). p50/p95 concurrentes e inflight son scripted.
