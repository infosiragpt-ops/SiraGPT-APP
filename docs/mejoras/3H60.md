# 3H60 — agent-loop robustness (no UI)

Ola engine-only posterior a 3H59. 32 helpers únicos, fail-open, sin OpenRouter.

## Helpers

1. `coerceToolArgTypes` — string→number/bool/array
2. `unwrapFencedToolArgs` — vallas markdown y `+{...}`
3. `repairEnumArgClosestMatch` — Levenshtein al enum permitido
4. `retryTransientToolError` — 429/5xx/timeout; nunca 4xx duro
5. `refuseNamelessToolAfterRepair`
6. `cutOscillatingToolPair` — A-B-A-B ≥ 4
7. `inheritSubagentRemainingSteps` — parent−1, cap 12
8. `refuseSubagentIfParentBudgetGone`
9. `compactFaithfulDroppedSummary`
10. `pruneMessagesByQueryOverlap`
11. `recoverPinnedMemoryFacts`
12. `neverDropLastUserOnCompact`
13. `checkpointFileByteSnapshot`
14. `rollbackFileByteSnapshot`
15. `verifyReadAfterWriteHash`
16. `revertWriteOnSyntaxFail`
17. `applyExactDiffRequiresMarkers`
18. `sandboxStreamChunkCap`
19. `sandboxFinallyCleanupOnAbort`
20. `sseReplayFromLastEventId`
21. `sseHeartbeatCommentNoSeq`
22. `sseAbortOnClientDisconnect`
23. `dropBufferedTokensOnSseCancel`
24. `sessionSingleWriterLock`
25. `sessionQueueDetectGap`
26. `settleCreditsOnError`
27. `neverChargeBeforeFirstToken`
28. `capPromptTokensOnErrorSettle`
29. `classifyEngine3h60Error`
30. `redactSecretsFromPublicError`
31. `observeScriptedLatencySample`
32. `refuseOpenRouterInWave3h60`

## Wiring (fail-open)

- `loop.js` — repair, oscilación, retry transitorio, compacto >24 msgs, créditos cancel/error
- `sse-writer.js` — replay Last-Event-ID, heartbeat sin seq, abort + drop buffer
- `agent-gateway/queue.js` — single-writer + gap
- `engine-gateway.js` — settle on error, never charge pre-token, DeepSeek lock
- `local-sandbox.js` — chunk cap + abort cleanup
- `engine-adapter.js` — snapshot `wave: 3H60`, bind si el nombre está libre

## Tests

`backend/tests/ola-3h60-invariants.test.js` (16). Incluye corte A-B-A-B en `runAgentLoop`.
