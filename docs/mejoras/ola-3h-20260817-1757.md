# Ola 3h13 — 2026-08-17-1757 ship log (America/Lima)

Backup: `/opt/siragpt/backups/ola-3h13-20260817T1757Z` (override snapshot + edited BE trees). Never copied `.env`.
UI lock: no layout moves, no Configuración avanzada, no composer/nav/office/Proactivo placement. No compose down. No `down -v`. No repo clone. No OpenRouter. No invented keys.
DeepSeek V4 Flash/Pro only. `SIRAGPT_REFRESH_TOKEN_ROTATION` stays unset (BE-013 still skipped).
Ola-200 A–G, 3H through 3H12 batches not redone. Numbered 3H13-BE so it does not collide.

**Frontend skipped.** Video/3H12 frontend image stayed up (`created=2026-08-17T17:40:11Z`, 12:40 Lima). This wave is **BACKEND ONLY**. No UI leftovers shipped. Did not touch chat Video composer, duration chips, fal client, or `ai.js` generate-video. Did not overwrite `FAL_*` keys.

This batch wires leftover reliability/agentic into live handlers: SSE resume (attachSseIds + Last-Event-ID) on remaining streams, sandbox fail-closed (user required, language/size, local queue retry), session DLQ user scoping + TTL + retryable, session queue max pending, cron-as-turn leftover (global cap + one-shot retry), memory-search query cap, skill registry hide admin/system on list/get, shared error taxonomy, health sandbox_backends honesty, leftover generate/stop rate-limit paths, oversized idempotency keys fail-closed. Not helper-only unused functions. Did not re-close OpenAI generate leaks from 3H11. Did not retouch video composer.

## Shipped this run (23 items)

### Backend (recreated `backend` only; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H13-BE-001** agent-task leftover SSE ids — live generate stream `attachSseIds(res, req)` so leftover `data:` frames get `id:`. `backend/src/routes/agent-task.js`. Verified: invariant attach count >= 2.
- **3H13-BE-002** agent-task leftover SSE ids — `streamTaskEvents` attachSseIds (2-space header site). Same file. Verified: invariant.
- **3H13-BE-003** chats leftover SSE resume — `writeSse` starts the counter from `parseLastEventId(res.req)` (was 0) + attachSseIds on the live pending-run SSE. `backend/src/routes/chats.js`. Verified: invariant.
- **3H13-BE-004** orgs leftover SSE ids — attachSseIds on live org event-stream. `backend/src/routes/orgs.js`. Verified: invariant.
- **3H13-BE-005** enterprise leftover SSE ids — attachSseIds when `Accept: text/event-stream`. `backend/src/routes/enterprise.js`. Verified: invariant.
- **3H13-BE-006** codex leftover Last-Event-ID — `startSeq` uses `parseCodexLastEventId(req)` when `afterSeq` is missing (helper was imported but unused). `backend/src/routes/codex.js`. Verified: invariant.
- **3H13-BE-007** sse leftover lastEventID — `parseLastEventId` honors `req.query.lastEventID` (EventSource polyfill). `backend/src/services/observability/sse-event-id.js`. Verified: invariant.
- **3H13-BE-008** sandbox leftover user required — live `POST /api/sandbox/session` fail-closed without userId. `backend/src/routes/sandbox.js`. Verified: invariant + live unauthenticated curl 401 `access_token_required` (auth runs first).
- **3H13-BE-009** sandbox leftover language/size — `executeCode` fail-closed `sandbox_code_too_long` (>64k) and `sandbox_language_not_allowed` (bash unless `SANDBOX_ALLOW_BASH=1`). `backend/src/services/sandbox/router.js`. Verified: invariant `assertExecuteArgs`.
- **3H13-BE-010** sandbox leftover job retry — local `sandbox_queue_timeout` retries executeCode once. Same file. Verified: live file marker `_retried`.
- **3H13-BE-011** session DLQ leftover userId + TTL + retryable — list is user-scoped; 24h prune; `retryable()` returns timeout/busy letters. `backend/src/services/agent-gateway/session-dlq.js`. Verified: invariant.
- **3H13-BE-012** gateway leftover DLQ userId — live abort/error DLQ pushes include `record.userId`. `backend/src/services/agent-gateway/index.js`. Verified: live file marker `userId: record.userId`.
- **3H13-BE-013** session queue leftover max pending — enqueue fail-closed `session_queue_full` at 8 pending turns per lane. `backend/src/services/agent-gateway/queue.js`. Verified: invariant.
- **3H13-BE-014** cron leftover max concurrent — global `MAX_CONCURRENT_CRON_TICKS=8` fail-closed `cron_busy`. `backend/src/services/cron-as-turn.js`. Verified: invariant export.
- **3H13-BE-015** cron leftover retry — one-shot retry on `cron_dispatch_unavailable`. Same file. Verified: invariant `_retried`. 3H12 DLQ test still uses `cron_error` (no retry).
- **3H13-BE-016** memory leftover query cap — `searchUserEpisodes` slices query to 200 chars. `backend/src/services/memory-search-persist.js`. Verified: invariant `MAX_QUERY_CHARS`.
- **3H13-BE-017** skills leftover list/get hide admin — `listSkills`/`getSkill` hide admin/system unless `clearance:"admin"` (searchSkills already did). `backend/src/services/skills-registry.js`. Verified: invariant.
- **3H13-BE-018** error taxonomy leftover — shared `error_codes.js` (`publicError` + retryable map) used by invariants and live codes. `backend/src/services/error_codes.js`. Verified: invariant.
- **3H13-BE-019** health leftover sandbox backends — live `/health` includes non-critical `sandbox_backends` (remote/e2b/local booleans, never URL). `backend/src/services/observability/health-check.js`. Verified: live `/health` `sandbox_backends healthy {remote:false,e2b:false,local:true}`.
- **3H13-BE-020** rate-limit leftover generate — shared budget adds `/api/circuit-attribution`, `/api/spotify`, `/api/apps-kv`, `/api/bookmarks`, `/api/link-preview`. `backend/src/middleware/rate-limit-policy.js`. Verified: invariant `isSharedGenerateAgentPath`.
- **3H13-BE-021** rate-limit leftover stop — skip generate budget for those abort/stop + `/api/orgs` + `/api/codex-runs`. Same file. Verified: invariant `isStopStreamPath` (generate remains false).
- **3H13-BE-022** idempotency leftover oversized key — `normalizeTurnKey` drops keys >256 chars (never identity). `backend/src/services/chat-turn-idempotency.js` (wired by live generate without editing `ai.js`). Verified: invariant `resolveTurnIdentity` null.
- **3H13-BE-023** chats leftover attachSseIds — live pending-run SSE wraps leftover `data:` writes. Counted with BE-003. Verified: invariant.

## Live proof (backend) — 13:36 PT
- `docker compose -f docker-compose.prod.yml -f docker-compose.production.override.yml --env-file .env up -d --no-deps --force-recreate backend` — Recreated + Started
- Container created **13:36 PT** (2026-08-17T18:36:12Z), healthy, pid 1
- `/health/live` 200 (pid 1 in this VPS backend; public https://siragpt.com/health/live same pid), `/health/ready` 200
- Full `/health`: status degraded remains pre-existing non-critical `mcp_policy`; no `DEEPSEEK_API_KEY` literal, no `openrouter.ai`
- generate_path healthy (path: F2, models deepseek-v4-flash / deepseek-v4-pro, openrouter: false)
- gvisor_runsc healthy (runtime: runsc)
- sandbox_backends healthy ({remote:false, e2b:false, local:true, preference:[remote,e2b,local]}) — local still the code-interpreter backend; bash now fail-closed unless SANDBOX_ALLOW_BASH=1
- GET `/api/gateway/status` → ok, models flash/pro only, `queueHonesty: {lanes:0,writers:0,aborted:0,abortedRuns:0}`, `scoped: {memory:true,skills:true,cron:true}`, deadLetterCount: 0, no openrouter
- POST `/api/webhooks/inbound` without signature → 503 `{code:hmac_secret_missing, retryable:false}`
- GET `/api/memory/search?q=test` unauthenticated → 401 `access_token_required`
- POST `/api/sandbox/session` unauthenticated → 401 `access_token_required`
- Rotation flag unset (BE-013 skip)
- Existing webtops still up: sira-dpc-cms2hkubp0013oz01mzf0m49e-ceo-office (47h), sira-dpc-testproj-sales (2d)
- Frontend container unchanged (created 2026-08-17T17:40:11Z / 12:40 Lima, healthy)
- http://127.0.0.1:3000/chat 200, /code 200; https://siragpt.com/chat 200, /code 200
- Video generate path unchanged: live `ai.js` still has `Bearer ${req.token}`, 1080p, duration 4–30s, image_urls/video_urls/audio_urls
- Invariants inside live backend: **39/39 passed** (`node --test tests/ola-3h13-invariants.test.js tests/ola-3h12-invariants.test.js`)

## Services
- Recreated: backend only
- Untouched: frontend, db, redis, caddy, runner, webtops
- Never ran `down` or `down -v`
- No frontend rebuild

## Honest partials
- Inbound webhook HMAC secret (`SIRAGPT_WEBHOOK_HMAC_SECRET`) is still unset, so live inbound fail-closes 503 `hmac_secret_missing`.
- Sandbox code-interpreter backend in this VPS is still `local` (no SANDBOX_SERVICE_URL / E2B). gVisor remains the agent-runner runtime (`runsc`). Local bash is now fail-closed unless explicitly enabled.
- `queueHonesty` live JSON does not yet surface the new `pending` field (snapshot has it; status mapper still lists lanes/writers/aborted/abortedRuns).
- RAG embeddings / rerank / graph still OpenAI (vector space). Generate path unchanged from 3H11.
- Gmail MCP / generate-chart / search-preview / computer-use / apps-ai (Cerebras) unchanged honest partials from 3H11.
- Health overall `degraded` remains pre-existing `mcp_policy`. `/health/ready` stays 200.

## Skipped (with reason)
- **All 3H13-FE** — backend-only; video FE image still warming/up from 12:40 Lima.
- **BE-013** — do not enable SIRAGPT_REFRESH_TOKEN_ROTATION until a dual-read window exists. Flag remains unset.
- Did not wire BE-081/BE-082 into live handlers (empty prod allowlist would 503).
- Did not re-close OpenAI generate leaks already closed in 3H11.
- Did not touch `ai.js` / video.js / fal client / duration chips.
- Did not rename live webtop containers.
- Did not touch mobile/stores.
- Did not start a frontend rebuild.

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
