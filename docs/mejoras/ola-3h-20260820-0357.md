# Ola 3H35 — 2026-08-20 03:57 America/Lima (08:57Z)

Numero 3H35. Caps 1-12 tenian huecos reales tras 3H34 (pgvector rank, last-N rollback, token audit, fair lock, GET cache, NFC/symlink, 0-token refund). Esta ola ataca ids de tool_call unicos y drop de tool_result huerfano, repair JSON entre chunks, apply de unified diff, ulimit nproc/nofile, stdout/stderr separados, credit hold-then-settle, resume sin re-exec, compact que no suelta SIRAGPT.md ni el last user, drop de eventos de run cancelado, rate per-tool, tope de image/pdf en contexto, clamp de max_tokens vs remaining, TTL con clock-skew, generate idempotente por requestId. Frontend untouched. Backend-only. No OpenRouter. DeepSeek Flash/Pro. No down -v.

## Por que

3H34 cerro ranking, rollback N, audit, fair lock, GET cache, timeouts split, schema strip, NFC/symlink, SSE comment, refund 0-token, compact budget, inherit steps, file lock. Leftovers de alto valor vs Claude Code/Cowork: tool_call ids duplicados; tool_result sin llamada; JSON partido entre chunks; edits solo str_replace (sin ---/+++); sandbox sin nproc/nofile; stdout mezclado con stderr; retry cobraba de nuevo; resume re-ejecutaba tools; compact podia tirar SIRAGPT.md o el last user; SSE seguia emitiendo un run cancelado; rate solo per-session; image/pdf inflaban contexto; max_tokens no miraba remaining; TTL fragil a skew; POST generate no era idempotente por requestId del cliente.

## Live files rewritten (not just new helpers)

- `backend/src/services/agent-runner/engine-adapter.js` — 3H34 intacto + 16 helpers 3H35.
- `backend/src/services/agent-runner/loop.js` — ids unicos, orphans, JSON chunks, unified diff, hold/settle, resume replay, compact pin+user, per-tool rate, media cap, max_tokens clamp.
- `backend/src/services/react-agent.js` + `agentic-chat-stream.js` — unique ids + max_tokens clamp + hold/release en /chat.
- `backend/src/utils/sse-writer.js` — drop events de runId cancelado.
- `backend/src/services/agent-gateway/queue.js` — requestId idempotency + clock-skew TTL + markRunCancelled.
- `backend/src/services/agent-runner/engine-gateway.js` — per-tool rate + drop cancelled run.
- `backend/src/services/sandbox/local-sandbox.js` — ulimit wrap + stdout/stderr split.
- `backend/src/services/error_codes.js`, `observability/public-stream-error.js`.
- `docker-compose.production.override.yml` — bind-mount ola-3h35-invariants.test.js.

## Proof live imports them

Container: `engine_loop.adapter. === true` y `wave: 3H35`.
`generate_path` flash/pro `openrouter: false`.
`sandbox_backends.local: true` (`usesRunsc: false`).
`chat_run_worker` skipped `dormant_ok: true`.

## Shipped (metric deltas)

1. ensureUniqueToolCallIds (cap 1)
2. dropOrphanToolResults (cap 1)
3. repairStreamingJsonAcrossChunks (cap 2)
4. applyUnifiedDiff ---/+++ (cap 6)
5. sandboxUlimit nproc/nofile (cap 7)
6. splitStdoutStderrToolResult (cap 7)
7. holdThenSettleCredits requestId (cap 10)
8. replayToolResultsOnResume (cap 5)
9. compactKeepPinnedSiragptAndLastUser (cap 4)
10. dropCancelledRunEvents by runId (cap 8)
11. perToolRateLimit (cap 2/9)
12. capImagePdfInContext (cap 4)
13. clampMaxTokensToRemainingContext (cap 3)
14. clockSkewSafeTtl (cap 12)
15. idempotentGenerateByRequestId (cap 9)
16. Tests 22/22 3H35
17. DeepSeek lock intacto
18. Interpreter `local`
19. chat_run_worker dormant
20. Health flags 3H34+3H35 (`wave: 3H35`)

Tests: 22/22 3H35 + 22/22 3H34 + 26/26 3H33 + 37/37 3H32 + 34/34 3H31 (unit, stub model, 0 DeepSeek credits).

## Live

backend recreated 1x (compose.prod + production.override, bind-mounts, no image rebuild, no down -v, no frontend rebuild).
live/ready 200. chat/code 200. engine_loop healthy. generate_path flash/pro openrouter:false.
computer/health 200. Frontend untouched.

## Leftovers (skipped a proposito)

- HMAC secret missing (`SIRAGPT_WEBHOOK_HMAC_SECRET`) — no inventar secreto de webhook.
- `mcp_policy` deny-all para hosts nuevos (`SIRAGPT_MCP_ALLOWED_HOSTS` vacio). Already-connected reuse no abre todo.
- `chat_run_worker` dormant (`dormant_ok:true`) — encenderlo cambiaria generate a cola async y romperia SSE in-process.
- `SIRAGPT_REFRESH_TOKEN_ROTATION` unset (no encender sin dual-read).
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto.
- Sandbox interpreter sigue `local` (gVisor `runsc` es el agent-runner docker driver).
- `create_file` / `delete_file` / `move_file` / `notebook_edit` no existen live.
- Live Flash TTFB no se midio con DeepSeek (no quemar creditos). p50/p95 siguen scripted.
