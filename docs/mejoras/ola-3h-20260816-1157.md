# Ola 3h4 — 2026-08-16-1157 ship log (America/Lima)

Backup: `/opt/siragpt/backups/ola-3h4-20260816T1157Z` (override snapshot + edited FE/BE trees)
UI lock: no layout moves, no Configuración avanzada, no composer/nav/office/Proactivo placement. No compose down. No `down -v`. No repo clone. No OpenRouter. No invented keys.
DeepSeek V4 Flash/Pro only. `SIRAGPT_REFRESH_TOKEN_ROTATION` stays unset (BE-013 still skipped).
Ola-200 A–G, 3H-BE-001..023 / 3H-FE-001..008, 3H2-BE-001..026 / 3H2-FE-001..006, and 3H3-BE-001..025 / 3H3-FE-001..005 not redone. Numbered 3H4-BE/FE so it does not collide.

This batch wires leftover gaps into live handlers (not helper-only unused functions): remaining SSE without Last-Event-ID, PII in logs, SW already covered by `/api/` prefix (not padded), verification, sandbox idle on remote driver, gateway honesty/scoping, memory/skills/cron leftovers that were not yet wired at HTTP.

## Shipped this run (25 items)

### Backend (recreated `backend` only; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H4-BE-001** remaining stream — `GET /api/orgs/:id/events` stamps `id: N` via `createSseEventCounter` and skips backfill on Last-Event-ID resume.
- **3H4-BE-002** remaining stream — `GET /api/codex/runs/:runId/events` stamps `id:` and slices already-sent events from Last-Event-ID.
- **3H4-BE-003** remaining SSE helper — `createSSEEvents` starts `sseSeq` from Last-Event-ID (`options.req`); enterprise pass-through.
- **3H4-BE-004** rate-limit leftover — shared generate budget adds `/api/thesis`, `/api/viz`, `/api/marco-teorico`, `/api/hosting`, `/api/deployments`, `/api/opencode`, `/api/paraphrase`, `/api/images`, `/api/gpts`, `/api/computer-use`, `/api/enterprise`.
- **3H4-BE-005** rate-limit leftover — `/api/computer-use/stop` and `/api/agent/task/.../cancel` skip the generate/api limiter (stop ≠ generate).
- **3H4-BE-006** PII leftover — impersonation denied/rate-limited/super-admin logs use `user.id`, never email.
- **3H4-BE-007** PII leftover — GDPR delete audit log uses `id=`, never `user.email`.
- **3H4-BE-008** sandbox leftover — remote driver now gets `attachIdleTimeout` + `attachDestroyOnAbort` (same TTL/hard max as docker/local). Never touches webtops.
- **3H4-BE-009** OAuth leftover — social-posts callback maps `access_denied`/`user_denied`/`consent_required`/`login_required`/`temporarily_unavailable`/`server_error`.
- **3H4-BE-010** gateway leftover — HTTP memory/skills/cron/agent take `req.user.id` only (`actorUserId`); ignore query/body `userId` spoof.
- **3H4-BE-011** cron leftover — `listJobs({ userId })` fail-closed (no userId → empty; never lists another user's jobs).
- **3H4-BE-012** skills leftover — `skills.delete` protocol method + `liveSkills.delete` + HTTP `DELETE /api/gateway/skills` wired to `deletePersistedSkill`.
- **3H4-BE-013** Word/PPT verify leftover — EDIT_TOOLS adds `add_slides` (pptx-followup).
- **3H4-BE-014** computer-use audit leftover — GET `/capabilities` and GET `/status/:sessionId` now audit.
- **3H4-BE-015** gateway honesty leftover — `/api/gateway/status` includes `scoped: { memory, skills, cron }` (no sessionKey leak).
- **3H4-BE-016** PII leftover — structured-logger keys include `ssn` / `iban` / `cvv` / `phone` / `card`.
- **3H4-BE-017** health secrets leftover — `stripHealthSecrets` also redacts `cookie` / `client_secret` / `id_token` keys.

### Frontend (ONE rebuild only; no UI moves; /chat and /code layout unchanged)
- **3H4-FE-001** use-gateway leftover — GET `/api/gateway/events` sends Last-Event-ID and persists event ids (backend replay already shipped).
- **3H4-FE-002** agent-task leftover — `getTaskEvents` sends `agentTaskResumeHeaders` (Last-Event-ID) with `after`.
- **3H4-FE-003** opencode leftover — `/events` fetch sends Last-Event-ID; parser persists `id:`.
- **3H4-FE-004** codex/run-stream leftover — reconnect fetch sends `Last-Event-ID` alongside `afterSeq`.
- **3H4-FE-005** deployments leftover — logs EventSource remount passes `lastEventId` query; persists `event.lastEventId`.
- **3H4-FE-006** client-logs leftover — SENSITIVE_KEY_RE adds `ssn` / `iban` / `cvv` / `phone` / `card`.
- **3H4-FE-007** authenticated-fetch leftover — one GET retry on 429 honoring `Retry-After` (capped 2s).
- **3H4-FE-008** stop-stream leftover — `notifyChatTurnStopped` uses `stopStreamHeaders()`; mobile stop sends `Cache-Control: no-store`.

## Live proof (backend) — 07:15 PT
- `docker compose ... up -d --no-deps --force-recreate backend` — Recreated + Started
- Container created **07:15 PT** (2026-08-16T12:15:18Z), healthy
- `/health/live` 200, `/health/ready` 200
- Full `/health`: deepseek healthy (dummy: false, api.deepseek.com), r2_artifacts healthy (init: ok), generate_path healthy (path: F2, models deepseek-v4-flash / deepseek-v4-pro, openrouter: false), gvisor_runsc healthy (runtime: runsc)
- `stripHealthSecrets` live: no `DEEPSEEK_API_KEY` in health JSON
- GET `/api/gateway/status` → ok, models flash/pro only, `queueHonesty: {lanes:0,writers:0,aborted:0,abortedRuns:0}`, `scoped: {memory:true,skills:true,cron:true}`, deadLetterCount: 0, no openrouter
- Rotation flag unset (BE-013 skip)
- Existing webtops still up: sira-dpc-cms2hkubp0013oz01mzf0m49e-ceo-office (16h), sira-dpc-testproj-sales (19h)
- POST `/api/gateway/abort` without sessionKey → 400 `{code:bad_request}`
- Invariants inside live backend: **61/61 passed** (`node --test tests/ola-3h4-invariants.test.js tests/ola-3h3-invariants.test.js tests/ola-3h2-invariants.test.js tests/ola-3h-invariants.test.js`)

## Live proof (frontend) — 07:23 PT
- One rebuild only; previous 3h3 image stayed up until the new image was ready
- Compiled successfully (~4.4 min next build + postbuild-slim); image `siragpt-frontend:latest` Built
- Frontend container created **07:23 PT** (2026-08-16T12:23:57Z), healthy, BUILD_ID `bwHjwEH51elBAfDQxe5Yp`
- http://127.0.0.1:3000/ 200
- https://siragpt.com/ 200
- https://siragpt.com/chat 200
- https://siragpt.com/code 200
- https://siragpt.com/api/health/live 200
- SW live SCHEMA_VERSION `sira-v4` + `/api/` never-cache prefix (3H3-FE-001 paths still present; not padded)
- Bundle markers: Last-Event-ID (chunk 7806 + chat page `249a9363`, 3H4-FE-001/002/004), `ssn|iban|cvv|phone` (chunk 37621, 3H4-FE-006), `Retry-After` (chunk 7806, 3H4-FE-007), `stop-stream` (chunk 37621, 3H4-FE-008)

## Services
- Recreated: backend, frontend (frontend via the single 3h4 rebuild)
- Untouched: db, redis, caddy, runner
- Never ran `down` or `down -v`

## Honest partials
- **3H4-BE-008** idle wrap applies to remote sandboxes created after this recreate; existing long-running doc containers keep previous lifetime. Webtops untouched.
- **3H4-BE-010/011** `/api/gateway` is still mounted without `authenticateToken`. Without `req.user`, `actorUserId` is empty → persist memory/skills/cron list fail-closed empty (closes the previous query-`userId` spoof). Built-in skill catalog can still list when persist returns [].
- **3H4-FE-001** minified bundle may not contain the literal `lastEventId:gateway` string; Last-Event-ID is in the live chat page chunk.
- Health overall `degraded` remains the pre-existing non-critical `mcp_policy` (MCP_ALLOWED_HOSTS_REQUIRED). `/health/ready` stays 200.

## Skipped (with reason)
- **BE-013** — do not enable SIRAGPT_REFRESH_TOKEN_ROTATION until a dual-read window exists. Flag remains unset.
- Did not wire BE-081/BE-082 into live handlers (empty prod allowlist would 503; free-shell would break).
- Did not send Last-Event-ID on fresh POST generate of design/marco (would skip events of a *new* run).
- Did not add more SW exact paths: `NEVER_CACHE_PREFIXES` already includes `/api/`.
- Did not rename live webtop containers.

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
