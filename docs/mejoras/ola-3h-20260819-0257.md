# Ola 3H25 — 2026-08-18 21:57 America/Lima (02:57Z 19 Aug)  [ship ~22:35 Lima]

Numero 3H25. Wire del bucle Codex `/code` (`agent-loop.js`) al mismo engine-control que 3H24 puso en `/chat` generate.
Frontend untouched. Backend-only. No OpenRouter. DeepSeek Flash/Pro. No down -v.

## Por que

3H24 reescribio react-agent + agentic-chat-stream + ai.js. Leftover de alto valor: Codex `agent-loop.js` (runs de proyecto en `/code`) seguia su propio loop, sin stopWhen, sin repair, sin token budget, sin telemetry, y el ladder llm-provider podia saltar a openrouter.ai.

## Live files rewritten (not just new helpers)

- `backend/src/services/codex/agent-loop.js` — require engine-control/advance/parity/codex; evaluateStopConditions cada paso; repair+alias en executeCall; cap de observaciones; Promise.allSettled isolate; sleep-time compact + credit release al cerrar; catalog inject surface=code; ckpt CAS; firstByte; engineLive:true.
- `backend/src/services/codex/llm-turn.js` — native DeepSeek Flash/Pro primero en production; nunca openrouter.ai.
- `backend/src/services/codex/llm-provider.js` — skipOpenRouter en production (tests NODE_ENV=test conservan el ladder historico).
- `backend/src/services/codex/tool-scheduler.js` — `apply_patch` es file-write.
- `backend/src/services/error_codes.js`, `observability/public-stream-error.js`, `observability/health-check.js`.
- `docker-compose.production.override.yml` — bind-mounts 3H25.

## New first-party module

- `backend/src/services/agent-runner/engine-codex.js` — puente Codex ↔ engine-control (repair, stopWhen, telemetry, sleep compact, catalog, alias, DeepSeek lock). Rewrite, no vendor copy.

## Proof Codex loop imports them

Container: `require("./src/services/codex/agent-loop").engineLive === true`.
health engine_loop: `codexEngineLive: true`, `codexControl.engineControlWired`, `openrouterGenerate: false`, `deepseekGenerateLock: true`.
generate_path: flash/pro, openrouter:false.

## Shipped (metric deltas)

1. StopWhen + token_budget + wall clock en el loop Codex vivo (cap 1)
2. Tool repair + schema coerce en executeCall de /code (cap 2)
3. Alias fail-closed (bash→run_command) sin remapear nombres del registry (cap 2)
4. unknown_tool tipado ES en Codex (cap 2/11)
5. Tool result cap en observaciones Codex (cap 2)
6. Promise.allSettled isolate: un tool throw no mata el batch (cap 2/7)
7. Circuit + error budget en tools Codex (cap 3)
8. Sleep-time compact al cerrar el run Codex (cap 4)
9. compactByTokenBudget ademas del compact de chars (cap 4)
10. Catalog 7 agentes DeepSeek inyectados en system prompt /code (cap 3)
11. Subagent kind map planner/qa→review, debugger→implement (cap 3)
12. ckpt CAS en context_snapshot (cap 5)
13. apply_patch como file-write en el scheduler (cap 6)
14. Credit hold/settle/release en el run Codex (cap 10)
15. firstByte en el primer narrative_delta Codex (cap 12)
16. Step telemetry por tool_call Codex (cap 12)
17. DeepSeek native first en llm-turn production (cap 12 / candado)
18. skipOpenRouter en llm-provider production (candado)
19. Codigos ES: codex_engine_stop / codex_openrouter_denied / codex_first_byte / git_hunk_ambiguous (cap 11)
20. Health flags 3H25 codexEngineLive
21. engineLive export en agent-loop
22. Tests 28/28 3H25

Tests: 28/28 3H25, 42/42 3H24, 38/38 3H22, 25/25 oss-rewrite, 30/30 codex-llm-provider+llm-turn.

## Live

backend recreated 1x (profile docker-backend, bind-mounts, no image rebuild, no down -v).
live/ready 200. chat/code 200 local+https. engine_loop healthy. generate_path flash/pro openrouter:false. codexEngineLive true.

## Leftovers (skipped a proposito)

- HMAC secret missing (`SIRAGPT_WEBHOOK_HMAC_SECRET`) — no inventar secreto de webhook.
- `mcp_policy` deny-all (`SIRAGPT_MCP_ALLOWED_HOSTS` vacio).
- `chat_run_worker` dormant (`dormant_ok:true`).
- `SIRAGPT_REFRESH_TOKEN_ROTATION` unset (no encender sin dual-read).
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto.
- Sandbox interpreter sigue `local` (gVisor `runsc` es el agent-runner).
- Live Flash TTFB no se midio con DeepSeek (no quemar creditos).
