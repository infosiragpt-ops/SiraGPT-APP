# Ola 3H36 — 2026-08-20 06:57 America/Lima (11:57Z)

Numero 3H36. Caps 1-12 tenian huecos reales tras 3H35 (ids unicos, orphans, JSON chunks, unified diff, ulimit, hold-settle, resume replay). Esta ola ataca allowlist vs nombres inventados, coerce anidado array/object, create-if-missing vs overwrite grande sin backup, sandbox net fail-closed si SANDBOX_NET_ALLOW unset (sin setear el env), kill del process group, SSE retry en el primer evento, no asentar credit hold si el stream nunca abrio, drop de system prompts duplicados, skip de memory facts vacios, stop si texto final Y tools, gzip de tool results grandes, redact de URLs con credenciales, PATCH generate resume token, map DeepSeek 429/402. Frontend untouched. Backend-only. No OpenRouter. DeepSeek Flash/Pro. No down -v.

## Por que

3H35 cerro ids, orphans, JSON streaming, unified diff, ulimit, stdout/stderr, hold-settle, resume replay, compact pin+user, drop cancel, per-tool rate, media cap, max_tokens clamp, clock-skew, requestId. Leftovers de alto valor vs Claude Code/Cowork: el modelo inventaba tool names; args anidados llegaban como strings; write pisa archivos grandes sin backup; sandbox net no fail-closed de verdad; kill solo al pid; EventSource sin retry; hold se asentaba aunque el SSE nunca abriera; system prompts se duplicaban; facts en blanco inflaban memoria; texto final + tools seguian el tool-chain; results enormes crudos; URLs con token en query; generate sin resume id; 429/402 de DeepSeek sin codigo estructurado.

## Live files rewritten (not just new helpers)

- `backend/src/services/agent-runner/engine-adapter.js` — 3H35 intacto + 15 helpers 3H36.
- `backend/src/services/agent-runner/loop.js` — allowlist, nested coerce, large overwrite, no-settle-unopened, dup system, empty facts, final+tools, gzip, URL redact, DeepSeek 429/402.
- `backend/src/services/react-agent.js` + `agentic-chat-stream.js` — allowlist + final+tools + settle-if-opened en /chat.
- `backend/src/utils/sse-writer.js` — `retry:` en el primer evento.
- `backend/src/services/agent-gateway/queue.js` — PATCH generate resume token.
- `backend/src/services/agent-runner/engine-gateway.js` — allowlist + 429/402 + resume token.
- `backend/src/services/sandbox/local-sandbox.js` — net fail-closed + kill PGID.
- `backend/src/services/error_codes.js`, `observability/public-stream-error.js`.
- `docker-compose.production.override.yml` — bind-mount ola-3h36-invariants.test.js.

## Proof live imports them

Container: `engine_loop.adapter. === true` y `wave: 3H36`.
`generate_path` flash/pro `openrouter: false`.
`sandbox_backends.local: true` (`usesRunsc: false`).
`chat_run_worker` skipped `dormant_ok: true`.

## Shipped (metric deltas)

1. allowlistToolName (cap 1)
2. coerceNestedArrayObjectTypes (cap 2)
3. createIfMissingOrRefuseLargeOverwrite (cap 6)
4. sandboxNetFailClosed (cap 7) — no se seteo SANDBOX_NET_ALLOW
5. killProcessGroup (cap 7)
6. sseRetryFieldOnFirstEvent (cap 8)
7. settleCreditHoldIfStreamOpened (cap 10)
8. dropDuplicateSystemPrompts (cap 4)
9. skipEmptyWhitespaceMemoryFacts (cap 4)
10. stopIfFinalTextWithTools (cap 1)
11. gzipToolResultOverSize (cap 2)
12. redactUrlsWithCredentials (cap 11)
13. patchGenerateResumeToken (cap 5/9)
14. health adapter.wave=3H36 (cap 12)
15. mapDeepSeekHttpError 429/402 (cap 3/11)
16. Tests 3H36 + 3H31–3H35 green
17. DeepSeek lock intacto
18. Interpreter `local`
19. chat_run_worker dormant
20. Health flags 3H35+3H36 (`wave: 3H36`)

## Live

backend recreated 1x (compose.prod + production.override, bind-mounts, no image rebuild, no down -v, no frontend rebuild).
live/ready 200. chat/code 200. engine_loop healthy. generate_path flash/pro openrouter:false.
computer/health 200. Frontend untouched.

## Leftovers (skipped a proposito)

- HMAC secret missing (`SIRAGPT_WEBHOOK_HMAC_SECRET`) — no inventar secreto de webhook.
- `mcp_policy` deny-all para hosts nuevos (`SIRAGPT_MCP_ALLOWED_HOSTS` vacio). Already-connected reuse no abre todo.
- `chat_run_worker` dormant (`dormant_ok:true`) — encenderlo cambiaria generate a cola async y romperia SSE in-process.
- `SIRAGPT_REFRESH_TOKEN_ROTATION` unset (no encender sin dual-read).
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto. 3H36 lo fail-closea.
- Sandbox interpreter sigue `local` (gVisor `runsc` es el agent-runner docker driver).
- `create_file` / `delete_file` / `move_file` / `notebook_edit` no existen live.
- Live Flash TTFB no se midio con DeepSeek (no quemar creditos). p50/p95 siguen scripted.
