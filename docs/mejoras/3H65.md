# 3H65 — anti-loop leftover, tool hygiene, file rollback, DeepSeek 402/413, cola SSE (no UI)

Ola engine-only posterior a 3H64. **No inventa nombres overlay** que choquen con exports vivos de 3H59–3H64. Compone **33 helpers #388 vivos** de `engine-adapter.js` y los aplica fail-closed en el hot path (`loop.js`, `ai.js` generate, `sse-writer.js`).

## Live #388 names cableados (33)

### A. Anti-loop / planning leftover

1. `detectDagCycle`
2. `rejectToolCallCycleAtoBtoA`
3. `deadLetterSameToolAfterN`
4. `identicalObservationLoopCut`
5. `budgetHintEveryFiveSteps`
6. `remainingStepBudgetReminder`
7. `maxConcurrentSubagents`
8. `maxSubagentDepth`
9. `maxInflightToolsPerSession8`
10. `perToolRateLimit`

### B. Tool arg/result hygiene + secrets

11. `capToolArgBytes`
12. `capToolArgBytes32KiB`
13. `enforceAdditionalPropertiesFalse`
14. `validateEnumArgs`
15. `validateToolResultShape`
16. `gzipToolResultOverSize`
17. `clampToolResultWithHash`
18. `redactSecretsInToolResult`
19. `redactAuthorizationBearerInToolResults`
20. `skipDuplicateWebFetchSameUrlTurn`

### C. File edit leftover after 3H63 checksum

21. `rollbackLastFileEdit`
22. `rollbackLastNFileEdits`
23. `afterWriteTestHint`
24. `createIfMissingOrRefuseLargeOverwrite`
25. `patchContextLinesMustMatch`

Uniqueness de `str_replace` (`old_str occurs more than once` / `not found`) **no** se trata como timeout ni syntax-revert.

### D. Credits / DeepSeek errors leftover

26. `neverChargeIfCancelledBeforeFirstToken`
27. `mapDeepSeekHttpError`
28. `neverRetry402`
29. `neverRetry413`

HTTP 4xx distinto de 429 no se reintenta. HTTP 402 sigue parando el loop con `stoppedReason: llm_402` (el código 3H65 `quota_exhausted` / `credit_ceiling` va en `errorCode`). No se mapea a `no_output`.

### E. SSE / session leftover

30. `fairQueueStarvationBound`
31. `maxQueuedGenerate16`
32. `requireSessionEventSeqIncrease`
33. `skipHeartbeatIfWriteWouldBlock`

## Apply layer (nombres 3H65, no colisionan)

`applyAntiLoopGuardsClosed` / `applyToolArgHygieneClosed` / `applyToolResultHygieneClosed` / `applyFileEditGuardsClosed` / `applyDeepSeekCreditGuardsClosed` / `applyGenerateQueueGuardsClosed` / `applySseSessionGuardsClosed` viven en `engine-3h65.js`. El hot path también nombra los helpers #388 vivos.

## Wiring fail-closed

- `loop.js` — DAG / A-B-A / dead-letter / observation cut; caps de subagente e inflight; rate limit; args/enum/result; redact secrets+Bearer; rollback de edits; 402/413 never-retry (`llm_402` contract); never-charge si cancel pre-token. Hints de presupuesto son **eventos `budget_hint`** (no reescriben el system). `gzipToolResultOverSize` se llama pero no sustituye la observación por `[gzip N->M]`. `validateEnumArgs` solo ve keys presentes/required (un `button` opcional omitido no corta `computer_click`).
- `ai.js` generate — cola cap 16 + starvation bound; never-charge pre-token; skip heartbeat si el write bloquearía
- `sse-writer.js` — seq monotónico + skip heartbeat under backpressure
- `version.js` — `GET /api/version/latency` marca ola 3H65 (mismo ring persistido)
- `engine-adapter.js` — snapshot `wave: 3H65`, bind si el nombre está libre (`engine-3h65` en `loadOptionalEngineWave`)

## Tests

`backend/tests/ola-3h65-invariants.test.js` + `engine-hotpath-wire.test.js`. Snapshots 3H59–3H64 aceptan **3H65**.

```bash
cd backend && node --test tests/ola-3h65-invariants.test.js tests/ola-3h64-invariants.test.js tests/engine-hotpath-wire.test.js
```

`3H65-H-001` usa una **EventSource de Node** (API `onmessage` / `lastEventId` / reconnect `Last-Event-ID`) contra un stream generate mock. No es Playwright de browser.

`3H65-I-001` **lee** el ring persistido de muestras scripted. No inventa p50/p95 de Flash.

## Fuera de alcance

Sin UI. Sin OpenRouter. DeepSeek Flash/Pro only. EventSource real en browser sigue pendiente. p50/p95 bajo carga VPS real (≥200 muestras de producción) sigue sin corpus: el instrumento ya persiste; esta ola no inventa números.
