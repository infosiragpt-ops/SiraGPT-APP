# Ola 3H20 — 2026-08-18 07:13 America/Lima (2026-08-18T12:13Z)

Motor-only: paridad Claude Code / Cowork en el **engine**, no en la UI.
Numero 3H20 (3H19 = `ola-3h-20260818-0857.md`, 3H18 = `ola-3h-20260818-0557.md`, 3H17 = `ola-3h-20260818-0325.md`, 3H16 = `ola-3h-20260818-0210.md`).
Run programado 06:57 Lima 8/18/2026 («Mejora constante chat y code»).

**Frontend skipped / untouched.** Container id `e2d0a0c2c064be49f88d9bb2c22de9598556f1acac8dddd08ea5b8f518fd4755` created `2026-08-18T02:36:50Z` (21:36 Lima) — last user-facing frontend expand-corner. Zero frontend rebuilds. Did not touch chat Video, duration chips, fal, `ai.js` generate catalog, webtop/computer/noVNC.

This batch is the next layer of the 12-capability rubric after 3H19: fence heartbeat + stale steal (crashed pid cannot pin a session forever), DLQ replay with exponential backoff + jitter, memory score+recency hybrid, pgvector retrieve fail-closed, checkpoint blob gzip, SSE replay-window metrics + event-hash TTL cleanup, credit audit vs ledger reconciliation + turn ceiling, per-tool timeout overrides, sandbox tmpfs size cap, stdout rate cap, tool enum fail-closed, classified ES codes, health probes (scripted, no Fal, no paid Flash). Did not invent HMAC secret, MCP allowlist, rotation flag, or `SIRAGPT_SANDBOX_NET_ALLOW`.

## Shipped this run (38 items)

### Backend (recreated `backend` only, once; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H20-BE-001** (cap 1/5) `heartbeatFence` refreshes matching token. `engine-layer.js`. Verified: invariant.
- **3H20-BE-002** (cap 1/5) heartbeat mismatch → `fence_expired`. Verified: invariant.
- **3H20-BE-003** (cap 1/5) `stealStaleFence` after TTL. Verified: second acquire ok.
- **3H20-BE-004** (cap 1/5) live heartbeat refuses steal (`fence_conflict`). Verified: invariant.
- **3H20-BE-005** (cap 4) `rankMemoryByScoreRecency` prefers recent high score. Verified: invariant.
- **3H20-BE-006** (cap 4) hybrid still drops low-score. Verified: keyword kept.
- **3H20-BE-007** (cap 5) `gzipCheckpointBlob` compresses large state. Verified: bytes < raw.
- **3H20-BE-008** (cap 5) gzip/gunzip roundtrip. Verified: invariant.
- **3H20-BE-009** (cap 5) tiny blobs skip gzip. Verified: invariant.
- **3H20-BE-010** (cap 8/12) `replayWindowMetrics` dropped/truncated. Verified: 12→5, code `replay_window`.
- **3H20-BE-011** (cap 8/9) `pruneHashTtl` expires old hashes. Verified: invariant.
- **3H20-BE-012** (cap 9) `rememberHashTtl` second → `duplicate_event`. Verified: invariant.
- **3H20-BE-013** (cap 10) audit vs ledger `credit_mismatch` if ledger behind. Verified: invariant.
- **3H20-BE-014** (cap 10) audit behind → `no_double_charge`. Verified: invariant.
- **3H20-BE-015** (cap 2) `toolTimeoutMs` per-tool overrides. Verified: bash 30s / write_file 8s / override 20ms.
- **3H20-BE-016** (cap 2) `withToolTimeout` → `tool_timeout`. Verified: invariant.
- **3H20-BE-017** (cap 7) `sandboxTmpfsCap` default 64MiB when unset. Verified: invariant.
- **3H20-BE-018** (cap 6/7) `assertTmpfsBudget` → `tmpfs_exceeded`. Verified: invariant.
- **3H20-BE-019** (cap 3) `scheduleDlqReplay` exponential, jitter off. Verified: 250/500/1000.
- **3H20-BE-020** (cap 3) exhausted DLQ skip. Verified: `dlq_exhausted`.
- **3H20-BE-021** (cap 4) `retrieveMemoryFailClosed` pgvector fail-closed. Verified: hits=[].
- **3H20-BE-022** (cap 4) generic retrieve error recovers empty (not fail-closed). Verified: invariant.
- **3H20-BE-023** (cap 2) `assertToolEnum` fail-closed. Verified: mode=hack.
- **3H20-BE-024** (cap 10) `assertCreditCeiling` fail-closed. Verified: 110>100.
- **3H20-BE-025** (cap 7) `capStdoutRate` over cap. Verified: `stdout_rate`.
- **3H20-BE-026** (cap 1/5) live loop steals stale fence then runs. Verified: `stoppedReason=final`.
- **3H20-BE-027** (cap 2) live loop tool timeout does not hang. Verified: called=1, `tool_timeout`.
- **3H20-BE-028** (cap 4) live loop score+recency ranks memory. Verified: memoryHits=2.
- **3H20-BE-029** (cap 4/11) live loop `pgvector_failed` classified, continues. Verified: event + final.
- **3H20-BE-030** (cap 2) live loop enum reject does **not** call executor. Verified: called=0.
- **3H20-BE-031** (cap 11) error_codes layer taxonomy. Verified: fence_expired/dlq_replay/pgvector/tmpfs/credit_*/stdout_rate/hash_expired.
- **3H20-BE-032** (cap 11) classifyLoopError + public-stream-error ES, no traces. Verified: invariant.
- **3H20-BE-033** (cap 12) `/health` `engine_loop` layer flags. Verified: live JSON.
- **3H20-BE-034** (cap 7) session-manager snapshot `tmpfsCap` 64MiB. Verified: invariant.
- **3H20-BE-035** (cap 5) durable put gzipes then get inflates. Verified: invariant.
- **3H20-BE-036** (cap 8/9) event-log hash TTL prune. Verified: invariant.
- **3H20-BE-037** (cap 3) session-dlq `replayWithJitter`. Verified: delay 250ms.
- **3H20-BE-038** (cap 1–12) loop.js / durability / engine-layer wired. Verified: source markers. No openrouter.ai.

## Live proof (backend) — 07:13 Lima / 12:13 UTC
- Recreated backend only (1×). Frontend id unchanged (`e2d0a0c2c064…` 21:36 Lima).
- Container created **07:13 Lima** (2026-08-18T12:13:04Z), healthy, pid 1
- `/health/live` 200, `/health/ready` 200
- Full `/health` status degraded remains pre-existing non-critical `mcp_policy`. No `openrouter.ai`
- `engine_loop` healthy `{fenceHeartbeat, staleFenceSteal, scoreRecency, blobGzip, replayMetrics, hashTtl, auditLedger, toolTimeoutOverride, tmpfsCap, dlqReplay, pgvectorFailClosed, creditCeiling, turnFence, coercionBounds, networkDeny}` — ttfb/turnEnd hydrated from Redis (scripted overhead, not Flash)
- `generate_path` healthy (F2, deepseek-v4-flash / deepseek-v4-pro, openrouter: false)
- `gateway_queue` healthy `{lanes:0,pending:0,maxPending:8,order:fifo}`
- `GET /api/gateway/status` queueHonesty `{order:fifo, maxPending:8}` models flash/pro only, surfaces chat+code
- POST `/api/webhooks/inbound` → 503 `{code:hmac_secret_missing, retryable:false}`
- POST `/api/ai/generate` unauthenticated → 401 `access_token_required`
- POST `/api/ai/stop-stream` unauthenticated → 401 `access_token_required`
- http://127.0.0.1:3000/chat 200, /code 200; https://siragpt.com/chat 200, /code 200
- Invariants inside live backend: **38/38** `ola-3h20` + **38/38** `ola-3h19` + **35/35** `ola-3h18` + **35/35** `ola-3h17` + **26/26** `ola-3h16` (134/134 prior)
- Redis smoke (live kv fence+gzip): `{lockA:true, hb:true, liveCode:"fence_conflict", staleStolen:true, lockC:true, gzip:true, roundtrip:true}`
- Existing webtops still up. No Fal video jobs.

## Services
- Recreated: backend only (1×)
- Untouched: frontend, db, redis, caddy, runner, webtops
- Never ran `down` or `down -v`
- No frontend rebuild (count = 0)

## Honest partials
- Inbound HMAC secret still unset (503 fail-closed). Do not invent a key.
- MCP deny-all until Luis sets `SIRAGPT_MCP_ALLOWED_HOSTS`.
- `chat_run_worker` dormant skipped, `dormant_ok:true`.
- Refresh-token rotation flag unset.
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto. No inventar hosts.
- Latency hydrate is **scripted engine overhead** (mock LLM). Not DeepSeek Flash TTFB. Do not report as Flash p50.
- PG put remains fail-open; Redis path verified for post-recreate resume + fence heartbeat/steal.
- Sandbox code-interpreter backend still `local`. tmpfs cap is policy (64MiB default); we do not remount a live tmpfs.
- Stale-fence steal requires a recorded heartbeat older than TTL. A pre-3H20 fence without heartbeat stays conflict (does not invent a steal).
- RAG embeddings still OpenAI vector space (retrieve in-loop uses injectable/keyword/LTM + ACL + score + recency; pgvector errors fail-closed). Generate path unchanged.

## Skipped (with reason)
- **All FE** — last user-facing frontend is expand-corner 21:36 Lima. Constraint: do not rebuild.
- Did not set HMAC / MCP hosts / rotation / SANDBOX_NET_ALLOW.
- Did not touch `ai.js` / video.js / fal / duration chips.
- Did not enable AGENT_RUNNER_ASYNC.
- Did not burn DeepSeek or Fal to measure live Flash TTFB.

## Rubric moved
See `docs/mejoras/benchmark-capacidades.md` v3H20. 12/12 capabilities moved (fence heartbeat+steal, per-tool timeout, DLQ replay jitter, score+recency, pgvector fail-closed, blob gzip, tmpfs cap, replay metrics, hash TTL, audit vs ledger, new ES codes, health flags).

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
Do not put a dummy value in SIRAGPT_MCP_ALLOWED_HOSTS or SIRAGPT_WEBHOOK_HMAC_SECRET or SIRAGPT_SANDBOX_NET_ALLOW.
