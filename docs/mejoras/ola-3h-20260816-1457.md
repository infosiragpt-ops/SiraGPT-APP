# Ola 3h5 — 2026-08-16-1457 ship log (America/Lima)

Backup: `/opt/siragpt/backups/ola-3h5-20260816T1457Z` (override snapshot + edited FE/BE trees)
UI lock: no layout moves, no Configuración avanzada, no composer/nav/office/Proactivo placement. No compose down. No `down -v`. No repo clone. No OpenRouter. No invented keys.
DeepSeek V4 Flash/Pro only. `SIRAGPT_REFRESH_TOKEN_ROTATION` stays unset (BE-013 still skipped).
Ola-200 A–G, 3H / 3H2 / 3H3 / 3H4 batches not redone. Numbered 3H5-BE/FE so it does not collide.

This batch wires leftover gaps into live handlers (not helper-only unused functions): design/marco POST Last-Event-ID skip on fresh generate, OpenRouter leftovers still alive in design/viz/plan/math/artifact/marco generators, credit abort-hold on images/paraphrase, PII in email/image logs, sandbox idle on list/collect, gateway memory/skills/cron HTTP leftovers, verification `create_chart`, rate-limit remaining generate-adjacent paths, health secret keys.

## Shipped this run (34 items)

### Backend (recreated `backend` only; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H5-BE-001** remaining stream leftover — `attachSseIds` ignores Last-Event-ID on fresh POST (design/marco POST skipped last time because a stale id would offset a *new* run). GET reconnect still resumes.
- **3H5-BE-002** design-generator leftover candado — `clientForModel` is `strictDeepSeekClientForModel` (never builds OpenRouter/OpenAI/Gemini clients). Default `deepseek-v4-flash`.
- **3H5-BE-003** viz-generator leftover candado — same DeepSeek-only client.
- **3H5-BE-004** plan-generator leftover candado — same DeepSeek-only client.
- **3H5-BE-005** math-solver leftover candado — same DeepSeek-only client.
- **3H5-BE-006** artifact-generator leftover candado — same DeepSeek-only client.
- **3H5-BE-007** design route leftover — generate refuses unless `DEEPSEEK_API_KEY` is usable (no OpenRouter/OpenAI/Gemini key check).
- **3H5-BE-008** marco-teorico leftover — live POST `/generate` uses `createNativeDeepSeekClient` + `resolveNativeDeepSeekModel`; saved chat model `deepseek-v4-flash` (was `gpt-4o` / `OPENAI_API_KEY`).
- **3H5-BE-009** cron leftover — `createJob` fail-closed without `userId`; `tick` skips unscoped jobs (they could fire but never be listed).
- **3H5-BE-010** cron leftover — `deleteJob({ id, userId })` scoped + protocol `cron.delete` + HTTP `DELETE /api/gateway/cron`.
- **3H5-BE-011** skills leftover — `liveSkills.persist` + protocol `skills.persist` + HTTP `POST /api/gateway/skills` wired to `persistUserSkill`.
- **3H5-BE-012** skills leftover — HTTP `GET /api/gateway/skills/load?name=` wired to existing `skills.load`.
- **3H5-BE-013** memory leftover — HTTP `POST /api/gateway/memory` persistEpisode (userId from `req.user` only).
- **3H5-BE-014** memory leftover — `deleteUserEpisodes` + HTTP `DELETE /api/gateway/memory` (never cross-user).
- **3H5-BE-015** gateway leftover — HTTP `POST /api/gateway/agent/wait` wired to existing `waitForRun`.
- **3H5-BE-016** gateway leftover — `abortSession(sessionKey, reason, actorUserId)`: when HTTP passes actor, owned sessions cannot be aborted by a spoofed/empty actor.
- **3H5-BE-017** gateway leftover — HTTP `connect` stamps `conn.userId` from `actorUserId(req)`, overwriting body spoof.
- **3H5-BE-018** gateway leftover — protocol `agent` prefers `conn.userId` over params.
- **3H5-BE-019** rate-limit leftover — shared generate budget adds `/api/figma`, `/api/cowork/auto-file`, `/api/cowork/enrich`, `/api/cowork/sessions`, `/api/se-agents`.
- **3H5-BE-020** rate-limit leftover — `/api/ai/video-cancel` and cowork `/reset` `/compact` skip the generate limiter (stop ≠ generate).
- **3H5-BE-021** credit-race leftover — `attachAbortHoldOnClose` on images generate/variation/upscale (the live `chargeCredits` routes).
- **3H5-BE-022** credit-race leftover — `attachAbortHoldOnClose` on paraphrase handler.
- **3H5-BE-023** PII leftover — `maskEmail` on live email send logs (`a***@domain`); no raw `user.email` in those lines.
- **3H5-BE-024** PII leftover — ai-service image generate logs `promptLen`, never the raw prompt.
- **3H5-BE-025** health secrets leftover — `stripHealthSecrets` also redacts `OPENAI_API_KEY` / `GEMINI_API_KEY` / `STRIPE_SECRET` / `WEBHOOK_SECRET` shapes and webhook/stripe/openai keys.
- **3H5-BE-026** PII leftover — structured-logger keys include `passport` / `dob` / `address` / `national_id` / `routing_number`.
- **3H5-BE-027** sandbox leftover — idle timer also bumps on `listFiles` / `collectOutputs`. Never touches webtops.
- **3H5-BE-028** remaining stream — attribution-stream-emitter stamps `id: N` via `attachSseIds` (live cowork attribution SSE).
- **3H5-BE-029** Word/PPT/verify leftover — EDIT_TOOLS adds live `create_chart` (agentic visual-media tool).
- **3H5-BE-030** protocol leftover — METHODS `skills.persist` / `memory.persist` / `memory.delete` / `cron.delete` dispatched in the live gateway.

### Frontend (ONE rebuild only; no UI moves; /chat and /code layout unchanged)
- **3H5-FE-001** design-service leftover — live `generate()` sends `Cache-Control: no-store`, does **not** send Last-Event-ID on a fresh POST, clamps model to DeepSeek Flash/Pro.
- **3H5-FE-002** marco-teorico-service leftover — same no-store / no Last-Event-ID on fresh POST / DeepSeek clamp.
- **3H5-FE-003** sse-client leftover — `freshGenerateHeaders()` + `clampDeepSeekModel()` wired into those two live generate handlers (not unused exports).
- **3H5-FE-004** client-logs leftover — SENSITIVE_KEY_RE adds `passport` / `dob` / `address` / `national_id` / `routing_number`.

## Live proof (backend) — 10:19 PT
- `docker compose ... up -d --no-deps --force-recreate backend` — Recreated + Started
- Container created **10:19 PT** (2026-08-16T15:19:43Z), healthy
- `/health/live` 200, `/health/ready` 200
- Full `/health`: deepseek healthy (dummy: false, api.deepseek.com), r2_artifacts healthy (init: ok), generate_path healthy (path: F2, models deepseek-v4-flash / deepseek-v4-pro, openrouter: false), gvisor_runsc healthy (runtime: runsc)
- `stripHealthSecrets` live: no `DEEPSEEK_API_KEY` literal and no `openrouter.ai` in health JSON
- GET `/api/gateway/status` → ok, models flash/pro only, `queueHonesty: {lanes:0,writers:0,aborted:0,abortedRuns:0}`, `scoped: {memory:true,skills:true,cron:true}`, deadLetterCount: 0, no openrouter
- POST `/api/gateway/agent/wait` without runId → 400 `{code:bad_request}`
- DELETE `/api/gateway/cron?id=x` without user → `{deleted:false,error:user_required}`
- POST `/api/gateway/abort` without sessionKey → 400 `{code:bad_request}`
- Rotation flag unset (BE-013 skip)
- Existing webtops still up: sira-dpc-cms2hkubp0013oz01mzf0m49e-ceo-office (19h), sira-dpc-testproj-sales (22h)
- Invariants inside live backend: **78/78 passed** (`node --test tests/ola-3h5-invariants.test.js tests/ola-3h4-invariants.test.js tests/ola-3h3-invariants.test.js tests/ola-3h2-invariants.test.js tests/ola-3h-invariants.test.js`)

## Live proof (frontend) — 10:27 PT
- One rebuild only; previous 3h4 image stayed up until the new image was ready
- Compiled successfully (~4.3 min next build + postbuild-slim); image `siragpt-frontend:latest` Built
- Frontend container created **10:27 PT** (2026-08-16T15:27:38Z), healthy, BUILD_ID `UW5-tisUjuW8zZH_-YVu9`
- http://127.0.0.1:3000/ 200
- https://siragpt.com/ 200
- https://siragpt.com/chat 200
- https://siragpt.com/code 200
- https://siragpt.com/api/health/live 200
- SW live SCHEMA_VERSION `sira-v4` + `/api/` never-cache prefix (not padded)
- Bundle markers: `deepseek-v4-flash` (chunk 7806 + 37621 + chat page `50d96ec0`, 3H5-FE-001/003), `no-store` (chunk 7806 + design `[id]` page, 3H5-FE-001/002), Last-Event-ID (chunk 7806), `passport` (chunk 37621, 3H5-FE-004)

## Services
- Recreated: backend, frontend (frontend via the single 3h5 rebuild)
- Untouched: db, redis, caddy, runner
- Never ran `down` or `down -v`

## Honest partials
- **3H5-BE-016** ownership check runs when HTTP passes `actorUserId`. Internal `abortSession(key, reason)` (no third arg) stays compatible with 3H2 tests / protocol.
- **3H5-BE-010/011/013/014** `/api/gateway` is still mounted without `authenticateToken`. Without `req.user`, `actorUserId` is empty → persist/delete fail-closed (`user_required`). Built-in skill catalog can still list when persist returns [].
- **3H5-BE-002..006** leftover UI model dropdowns may still *send* `gpt-4o` / OpenRouter slugs; live generators remap to DeepSeek Flash/Pro and never construct those clients.
- **3H5-BE-027** idle wrap applies to sandboxes created after this recreate; existing long-running doc containers keep previous lifetime. Webtops untouched.
- Health overall `degraded` remains the pre-existing non-critical `mcp_policy` (MCP_ALLOWED_HOSTS_REQUIRED). `/health/ready` stays 200.
- Did not add more SW exact paths: `NEVER_CACHE_PREFIXES` already includes `/api/`.

## Skipped (with reason)
- **BE-013** — do not enable SIRAGPT_REFRESH_TOKEN_ROTATION until a dual-read window exists. Flag remains unset.
- Did not wire BE-081/BE-082 into live handlers (empty prod allowlist would 503; free-shell would break).
- Did not send Last-Event-ID on fresh POST generate of design/marco (would skip/offset events of a *new* run) — backend now ignores it on POST; FE does not send it.
- Did not rename live webtop containers.

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
