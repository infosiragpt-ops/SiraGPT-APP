# Ola 3h2 — 2026-08-16-0557 ship log (America/Lima)

Backup: `/opt/siragpt/backups/ola-3h2-20260816T0557Z` (override snapshot + edited FE/BE trees)
UI lock: no layout moves, no Configuración avanzada, no composer/nav/office/Proactivo placement. No compose down. No `down -v`. No repo clone. No OpenRouter. No invented keys.
DeepSeek V4 Flash/Pro only. `SIRAGPT_REFRESH_TOKEN_ROTATION` stays unset (BE-013 still skipped).
Ola-200 A–G and previous 3h batch (3H-BE-001..023 / 3H-FE-001..008) not redone. Numbered 3H2-BE/FE so it does not collide.

## Shipped this run (32 items)

### Backend (recreated `backend` only; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H2-BE-001** health leftover — `sendHealthReport` now runs `stripHealthSecrets` (env/api_key/token stripped before JSON).
- **3H2-BE-002** rate-limit leftover — `makeJwtAwareKeyGenerator` actually uses `isSharedGenerateAgentPath` / `sharedGenerateAgentTaskKey`.
- **3H2-BE-003** shared budget leftover — generate-adjacent paths share one user key (`/api/doc`, `/api/generate-document`, `/api/search/agentic`, `/api/cowork/analyze`, `/api/codex`, `/api/gateway/agent`, `/api/agent/run`).
- **3H2-BE-004** remaining SSE helper — `attachSseIds(res, req)` prepends `id: N` to leftover `data:`/`event:` frames and honors Last-Event-ID.
- **3H2-BE-005** chats pending-run SSE — `writeSse` emits `id:` + resume from Last-Event-ID.
- **3H2-BE-006** doc.js generate + idempotent replay streams attach `id:`.
- **3H2-BE-007** generate-document.js Word SSE attaches `id:`.
- **3H2-BE-008** search-agentic.js SSE attaches `id:`.
- **3H2-BE-009** search.js SSE attaches `id:`.
- **3H2-BE-010** cowork analyze-stream + analyze-pro/stream attach `id:`.
- **3H2-BE-011** agent.js `/run` SSE attaches `id:`.
- **3H2-BE-012** upload-progress-sse.js attaches `id:`.
- **3H2-BE-013** Word/PPT verify leftover — EDIT_TOOLS adds `append_text_slide`, `create_pptx`, `create_xlsx`, `replace_text`, `insert_image`, `edit_slide`, `set_slide_title`, `apply_edit`.
- **3H2-BE-014** sandbox timeout leftover — `attachIdleTimeout` auto-`destroy()` after TTL (default 10 min); never touches webtops.
- **3H2-BE-015** per-session queue leftover — `abortSession()` fail-closed: enqueue rejects `session_aborted` until next `claimWriter`.
- **3H2-BE-016** gateway fail-closed abort — `abortSession` + executeTurn skips persist/commit when aborted.
- **3H2-BE-017** OpenClaw leftover rewritten — protocol `agent.abort` + HTTP `POST /api/gateway/abort`.
- **3H2-BE-018** Hermes searchable-memory leftover — `searchUserEpisodes` + TTL prune; persist/search pass `userId` (never cross-user).
- **3H2-BE-019** Hermes skill-persist leftover — `deletePersistedSkill` + path-escape; gateway list/load scoped by userId.
- **3H2-BE-020** cron-as-agent-turn leftover — overlap skip, DeepSeek-only `assertCronModel`, no OpenRouter.
- **3H2-BE-021** gateway event-ring leftover — stamp `seq`/`id` + TTL prune (max 200 / 30 min).
- **3H2-BE-022** OAuth error-map leftover — remaining social codes (`user_denied`, `consent_required`, …) + figma errors mapped.
- **3H2-BE-023** PII leftover — no prompt/email in figma/telegram/search-mailto/generate-document logs; logger keys include completion/refresh_token/mailto.
- **3H2-BE-024** credit-race leftover — generate-document `model_forbidden` abort calls `completeGenerateHold(..., aborted: true)` (no capture).
- **3H2-BE-025** computer-use audit leftover — `POST /resume` now audits (start/stop already shipped).
- **3H2-BE-026** agent.js leftover — refuse missing `DEEPSEEK_API_KEY` (`no_llm`); no `OPENAI_API_KEY` required.

### Frontend (ONE rebuild only; no UI moves; /chat and /code layout unchanged)
- **3H2-FE-001** public/sw.js NEVER_CACHE_EXACT leftover generate paths (`/api/doc/generate`, `/api/generate-document`, `/api/search`, `/api/gateway/agent`, `/api/gateway/abort`, `/api/cowork/analyze-pro/stream`, `/api/files/progress-stream`, `/api/chats/`). SCHEMA_VERSION `sira-v3`.
- **3H2-FE-002** sse-client leftover resume helpers: `docStreamResumeHeaders`, `searchStreamResumeHeaders`, `uploadProgressResumeHeaders`.
- **3H2-FE-003** `generateIdempotencyKey` leftover uses `safeUUID()` (WebView fallback).
- **3H2-FE-004** client-logs leftover — redact emails in string values.
- **3H2-FE-005** cowork-api leftover — `coworkAnalyzeProStreamResumeHeaders`.
- **3H2-FE-006** agentic-search leftover — fetch sends Last-Event-ID; iterator persists event ids (Wave G helper was unused at the header).

## Live proof (backend) — 01:21 PT
- `docker compose ... up -d --no-deps --force-recreate backend` — Recreated + Started
- Container created **01:21 PT** (2026-08-16T06:21:19Z), healthy
- `/health/live` 200, `/health/ready` 200
- Full `/health`: deepseek healthy (dummy: false, api.deepseek.com), r2_artifacts healthy (init: ok), generate_path healthy (path: F2, models deepseek-v4-flash / deepseek-v4-pro, openrouter: false), gvisor_runsc healthy (runtime: runsc)
- `stripHealthSecrets` live: no `DEEPSEEK_API_KEY` in health JSON
- GET `/api/gateway/status` → ok, models flash/pro only, deadLetterCount: 0, no openrouter
- Rotation flag unset (BE-013 skip)
- Existing webtops still up: sira-dpc-cms2hkubp0013oz01mzf0m49e-ceo-office (10h), sira-dpc-testproj-sales (13h)
- Invariants inside live backend: **30/30 passed** (`node --test tests/ola-3h2-invariants.test.js tests/ola-3h-invariants.test.js`) — previous 3h batch still green

## Live proof (frontend) — 01:30 PT
- One rebuild only; previous 3h image stayed up until the new image was ready
- Compiled successfully (~5.5 min next build + postbuild-slim); image `siragpt-frontend:latest` Built
- Frontend container created **01:30 PT** (2026-08-16T06:30:09Z), healthy, BUILD_ID `XZms_mRusUonzjOCzP81w`
- http://127.0.0.1:3000/ 200
- https://siragpt.com/ 200
- https://siragpt.com/chat 200
- https://siragpt.com/code 200
- https://siragpt.com/api/health/live 200
- SW live SCHEMA_VERSION `sira-v3` + `/api/generate-document` + `/api/gateway/abort` + `/api/cowork/analyze-pro/stream` (3H2-FE-001)
- Bundle markers: Last-Event-ID (chunk 7806), `REDACTED:email` (chunk 37621, 3H2-FE-004)
- POST `/api/gateway/abort` without sessionKey → 400 `{code:bad_request}` (3H2-BE-017 fail-closed)

## Services
- Recreated: backend, frontend (frontend via the single 3h2 rebuild)
- Untouched: db, redis, caddy, runner
- Never ran `down` or `down -v`

## Honest partials
- **3H2-BE-014** idle timer is per sandbox object; existing long-running doc containers created before this recreate keep their previous lifetime.
- **3H2-BE-017** abort is fail-closed on the gateway queue; ChatInterface Stop still uses `/api/ai/stop-stream` (already shipped). New path is `POST /api/gateway/abort`.
- **3H2-FE-002/005** extra resume helpers are exported; agentic-search (3H2-FE-006) is the fetch-site wire that lands in the chat bundle.
- Health overall `degraded` remains the pre-existing non-critical `mcp_policy` (MCP_ALLOWED_HOSTS_REQUIRED). `/health/ready` stays 200.

## Skipped (with reason)
- **BE-013** — do not enable SIRAGPT_REFRESH_TOKEN_ROTATION until a dual-read window exists. Flag remains unset.
- Did not wire BE-081/BE-082 into live handlers.
- Did not rename live webtop containers.

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
