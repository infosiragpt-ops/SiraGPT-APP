# 3H61 — checkpoints, sandbox cleanup, cancel accounting (no UI)

Ola engine-only posterior a 3H60 + hot path #399. **No inventa nombres overlay** que choquen con exports vivos de 3H59/3H60. Reexporta los helpers #388 / 3H59 por `engine-adapter` y los llama por nombre en `loop.js` y generate SSE.

## Live #388 names (adapter → loop / generate)

Estos vivían solo como flags `HELPERS` en `engine-3h59.js` (cero callsites en loop/generate). 3H61 los exporta y los usa:

1. `checkpointHookBeforeMutatingTool` — `loop.js` + `doc-agent/tools.js` antes de writes
2. `rollbackHookOnTimedOutWrite` — `loop.js` restaura bytes si el write falla/timeout
3. `skipCheckpointIfUnchanged` — `loop.js` si el hash no cambió
4. `sandboxTimeoutThenCleanup` — `loop.js` en timeout/abort de tool sandbox + `local-sandbox.js`
5. `sandboxReapOrphanWorkdirs` — `loop.js` + reap de `sira-sbx-*`
6. `sseResumeDropsPriorListeners` — `ai.js` generate + `sse-writer.js`
7. `sseCancelClearsHeartbeat` — `ai.js` `stopGenerateSseHeartbeat` + writer done/close
8. `sseResumeRejectsSeqPastHead` — `ai.js` `inclusiveReplayStartFromRing` + writer resume

## Apply layer (nombres 3H61, no colisionan)

`guardMutatingWriteClosed` / `cleanupSandboxOnTimeoutClosed` siguen en `engine-3h61.js` para tests unitarios y apply de `rm` seguro. El scout / hot path mira los nombres 3H59 en `loop.js` y `engine-adapter.js`.

## Wiring fail-closed

- `engine-adapter.js` — `bindLive388CheckpointSandboxSse` fuerza los 8 exports 3H59
- `loop.js` — `executeWith3h59Checkpoint` (local, no export overlay)
- `doc-agent/tools.js` — hook 3H59 en `write_file` / `str_replace`; cleanup en bash timeout
- `local-sandbox.js` — cleanup en timeout + reap
- `sse-writer.js` + `ai.js` generate — resume/cancel por nombre 3H59
- `engine-gateway.js` — `input.cancelUsage` + `input.publicError`

## Tests

`backend/tests/ola-3h61-invariants.test.js` + `engine-hotpath-wire.test.js` (nombres #388 en loop/ai).

## Fuera de alcance

Sin UI. Sin OpenRouter. Sin PRs 381–385. DeepSeek Flash/Pro only.
