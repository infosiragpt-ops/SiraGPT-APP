# 3H61 — checkpoints, sandbox cleanup, cancel accounting (no UI)

Ola engine-only posterior a 3H60 + hot path #399. No inventa nombres que choquen con exports vivos de 3H59/3H60. Cablea los helpers #388 que seguían fail-open / sin efecto.

## Helpers 3H61 (orquestadores únicos)

1. `guardMutatingWriteClosed` — `checkpointHookBeforeMutatingTool` + snapshot de bytes + `rollbackHookOnTimedOutWrite` + `skipCheckpointIfUnchanged` + RAW hash
2. `cleanupSandboxOnTimeoutClosed` — `sandboxTimeoutThenCleanup` + `rm` solo de `sira-sbx-*` bajo `os.tmpdir()`
3. `reapOrphanSandboxDirsClosed` — `sandboxReapOrphanWorkdirs` + delete real
4. `settleCancelUsageClosed` — `accountPartialTokensOnCancel` + `neverDoubleCountCancelUsage`
5. `classifyPublicLoopErrorClosed` — `classifyEngine3h59Error` / 3H60 + tabla ES (0 stacks)
6. `applySseResumeGuardsClosed` — `sseResumeDropsPriorListeners` + `sseResumeRejectsSeqPastHead` (reset si last > head)
7. `applySseCancelHeartbeatClosed` — `sseCancelClearsHeartbeat`
8. `enforceSubtaskProgressClosed` — `cutSubtaskIfNoProgress`
9. `sliceVerificationTokenBudgetClosed` — `sliceSubtaskTokenBudget`

## Wiring fail-closed

- `loop.js` — checkpoint/rollback en writes; corte `subtask_no_progress`; usage exacto en cancel; errores ES
- `doc-agent/tools.js` — `write_file` / `str_replace` con snapshot raw del sandbox
- `local-sandbox.js` — cleanup en timeout + reap de huérfanos
- `sse-writer.js` — drop listeners + reject Last-Event-ID past head
- `ai.js` generate — settle cancel una vez; resume past-head → replay desde 0; clear heartbeat
- `engine-gateway.js` — `input.cancelUsage` + `input.publicError`
- `engine-adapter.js` — snapshot `wave: 3H61`, bind si el nombre está libre

## Tests

`backend/tests/ola-3h61-invariants.test.js` (17). Incluye rollback real en `runAgentLoop` y corte idle.

## Fuera de alcance

Sin UI. Sin OpenRouter. Sin PRs 381–385. DeepSeek Flash/Pro only.
