# Ola 3H32 — 2026-08-19 18:57 America/Lima (23:57Z)

Numero 3H32. Caps 1–12 tenian huecos reales tras 3H31 (event-order / schema repair / file pins / cancel mid-stream / str_replace syntax / sandbox wall+RSS / first-token watchdog / taxonomia proveedor). Esta ola ataca retry backoff+jitter, consecutive identical tool+args, presupuesto de pasos por sesion, compact que tira cuerpos viejos y guarda last-N nombres, rollback del ultimo edit, fuzzy-whitespace str_replace, stdout cap + tmp on cancel, generate duplicado (no steal), creditos en tool-error + conteo en cancel, taxonomia de tool + 0 stacks, p50/p95 muestreado, deny-list peligrosa, audit duration+tokens, un writer por path, empty-response retry-once, abort cascade + TTL de claim, refuse OpenRouter, overlay de timeouts, env sanitizado, hint de presupuesto, heartbeat SSE `: ping` 15s, MCP already-connected. Frontend untouched. Backend-only. No OpenRouter. DeepSeek Flash/Pro. No down -v.

## Por que

3H31 cerro seq monotonica, schema repair, pins file-backed, cancel mid-stream, str_replace revert, sandbox wall/RSS, first-token watchdog, taxonomia 429/401/5xx. Leftovers de alto valor vs Claude Code/Cowork: tool retry sin jitter; loop cut no era consecutive; compact no recortaba cuerpos; no habia rollback del ultimo archivo; str_replace fallaba por whitespace; stdout de comando no se capaba en cancel; un POST generate duplicado podia colarse; tool-error no liberaba hold; stacks podian filtrarse; no habia deny-list; audit sin duration/tokens; empty model loop; claim de sesion sin TTL; OpenRouter si alguien lo ponia en env; web y shell el mismo timeout; sandbox PATH podia filtrar /home; SSE sin `: ping` cada 15s.

## Live files rewritten (not just new helpers)

- `backend/src/services/agent-runner/loop.js` — retry backoff, consecutive cut, session remaining, compact bodies, empty retry, deny dangerous, replay call-id, fuzzy replace, test hint, path lock, abort cascade, credit on tool-error, tool-count, overlay timeouts, budget hint.
- `backend/src/services/react-agent.js` + `agentic-chat-stream.js` — same wire on /chat (deny, empty retry, abort cascade, comment heartbeat 15s).
- `backend/src/services/codex/agent-loop.js` — same wire on /code (deny, replay, abort cascade, fuzzy, tool-count).
- `backend/src/services/sandbox/local-sandbox.js` — sanitizeSandboxEnvHard + capCommandStdout + tmpCleanupOnCancel.
- `backend/src/utils/sse-writer.js` — comment heartbeat `: ping` 15s, mark on write, stop on done/close.
- `backend/src/services/agent-gateway/queue.js` — dropDuplicateInFlightGenerate + expireGatewayClaimTtl.
- `backend/src/services/agent-runner/engine-gateway.js` — durationMs + tokens on audit.
- `backend/src/services/agent-harness/mcp-policy.js` — already-connected host reuse without opening allowlist.
- `backend/src/services/error_codes.js`, `observability/public-stream-error.js`, `observability/health-check.js`, `engine-reliability.js`.
- `docker-compose.production.override.yml` — bind-mounts engine-adapter.js + ola-3h32-invariants.test.js + mcp-policy.js.

## New first-party module

- `backend/src/services/agent-runner/engine-adapter.js` — 20 holes listed above. Rewrite, no vendor copy. No OpenRouter. `engine-lifecycle.js` (3H31) se deja intacto.

## Proof live imports them

Container: `engine_loop.adapter.retryBackoffJitter/denyDangerousTools/emptyResponseRetryOnce/sseCommentHeartbeat/refuseOpenRouter === true`.
`generate_path` flash/pro `openrouter: false` (unhealthy if env apunta a openrouter.ai).
`sandbox_backends.local: true` (interpreter sigue local; `usesRunsc: false` en este path).
`chat_run_worker` skipped `dormant_ok: true`.

## Shipped (metric deltas)

1. retryToolWithBackoff + jitter (cap 2)
2. createConsecutiveRepeatCut identical tool+args (cap 1/3)
3. sessionRemainingSteps per-session (cap 3)
4. compactDropStaleBodies + last-N names (cap 4)
5. rollbackLastFileEdit (cap 5)
6. fuzzyWhitespaceReplace + verify (cap 6)
7. capCommandStdout per-command (cap 7)
8. tmpCleanupOnCancel (cap 7)
9. sanitizeSandboxEnvHard (cap 7)
10. overlayToolTimeoutMs web 8s / shell 30s (cap 7)
11. dropDuplicateInFlightGenerate (cap 9)
12. expireGatewayClaimTtl 45s (cap 9)
13. creditOnToolError no charge (cap 10)
14. recordTurnToolCount on cancel (cap 10)
15. classifyToolFailure taxonomy (cap 11)
16. sanitizeClientError 0 stacks (cap 11)
17. denyDangerousGenerateTools (cap 2)
18. stampAuditDurationTokens (cap 12)
19. claimPathMutation one writer (cap 6/9)
20. emptyResponseRetryOnce (cap 1)
21. replaySameCallId (cap 2)
22. abortCascade user→model→sandbox (cap 1)
23. refuseOpenRouterEnv (cap 11)
24. stopOnFinalAnswer (cap 1)
25. formatRemainingBudgetHint (cap 3)
26. afterWriteTestHint (cap 6)
27. startCommentHeartbeat 15s (cap 8)
28. honorLastEventId (cap 5/8)
29. allowAlreadyConnectedMcp (MCP leftover, no allowlist)
30. observeAdapterLatency p50/p95 scripted (cap 12)
31. repairTruncatedJson + coerceStringyPrimitives (cap 2)
32. Health flags 3H32 adapter.*
33. Tests 37/37 3H32
34. DeepSeek lock intacto (`openrouterGenerate: false`)
35. Interpreter declarado `local`
36. chat_run_worker sigue dormant

Tests: 37/37 3H32 (unit, stub model, 0 DeepSeek credits).

## Live

backend recreated 1x (compose.prod + production.override, bind-mounts, no image rebuild, no down -v, no frontend rebuild).
live/ready 200. chat/code 200 local+https. engine_loop healthy. generate_path flash/pro openrouter:false.
Frontend StartedAt stays 2026-08-19T23:47:15Z. dept-computer.js / dept-real-pc.js no tocados.

## Leftovers (skipped a proposito)

- HMAC secret missing (`SIRAGPT_WEBHOOK_HMAC_SECRET`) — no inventar secreto de webhook.
- `mcp_policy` deny-all para hosts nuevos (`SIRAGPT_MCP_ALLOWED_HOSTS` vacio). Already-connected reuse no abre todo.
- `chat_run_worker` dormant (`dormant_ok:true`) — encenderlo cambiaria generate a cola async y romperia SSE in-process.
- `SIRAGPT_REFRESH_TOKEN_ROTATION` unset (no encender sin dual-read).
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto.
- Sandbox interpreter sigue `local` (gVisor `runsc` es el agent-runner docker driver, no este path). Env/PATH/stdout de `local` si se endurecieron.
- `create_file` / `delete_file` / `move_file` / `notebook_edit` no existen live — no se fabricaron tools.
- Live Flash TTFB no se midio con DeepSeek (no quemar creditos). p50/p95 son scripted.
