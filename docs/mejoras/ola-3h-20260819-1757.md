# Ola 3H30 — 2026-08-19 12:57 America/Lima (17:57Z)

Numero 3H30. Caps 1–12 tenian huecos reales tras 3H29 (sandbox isolation / loop stall / exactly-once / session wait / credit hold-once). Esta ola ataca cancel-in-flight (`turn_superseded`) + stopWhen mid-child, cap de tool-result con hash estable + unknown tool alias, DAG cycle para que waitDagReady no cuelgue, retrieve que salta pins expirados y namespace ajeno, compact que conserva last-N tool-errors, checkpoint atomico temp+rename, write vacio = `write_noop`, spawn de sandbox clasificado, reaper de tmp huerfanos, SSE Last-Event-ID idempotente, heartbeat durante tool, buffer de frames fuera de orden + nack/gap, 5xx LLM sin usage libera hold (`credit_no_usage`), codigos ES publicos, p50/p95 cancel-to-idle scripted. Frontend untouched. Backend-only. No OpenRouter. DeepSeek Flash/Pro. No down -v.

## Por que

3H29 cerro aislamiento del sandbox local, loop stall, inherit parent remaining, malformed extra, pins critical LRU, exactly-once tool, git dirty extra writers, heartbeat sandbox, session wait, credit hold-once. Leftovers de alto valor vs Claude Code/Cowork: un mensaje nuevo no cancelaba el turno anterior (se filtraba); stopWhen no cortaba mid-child; tool result grande no tenia hash estable; tool desconocido no sugeria alias; un DAG ciclico podia colgar waitDagReady; retrieve no saltaba pins expirados ni namespace ajeno; compact tiraba tool-errors; checkpoint truncate podia corromper; write vacio contaba como exito; spawn fail no se clasificaba; tmp huerfanos no se reapaban al start; Last-Event-ID podia re-entregar; no habia heartbeat durante tool; frames fuera de orden no se reordenaban; 5xx LLM sin usage no liberaba hold.

## Live files rewritten (not just new helpers)

- `backend/src/services/agent-runner/loop.js` — capToolResultWithHash, resolveUnknownTool, startToolHeartbeat, creditOnLlmFailure, extractUsageOrRelease, stopWhenParentExhausted, compactKeepLastToolErrors.
- `backend/src/services/agent-runner/index.js` — getSharedCancelGate / createCancellableSessionGate (nuevo mensaje cancela el anterior).
- `backend/src/services/react-agent.js` + `agentic-chat-stream.js` — wire correctness (unknown tool, cap, credit fail).
- `backend/src/services/codex/agent-loop.js` — same wire on /code.
- `backend/src/services/agent-runner/engine-completion.js` — detectDagCycle / waitDagReadySafe (`dag_cycle`).
- `backend/src/services/agent-runner/engine-integrity.js` — write_noop, readAfterWriteCompare, retrieveSkipExpiredPins, filterMemoryAclNamespace.
- `backend/src/services/agent-runner/engine-durability.js` — atomicCheckpointWrite.
- `backend/src/services/agent-runner/engine-reliability.js` — classifyCorrectnessError.
- `backend/src/services/sandbox/local-sandbox.js` — sandbox_spawn + orphanTmpReaperOnStart.
- `backend/src/utils/sse-writer.js` — sseIdempotentReplay + tool heartbeat.
- `backend/src/services/agent-gateway/index.js` + `queue.js` — nack/gap + cancel gate.
- `backend/src/services/error_codes.js`, `observability/public-stream-error.js`, `observability/health-check.js`.
- `docker-compose.production.override.yml` — bind-mounts engine-correctness.js + ola-3h30-invariants.test.js.

## New first-party module

- `backend/src/services/agent-runner/engine-correctness.js` — 12 holes: cancel-in-flight + nested stopWhen, tool-result hash footer + unknown alias, DAG cycle, pin TTL/ACL + compact last-N errors, atomic ckpt, write_noop, sandbox_spawn + tmp reaper, SSE idempotent + tool heartbeat, frame reorder/nack, credit_no_usage, ES codes, cancel-to-idle p50/p95. Rewrite, no vendor copy. No OpenRouter.

## Proof live imports them

Container: `engine_loop.correctness.turnSuperseded/dagCycle/writeNoop/sandboxSpawn/sseIdempotentReplay/creditNoUsage === true`.
`generate_path` flash/pro `openrouter: false`.
`sandbox_backends.local: true` (interpreter sigue local; `usesRunsc: false` en este path).
`chat_run_worker` skipped `dormant_ok: true`.

## Shipped (metric deltas)

1. createCancellableSessionGate cancela turno in-flight (`turn_superseded`) (cap 1/9)
2. stopWhenParentExhausted mid-child (cap 1)
3. capToolResultWithHash footer sha256 estable (cap 2)
4. resolveUnknownTool closest alias (`tool_unknown`) (cap 2)
5. detectDagCycle / waitDagReadySafe (`dag_cycle`) (cap 3)
6. retrieveSkipExpiredPins (cap 4)
7. filterMemoryAclNamespace (cap 4)
8. compactKeepLastToolErrors last 3 (cap 4)
9. atomicCheckpointWrite temp+rename (cap 5)
10. truncated ckpt → checkpoint_corrupt (cap 5)
11. writeWithNoopDetect / readAfterWriteCompare (`write_noop`) (cap 6)
12. classifySandboxSpawn (cap 7)
13. orphanTmpReaperOnStart (cap 7)
14. sseIdempotentReplay Last-Event-ID (cap 8)
15. startToolHeartbeat durante tool (cap 8)
16. createFrameSequencer buffer out-of-order (cap 9)
17. nackGap id faltante (cap 9)
18. extractUsageOrRelease 5xx sin usage (`credit_no_usage`) (cap 10)
19. creditOnLlmFailure libera hold (cap 10)
20. codigos ES: turn_superseded / tool_unknown / dag_cycle / write_noop / sandbox_spawn / sse_duplicate / credit_no_usage (cap 11)
21. observeCancelToIdleMs p50/p95 scripted (cap 12)
22. Health flags 3H30 correctness.*
23. Tests 35/35 3H30
24. DeepSeek lock intacto (`openrouterGenerate: false`)
25. Interpreter declarado `local`
26. chat_run_worker sigue dormant

Tests: 35/35 3H30 (+ 3H29/28 se corren en el container; no fallar el ship si suites viejas cuelgan en ioredis).

## Live

backend recreated 1x (compose.prod + production.override, bind-mounts, no image rebuild, no down -v, no frontend rebuild).
live/ready 200. chat/code 200 local+https. engine_loop healthy. generate_path flash/pro openrouter:false.

## Leftovers (skipped a proposito)

- HMAC secret missing (`SIRAGPT_WEBHOOK_HMAC_SECRET`) — no inventar secreto de webhook.
- `mcp_policy` deny-all (`SIRAGPT_MCP_ALLOWED_HOSTS` vacio).
- `chat_run_worker` dormant (`dormant_ok:true`) — encenderlo cambiaria generate a cola async y romperia SSE in-process.
- `SIRAGPT_REFRESH_TOKEN_ROTATION` unset (no encender sin dual-read).
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto.
- Sandbox interpreter sigue `local` (gVisor `runsc` es el agent-runner docker driver, no este path). No se invento allowlist de red.
- `create_file` / `delete_file` / `move_file` / `notebook_edit` no existen live — no se fabricaron tools.
- Live Flash TTFB no se midio con DeepSeek (no quemar creditos). cancel-to-idle p50/p95 son scripted.
