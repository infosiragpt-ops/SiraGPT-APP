# Ola 3H43 — 2026-08-21 09:57 America/Lima (14:57Z) — SHIPPED

Numero 3H43 **aplicado**. Motor only. UI / OpenRouter / companies / iliagpt DNS no tocados. DeepSeek only (`openrouterGenerate=false`).

## STEP 0 — 402

- StartedAt previo 3H42: `2026-08-21T12:14:25Z`.
- `docker logs --since 12:14Z` con `Insufficient Balance` / `Error from DeepSeek` / `quota_exhausted`: **0**.
- No se llamo live DeepSeek en smoke largo de esta oleada (regla: no reventar creditos). TTFB live **unmeasured**.
- Health 200 (status=degraded, MCP deny-all no critico). generate_path F2 DeepSeek Flash/Pro, openrouter=false.

## Helpers nuevos (30)

capToolArgBytes32KiB, repairUnescapedNewlinesInJsonStrings, coerceNullStringToNullOptional, maxSseBuffersPerSession16, compactKeepPinnedFactsAndLast3UserTurns, refuseWriteIfDestDirMissing, ceilTokensOnCancel, classifyEconnresetAsCancelled, queueMaxWait60sThen503, skipUpsertIfEmbeddingDimMismatch, stallIfNoEvent20sMidStream, stripUtf16NulPadding, rejectToolNameLongerThan64, rejectRecursiveSameToolNameOver8, skipCompletedPlanStepsOnResume, gzipCheckpointIfOver64KiB, parseLastEventIdIntOnly, capGlobMatchFileSize1MiB, redactAuthorizationBearerInToolResults, refuseHostBashIfComputerOnlyTurn, subagentInheritRemainingStepBudget, concatenateSplitToolCallFragments, neverRetry402, combinedStdoutStderr96KiB, pingOnlyIfLastWriteOver15s, rejectUnicodeSlashHomoglyph, sessionLockOwnerPidCheck, mapPrismaDisconnectRetryable, defaultToolTimeout30sIfMissing, closeSseThenSettleCredits.

3H42 se conserva (resume tool ids, schema min/max, missing-brace JSON, refund 0-token hold, pin last tool error, SSE replay 32, identical-prompt reject, write >2MiB, empty embeddings, observation no-charge, 120s wall, enum case-repair, zero-width strip, array 256, Retry-After jitter, ckpt tombstone, stderr 64KiB, drop tool results >6 steps, whitespace tool name, id strings stay strings, seq increase, abort on cancel token, email redact, 8 hb/min, symlink read refuse, plan step failed twice, restore last SSE id).

Hotfix chat 02:56Z intacto: `persistentComputerTools`, `computerOnly` gated a intent, CDP observe cap 8KiB, MAX_CONTROL_STEPS=25, 402 clasificado ES, wrapExecutors no-throw. No se toco ACS/ai.js.

No se reabrio `SIRAGPT_SANDBOX_NET_ALLOW`. glob ignore node_modules/.git y ulimit nproc ya existian. killProcessGroup / toolArgByteCap per-string 8KiB / first-token watchdog / BOM strip ya existian — no se rehicieron.

## Verificacion

- `tests/ola-3h43-invariants.test.js` + `ola-3h42` + `ola-3h41` (wave 3H41|3H42|3H43).
- `/health` wave=3H43, openrouterGenerate=false.
- Frontend no rebuild. Recreate solo `siragpt-backend-1 --no-deps`.

## Fuera de alcance

- UI, iliagpt DNS, companies/data, OpenRouter, compose down -v, clone de repos, push a main: no tocados.
