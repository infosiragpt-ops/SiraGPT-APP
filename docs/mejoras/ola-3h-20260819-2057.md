# Ola 3H31 — 2026-08-19 15:57 America/Lima (20:57Z)

Numero 3H31. Caps 1–12 tenian huecos reales tras 3H30 (cancel-in-flight / DAG cycle / write_noop / sandbox_spawn / SSE idempotent / credit_no_usage). Esta ola ataca event-order monotonico + tool_result_dup + single-gateway claim, schema repair mas alla de 3H28 (unknown → nearest allowed, extra keys, enum/type coerce, defaults, maxItems), pins file-backed + hook de memoria searchable (pgvector si ya hay retrieve; si no, archivo — no DB nueva), cancel mid-stream (abort tools + hold once + terminal SSE once), str_replace syntax+revert, sandbox wall-clock/RSS/tmp crash, first-token watchdog, taxonomia 429/401/5xx sin fugas de clave. Frontend untouched. Backend-only. No OpenRouter. DeepSeek Flash/Pro. No down -v.

## Por que

3H30 cerro turn_superseded, DAG cycle, write_noop, sandbox_spawn, SSE Last-Event-ID, credit_no_usage. Leftovers de alto valor vs Claude Code/Cowork: seq podia retroceder; tool result se re-emitia; unknown tool solo sugeria; extra keys/enums sucios llegaban al executor; compact podia perder pins criticos; cancel no abortaba tools in-flight ni ponia DONE una sola vez; str_replace no revertia parse fail; sandbox no mataba por RSS independiente del CPU ni limpiaba tmp en crash; sin token el cliente se quedaba mudo; 401/429 podian filtrar texto de proveedor.

## Live files rewritten (not just new helpers)

- `backend/src/services/agent-runner/loop.js` — rewriteUnknownToNearest, repairToolCallSchema, pinAcrossCompact, searchableMemoryHook, cancelMidStream, verifyStrReplace, startFirstTokenWatchdog, mapProviderHttp, event-order gate.
- `backend/src/services/agent-runner/index.js` — require engine-lifecycle.
- `backend/src/services/react-agent.js` + `agentic-chat-stream.js` — same wire on /chat (schema, watchdog, terminal SSE, taxonomia).
- `backend/src/services/codex/agent-loop.js` — same wire on /code.
- `backend/src/services/sandbox/local-sandbox.js` — sandboxWatchdogTick + guaranteedTmpCleanup + termThenKill.
- `backend/src/utils/sse-writer.js` — assertMonotonicSeq + emitTerminalSseOnce.
- `backend/src/services/agent-gateway/queue.js` — claimSingleGateway (steal en turno nuevo).
- `backend/src/services/error_codes.js`, `observability/public-stream-error.js`, `observability/health-check.js`, `engine-reliability.js`.
- `docker-compose.production.override.yml` — bind-mounts engine-lifecycle.js + ola-3h31-invariants.test.js.

## New first-party module

- `backend/src/services/agent-runner/engine-lifecycle.js` — 8 holes: event-order, schema repair, file pins + memory hook, cancel mid-stream, str_replace revert, sandbox wall/RSS/tmp, first-token watchdog, provider taxonomy. Rewrite, no vendor copy. No OpenRouter. `engine-runtime.js` (3H26) se deja intacto.

## Proof live imports them

Container: `engine_loop.lifecycle.eventOrderMonotonic/rewriteUnknownNearest/abortInFlightTools/strReplaceSyntaxRevert/sandboxWallClock/firstTokenWatchdog/providerTaxonomy === true`.
`generate_path` flash/pro `openrouter: false`.
`sandbox_backends.local: true` (interpreter sigue local; `usesRunsc: false` en este path).
`chat_run_worker` skipped `dormant_ok: true`.

## Shipped (metric deltas)

1. createEventOrderGate seq monotonica (`event_order`) (cap 9)
2. recordToolResultOnce (`tool_result_dup`) (cap 9)
3. claimSingleGateway (`gateway_busy`; steal en turno nuevo) (cap 9)
4. drainOrNackOnClose (cap 9)
5. rewriteUnknownToNearest auto-map d≤2 (cap 2)
6. repairToolCallSchema strip/enum/type/default/maxItems (cap 2)
7. pinAcrossCompact hechos criticos (cap 4)
8. file-backed pin list + size cap (cap 4)
9. searchableMemoryHook (pgvector retrieve o archivo) (cap 4)
10. cancelMidStream abort tools + hold once + terminal SSE once (cap 1/10)
11. verifyStrReplace syntax + auto-revert (cap 6)
12. wallClockExceeded + rssKillIfOver + termThenKill (cap 7)
13. guaranteedTmpCleanup crash path (cap 7)
14. startFirstTokenWatchdog + first_token_stall (cap 8/12)
15. mapProviderHttp 429/401/5xx/timeout + scrubSecrets (cap 11)
16. Health flags 3H31 lifecycle.*
17. Tests 34/34 3H31
18. DeepSeek lock intacto (`openrouterGenerate: false`)
19. Interpreter declarado `local`
20. chat_run_worker sigue dormant

Tests: 34/34 3H31 (+ 3H30/29 se corren en el container; no fallar el ship si suites viejas cuelgan en ioredis).

## Live

backend recreated 1x (compose.prod + production.override, bind-mounts, no image rebuild, no down -v, no frontend rebuild).
live/ready 200. chat/code 200 local+https. engine_loop healthy. generate_path flash/pro openrouter:false.

## Leftovers (skipped a proposito)

- HMAC secret missing (`SIRAGPT_WEBHOOK_HMAC_SECRET`) — no inventar secreto de webhook.
- `mcp_policy` deny-all (`SIRAGPT_MCP_ALLOWED_HOSTS` vacio).
- `chat_run_worker` dormant (`dormant_ok:true`) — encenderlo cambiaria generate a cola async y romperia SSE in-process.
- `SIRAGPT_REFRESH_TOKEN_ROTATION` unset (no encender sin dual-read).
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto.
- Sandbox interpreter sigue `local` (gVisor `runsc` es el agent-runner docker driver, no este path). No se invento allowlist de red.
- `create_file` / `delete_file` / `move_file` / `notebook_edit` no existen live — no se fabricaron tools.
- Live Flash TTFB no se midio con DeepSeek (no quemar creditos). first-token watchdog p50/p95 son scripted.
