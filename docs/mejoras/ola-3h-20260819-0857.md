# Ola 3H27 — 2026-08-19 03:57 America/Lima (08:57Z)  [ship ~04:20 Lima]

Numero 3H27. Caps 3/4/5/6/10/11/12 tenian huecos reales tras 3H26 (sandbox/SSE/queue). Esta ola ataca retrieve-before-generate, CAS durable, exact-diff+syntax revert en mas writes, credit settle on cancel, first-byte real hidratable, plan budget anidado. Frontend untouched. Backend-only. No OpenRouter. DeepSeek Flash/Pro. No down -v.

## Por que

3H26 cerro stream/reap/SSE drain/queue lease. Leftovers de alto valor vs Claude Code/Cowork: retrieve no corria si el caller no pasaba `memoryRetrieve`; write_file revertia a string vacio; str_replace no exigia hunk unico; persistCkpt no hacia CAS; cancel hacia `release()` sin `settle(used)`; firstByte del writer quedaba en 0 tras recreate; no habia auto-resume del latest Redis.

## Live files rewritten (not just new helpers)

- `backend/src/services/agent-runner/loop.js` — retrieveBeforeGenerate siempre (iter 1); auto-resume latest tras recreate; CAS expectedVersion + pins en ckpt; credit settle-then-release on abort; observeRealFirstByte; remainingPlanBudget.
- `backend/src/services/agent-runner/tools.js` — write_file restaura original (no ''); str_replace/edit_file unique occurrence; apply_patch via applyHunksExact.
- `backend/src/services/agent-runner/engine-durability.js` — CAS head en put; default recall pgvector/keyword fail-closed.
- `backend/src/services/agent-runner/engine-reliability.js` — classifyLoopError 3H27.
- `backend/src/services/error_codes.js`, `observability/public-stream-error.js`, `observability/health-check.js`.
- `docker-compose.production.override.yml` — bind-mounts 3H27 (loop, durability, reliability, integrity, tests).

## New first-party module

- `backend/src/services/agent-runner/engine-integrity.js` — retrieve-before, pin hash dedup, durable CAS, auto-resume recreate, unique replace, syntax revert original, credit cancel, plan budget, first-byte real+hydrate. Rewrite, no vendor copy.

## Proof live imports them

Container: `engine_loop.integrity.retrieveBeforeGenerate/ckptCasDurable/creditCancelSettle/firstByteReal === true`.
`generate_path` flash/pro `openrouter: false`.
`sandbox_backends.local: true` (interpreter sigue local).
`chat_run_worker` skipped `dormant_ok: true`.

## Shipped (metric deltas)

1. retrieve-before-generate por defecto (no depende de memoryRetrieve) (cap 4)
2. pgvector fail-closed en retrieve-before (cap 4)
3. pin content-hash dedup (cap 4)
4. pins persistidos en checkpoint (cap 4/5)
5. CAS expectedVersion en durable put (cap 5)
6. auto-resume latest Redis tras recreate (cap 5)
7. restore pins al reanudar (cap 5)
8. write_file restaura original en syntax fail (cap 6)
9. str_replace hunk unico (cap 6)
10. edit_file hunk unico (cap 6)
11. apply_patch unique hunk via applyExactDiff (cap 6)
12. credit settle(used)+release on cancel (cap 10)
13. persistUsage en abort intacto (cap 10)
14. remainingPlanBudget anidado (cap 3)
15. first-byte real (stream/sse/loop) (cap 12)
16. persist+hydrate first-byte tras recreate (cap 12)
17. consumeStreamUntilFirstByte (cap 12)
18. Codigos ES: retrieve_before / pin_dedup / credit_cancel / resume_recreate / write_syntax_revert / plan_budget / first_byte_real (cap 11)
19. Health flags 3H27 integrity.*
20. Tests 31/31 3H27
21. 32/32 3H26 siguen verdes
22. DeepSeek lock intacto (`openrouterGenerate: false`)
23. Interpreter declarado `local`
24. chat_run_worker sigue dormant

Tests: 31/31 3H27, 32/32 3H26, 28/28 3H25.

## Live

backend recreated 1x (compose.prod + production.override + published-apps.override, bind-mounts, no image rebuild, no down -v).
live/ready 200. chat/code 200 local+https. engine_loop healthy. generate_path flash/pro openrouter:false.

## Leftovers (skipped a proposito)

- HMAC secret missing (`SIRAGPT_WEBHOOK_HMAC_SECRET`) — no inventar secreto de webhook.
- `mcp_policy` deny-all (`SIRAGPT_MCP_ALLOWED_HOSTS` vacio).
- `chat_run_worker` dormant (`dormant_ok:true`) — encenderlo cambiaria generate a cola async y romperia SSE in-process.
- `SIRAGPT_REFRESH_TOKEN_ROTATION` unset (no encender sin dual-read).
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto.
- Sandbox interpreter sigue `local` (gVisor `runsc` es el agent-runner).
- Live Flash TTFB no se midio con DeepSeek (no quemar creditos). first-byte real es stream/SSE/loop hidratable; p50 Flash cobrado no se inventa.
