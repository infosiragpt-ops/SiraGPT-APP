# Ola 3H39 — 2026-08-20 15:57 America/Lima (20:57Z)

Numero 3H39. Caps 1-12 tenian huecos reales tras 3H38 (max concurrent tools, subagent 8KiB, repair required, result shape, timeout budget, dead-letter, plan progress, compact last errors, pin:true, CAS, checksum, js/py syntax, C0 paths, create exclusive, tmpfs 64MB, $HOME redact, SSE idle ping, SSE gap, starvation 15s, credit audit, FS taxonomy, skip memory if busy). Esta ola ataca orden estable de resultados paralelos (call order), cancel de tools in-flight, repair de trailing commas en JSON, alias de nombres de tool, truncado de args anidados >6, profundidad max de subagente 2, corte de wall-clock <5s, merge de users duplicados en compact, dedupe de memoria por hash, refuse edit si sha256 cambio desde el read, contexto de hunk git, write atomico via .tmp+rename, reject UNC/Windows paths, no-new-privs en sandbox, scrub LD_PRELOAD, drop de tokens bufferizados al cancelar, SSE id monotonico, coalescing del mismo tool_call id in-flight, settle de creditos si el cliente se fue, taxonomy json_parse/cancelled, skip web_fetch duplicado en el mismo turno. Frontend untouched. Backend-only. No OpenRouter. DeepSeek Flash/Pro. No down -v. Computer persistente intacto (no se toco agentic-chat-stream.js).

## Por que

3H38 cerro concurrent cap, subagent cap, repair required, result shape, timeout budget, dead-letter, plan progress, compact errors, pin:true, CAS, checksum, syntax, C0 path, exclusive create, tmpfs, $HOME redact, SSE ping, SSE gap, starvation boost, credit audit, FS taxonomy, skip memory. Leftovers de alto valor vs Claude Code/Cowork: resultados paralelos llegaban en orden de finish; abort dejaba promises colgadas; JSON con trailing comma fallaba; el modelo inventaba strreplace/bash; args anidados explotaban el prompt; subagentes se anidaban sin tope; el turno seguia con <5s de wall; compact duplicaba users identicos; retrieve repetia el mismo fact hash; edit pisa archivos mutados desde el read; hunks aplicaban con contexto distinto; write no era atomico; paths UNC/C:\; sandbox podia ganar privs; LD_PRELOAD filtraba al child; abort flusheaba tokens bufferizados; Last-Event-ID del cliente > last+window no pedia replay; el mismo tool_call id in-flight se reejecutaba; cliente desconectado no liquidaba el hold; SyntaxError/AbortError iban crudos; web_fetch repetia la misma URL en un turno.

## Live files rewritten (not just new helpers)

- `backend/src/services/agent-runner/engine-adapter.js` — 3H38 intacto + 22 helpers 3H39.
- `backend/src/services/agent-runner/loop.js` — orden paralelo, cancel in-flight, trailing comma, alias, depth cap, subagent depth, wall-clock, merge users, hash dedupe, stale checksum, hunk context, atomic write, UNC paths, coalesced call id, dup web_fetch, settle if gone, json/abort taxonomy.
- `backend/src/services/react-agent.js` — aliasCommonToolNames + jsonRepairTrailingComma en /chat.
- `backend/src/utils/sse-writer.js` — sseEventIdMonotonic + cancelDropsBufferedTokens.
- `backend/src/services/sandbox/local-sandbox.js` — sandboxNoNewPrivs + envScrubLdPreload.
- `backend/src/services/agent-runner/engine-gateway.js` — json_parse, cancelled, subagent_depth, wall_clock, settle if gone.
- `backend/src/services/error_codes.js`, `observability/public-stream-error.js`.
- `docker-compose.production.override.yml` — bind-mount ola-3h39-invariants.test.js.
- NO se toco `agentic-chat-stream.js` (persistentComputerTools intacto).

## Proof live imports them

Container: `engine_loop.adapter.* 3H39 flags === true` y `wave: 3H39`.
`generate_path` flash/pro `openrouter: false`.
`sandbox_backends.local: true` (`usesRunsc: false`).
`chat_run_worker` skipped `dormant_ok: true`.

## Shipped (metric deltas)

1. joinParallelToolResultsStableOrder (cap 1)
2. cancelInflightToolsOnStop (cap 1)
3. jsonRepairTrailingComma (cap 2)
4. aliasCommonToolNames (cap 2)
5. truncateNestedToolArgsDepth (cap 2)
6. maxSubagentDepth (cap 3)
7. remainingWallClockCut (cap 3)
8. compactMergeAdjacentDuplicateUsers (cap 4)
9. memoryRetrieveDedupeByHash (cap 4)
10. refuseEditIfChecksumChangedSinceRead (cap 5)
11. patchContextLinesMustMatch (cap 6)
12. atomicWriteViaTempRename (cap 6)
13. rejectUncAndWindowsPaths (cap 6)
14. sandboxNoNewPrivs (cap 7)
15. envScrubLdPreload (cap 7)
16. cancelDropsBufferedTokens (cap 8)
17. sseEventIdMonotonic (cap 8)
18. idempotentSameCallIdInflight (cap 9)
19. settleCreditsIfClientGone (cap 10)
20. classifyJsonParseErrors (cap 11)
21. classifyAbortErrors (cap 11)
22. skipDuplicateWebFetchSameUrlTurn (cap 12)
23. Tests 3H39 + 3H31–3H38 green
24. DeepSeek lock intacto
25. Interpreter `local`
26. Health flags 3H38+3H39 (`wave: 3H39`)

## Live

backend recreated 1x (compose.prod + production.override, bind-mounts, no image rebuild, no down -v, no frontend rebuild).
live/ready 200. chat/code 200. engine_loop healthy. generate_path flash/pro openrouter:false.
computer/health 200. persistentComputerTools intact. Frontend untouched.

## Leftovers (skipped a proposito)

- HMAC secret missing (`SIRAGPT_WEBHOOK_HMAC_SECRET`) — no inventar secreto de webhook.
- `mcp_policy` deny-all para hosts nuevos (`SIRAGPT_MCP_ALLOWED_HOSTS` vacio). Already-connected reuse no abre todo.
- `chat_run_worker` dormant (`dormant_ok:true`) — encenderlo cambiaria generate a cola async y romperia SSE in-process.
- `SIRAGPT_REFRESH_TOKEN_ROTATION` unset (no encender sin dual-read).
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto. 3H36 lo fail-closea.
- Sandbox interpreter sigue `local` (gVisor `runsc` es el agent-runner docker driver).
- `create_file` / `delete_file` / `notebook_edit` no existen live.
- Live Flash TTFB no se midio con DeepSeek (no quemar creditos). p50/p95 siguen scripted.
