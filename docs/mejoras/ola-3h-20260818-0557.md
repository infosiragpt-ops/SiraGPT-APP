# Ola 3H18 — 2026-08-18 01:10 America/Lima (2026-08-18T06:10Z)

Motor-only: paridad Claude Code / Cowork en el **engine**, no en la UI.
Numero 3H18 (3H17 = `ola-3h-20260818-0325.md`, 3H16 = `ola-3h-20260818-0210.md`).

**Frontend skipped / untouched.** Container id `e2d0a0c2c064be49f88d9bb2c22de9598556f1acac8dddd08ea5b8f518fd4755` created `2026-08-18T02:36:50Z` (21:36 Lima) — last user-facing frontend expand-corner. Zero frontend rebuilds. Did not touch chat Video, duration chips, fal, `ai.js` generate catalog, webtop/computer/noVNC.

This batch is the next layer of the 12-capability rubric after 3H16/3H17: checkpoint TTL expiry + concurrent resume SET NX, tool JSON-schema fail-closed, subagent isolation (cloned messages, sliced budget, tool allowlist), memory ACL by userId, SSE Last-Event-ID gap fill, idempotent gateway enqueue, usage merge-max vs Redis drift (never double-charge), sandbox cgroup detect + ulimit fallback, symlink/binary write reject, classified ES codes, tool-exec latency samples (scripted, no Fal, no paid Flash). Did not invent HMAC secret, MCP allowlist, or rotation flag.

## Shipped this run (35 items)

### Backend (recreated `backend` only, once; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H18-BE-001** (cap 5) `isCheckpointExpired` after TTL. `engine-durability.js`. Verified: invariant fresh=false / 7h=true.
- **3H18-BE-002** (cap 5) `createResumeLock` SET NX first-wins. `engine-hardening.js`. Verified: second acquire `resume_conflict`; release then ok.
- **3H18-BE-003** (cap 5) `assertResumeSafe` conflict vs skipped. Verified: invariant.
- **3H18-BE-004** (cap 5) durable `get` marks `expired:true`. Verified: invariant.
- **3H18-BE-005** (cap 2) `validateToolArgs` required fields fail-closed. Verified: missing content → `schema_invalid`.
- **3H18-BE-006** (cap 2) additionalProperties rejected. Verified: `unexpected:hack`.
- **3H18-BE-007** (cap 2) empty required string fails. Verified: `missing:path`.
- **3H18-BE-008** (cap 3) `createSubagentContext` clones messages; parent not mutated. Verified: invariant.
- **3H18-BE-009** (cap 3) `sliceSubagentBudget` 40→8. Verified: invariant.
- **3H18-BE-010** (cap 1/3) subagent allowlist filters write tools; `abortParent=false`. Verified: invariant.
- **3H18-BE-011** (cap 4) `aclMemoryHits` drops other `userId`. Verified: u2 stripped.
- **3H18-BE-012** (cap 4) missing userId fail-closed on tagged hits. Verified: only untagged remains.
- **3H18-BE-013** (cap 8) `detectSseGaps` seq 1,2,4 → hole at 3. Verified: invariant.
- **3H18-BE-014** (cap 8) `fillSseGaps` inserts `sse_gap` marker. Verified: invariant.
- **3H18-BE-015** (cap 8/9) `persistEventFrame` idempotent same seq+type. Verified: replay length 1.
- **3H18-BE-016** (cap 10) `compareUsageDrift` redis-ahead vs ledger-ahead. Verified: skip / catchup.
- **3H18-BE-017** (cap 10) `reconcileUsage` never double-charges. Verified: `no_double_charge` max tokens.
- **3H18-BE-018** (cap 10) `persistUsage` merges max so cancel cannot lose tokens. Verified: 7+8.
- **3H18-BE-019** (cap 7) `sandboxResourceLimits` cgroup vs ulimit-fallback. Verified: injectable existsSync.
- **3H18-BE-020** (cap 7) `detectCgroup` v1/v2/absent. Verified: invariant.
- **3H18-BE-021** (cap 6) `rejectSymlinkWrite`. Verified: invariant.
- **3H18-BE-022** (cap 6) `rejectBinaryPatch` NUL. Verified: invariant.
- **3H18-BE-023** (cap 9) queue `idempotencyKey` rejects in-flight duplicate (`duplicate_turn`). Verified: started=1.
- **3H18-BE-024** (cap 2) live loop schema_invalid does **not** call executor. Verified: scripted write_file missing content, called=0.
- **3H18-BE-025** (cap 5/11) live loop expired ckpt classified, continues. Verified: `checkpoint_expired`, `resumedFrom=null`.
- **3H18-BE-026** (cap 5/11) live loop `resume_conflict` skips restore. Verified: event + no resume.
- **3H18-BE-027** (cap 4) live loop memory ACL filters foreign hits. Verified: memoryHits=1.
- **3H18-BE-028** (cap 12) tool-exec latency samples without paid LLM. Verified: count≥3.
- **3H18-BE-029** (cap 11) error_codes hardening taxonomy. Verified: expired/conflict/schema/acl/gap/drift/symlink.
- **3H18-BE-030** (cap 11) classifyLoopError + public-stream-error ES, no traces. Verified: invariant.
- **3H18-BE-031** (cap 12) `/health` `engine_loop` hardening/resumeLock/schemaFailClosed/memoryAcl/sseGapFill. Verified: live JSON.
- **3H18-BE-032** (cap 7) session-manager snapshot `resourceLimits`. Verified: cpuMs + mode.
- **3H18-BE-033** (cap 8) event-log replay fills Last-Event-ID gaps. Verified: `sse_gap` in replayFrom.
- **3H18-BE-034** (cap 4) `retrieveMemoryForLoop` applies ACL. Verified: u2 dropped.
- **3H18-BE-035** (cap 2/5) loop.js wires schema/resumeLock/ACL/hardening. Verified: source markers.

## Live proof (backend) — 01:10 Lima / 06:10 UTC
- Recreated backend only (1×). Frontend id unchanged (`e2d0a0c2c064…` 21:36 Lima).
- Container created **01:08 Lima** (2026-08-18T06:08:20Z), healthy, pid 1
- `/health/live` 200, `/health/ready` 200
- Full `/health` status degraded remains pre-existing non-critical `mcp_policy`. No `openrouter.ai`
- `engine_loop` healthy `{hardening:true, resumeLock:true, schemaFailClosed:true, memoryAcl:true, sseGapFill:true, durableCheckpoint:true, memoryRetrieve:true, heartbeat:true}` — ttfb/turnEnd hydrated from Redis (scripted overhead, not Flash)
- `generate_path` healthy (F2, deepseek-v4-flash / deepseek-v4-pro, openrouter: false)
- `gateway_queue` healthy `{lanes:0,pending:0,maxPending:8,order:fifo}`
- `GET /api/gateway/status` queueHonesty `{order:fifo, maxPending:8}` models flash/pro only, surfaces chat+code
- POST `/api/webhooks/inbound` → 503 `{code:hmac_secret_missing, retryable:false}`
- POST `/api/ai/generate` unauthenticated → 401 `access_token_required`
- POST `/api/ai/stop-stream` unauthenticated → 401 `access_token_required`
- http://127.0.0.1:3000/chat 200, /code 200; https://siragpt.com/chat 200, /code 200
- Invariants inside live backend: **35/35** `ola-3h18` + **35/35** `ola-3h17` + **26/26** `ola-3h16`
- Redis smoke (same modules, live kv): `{redisResume:"resume", replayTypes:["a","sse_gap","c"], lockA:true, lockB:false}`
- Gateway 9th enqueue → `session_queue_full` pending max 8
- Existing webtops still up. No Fal video jobs.

## Services
- Recreated: backend only
- Untouched: frontend, db, redis, caddy, runner, webtops
- Never ran `down` or `down -v`
- No frontend rebuild (count = 0)

## Honest partials
- Inbound HMAC secret still unset (503 fail-closed). Do not invent a key.
- MCP deny-all until Luis sets `SIRAGPT_MCP_ALLOWED_HOSTS`.
- `chat_run_worker` dormant skipped, `dormant_ok:true`.
- Refresh-token rotation flag unset.
- Latency hydrate is **scripted engine overhead** (mock LLM). Not DeepSeek Flash TTFB. Do not report as Flash p50.
- PG put remains fail-open; Redis path verified for post-recreate resume + resume lock.
- Sandbox code-interpreter backend still `local`. cgroup detect reports mode; we do not attach a new cgroup to running processes.
- RAG embeddings still OpenAI vector space (retrieve in-loop uses injectable/keyword/LTM + ACL; generate path unchanged).

## Skipped (with reason)
- **All FE** — last user-facing frontend is expand-corner 21:36 Lima. Constraint: do not rebuild.
- Did not set HMAC / MCP hosts / rotation.
- Did not touch `ai.js` / video.js / fal / duration chips.
- Did not enable AGENT_RUNNER_ASYNC.
- Did not burn DeepSeek or Fal to measure live Flash TTFB.

## Rubric moved
See `docs/mejoras/benchmark-capacidades.md` v3H18. 12/12 capabilities moved (TTL+resume lock, schema fail-closed, subagent isolation, memory ACL, SSE gap fill, idempotent queue, usage merge-max, sandbox limits, new ES codes, tool-exec samples).

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
Do not put a dummy value in SIRAGPT_MCP_ALLOWED_HOSTS or SIRAGPT_WEBHOOK_HMAC_SECRET.
