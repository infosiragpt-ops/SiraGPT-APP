# Ola 3H37 — 2026-08-20 09:57 America/Lima (14:57Z)

Numero 3H37. Caps 1-12 tenian huecos reales tras 3H36 (allowlist, nested coerce, large overwrite, net fail-closed, PGID kill, SSE retry, no-settle-unopened, dup system, empty facts, final+tools, gzip, URL creds, resume token, 429/402). Esta ola ataca corte por observacion identica (N>=3), abort de subagentes hermanos al cancelar el padre, enum args, truncado de args >8KiB, cache same-turn, ciclo DAG, recordatorio de pasos <=3, compact que conserva pares tool_call/tool_result, min-score 0.25 en memoria, checkpoint tras write verificado, refuse de edit binario, CRLF→LF antes del diff, move same-volume, ulimit RSS+CPU, scrub de secretos en env hijo, tmpdir finally, SSE buffer overflow, heartbeat con jitter, generate overloaded retry-after, refund del hold de completion al cancelar, taxonomy de red, skip compact bajo 70%. Frontend untouched. Backend-only. No OpenRouter. DeepSeek Flash/Pro. No down -v.

## Por que

3H36 cerro allowlist, coerce anidado, overwrite grande, net fail-closed, PGID, SSE retry, hold sin stream, dup system, facts vacios, final+tools, gzip, URL creds, resume token, 429/402. Leftovers de alto valor vs Claude Code/Cowork: el bucle repetia el mismo RESULTADO (no solo tool+args); cancelar el padre dejaba hermanos en vuelo; enum del schema no se honraba; args enormes inflaban el turno; la misma llamada se reejecutaba en el mismo turno; un DAG ciclico no se detectaba; el compact podia romper pares tool_call/result; facts con score basura; writes sin checkpoint de hash; edit de binarios; diffs CRLF; move cruzando volumen; sandbox sin RSS/CPU; env hijo con KEY/TOKEN; tmp dirs huerfanos; SSE sin tope de buffer; heartbeats alineados; cola sin retry-after; hold de completion no se devolvia al cancelar con 0 completion; ECONNRESET crudo; compact siempre aunque el contexto cupiera.

## Live files rewritten (not just new helpers)

- `backend/src/services/agent-runner/engine-adapter.js` — 3H36 intacto + 22 helpers 3H37.
- `backend/src/services/agent-runner/loop.js` — observation cut, abort siblings, enum, truncate, same-turn cache, DAG, step reminder, pair compact, min-score, write checkpoint, binary edit, CRLF, skip compact, refund, net errors.
- `backend/src/services/react-agent.js` + `agentic-chat-stream.js` — enum + abort siblings + refund/net en /chat.
- `backend/src/utils/sse-writer.js` — buffer overflow disconnect + heartbeat jitter.
- `backend/src/services/agent-gateway/queue.js` — generateWaitRetryAfter (no roba lock).
- `backend/src/services/agent-runner/engine-gateway.js` — enum + net + DAG.
- `backend/src/services/sandbox/local-sandbox.js` — RSS+CPU ulimit + scrub env + tmpdir finally.
- `backend/src/services/error_codes.js`, `observability/public-stream-error.js`.
- `docker-compose.production.override.yml` — bind-mount ola-3h37-invariants.test.js.

## Proof live imports them

Container: `engine_loop.adapter.* 3H37 flags === true` y `wave: 3H37`.
`generate_path` flash/pro `openrouter: false`.
`sandbox_backends.local: true` (`usesRunsc: false`).
`chat_run_worker` skipped `dormant_ok: true`.

## Shipped (metric deltas)

1. identicalObservationLoopCut (cap 1)
2. abortSiblingsOnParentCancel (cap 1)
3. validateEnumArgs (cap 2)
4. truncateOverlongArgStrings (cap 2)
5. cacheIdenticalToolCallSameTurn (cap 2)
6. detectDagCycle (cap 3)
7. remainingStepBudgetReminder (cap 3)
8. compactKeepToolCallResultPairs (cap 4)
9. minScoreMemoryRetrieve (cap 4)
10. checkpointAfterSuccessfulWrite (cap 5)
11. refuseBinaryFileEdit (cap 6)
12. normalizeLineEndingsBeforeDiff (cap 6)
13. moveFileSameVolume (cap 6)
14. sandboxRssCpuUlimit (cap 7)
15. scrubSecretsFromChildEnv (cap 7)
16. tmpdirCleanupFinally (cap 7)
17. sseMaxBufferDisconnect (cap 8)
18. heartbeatJitter (cap 8)
19. generateWaitRetryAfter (cap 9)
20. refundPartialTokensOnCancel (cap 10)
21. classifyNetErrors (cap 11)
22. skipCompactIfUnderBudget (cap 12)
23. Tests 3H37 + 3H31–3H36 green
24. DeepSeek lock intacto
25. Interpreter `local`
26. Health flags 3H36+3H37 (`wave: 3H37`)

## Live

backend recreated 1x (compose.prod + production.override, bind-mounts, no image rebuild, no down -v, no frontend rebuild).
live/ready 200. chat/code 200. engine_loop healthy. generate_path flash/pro openrouter:false.
computer/health 200. Frontend untouched.

## Leftovers (skipped a proposito)

- HMAC secret missing (`SIRAGPT_WEBHOOK_HMAC_SECRET`) — no inventar secreto de webhook.
- `mcp_policy` deny-all para hosts nuevos (`SIRAGPT_MCP_ALLOWED_HOSTS` vacio). Already-connected reuse no abre todo.
- `chat_run_worker` dormant (`dormant_ok:true`) — encenderlo cambiaria generate a cola async y romperia SSE in-process.
- `SIRAGPT_REFRESH_TOKEN_ROTATION` unset (no encender sin dual-read).
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto. 3H36 lo fail-closea.
- Sandbox interpreter sigue `local` (gVisor `runsc` es el agent-runner docker driver).
- `create_file` / `delete_file` / `notebook_edit` no existen live.
- Live Flash TTFB no se midio con DeepSeek (no quemar creditos). p50/p95 siguen scripted.
