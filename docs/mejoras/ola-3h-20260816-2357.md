# Ola 3h8 — 2026-08-16-2357 ship log (America/Lima)

Backup: `/opt/siragpt/backups/ola-3h8-20260816T2357Z` (override snapshot + edited FE/BE trees)
UI lock: no layout moves, no Configuración avanzada, no composer/nav/office/Proactivo placement. No compose down. No `down -v`. No repo clone. No OpenRouter. No invented keys.
DeepSeek V4 Flash/Pro only. `SIRAGPT_REFRESH_TOKEN_ROTATION` stays unset (BE-013 still skipped).
Ola-200 A–G, 3H / 3H2 / 3H3 / 3H4 / 3H5 / 3H6 / 3H7 batches not redone. Numbered 3H8-BE/FE so it does not collide.

This batch wires leftover generate candado into live handlers that still defaulted to OpenAI/gpt-4o or only locked OpenRouter (chat lock, artifact branch, excel/ppt/webdev, agent-task, agent-entry, task-contract), leftover rate-limit generate/stop paths, leftover LATAM PII + health secret shapes, leftover Word/PPT verify tools, leftover FE generate clamps. Not helper-only unused functions.

## Shipped this run (22 items)

### Backend (recreated `backend` only; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H8-BE-001** leftover generate lock — `lockTextGenerationToNativeDeepSeek` remaps OpenAI/Gemini/Anthropic/gpt-* too (was OpenRouter-only), so live `/api/ai/generate` + agentic loop cannot hop.
- **3H8-BE-002** artifact leftover candado — live /chat artifact branch uses `createNativeDeepSeekClient` + Flash; never `OPENAI_API_KEY`.
- **3H8-BE-003** artifact-generator leftover — `DEFAULT_MODEL` / `DEFAULT_VISION_MODEL` are Flash (were `gpt-4o-mini` / `gpt-4o`).
- **3H8-BE-004** excel leftover candado — POST `/generate-excel` locks provider/model to DeepSeek and uses the native client (was `createProviderClient(provider)` with OpenAI/gpt-4o defaults).
- **3H8-BE-005** ppt leftover candado — `/generate-ppt` and `/generate-ppt2` default/clamp to DeepSeek Flash/Pro.
- **3H8-BE-006** webdev leftover candado — `/generate-webdev` clamps to DeepSeek; `attachSseIds` on the live SSE (stop ≠ generate still skipped elsewhere).
- **3H8-BE-007** generatePPT leftover — `ai-service.generatePPT` uses native DeepSeek (was `getClient(provider)` default OpenAI/gpt-4o). Vector PPT wrapper defaults Flash.
- **3H8-BE-008** agent-task leftover gate — live job requires `DEEPSEEK_API_KEY` (not `OPENAI_API_KEY`); default model Flash.
- **3H8-BE-009** agent-task leftover runtime — leftover `gpt-*` / `gemini-*` ids detect as native DeepSeek Flash/Pro (never OpenAI/Gemini generate).
- **3H8-BE-010** attachment-recovery leftover — LLM recovery is DeepSeek only; OpenAI/Gemini hops removed.
- **3H8-BE-011** agent-entry leftover candado — `runAgent` uses native DeepSeek and fails closed without `DEEPSEEK_API_KEY` (was `gpt-4o` + `OPENAI_API_KEY`).
- **3H8-BE-012** runSubAgent leftover — model/client are native DeepSeek Flash (was `gpt-4o-mini` + OpenAI).
- **3H8-BE-013** task-contract leftover — side-channel generate is native DeepSeek (was OpenAI `gpt-4o-mini`).
- **3H8-BE-014** rate-limit leftover — shared generate budget adds `/api/gmail`, `/api/search-brain`, `/api/telegram`, `/api/intent`, `/api/x-search`, `/api/agent-harness`, `/api/agent-runs`, `/api/orchestration`, `/api/context-intelligence`, `/api/github`.
- **3H8-BE-015** rate-limit leftover — stop/cancel skips for agent-runs, chats run cancel, code-runner stop, codex cancel, opencode abort/stop, goals cancel, hosting cancel, github stop, voice stop (stop ≠ generate).
- **3H8-BE-016** PII leftover — structured-logger keys include `clabe` / `cci` / `cuit` / `cuil` / `nie` / `nif` / `nss`.
- **3H8-BE-017** health secrets leftover — `stripHealthSecrets` also redacts JWT/SESSION/TWILIO/SENDGRID/R2/RESEND shapes.
- **3H8-BE-018** Word/PPT verify leftover — EDIT_TOOLS adds live `create_comparison_table`, `create_moscow_chart`, `create_radar_chart`.

### Frontend (ONE rebuild only; no UI moves; /chat and /code layout unchanged)
- **3H8-FE-001** client-logs leftover — SENSITIVE_KEY_RE adds `clabe` / `cci` / `cuit` / `cuil` / `nie` / `nif` / `nss`.
- **3H8-FE-002** generateChart leftover — live POST `/ai/generate-chart` sends `freshGenerateHeaders()` (no-store).
- **3H8-FE-003** generatePlan leftover — live POST `/plan/generate` clamps model via `clampDeepSeekModel` + DeepSeek provider + no-store.
- **3H8-FE-004** remaining stream leftover — `_sseStream` (math and other live SSE POSTs) sends `freshGenerateHeaders()` and clamps model on a fresh POST.

## Live proof (backend) — 19:11 PT
- `docker compose ... up -d --no-deps --force-recreate backend` — Recreated + Started
- Container created **19:11 PT** (2026-08-17T00:11:21Z), healthy
- `/health/live` 200, `/health/ready` 200
- Full `/health`: deepseek healthy (dummy: false, api.deepseek.com), r2_artifacts healthy (init: ok), generate_path healthy (path: F2, models deepseek-v4-flash / deepseek-v4-pro, openrouter: false), gvisor_runsc healthy (runtime: runsc)
- `stripHealthSecrets` live: no `DEEPSEEK_API_KEY` literal, no `openrouter.ai`, no `JWT_SECRET` in health JSON
- GET `/api/gateway/status` → ok, models flash/pro only, `queueHonesty: {lanes:0,writers:0,aborted:0,abortedRuns:0}`, `scoped: {memory:true,skills:true,cron:true}`, deadLetterCount: 0, no openrouter
- POST `/api/gateway/abort` without sessionKey → `{code:bad_request}`
- Rotation flag unset (BE-013 skip)
- Existing webtops still up: sira-dpc-cms2hkubp0013oz01mzf0m49e-ceo-office (28h), sira-dpc-testproj-sales (31h)
- Invariants inside live backend: **129/129 passed** (`node --test tests/ola-3h8-invariants.test.js tests/ola-3h7-invariants.test.js tests/ola-3h6-invariants.test.js tests/ola-3h5-invariants.test.js tests/ola-3h4-invariants.test.js tests/ola-3h3-invariants.test.js tests/ola-3h2-invariants.test.js tests/ola-3h-invariants.test.js`)

## Live proof (frontend) — 19:20 PT
- One rebuild only; previous 3h7 image stayed up until the new image was ready
- Compiled successfully (~4.5 min next build + postbuild-slim); image `siragpt-frontend:latest` Built
- Frontend container created **19:20 PT** (2026-08-17T00:20:28Z), healthy, BUILD_ID `xlyb6zWuV-GxUvYh-pZIA`
- http://127.0.0.1:3000/ 200
- https://siragpt.com/ 200
- https://siragpt.com/chat 200
- https://siragpt.com/code 200
- https://siragpt.com/api/health/live 200
- SW live SCHEMA_VERSION `sira-v4` + `/api/` never-cache prefix (not padded)
- Bundle markers: `clabe`/`cuit` (chunk 37621, 3H8-FE-001), `generate-chart` + `/plan/generate` + `deepseek-v4-flash` (chunk 37621, 3H8-FE-002/003), `no-store` + `Last-Event-ID` (chunk 7806, 3H8-FE-004), chat page `d25df406` still has `deepseek-v4-flash` + Last-Event-ID

## Services
- Recreated: backend, frontend (frontend via the single 3h8 rebuild)
- Untouched: db, redis, caddy, runner
- Never ran `down` or `down -v`

## Honest partials
- **Gmail MCP** (`/generate-gmail`) still uses OpenAI Responses API `connector_gmail` — DeepSeek has no MCP connector. Local `gmailService` fallback unchanged. FE still clamps the requested model id.
- **generate-chart** still uses OpenAI containers/code-interpreter (not text generate). FE now sends no-store only.
- **agenticToolOpenAI** still builds an OpenAI client when the agentic loop is Anthropic, for leftover RAG embeddings/judge ids (embeddings stay OpenAI).
- `createProviderClient` still has Gemini/OpenAI branches for legacy `/generate-image-old`. Live `/generate` is locked before that client is used.
- RAG embeddings still use OpenAI (vector space). Search-preview still OpenAI (search, not generate). Lyria/image-engine unchanged.
- Health overall `degraded` remains the pre-existing non-critical `mcp_policy` (MCP_ALLOWED_HOSTS_REQUIRED). `/health/ready` stays 200.

## Skipped (with reason)
- **BE-013** — do not enable SIRAGPT_REFRESH_TOKEN_ROTATION until a dual-read window exists. Flag remains unset.
- Did not wire BE-081/BE-082 into live handlers (empty prod allowlist would 503; free-shell would break).
- Did not replace OpenAI Gmail MCP connector, code-interpreter charts, search-preview, embeddings, or image/music providers.
- Did not rename live webtop containers.

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
