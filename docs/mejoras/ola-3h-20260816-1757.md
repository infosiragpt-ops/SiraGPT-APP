# Ola 3h6 — 2026-08-16-1757 ship log (America/Lima)

Backup: `/opt/siragpt/backups/ola-3h6-20260816T1757Z` (override snapshot + edited FE/BE trees; chat-interface copy under `components/`)
UI lock: no layout moves, no Configuración avanzada, no composer/nav/office/Proactivo placement. Design-composer only changed the default model id (not buttons/bars). No compose down. No `down -v`. No repo clone. No OpenRouter. No invented keys.
DeepSeek V4 Flash/Pro only. `SIRAGPT_REFRESH_TOKEN_ROTATION` stays unset (BE-013 still skipped).
Ola-200 A–G, 3H / 3H2 / 3H3 / 3H4 / 3H5 batches not redone. Numbered 3H6-BE/FE so it does not collide.

This batch wires leftover generate candado into live handlers that still constructed OpenAI/Gemini/OpenRouter clients (research/thesis/api completions/gpts preview/cowork/se-agents/files summary/computer-use/rag rgb/projects), plus leftover rate-limit/PII/health/verify/FE clamps. Not helper-only unused functions.

## Shipped this run (26 items)

### Backend (recreated `backend` only; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H6-BE-001** research leftover candado — POST `/api/research/investigate` refuses unless `DEEPSEEK_API_KEY` is usable; planner/critic/synth use `createNativeDeepSeekClient`.
- **3H6-BE-002** research leftover models — planner/critic `FLASH`, synthesizer `PRO`. Search-preview (not generate) still uses OpenAI only if that key exists; otherwise empty sources + existing warning.
- **3H6-BE-003** thesis leftover candado — live thesis LLM calls go through `getThesisClient()` → native DeepSeek (no module-level `OPENAI_API_KEY` client).
- **3H6-BE-004** thesis leftover models — generate list is Pro/Flash; `getMaxTokensForModel` includes `deepseek-v4-flash` / `deepseek-v4-pro`.
- **3H6-BE-005** api leftover candado — POST `/chat/completions` is DeepSeek Flash/Pro only; Gemini hop removed.
- **3H6-BE-006** gpts leftover candado — `resolvePreviewClient` uses native DeepSeek; never OpenAI/OpenRouter. Cerebras only if DeepSeek key is missing (existing free preview).
- **3H6-BE-007** cowork leftover candado — headless default runtime is DeepSeek, not `gpt-4o-mini`/OpenAI.
- **3H6-BE-008** cowork leftover — scheduler delivery chat model defaults to Flash.
- **3H6-BE-009** se-agents leftover candado — live `chat.completions` (benchmarks / alignment-tax / benchmarks-all) use `requireDeepSeek` + Flash.
- **3H6-BE-010** document-summarizer leftover — `DEFAULT_MODEL` is Flash.
- **3H6-BE-011** query-decomposer leftover — `DEFAULT_MODEL` is Flash.
- **3H6-BE-012** files leftover — `GET /:id/summary` and `POST /:id/decompose-query` pass `getFileLlm()` (DeepSeek). OpenAI Files API `files.create` unchanged.
- **3H6-BE-013** computer-use leftover candado — live `analyzeDomAndPlan` + HTML generate use native DeepSeek (Flash / Pro).
- **3H6-BE-014** rag leftover candado — POST `/rgb` answerer is DeepSeek; 503 without `DEEPSEEK_API_KEY`.
- **3H6-BE-015** projects leftover — `POST /:id/chat` clamps `model` via `resolveNativeDeepSeekModel` (was `gpt-4o`).
- **3H6-BE-016** synthetic-ping leftover — defaults to `api.deepseek.com` + Flash + `DEEPSEEK_API_KEY`.
- **3H6-BE-017** rate-limit leftover — shared generate budget adds `/api/files`, `/api/rag`, `/api/scientific-search`.
- **3H6-BE-018** PII leftover — structured-logger keys include `tax_id` / `driver_license` / `bank_account` / `swift` / `bic`.
- **3H6-BE-019** health secrets leftover — `stripHealthSecrets` also redacts Anthropic / HuggingFace / ElevenLabs shapes.
- **3H6-BE-020** Word/PPT verify leftover — EDIT_TOOLS adds `set_slide_notes`, `insert_textbox`, `insert_shape`, `delete_row`, `delete_column`.

### Frontend (ONE rebuild only; no UI moves; /chat and /code layout unchanged)
- **3H6-FE-001** plan-service leftover — live `generate()` sends `freshGenerateHeaders()` (no-store, no Last-Event-ID on fresh POST) and `clampDeepSeekModel`.
- **3H6-FE-002** gpts/create leftover — default model is DeepSeek Flash/Pro, never `gpt-4o-mini`.
- **3H6-FE-003** design-composer leftover candado — default/fallback model id is `deepseek-v4-flash` (no buttons moved).
- **3H6-FE-004** client-logs leftover — SENSITIVE_KEY_RE adds `tax_id` / `driver_license` / `bank_account` / `swift` / `bic`.
- **3H6-FE-005** /chat leftover — `/research` slash saves the chat with `clampDeepSeekModel` (no layout change).
- **3H6-FE-006** sse-client leftover — `planGenerateHeaders()` aliases fresh generate headers (wired by 3H6-FE-001).

## Live proof (backend) — 13:14 PT
- `docker compose ... up -d --no-deps --force-recreate backend` — Recreated + Started
- Container created **13:14 PT** (2026-08-16T18:14:18Z), healthy
- `/health/live` 200, `/health/ready` 200
- Full `/health`: deepseek healthy (dummy: false, api.deepseek.com), r2_artifacts healthy (init: ok), generate_path healthy (path: F2, models deepseek-v4-flash / deepseek-v4-pro, openrouter: false), gvisor_runsc healthy (runtime: runsc)
- `stripHealthSecrets` live: no `DEEPSEEK_API_KEY` literal and no `openrouter.ai` in health JSON
- GET `/api/gateway/status` → ok, models flash/pro only, `queueHonesty: {lanes:0,writers:0,aborted:0,abortedRuns:0}`, `scoped: {memory:true,skills:true,cron:true}`, deadLetterCount: 0, no openrouter
- POST `/api/gateway/abort` without sessionKey → `{code:bad_request}`
- POST `/api/gateway/agent/wait` without runId → `{code:bad_request}`
- Rotation flag unset (BE-013 skip)
- Existing webtops still up: sira-dpc-cms2hkubp0013oz01mzf0m49e-ceo-office (22h), sira-dpc-testproj-sales (25h)
- Invariants inside live backend: **97/97 passed** (`node --test tests/ola-3h6-invariants.test.js tests/ola-3h5-invariants.test.js tests/ola-3h4-invariants.test.js tests/ola-3h3-invariants.test.js tests/ola-3h2-invariants.test.js tests/ola-3h-invariants.test.js`)

## Live proof (frontend) — 13:23 PT
- One rebuild only; previous 3h5 image stayed up until the new image was ready
- Compiled successfully (~4.2 min next build + postbuild-slim); image `siragpt-frontend:latest` Built
- Frontend container created **13:23 PT** (2026-08-16T18:23:43Z), healthy, BUILD_ID `kzQ9fzP7p_N0-OY3bY5Do`
- http://127.0.0.1:3000/ 200
- https://siragpt.com/ 200
- https://siragpt.com/chat 200
- https://siragpt.com/code 200
- https://siragpt.com/api/health/live 200
- SW live SCHEMA_VERSION `sira-v4` + `/api/` never-cache prefix (not padded)
- Bundle markers: `deepseek-v4-flash` (chat page `d25df406`, gpts/create `4c73191c`, design `[id]` `d723bae7`, 3H6-FE-002/003/005), `bank[_-]?account|swift|bic` (chunk 37621, 3H6-FE-004), Last-Event-ID + `no-store` (chunk 7806, 3H6-FE-001/006)

## Services
- Recreated: backend, frontend (frontend via the single 3h6 rebuild)
- Untouched: db, redis, caddy, runner
- Never ran `down` or `down -v`

## Honest partials
- **3H6-BE-002** searcher is still OpenAI search-preview when `OPENAI_API_KEY` is set (search, not generate). Without that key, sources are empty and synth warns. Planner/critic/synth are DeepSeek.
- **3H6-BE-006** Cerebras remains a last-resort preview if DeepSeek is unconfigured. OpenAI/OpenRouter fallbacks are gone.
- **3H6-BE-007** explicit `SIRAGPT_COWORK_HEADLESS_MODEL` Cerebras override still honored; default path is DeepSeek.
- **3H6-BE-012** `openai.files.create` stays on the OpenAI Files API (not a generate path).
- **3H6-BE-009** RAG embeddings in se-agents still use `rag.getOpenAI()`; only the live `chat.completions` sites were locked.
- **3H6-FE-003** leftover featured-pattern chips may still *list* gpt-4o if the catalog returns it; default/fallback id is Flash and generate is clamped.
- Health overall `degraded` remains the pre-existing non-critical `mcp_policy` (MCP_ALLOWED_HOSTS_REQUIRED). `/health/ready` stays 200.

## Skipped (with reason)
- **BE-013** — do not enable SIRAGPT_REFRESH_TOKEN_ROTATION until a dual-read window exists. Flag remains unset.
- Did not wire BE-081/BE-082 into live handlers (empty prod allowlist would 503; free-shell would break).
- Did not replace OpenAI search-preview with DeepSeek (would hallucinate URLs).
- Did not rename live webtop containers.

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
