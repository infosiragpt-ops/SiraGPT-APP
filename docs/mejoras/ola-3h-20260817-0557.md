# Ola 3h10 — 2026-08-17-0557 ship log (America/Lima)

Backup: `/opt/siragpt/backups/ola-3h10-20260817T0557Z` (override snapshot + edited BE trees). Never copied `.env`.
UI lock: no layout moves, no Configuración avanzada, no composer/nav/office/Proactivo placement. No Video composer / duration chips / fal client files. No compose down. No `down -v`. No repo clone. No OpenRouter. No invented keys.
DeepSeek V4 Flash/Pro only. `SIRAGPT_REFRESH_TOKEN_ROTATION` stays unset (BE-013 still skipped).
Ola-200 A–G, 3H through 3H9 batches not redone. Numbered 3H10-BE so it does not collide.

**Frontend skipped (video wave).** At start a `next build` was in flight; it finished and `siragpt-frontend:latest` was recreated **01:02 PT** (image `7e6064fd00db`, container `ed58ea5a…` created 2026-08-17T06:02:15Z). Per conflict rules this wave is **BACKEND ONLY**. Frontend container id unchanged after backend recreate. No FE leftovers shipped. Did not touch chat Video composer, duration chips, or fal client files. Did not overwrite `FAL_*` keys (`.env` mtime still 05:53Z).

This batch wires leftover generate candado into live handlers that still defaulted to gpt-4o / gpt-4o-mini (task-contract resolver OpenAI side-channel, se-agents align/taxonomy/best-of-n/graphrag/mbpp/humaneval, agent-collaboration judge, appshots capture), leftover SSE ids on duplicate-turn + public-web replay + turn-events `_end` + gateway events, leftover OpenClaw wait/events fail-closed + abort DLQ, leftover rate-limit generate/stop paths, leftover PII/health secret shapes, leftover sandbox owner check + abort/stop. Not helper-only unused functions (model-router still unwired — skipped).

## Shipped this run (22 items)

### Backend (recreated `backend` only; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H10-BE-001** task-contract leftover candado — live `resolveTaskContract` default is Flash; always uses native DeepSeek when `DEEPSEEK_API_KEY` is set (OpenAI-native caller no longer side-channels gpt-4o-mini).
- **3H10-BE-002** align-wrapper leftover — `DEFAULT_ALIGN_MODEL` is Flash (live se-agents `align:true`).
- **3H10-BE-003** prompt-taxonomy leftover — `DEFAULT_MODEL` is Flash (live se-agents classify).
- **3H10-BE-004** best-of-n leftover — `DEFAULT_MODEL` is Flash (live align pipeline).
- **3H10-BE-005** agent-collaboration leftover — live `judgeModel` / `model` default Flash (used by agent-task forkJoin).
- **3H10-BE-006** graphrag leftover — community-summaries / map-reduce-qa / eval-criteria / adaptive-benchmark `DEFAULT_MODEL` is Flash (live `/api/se-agents/graphrag/*`).
- **3H10-BE-007** mbpp/humaneval leftover — `solveDirect` / runner default model is Flash (live se-agents benchmarks).
- **3H10-BE-008** appshots leftover — live POST `/capture` stores `resolveNativeDeepSeekModel` (was `gpt-4o-mini`).
- **3H10-BE-009** SSE leftover — live `streamDuplicateTurnReplay` attaches `id:` via `attachSseIds`.
- **3H10-BE-010** SSE leftover — live public-web replay branch attaches `attachSseIds`.
- **3H10-BE-011** SSE leftover — `turn-events` `_end` frame now has `id:` (enterprise /chat SSE).
- **3H10-BE-012** SSE leftover — gateway `GET /api/gateway/events` `attachSseIds` resume.
- **3H10-BE-013** gateway leftover wait scoping — `waitForRun` fail-closed when actor ≠ run owner; AGENT_WAIT passes conn user.
- **3H10-BE-014** gateway leftover HTTP wait — `POST /api/gateway/agent/wait` 401 `user_required` without `req.user`.
- **3H10-BE-015** gateway leftover HTTP events — `GET /api/gateway/events` 401 `user_required`; 403 if session owner mismatches.
- **3H10-BE-016** gateway leftover abort DLQ — `abortSession` pushes `user_abort` to session DLQ (timeout/error already did).
- **3H10-BE-017** rate-limit leftover generate — shared budget adds `/api/appshots`, `/api/code-runner`.
- **3H10-BE-018** rate-limit leftover stop — skip generate budget for sandbox session abort/stop, agent/task abort, doc abort/cancel, `/api/ai/abort`, cowork abort/stop, thesis/plan abort, apps-ai abort/stop.
- **3H10-BE-019** PII leftover — structured-logger keys include camelCase `accessToken`/`refreshToken`/`clientSecret` plus `x-api-key` / `xsrf` / `otp` / `totp` / `webhook_secret`.
- **3H10-BE-020** health secrets leftover — `stripHealthSecrets` / `stripSecretShaped` also redact xsrf/otp/totp/x-api-key/accesstoken.
- **3H10-BE-021** sandbox leftover owner — live GET/DELETE/finalize/download require session owner (403/401). No IDOR on another user's workdir.
- **3H10-BE-022** sandbox leftover abort — live `POST /api/sandbox/session/:id/abort` and `/stop` destroy the owned session (stop ≠ generate).

## Live proof (backend) — 01:10 PT
- `docker compose -f docker-compose.prod.yml -f docker-compose.production.override.yml --env-file .env up -d --no-deps --force-recreate backend` — Recreated + Started
- Container created **01:10 PT** (2026-08-17T06:10:49Z), healthy
- `/health/live` 200, `/health/ready` 200
- Full `/health`: deepseek healthy (dummy: false, api.deepseek.com), r2_artifacts healthy (init: ok), generate_path healthy (path: F2, models deepseek-v4-flash / deepseek-v4-pro, openrouter: false), gvisor_runsc healthy (runtime: runsc)
- `stripHealthSecrets` live: no `DEEPSEEK_API_KEY` literal, no `openrouter.ai`
- GET `/api/gateway/status` → ok, models flash/pro only, `queueHonesty: {lanes:0,writers:0,aborted:0,abortedRuns:0}`, `scoped: {memory:true,skills:true,cron:true}`, deadLetterCount: 0, `turnTimeoutMs: 180000`, no openrouter
- POST `/api/gateway/abort` without sessionKey → `{code:bad_request}`
- POST `/api/gateway/agent` unauthenticated → `{code:user_required}`
- GET `/api/gateway/events?sessionKey=x` unauthenticated → 401 `{code:user_required}`
- POST `/api/gateway/agent/wait` unauthenticated → `{code:user_required}`
- Rotation flag unset (BE-013 skip)
- Existing webtops still up: sira-dpc-cms2hkubp0013oz01mzf0m49e-ceo-office (34h), sira-dpc-testproj-sales (37h)
- Frontend container id unchanged (`ed58ea5a…` / image `7e6064fd00db` from video wave)
- `.env` mtime still 2026-08-17 05:53Z; FAL_KEY / FAL_API_KEY / FAL_KEY_ID / FAL_KEY_SECRET names present, not rewritten
- http://127.0.0.1:3000/chat 200, /code 200; https://siragpt.com/chat 200, /code 200
- Invariants inside live backend: **173/173 passed** (`node --test tests/ola-3h10-invariants.test.js tests/ola-3h9-invariants.test.js tests/ola-3h8-invariants.test.js tests/ola-3h7-invariants.test.js tests/ola-3h6-invariants.test.js tests/ola-3h5-invariants.test.js tests/ola-3h4-invariants.test.js tests/ola-3h3-invariants.test.js tests/ola-3h2-invariants.test.js tests/ola-3h-invariants.test.js`)

## Services
- Recreated: backend only
- Untouched: frontend (video wave image), db, redis, caddy, runner, webtops
- Never ran `down` or `down -v`
- No frontend rebuild

## Honest partials
- **Gmail MCP** (`/generate-gmail`) still uses OpenAI Responses API `connector_gmail` — DeepSeek has no MCP connector.
- **generate-chart** still uses OpenAI containers/code-interpreter (not text generate).
- RAG embeddings still use OpenAI (vector space). Search-preview still OpenAI (search, not generate).
- **apps-ai** remains Cerebras free-tier by design (generated apps, not /chat generate).
- **computer-use** remains OpenAI CUA (not text generate).
- Health overall `degraded` remains the pre-existing non-critical `mcp_policy` (MCP_ALLOWED_HOSTS_REQUIRED). `/health/ready` stays 200.
- Provider inventory still reports `openrouter: true` because `OPENROUTER_API_KEY` exists in env; generate_path `openrouter: false` (not used for text generate).
- model-router.js still unwired — skipped (unused helper).

## Skipped (with reason)
- **All 3H10-FE** — video wave just rebuilt frontend (01:02 PT). Backend-only per conflict rules.
- **BE-013** — do not enable SIRAGPT_REFRESH_TOKEN_ROTATION until a dual-read window exists. Flag remains unset.
- Did not wire BE-081/BE-082 into live handlers (empty prod allowlist would 503; free-shell would break).
- Did not replace OpenAI Gmail MCP connector, code-interpreter charts, search-preview, embeddings, computer-use, or image/music providers.
- Did not rename live webtop containers.
- Did not touch mobile/stores.
- Did not overwrite `/opt/siragpt/.env` FAL_* keys.
- Did not start a frontend rebuild.

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
