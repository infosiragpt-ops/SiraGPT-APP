# Ola 3H16 — 2026-08-17 21:10 America/Lima (2026-08-18T02:10Z)

Motor-only: paridad Claude Code / Cowork en el **engine**, no en la UI.
Numero 3H16 para no chocar con 3H15 (`ola-3h-20260817-2357.md`).

**Frontend skipped / untouched.** Container id `c18e36f496a0142003ba8fe0f91334d37866f631fc1a032d49e0bdaab411e640` created `2026-08-18T01:58:50Z` (20:58 Lima) — another worker owns UI. Zero frontend rebuilds this run. Did not touch chat Video, duration chips, fal, `ai.js`, webtop/computer/noVNC.

This batch wires engine reliability into the live AgentRunner loop used by `/chat` and `/code`: JSON tool-arg repair, transient LLM retry+backoff, infinite-loop cut, step budget, live context compact + pinned facts, in-memory checkpoints + rollback, token usage on cancel, classified errors, first-token/turn-end P², read-after-write + syntax revert, unique-match file patch, sandbox guaranteed cleanup, per-session SSE seq, fifo queue honesty. Did not invent HMAC secret, MCP allowlist, or rotation flag.

## Shipped this run (32 items)

### Backend (recreated `backend` only, twice; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H16-BE-001** (cap 2) `repairToolArgs` — trailing comma, single quotes, truncated braces. `agent-runner/engine-reliability.js`. Verified: invariant.
- **3H16-BE-002** (cap 2) `normalizeToolCalls` — missing id/name, object args, dedupe. Same file. Verified: invariant.
- **3H16-BE-003** (cap 2) `isTransientLlmError` — 429/5xx/ECONNRESET, never 402/abort. Same file. Verified: invariant.
- **3H16-BE-004** (cap 2) `backoffMs` exponential cap 4s. Same file. Verified: invariant (0→200, 1→400, 10→4000).
- **3H16-BE-005** (cap 3) `createRepeatGuard` same tool+args → cut @ 3. Same file. Verified: invariant.
- **3H16-BE-006** (cap 3) `createStepBudget` 40 tool-steps distinct from iteration cap. Same file. Verified: invariant.
- **3H16-BE-007** (cap 4) `compactMessagesIfNeeded` wires `trajectory-compactor` into live loop (was eval-only). Same + `loop.js`. Verified: 20 turns compressed.
- **3H16-BE-008** (cap 4) `pinCriticalFacts` never dropped on compact. Same file. Verified: invariant PINNED FACTS.
- **3H16-BE-009** (cap 5) `createCheckpoint` save/rollback in-memory. Same file. Verified: invariant.
- **3H16-BE-010** (cap 10) `createUsageAccumulator` prompt+completion even mid-run. Same file. Verified: 12+8=20.
- **3H16-BE-011** (cap 12) P² observe first-token / turn-end. Same file + `/health` `engine_loop`. Verified: live check healthy, count 0 (no live turns since recreate).
- **3H16-BE-012** (cap 11) `classifyLoopError` actionable ES, no path traces. Same file. Verified: `ECONNRESET boom at /opt/siragpt` does not leak path.
- **3H16-BE-013** (cap 2) `callModel` retries transient 3× then succeeds. `loop.js`. Verified: scripted 503×2 then ok, n=3.
- **3H16-BE-014** (cap 2) live loop repairs invalid JSON args (`{"path":"notes.md",}`). `loop.js`. Verified: executor received `{path:'notes.md'}`.
- **3H16-BE-015** (cap 3) live `loop_cut` on repeated read_file. `loop.js`. Verified: `stoppedReason=loop_cut` + event.
- **3H16-BE-016** (cap 1) long tool-chain write→python→preview without human. Verified: final Listo, 3 tools ordered.
- **3H16-BE-017** (cap 10) mid-stream cancel attaches `err.usage`. `loop.js` catch. Verified: AbortError.usage.promptTokens=7.
- **3H16-BE-018** (cap 6) `syntaxValidate` JSON/JS compile-only (vm.Script, never run). Verified: invariant.
- **3H16-BE-019** (cap 6) `write_file` read-after-write + syntax fail revert. `agent-runner/tools.js`. Verified: file markers.
- **3H16-BE-020** (cap 6) `str_replace`/`edit_file` syntax revert. Same. Verified: file markers.
- **3H16-BE-021** (cap 7) execute_python/bash `onChunk` stdout/stderr streaming events. Same. Verified: file markers + loop `tool_stdout`.
- **3H16-BE-022** (cap 6) `patchFile` unique-match only (`old_text_not_unique`). `sandbox/session-manager.js`. Verified: live invariant.
- **3H16-BE-023** (cap 7) `destroySession` guaranteed cleanup + `destroyExpired` + `snapshot`. Same. Verified: invariant.
- **3H16-BE-024** (cap 8) event-log **per-session** seq (was global) + `lastSeq` for Last-Event-ID. `agent-gateway/event-log.js`. Verified: s1 lastSeq=2, replayFrom(1)→c.
- **3H16-BE-025** (cap 9) session queue snapshot `order=fifo maxPending=8` live on `/api/gateway/status` + `/health` `gateway_queue`. `queue.js` + `agent-gateway/index.js`. Verified: live JSON.
- **3H16-BE-026** (cap 9) concurrent enqueue same sessionKey stays fifo. Verified: order [1,2].
- **3H16-BE-027** (cap 11) error_codes `loop_cut`/`tool_args_invalid`/`budget_exceeded`/`syntax_invalid`/`checkpoint_rollback`. `error_codes.js`. Verified: invariant.
- **3H16-BE-028** (cap 11) public-stream-error rules for those codes; `sanitizePublicStoppedReason` loop_cut→limit_reached. Verified: invariant.
- **3H16-BE-029** (cap 2) ReAct `parseJsonish` uses `repairToolArgs`. `react.js`. Verified: Action Input trailing comma.
- **3H16-BE-030** (cap 12/5) `/health` non-critical `engine_loop` (ttfbMs/turnEndMs/repair/compact/checkpoint). `health-check.js`. Verified: live healthy. Keys avoid `token` so stripHealthSecrets does not drop them.
- **3H16-BE-031** (cap 11) failing unknown tool does not kill the loop — recovers with final text. Verified: invariant.
- **3H16-BE-032** (cap 5) successful tool turn returns `checkpointId` + `canResume=true`. Verified: invariant.

## Live proof (backend) — 21:10 Lima / 02:10 UTC
- Recreated backend only (second recreate after ttfb key + queueHonesty.order). Frontend id unchanged.
- Container created **21:09 Lima** (2026-08-18T02:09:51Z then 02:11 recreate), healthy, pid 1
- `/health/live` 200, `/health/ready` 200
- Full `/health` status degraded remains pre-existing non-critical `mcp_policy` (+ oauth github, startup_env MCP allowlist). No `DEEPSEEK_API_KEY` literal, no `openrouter.ai`
- `engine_loop` healthy `{ttfbMs:{p50:null,p95:null,count:0}, turnEndMs:{count:0}, repair:true, compact:true, checkpoint:true}`
- `generate_path` healthy (F2, deepseek-v4-flash / deepseek-v4-pro, openrouter: false)
- `gateway_queue` healthy `{lanes:0,pending:0,maxPending:8,order:fifo,deadLetterCount:0}`
- `GET /api/gateway/status` queueHonesty `{order:fifo, maxPending:8, pending:0}` models flash/pro only
- POST `/api/webhooks/inbound` → 503 `{code:hmac_secret_missing, retryable:false}` (unchanged leftover)
- POST `/api/ai/generate` unauthenticated → 401 `access_token_required`
- POST `/api/ai/stop-stream` unauthenticated → 401 `access_token_required`
- http://127.0.0.1:3000/chat 200, /code 200; https://siragpt.com/chat 200, /code 200
- Invariants inside live backend: **26/26** `ola-3h16` + **38/38** `ola-3h15` + 3H14 regression
- Existing webtops still up. No Fal video jobs.

## Services
- Recreated: backend only
- Untouched: frontend, db, redis, caddy, runner, webtops
- Never ran `down` or `down -v`
- No frontend rebuild (count = 0)

## Honest partials
- Inbound HMAC secret still unset (503 fail-closed). Do not invent a key.
- MCP deny-all until Luis sets `SIRAGPT_MCP_ALLOWED_HOSTS`. Empty allowlist would 503 if wired fail-closed.
- `chat_run_worker` dormant skipped, `dormant_ok:true`.
- Refresh-token rotation flag unset.
- Live ttfb/turn-end count=0: P² needs 5 real turns in this process. Instrumented, not faked.
- Checkpoints are in-memory per loop invocation (rollback works); durable PG `agent_checkpoints` still langgraph-only.
- Sandbox code-interpreter backend still `local`. gVisor `runsc` remains agent-runner runtime.
- RAG embeddings still OpenAI vector space (generate path unchanged).

## Skipped (with reason)
- **All FE** — another worker owns UI /chat expand-collapse rebuild.
- Did not set HMAC / MCP hosts / rotation.
- Did not touch `ai.js` / video.js / fal / duration chips.
- Did not enable AGENT_RUNNER_ASYNC.
- Did not persist AgentRunner checkpoints to Postgres this run (needs schema confidence + no data delete).

## Rubric moved
See `docs/mejoras/benchmark-capacidades.md` v3H16. 12/12 capabilities went from unmeasured/unwired → concrete number or live check.

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
Do not put a dummy value in SIRAGPT_MCP_ALLOWED_HOSTS or SIRAGPT_WEBHOOK_HMAC_SECRET.
