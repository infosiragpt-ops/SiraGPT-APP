# Ola 3H21 — 2026-08-18 10:12 America/Lima (2026-08-18T15:12Z)

Motor-only: paridad Claude Code / Cowork en el **engine**, no en la UI.
Numero 3H21 (3H20 = `ola-3h-20260818-1157.md`, 3H19 = `ola-3h-20260818-0857.md`, 3H18 = `ola-3h-20260818-0557.md`, 3H17 = `ola-3h-20260818-0325.md`, 3H16 = `ola-3h-20260818-0210.md`).
Run programado 09:57 Lima 8/18/2026 («Mejora constante chat y code»).

**Frontend skipped / untouched.** Container id `e2d0a0c2c064be49f88d9bb2c22de9598556f1acac8dddd08ea5b8f518fd4755` created `2026-08-18T02:36:50Z` (21:36 Lima) — last user-facing frontend expand-corner. Zero frontend rebuilds. Did not touch chat Video, duration chips, fal, `ai.js` generate catalog, webtop/computer/noVNC, document extractors, RAG attach-to-prompt, or upload pipelines.

This batch is the next layer of the 12-capability rubric after 3H20: DLQ replay hard max-attempts + poison, fence steal metrics, per-tool timeout defaults table in health, credit-ceiling **enforcement** that actually stops generate, SSE replay counters in health, tmpfs cleanup on cancel, gzip checkpoint versioning (reject unknown), hash TTL sweep interval, retrieve_memory tool throw fail-closed. Did not invent HMAC secret, MCP allowlist, rotation flag, or `SIRAGPT_SANDBOX_NET_ALLOW`.

## Shipped this run (38 items)

### Backend (recreated `backend` only, once at end; bind-mounts + force-recreate; no image rebuild; no `down -v`)
- **3H21-BE-001** (cap 3) `capDlqReplayAttempts` below cap is ok. `engine-ops.js`. Verified: invariant.
- **3H21-BE-002** (cap 3) at cap → `dlq_poison`. Verified: invariant.
- **3H21-BE-003** (cap 3) exhausted still `dlq_exhausted` (not poison). Verified: invariant.
- **3H21-BE-004** (cap 3) `scheduleDlqReplayCapped` exponential under cap. Verified: 250/1000.
- **3H21-BE-005** (cap 3) capped replay poisons at max. Verified: invariant.
- **3H21-BE-006** (cap 1) `recordFenceSteal` stolen/refused/vacant counters. Verified: 1/1/1.
- **3H21-BE-007** (cap 1/5) `stealStaleFenceMetered` increments stolen. Verified: invariant.
- **3H21-BE-008** (cap 1) live heartbeat steal is refused + counted. Verified: `fence_conflict`.
- **3H21-BE-009** (cap 2/12) `toolTimeoutDefaultsTable` complete (bash 30s / write 8s / retrieve 4s). Verified: missing=[].
- **3H21-BE-010** (cap 10) `enforceCreditCeiling` under cap does not stop. Verified: remaining 85.
- **3H21-BE-011** (cap 10) over cap **stops** generate (`stop:true`, `credit_ceiling`). Verified: invariant.
- **3H21-BE-012** (cap 8/12) `recordReplayMetrics` accumulates dropped/truncated. Verified: 12→5, dropped 7.
- **3H21-BE-013** (cap 6/7) `cleanupTmpfsOnCancel` unlinks tracked paths. Verified: cleaned=2.
- **3H21-BE-014** (cap 6) tmpfs cleanup rejects `..` traversal. Verified: skipped=2, unlinked only `/tmp/ok`.
- **3H21-BE-015** (cap 5) `gzipCheckpointBlobVersioned` stamps `__gzipVersion:1`. Verified: invariant.
- **3H21-BE-016** (cap 5) gzip v1 roundtrip. Verified: invariant.
- **3H21-BE-017** (cap 5) unknown gzip version fail-closed `gzip_version`. Verified: state=null.
- **3H21-BE-018** (cap 5) legacy gzip without version still inflates. Verified: invariant.
- **3H21-BE-019** (cap 9) `hashSweepDue` respects 60s interval. Verified: invariant.
- **3H21-BE-020** (cap 9) `sweepHashTtl` skip-then-prune. Verified: pruned=1.
- **3H21-BE-021** (cap 4) retrieve_memory generic throw fail-closed. Verified: hits=[].
- **3H21-BE-022** (cap 4) retrieve_memory pgvector still `pgvector_failed`. Verified: invariant.
- **3H21-BE-023** (cap 4) retrieve_memory success returns hits. Verified: count=1.
- **3H21-BE-024** (cap 10) live loop credit ceiling **stops** after 1 generate. Verified: `stoppedReason=credit_ceiling`, calls=1.
- **3H21-BE-025** (cap 2/4) live loop retrieve_memory tool throw does not leak hits. Verified: called=1, `retrieve_memory_failed`.
- **3H21-BE-026** (cap 7) live loop tmpfs cleanup on cancel. Verified: both paths unlinked.
- **3H21-BE-027** (cap 1) live loop stale steal records metrics. Verified: stolen≥1, `stoppedReason=final`.
- **3H21-BE-028** (cap 3) live loop DLQ poison/exhausted after max attempts. Verified: event present.
- **3H21-BE-029** (cap 11) error_codes ops taxonomy. Verified: dlq_poison/gzip_version/hash_sweep/retrieve_memory_failed/tmpfs_cleanup/timeout_table.
- **3H21-BE-030** (cap 11) classifyLoopError + public-stream-error ES, no traces. Verified: invariant.
- **3H21-BE-031** (cap 12) `/health` `engine_loop` ops flags + steal/replay/timeout table. Verified: live JSON.
- **3H21-BE-032** (cap 7) session-manager snapshot `tmpfsCleanupOnCancel`. Verified: invariant.
- **3H21-BE-033** (cap 5) durable put versions gzip then get inflates. Verified: invariant.
- **3H21-BE-034** (cap 9) event-log `pruneHashes` sweep interval. Verified: skip then sweep.
- **3H21-BE-035** (cap 8) event-log remember records replay metrics. Verified: cap 8→5.
- **3H21-BE-036** (cap 3) session-dlq `replayWithJitter` poisons at max. Verified: invariant.
- **3H21-BE-037** (cap 5) tiny blobs skip gzip version stamp. Verified: version=0.
- **3H21-BE-038** (cap 1–12) loop.js / durability / engine-ops wired. Verified: source markers. No openrouter.ai.

## Live proof (backend) — 10:12 Lima / 15:12 UTC
- Recreated backend only (1× at end). Frontend id unchanged (`e2d0a0c2c064…` 21:36 Lima).
- Container created **10:11 Lima** (2026-08-18T15:11:32Z), healthy, pid 1
- `/health/live` 200, `/health/ready` 200
- Full `/health` status degraded remains pre-existing non-critical `mcp_policy`. No `openrouter.ai`
- `engine_loop` healthy `{dlqMaxAttempts, fenceStealMetrics, toolTimeoutTable, creditCeilingEnforce, sseReplayHealth, tmpfsCleanupCancel, gzipVersion, hashSweepInterval, retrieveMemoryFailClosed, stealCounters, replayCounters, timeoutTable}` — ttfb/turnEnd hydrated from Redis (scripted overhead, not Flash)
- `generate_path` healthy (F2, deepseek-v4-flash / deepseek-v4-pro, openrouter: false)
- `gateway_queue` healthy `{lanes:0,pending:0,maxPending:8,order:fifo}`
- `GET /api/gateway/status` queueHonesty `{order:fifo, maxPending:8}` models flash/pro only, surfaces chat+code
- POST `/api/webhooks/inbound` → 503 `{code:hmac_secret_missing, retryable:false}`
- POST `/api/ai/generate` unauthenticated → 401 `access_token_required`
- POST `/api/ai/stop-stream` unauthenticated → 401 `access_token_required`
- http://127.0.0.1:3000/chat 200, /code 200; https://siragpt.com/chat 200, /code 200
- Invariants inside live backend: **38/38** `ola-3h21` + **38/38** `ola-3h20` + **38/38** `ola-3h19` + **35/35** `ola-3h18` + **35/35** `ola-3h17` + **26/26** `ola-3h16`
- Existing webtops still up. No Fal video jobs.
- Did not edit document extractors / RAG / upload pipelines (other executor).

## Services
- Recreated: backend only (1× at end)
- Untouched: frontend, db, redis, caddy, runner, webtops
- Never ran `down` or `down -v`
- No frontend rebuild (count = 0)

## Honest partials
- Inbound HMAC secret still unset (503 fail-closed). Do not invent a key.
- MCP deny-all until Luis sets `SIRAGPT_MCP_ALLOWED_HOSTS`.
- `chat_run_worker` dormant skipped, `dormant_ok:true`.
- Refresh-token rotation flag unset.
- `SIRAGPT_SANDBOX_NET_ALLOW` unset — deny-all honesto. No inventar hosts.
- Latency hydrate is **scripted engine overhead** (mock LLM). Not DeepSeek Flash TTFB. Do not report as Flash p50.
- PG put remains fail-open; Redis path verified for post-recreate resume + fence heartbeat/steal + gzip v1.
- Sandbox code-interpreter backend still `local`. tmpfs cleanup is policy+unlink of tracked paths on cancel; we do not remount a live tmpfs.
- Legacy gzip blobs without `__gzipVersion` still inflate (accepted as v1). Unknown versions fail-closed.

## Skipped (with reason)
- **All FE** — last user-facing frontend is expand-corner 21:36 Lima. Constraint: do not rebuild.
- Did not set HMAC / MCP hosts / rotation / SANDBOX_NET_ALLOW.
- Did not touch `ai.js` / video.js / fal / duration chips / document extractors / RAG attach.
- Did not enable AGENT_RUNNER_ASYNC.
- Did not burn DeepSeek or Fal to measure live Flash TTFB.

## Rubric moved
See `docs/mejoras/benchmark-capacidades.md` v3H21. 12/12 capabilities moved (DLQ max-attempts+poison, fence steal metrics, timeout table, credit-ceiling stop, SSE replay health counters, tmpfs cleanup on cancel, gzip v1, hash sweep interval, retrieve_memory fail-closed, new ES codes, health flags).

Do not enable SIRAGPT_REFRESH_TOKEN_ROTATION=1 until a dual-read window exists.
Do not put a dummy value in SIRAGPT_MCP_ALLOWED_HOSTS or SIRAGPT_WEBHOOK_HMAC_SECRET or SIRAGPT_SANDBOX_NET_ALLOW.
