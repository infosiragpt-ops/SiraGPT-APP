# patches — dependency patch policy

SiraGPT usa overrides en `package.json` cuando hace falta.

No hay parches vendorizados activos en esta carpeta. Añadir aquí solo patches documentados con justificación y test asociado.

Validar: `npm run agent:platform:map`
