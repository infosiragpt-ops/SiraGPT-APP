# SiraGPT — Plan de 200 mejoras (100 profesionales + 100 backend) + validacion E2E en vivo

> Generado el 2026-06-09. **Restriccion dura:** ninguna propuesta toca la UI (botones, layout, colores, componentes). Todo es interno/funcional. Este documento es un *plan/propuesta*; nada esta implementado todavia.

## Validacion end-to-end EN VIVO (real, sin mocks)

Ejecutado contra el backend real (`/api/files/upload` + `/api/ai/generate`, stack de enrichment completo) con documentos reales (docx/xlsx/pdf/png-OCR) y modelo `Cerebras/gpt-oss-120b`. **Resultado: 90/101 (89%)**, umbral 75% -> PASA.

| Categoria | Pass |
|---|---|
| excel | 11/13 (85%) |
| docx | 14/15 (93%) |
| pdf | 12/12 (100%) |
| image | 7/8 (88%) |
| cross-doc | 3/8 (38%) |
| multi-turn | 10/11 (91%) |
| reasoning | 12/12 (100%) |
| multilingual | 9/10 (90%) |
| coding | 12/12 (100%) |
| OVERALL | 90/101 (89%) — threshold 75% |

**Hallazgos clave (alimentan el plan):**
- 100% en pdf, reasoning, coding. Solido en docx (93%), multi-turn (91%), multilingue (90%), image-OCR (88%), excel (85%).
- **cross-doc 38% (5/8)** — la sintesis cruzando *dos* documentos es la mayor debilidad.
- **Sobre-clarificacion**: varios fallos (d9, x4, m2) el modelo pregunta "que formato quieres?" en vez de responder.
- **OCR**: 1 imagen (i1) con texto insuficiente extraido.

---

### Professional improvements

## Testing & Quality Assurance

**1. Prisma contract matchers for tests** — Tests mock Prisma inline so schema drift silently breaks 200+ mocks; add `backend/tests/lib/prisma-contract-matcher.js` validating method calls (argument shapes/return types) against the schema and migrate high-impact suites. _(files: backend/tests/lib/prisma-contract-matcher.js, backend/tests/chat-run-endpoints.test.js, backend/prisma/schema.prisma; effort: M; impact: high)_
**2. Deterministic E2E fixture registry** — The live harness grades by substring match against ephemeral, unversioned facts; add `e2e-fixture-registry.json` with per-fact hashes + a `test:e2e:baseline:update` command to detect silent fixture regressions. _(files: backend/scripts/run-live-chat-e2e.js, backend/scripts/lib/e2e-fixtures.js; effort: M; impact: medium)_
**3. Transactional test isolation wrapper** — Shared Postgres lets tests writing to `auditLog`/shared tables contaminate parallel shards; add `test-transaction-wrapper.js` (`withTransaction`, opt-out `skipRollback`) applied by default in the root test script. _(files: backend/tests/lib/test-transaction-wrapper.js, backend/package.json, .github/workflows/ci.yml; effort: L; impact: high)_
**4. Stack-aware async assertion helpers** — 4300+ async tests use bare `assert.ok`, so rejections deep in services lose context; add `async-assert.js` (`rejectsWithMessage`, `resolvesToMatch`, `operationCompletes`) that captures the pre-await stack and operation context. _(files: backend/tests/lib/async-assert.js, backend/tests/agent-core.test.js, backend/tests/rag-service.hybrid.test.js; effort: M; impact: medium)_
**5. Centralized mock cleanup registry** — 214 test files restore mocks inconsistently, leaking into sequential runs; add `mock-registry.js` (`mock`/`restore`/`assertEmpty`) wired into a root `afterEach` plus a CI leak check. _(files: backend/tests/lib/mock-registry.js, backend/tests/github-search.test.js, backend/scripts/test-shard.sh; effort: M; impact: high)_
**6. Property tests for ranking algorithms** — MMR, citation, and RRF fusion use example-based asserts that miss monotonicity/symmetry violations; add `fast-check` `.property.test.js` suites (diversity penalty ≤0, citations ⊆ corpus, fused scores ∈ [0,1]). _(files: backend/tests/mmr.test.js, backend/tests/citation-engine.test.js, backend/tests/rag-service.hybrid.test.js; effort: M; impact: medium)_
**7. API spec ↔ implementation parity tests** — `api-contract.test.js` confirms routes are documented but not request/response shape compliance; extend it to validate sample payloads against Zod schemas and assert documented error codes for invalid input. _(files: backend/tests/api-contract.test.js, backend/src/services/openapi/route-scanner.js; effort: M; impact: medium)_
**8. Per-test metrics + slow-test gate in CI** — No visibility into which sharded tests are slow/flaky and coverage is shard-1-only; add `test-metrics.js` emitting `.test-metrics.jsonl` and a CI step failing on >5s tests or >10% aggregate growth. _(files: .github/workflows/ci.yml, backend/package.json, backend/tests/lib/test-metrics.js; effort: M; impact: medium)_
**9. Test naming convention + coverage-tier reporting** — Names mix unit/integration/contract with no signal of DB dependence; adopt `[UNIT]`/`[INT]`/`[PROP]`/`[E2E]` prefixes, document in `backend/TESTING.md`, and report counts by tier in CI. _(files: backend/TESTING.md, backend/tests, .github/workflows/ci.yml; effort: M; impact: medium)_
**10. Shared test data factory library** — Domain objects are hardcoded inline, so schema changes touch 50+ files; add `factories.js` (`users.one`, `chats.one`, `files.one`) returning valid Prisma-shaped defaults with overrides. _(files: backend/tests/lib/factories.js, backend/tests/agent-core.test.js, backend/package.json; effort: M; impact: medium)_
**11. Mutation-testing pilot for critical modules** — Passing tests don't prove assertions exercise the code; run Stryker on `agent-core.js`/`rag-service.js`/`citation-engine.js` with a 75% kill-rate gate and a GitHub check. _(files: .github/workflows/ci.yml, backend/package.json, backend/src/services/agents/agent-core.js; effort: L; impact: high)_
**12. Linear-complexity latency assertions** — Correctness tests can't catch O(n²) regressions under load; add `latency-assert.js` `benchmarkLinear(fn, sizes)` for MMR/RAG retrieval/memory eviction as opt-in `[PERF]` tests on main. _(files: backend/tests/lib/latency-assert.js, backend/tests/mmr.test.js, backend/tests/long-term-memory-lifecycle.test.js; effort: M; impact: medium)_
**13. Snapshot tests for multi-turn agent transcripts** — Non-deterministic intermediate states break substring grading and hide regressions; record full transcripts as YAML snapshots in `backend/tests/snapshots/` with metadata + diffing in CI. _(files: backend/scripts/run-live-chat-e2e.js, backend/scripts/lib/e2e-corpus.js, backend/tests/snapshots; effort: M; impact: high)_

## CI/CD & Build Pipeline

**14. `npm ci` retry with backoff** — Transient registry ECONNRESETs fail the whole workflow; wrap `npm ci` in a 3-attempt exponential-backoff helper and set `fetch-timeout`/`fetch-retries` in `.npmrc`. _(files: .github/workflows/ci.yml, .npmrc; effort: M; impact: high)_
**15. BuildKit cache mounts for `npm ci`** — Docker re-downloads the full dep tree each build; add `--mount=type=cache,target=/root/.npm` to both Dockerfiles and enable `DOCKER_BUILDKIT=1` to cut cold builds ~3x. _(files: Dockerfile, backend/Dockerfile, docker-compose.yml; effort: M; impact: high)_
**16. Drop `node_modules` from frontend runner stage** — The Next.js standalone output already traces runtime deps, yet the runner copies ~1.5GB of `node_modules`; remove that COPY and rely on `.next/standalone`. _(files: Dockerfile, scripts/postbuild-slim.js; effort: S; impact: high)_
**17. Reusable setup-and-cache composite action** — Frontend/backend jobs duplicate Prisma-engine and npm cache logic that drifts; extract `.github/actions/setup-node-and-cache/action.yml` parameterized by workspace + schema path. _(files: .github/workflows/ci.yml, .github/actions/setup-node-and-cache/action.yml; effort: M; impact: medium)_
**18. GHA cache for Docker BuildKit layers** — `build-push-action` starts a cold builder each run; add `cache-from`/`cache-to: type=gha` scoped per image to halve CI Docker builds. _(files: .github/workflows/ci.yml; effort: S; impact: high)_
**19. Timing-aware backend shard assignment** — Round-robin sharding spreads files, not runtime, so one slow shard nears the 15m ceiling; add per-shard timeout overrides and timing-aware chunking. _(files: .github/workflows/ci.yml, backend/scripts/test-shard.sh; effort: M; impact: medium)_
**20. Branch-tiered Next.js build cache** — Concurrent PRs miss the `.next/cache` on slight source-hash diffs; restructure the key to per-branch primary with a main-branch restore-key fallback. _(files: .github/workflows/ci.yml, next.config.mjs; effort: M; impact: medium)_
**21. Hard migration-safety gate in CI** — `check-migration-safety.js` only runs at deploy time, letting unsafe migrations merge to main; add a `migration-safety-audit` job to the required `ci` `needs:` list. _(files: .github/workflows/ci.yml, scripts/check-migration-safety.js; effort: S; impact: high)_
**22. Persisted bundle-size & coverage baselines** — Metrics recompute each run with no rolling baseline for regression detection; commit reports to a `ci-metrics` branch on green and compare PRs against recent baselines. _(files: .github/workflows/ci.yml, scripts/bundle-size-check.js; effort: M; impact: medium)_
**23. UI-lock verification on source dirs only** — Hashing config files (postcss/tailwind) triggers false-positive lock violations on no-op changes; restrict `verify-ui-lock.sh` to app/components/hooks/lib/styles and diff against main. _(files: scripts/verify-ui-lock.sh, .github/workflows/ci.yml; effort: M; impact: medium)_
**24. Split concurrency groups for gates vs informational jobs** — `continue-on-error` jobs ignore cancellation and pile up queued runs; give hard gates and informational jobs separate concurrency groups. _(files: .github/workflows/ci.yml; effort: M; impact: medium)_
**25. Playwright E2E diagnostics + retries** — A single pre-warm flake fails the suite and logs aren't uploaded; add curl backoff loops, `--retries=2`, and always-upload `.next-e2e.log`/`playwright-report`. _(files: .github/workflows/ci.yml; effort: M; impact: medium)_
**26. CI-enforced API-types generation check** — `generate-api-types.js` isn't wired into CI so `lib/api-types.ts` drifts; add `generate:api-types:check` to the frontend job and a husky pre-commit regen hook. _(files: lib/api-types.ts, .github/workflows/ci.yml, backend/package.json, .husky; effort: S; impact: medium)_
**27. OpenAPI spec ↔ route divergence validator** — `docs/openapi.json` is a static artifact that silently drifts from routes; add `validate-openapi.js` scanning Express routes and failing CI past a divergence threshold. _(files: docs/openapi.json, .github/workflows/ci.yml, backend/package.json; effort: M; impact: medium)_

## Database & Query Performance

**28. Request-scoped Prisma query dedup** — `ai.js` issues 47+ scattered finds, repeating identical history queries within one request; add an AsyncLocalStorage query interceptor that fingerprints and caches per-request reads. _(files: backend/src/routes/ai.js, backend/src/config/database.js, backend/src/middleware; effort: M; impact: high)_
**29. Eager-load message history relations** — Messages then `message.files` are fetched separately, causing 80–100 extra round-trips; replace with `include` plus a `buildEagerMessageContext()` that picks the include set by intent. _(files: backend/src/routes/ai.js; effort: M; impact: high)_
**30. Selective field projection sets** — Bare `findFirst/findMany` ship full User/Message blobs (phone, tokens) over the wire; define reusable projection sets and a lint rule failing CI on selectless queries. _(files: backend/src/routes/ai.js, backend/src/config/database.js; effort: M; impact: medium)_
**31. CPU-aware connection-pool tuning** — Hardcoded POOL_MIN=2/MAX=10 ignores deployment size, causing pool-exhaustion cascades under load; compute pool size from `os.cpus()`, allow `DATABASE_POOL_MAX` override, and log utilization. _(files: backend/src/config/database.js, backend/src/middleware; effort: M; impact: high)_
**32. Cursor-based message-history pagination** — `.take(80)` with no cursor rescans/discards thousands of rows in long chats; switch to `id > cursor` ordering plus a `(userId, timestamp DESC)` index. _(files: backend/src/routes/ai.js, backend/prisma/schema.prisma; effort: M; impact: high)_
**33. Prisma query-latency & pool metrics** — DB bottlenecks are invisible until `/health/ready` fails; attach a Prisma query event listener emitting `siragpt_prisma_query_duration_ms` + pool gauges and WARN on >1s queries. _(files: backend/src/config/database.js, backend/src/routes/metrics.js, backend/src/services/observability/health-check.js; effort: L; impact: high)_
**34. Runtime Zod validation on hot Prisma reads** — Compile-time types don't catch a stale migration until a field is dereferenced; wrap high-frequency user/org/credit reads with `schema.parse()` to fail fast at the query. _(files: backend/src/routes/ai.js, backend/src/schemas; effort: M; impact: medium)_

## Scalability & Performance

**35. Deferred document-pipeline worker queue** — Upload parses + RAG-indexes synchronously, blocking Express workers 10–30s for large batches; return 202 + jobId, process in a BullMQ worker, and poll `/files/:id/status`. _(files: backend/src/routes/files.js, backend/src/services/document-pipeline; effort: L; impact: high)_
**36. Micro-batched SSE chunk writer** — A `res.write()`+flush per token adds 3–5s of flush overhead per long response; buffer 10–50ms or 16KB before flushing, tunable via `SIRAGPT_SSE_BATCH_MS/_BYTES`. _(files: backend/src/routes/ai.js; effort: S; impact: medium)_
**37. Proxy-aware SSE heartbeat tuning** — Cloud proxies drop idle SSE at 30–60s, leaving zombie connections on long agentic runs; emit `:heartbeat` every `SIRAGPT_SSE_HEARTBEAT_MS` and send `X-Accel-Buffering: no`. _(files: backend/src/routes/ai.js; effort: S; impact: medium)_
**38. Lazy-require heavy backend modules** — `ai.js` top-level loads 100+ modules into every worker even for `/api/models`; wrap visual-media/attribution/agent modules in a cached `requireLazy()` gated by detected intent. _(files: backend/src/routes/ai.js; effort: M; impact: medium)_
**39. Long-term-memory pruning + compression** — LTM grows unbounded so recall scans an unindexed list and DB backups bloat; cap to N≈1000 salient entries per user with recency-decay pruning, TTL archival, and a `(userId, strength DESC)` index. _(files: backend/src/services/long-term-memory.js, backend/prisma/schema.prisma; effort: M; impact: medium)_
**40. Request prioritization middleware** — A runaway admin query starves chat users on the shared pool with no SLA notion; tag requests with priority via AsyncLocalStorage and shed lower-priority load past per-tier wait SLAs. _(files: backend/src/middleware, backend/src/routes; effort: M; impact: high)_
**41. Frontend route-prefix chunk manifest (non-visual)** — Lazy chunks still bundle transitive deps (PDF.js, ExcelJS) into the chat route; add build-time async-import barriers + a `chunks-manifest.json` so clients fetch chunks on demand. _(files: components/chat-interface-enhanced.tsx, next.config.mjs; effort: L; impact: high)_

## Security & Authentication

**42. Outbound URL allowlist (SSRF guard)** — Stripe invoice PDFs and image URLs are fetched without domain/IP validation, enabling SSRF to internal/metadata services; add `url-allowlist.js` `validateAndFetchUrl()` blocking private ranges and non-HTTP(S) schemes. _(files: backend/src/utils/url-allowlist.js, backend/src/routes/payments.js, backend/src/routes/ai.js; effort: M; impact: high)_
**43. Opaque rotating refresh tokens** — 7-day re-signed JWT refresh tokens are fully replayable if stolen; replace with opaque DB-persisted tokens, 15-min access tokens, and atomic rotation on `/refresh`. _(files: backend/src/routes/auth.js, backend/prisma/schema.prisma, backend/src/services/auth/refresh-token-rotation.ts; effort: L; impact: high)_
**44. Gate hardcoded dev-admin credentials** — `LoginService.js` embeds a real dev email/password bypass that could leak into prod; move to `dev-fixtures.js` behind `NODE_ENV==='development' && ENABLE_DEV_ADMIN`, with a test asserting unreachability. _(files: backend/src/services/LoginService.js, backend/src/services/auth/dev-fixtures.js; effort: S; impact: high)_
**45. Whitelist dynamic SQL field names** — `$executeRawUnsafe` interpolates unvalidated field names into SET clauses in the scheduler store; validate each against the allowed `fields` array via `validateFieldName()` (or migrate to Prisma `update`). _(files: backend/src/scheduler/store.js, backend/src/utils/sql-field-validator.js; effort: S; impact: medium)_
**46. CSP enforcing mode in production** — CSP defaults to report-only with `unsafe-inline`/`unsafe-eval`, negating its benefits; set `CSP_STRICT=true`/`CSP_REPORT_ONLY=false` for prod, adopt nonce injection, and add a CI header audit. _(files: backend/src/middleware/csp-policy.js, .env.example, backend/src/config/csp-production-defaults.js; effort: M; impact: high)_
**47. Redis-backed auth rate limits (fail-closed)** — In-memory limiters don't coordinate across workers and fail open on Redis errors; require sliding-window Redis counters with per-email+IP limits, backoff, and `Retry-After`. _(files: backend/src/routes/auth.js, backend/src/middleware/rate-limit-auth.js, backend/src/middleware/rate-limit-store.js; effort: M; impact: high)_
**48. Filename sanitization before disk write** — Upload appends a suffix without stripping path separators/null bytes from the original name, risking traversal; add `sanitizeUploadFilename()` and store the raw name only as metadata. _(files: backend/src/middleware/upload.js, backend/src/utils/filename-sanitizer.js; effort: S; impact: medium)_
**49. Structured log redaction for PII/secrets** — Handlers logging `req.body`/headers can leak API keys, card tokens, and PII; add a pino-integrated `LogRedactor` with a denylist (password/token/apiKey/ssn/creditCard) applied to bodies and stack traces. _(files: backend/src/middleware/logger.js, backend/src/middleware/pino-logger.js, backend/src/utils/log-redactor.js; effort: M; impact: high)_
**50. Upstream rate-limit header backoff** — Provider calls ignore `Retry-After`/`X-RateLimit-*`, burning quota and triggering stricter limits; add `parseRateLimitHeaders()`, Redis-tracked backoff, and a 429 circuit breaker. _(files: backend/src/routes/ai.js, backend/src/routes/video.js, backend/src/utils/rate-limit-header-parser.js; effort: M; impact: medium)_
**51. Per-feature API-key scopes** — User API keys grant full account access with no read-only or endpoint-restricted option; add a `scopes` field to `ApiKey` and enforce it in the rate-limit/scope middleware. _(files: backend/src/middleware/enforce-api-key-rate-limit.js, backend/src/middleware/require-scope.js, backend/prisma/schema.prisma; effort: L; impact: medium)_
**52. Webhook signature + replay-protection framework** — Only Stripe verifies signatures; other webhooks lack HMAC checks and idempotency; add a `WebhookValidator` + registry enforcing signatures and `X-Webhook-Delivery-Id` dedup (Redis, 24h TTL). _(files: backend/src/routes/webhooks.js, backend/src/services/webhook-validator.js, backend/src/services/webhook-registry.js; effort: M; impact: high)_

## Observability & Operations

**53. Auto-propagating OTel trace-context middleware** — Incoming `traceparent` isn't bound or injected into outbound calls, breaking distributed traces across boundaries; add `otel-trace-context.js` binding via AsyncLocalStorage and a fetch injector, mounted before routes. _(files: backend/src/services/observability/trace-context.js, backend/index.js, backend/src/middleware/otel-trace-context.js; effort: M; impact: high)_
**54. Global per-route HTTP span middleware** — Only `/api/ai/generate` emits OTel spans, leaving chats/files/agent-task untraced; mount the existing `httpSpanMiddleware()` globally behind `OTEL_HTTP_MIDDLEWARE=1`. _(files: backend/src/utils/otel-spans.js, backend/index.js, backend/src/routes/agent-task.js, backend/src/routes/chats.js; effort: S; impact: high)_
**55. Structured agent-execution logging** — The agent loop logs via `console.log`/Langfuse without consistent `{agentId,taskId,userId,step}` bindings, blocking correlation; add `createAgentLogger()` emitting at task/tool/step waypoints. _(files: backend/src/services/observability/structured-logger.js, backend/src/routes/agent-task.js, backend/src/services/agents/agent-telemetry.js; effort: M; impact: high)_
**56. Split liveness/readiness + circuit-breaker probe** — `/api/health` conflates liveness and readiness and hides breaker state; add `/health/providers` exposing breaker states and surface `circuit_breakers` in `/health/ready`. _(files: lib/next-health.ts, backend/src/routes/health-routes.js, backend/src/services/circuit-breaker.js; effort: M; impact: high)_
**57. True percentile SLO histograms** — `slo-tracker.js` uses coarse buckets that hide tail latency; replace with the existing p2-quantile sketch exposing p50/p95/p99/p999 per route as Prometheus quantiles. _(files: backend/src/services/slo-tracker.js, backend/src/services/observability/p2-quantile.js, backend/src/routes/metrics.js; effort: M; impact: medium)_
**58. LLM-call OTel span wrapper** — LLM latency/errors/tokens are scattered across Langfuse/Sentry/console; add `llm-span-wrapper.js` tagging spans by provider/model/tokens and a `siragpt_llm_request_duration_seconds` metric. _(files: backend/src/utils/otel-spans.js, backend/src/orchestration/llm-gateway.js, backend/src/routes/metrics.js; effort: L; impact: high)_
**59. Logging cardinality guardrails** — High-cardinality `userId`/`fileId` fields can explode log volume; extend `structured-logger.js` with a `maxFieldCardinality` map that hash-buckets values and counts violations. _(files: backend/src/services/observability/structured-logger.js, backend/index.js; effort: S; impact: medium)_
**60. Circuit-breaker Prometheus metrics + alerts** — Breaker transitions are logged but never exported and `allStats()` is unused; emit state/failure/transition metrics, alert on CLOSED→OPEN, and add `/api/circuit-breakers`. _(files: backend/src/services/circuit-breaker.js, backend/src/routes/metrics.js, backend/src/services/alerting.js; effort: M; impact: high)_
**61. Error classification & aggregation middleware** — Error cascades flood Sentry/Slack with one alert per request; add `error-aggregator.js` deduping by (route,status,message) in 30s windows into a single counted alert. _(files: backend/src/middleware/error-handler.js, backend/src/services/alerting.js, backend/src/routes/metrics.js; effort: M; impact: medium)_
**62. Trace-context propagation into BullMQ jobs** — Worker failures are disconnected from the originating request because trace context isn't embedded in job data; capture context on enqueue and re-establish it in workers. _(files: backend/src/services/agents/agent-task-queue.js, backend/src/services/goal-queue.js, backend/src/services/agents/agent-task-worker.js; effort: M; impact: high)_
**63. Cognitive routing & faithfulness metrics** — Routing decisions and faithfulness scores live only in logs; export `siragpt_routing_decision_total` and `siragpt_faithfulness_score` from `cognitive-metrics.js` plus a recent-decisions endpoint. _(files: backend/src/services/cognitive-metrics.js, backend/src/routes/metrics.js, backend/src/routes/ai.js; effort: M; impact: medium)_
**64. End-to-end X-Request-ID correlation** — Frontend and backend mint independent request IDs that don't match across logs; generate one in `middleware.ts` and forward it (including health checks) for the backend logger to reuse. _(files: middleware.ts, lib/next-health.ts, backend/src/middleware/request-logger.js; effort: S; impact: medium)_

## Type Safety & Code Quality

**65. JSDoc types on backend service entry points** — 598 async functions lack `@param`/`@returns`, breaking autocomplete; annotate 50+ highest-impact route/service signatures and add an eslint rule flagging missing `@returns` on async functions. _(files: backend/src/routes/ai.js, backend/src/middleware/charge-credits.js, backend/src/services/ai-service.js; effort: M; impact: high)_
**66. Typed error guards for catch blocks** — 339 untyped/swallowed catches hide real failures (e.g. JSON.parse); add `error-normalizer.js` guards (`isNetworkError`, `isRateLimitError`), rethrow unknowns, and enforce `catch (e: unknown)`. _(files: backend/src/utils/error-normalizer.js, lib/api.ts, backend/src/routes/ai.js; effort: M; impact: high)_
**67. Typed response-envelope factories** — 21+ call sites hand-build envelopes with mismatched pagination keys; add `response-envelope.js` (`success`/`paginated`/`error`) with Zod validation before serialization. _(files: lib/api.ts, backend/src/routes/ai.js, backend/src/utils/response-envelope.js; effort: M; impact: medium)_
**68. Shared app-error class hierarchy** — 2075+ raw `new Error()` give frontend no way to distinguish a 429 from a timeout; add `app-errors.js` (`ValidationError`, `RateLimitError`, `InsufficientCreditsError`…) surfaced as `error.code`. _(files: backend/src/utils/app-errors.js, backend/src/middleware/error-handler.js, lib/api.ts; effort: M; impact: medium)_
**69. Tighten ESLint max-warnings ratchet** — The gate sits at 50 with 47 actual warnings, so any new one breaks the build; triage/fix dead-code and console warnings, then lower max-warnings to 25 and repeat quarterly. _(files: .eslintrc.json, package.json; effort: S; impact: medium)_
**70. Shared safe-response-header utility** — 5 middlewares copy-paste `try{res.setHeader}catch{}` 12+ times each; extract `safe-response-headers.js` `setHeader()` returning success/fail with optional instrumentation. _(files: backend/src/middleware/enforce-api-key-rate-limit.js, backend/src/middleware/enforce-org-quota.js, backend/src/utils/safe-response-headers.js; effort: S; impact: low)_
**71. Logged `Promise.allSettled` helper** — `Promise.all(...).catch(()=>null)` masks which items failed across 187 sites; add `allSettledWithLogging()` returning `{results, failures}` and use it in file-loading paths. _(files: backend/src/routes/ai.js, backend/src/utils/promise-helpers.js; effort: M; impact: medium)_
**72. Zod schemas for every POST handler** — Mixed inline/express-validator checks leave request shapes undiscoverable; add `backend/src/schemas/*` Zod schemas consumed by `validateBody` with a hook flagging schemaless POST routes. _(files: backend/src/routes/ai.js, backend/src/schemas, backend/src/middleware/validate-body.js; effort: L; impact: high)_
**73. Coded audit of silent error suppressions** — `catch (_) {}` can't be told from a bug; tag each with a code (`[H001]` headers-sent, `[N001]` optional service) documented in `ERRORS-SUPPRESSED.md` with a generator script. _(files: backend/src/middleware, ERRORS-SUPPRESSED.md; effort: M; impact: medium)_
**74. Typed request-context declaration** — `req.user`, `req.validatedBody`, `req.orgContext` etc. are untyped, so reorderings cause prod undefined errors; add `request-context.d.ts` and a `tsc --noEmit` CI check over `backend/src`. _(files: backend/src/types/request-context.d.ts, backend/src/routes/ai.js, backend/src/middleware; effort: M; impact: medium)_
**75. Consolidate provider-client init boilerplate** — Four near-identical `create*Client`/`*Safe` functions copy try/catch/fallback logic; replace with `safe-require.js` `loadModuleWithFallback()`. _(files: backend/src/routes/ai.js, backend/src/utils/safe-require.js; effort: S; impact: low)_
**76. Centralized provider failover resolver** — Failover/retry is reimplemented inline in `ai.js` and `chargeCredits` with no health tracking; add `provider-resolver.js` returning `{provider, model, via, metrics}` backed by a Redis health cache. _(files: backend/src/routes/ai.js, backend/src/services/provider-resolver.js, backend/src/services/ai/provider-inference.js; effort: M; impact: high)_
**77. Eliminate `as any` casts with real types** — 128+ `as any`/`as unknown` around uploads/attachments/metadata make refactors unsafe; replace with interfaces, type guards, or `.d.ts` shims, targeting zero `as any`. _(files: lib/chat-context-integrated.tsx, lib/auth-context-integrated.tsx; effort: L; impact: medium)_
**78. Resolve WordConnector `@ts-ignore` dual-tiptap conflict** — 3 `@ts-ignore` hide a duplicate tiptap install; dedupe via package-lock (or a type bridge) and replace with real assertions. _(files: components/WordConnector.tsx, package.json; effort: S; impact: low)_

## Frontend Architecture (non-visual)

**79. Validated env config module** — `NEXT_PUBLIC_*` vars are read raw with string fallbacks and no validation; add `lib/env.ts` with a Zod schema validated at startup and a typed config object. _(files: lib/settings-context.tsx, lib/chat-context-integrated.tsx, lib/api-base-url.ts; effort: M; impact: medium)_
**80. Route console logs through structured client logging** — 128+ raw `console.error`/`warn` lack context and never reach error tracking; replace with `reportClientLog({action, source, severity})`. _(files: lib/chat-context-integrated.tsx, lib/client-logs.ts; effort: M; impact: medium)_
**81. Safe context-hook helper** — `useNavigationTransition` hard-throws outside its provider with no recovery; add `useContextSafe<T>` returning `[value, isReady]` and wrap custom context hooks. _(files: components/navigation-transition-context.tsx, lib/auth-context-integrated.tsx, lib/chat-context-integrated.tsx; effort: M; impact: medium)_
**82. Unified storage abstraction** — 40+ direct `localStorage` calls silently fail in private mode and swallow quota errors; add `lib/storage.ts` with error handling, in-memory fallback, and schema versioning. _(files: lib/settings-context.tsx, hooks/use-chat-draft.ts, lib/client-logs.ts; effort: M; impact: high)_
**83. Reducer-based chat state** — 15+ overlapping `useState`s in a 3258-line context cause race conditions and stale closures; consolidate into a single `ChatState` reducer with memoized actions. _(files: lib/chat-context-integrated.tsx; effort: L; impact: high)_
**84. Fix missing useEffect dependencies** — Effects reference `user`/`token`/`selectedChatId` without listing them, capturing stale values; audit deps, wrap in `useCallback`, and set `react-hooks/exhaustive-deps` to error. _(files: lib/chat-context-integrated.tsx, lib/auth-context-integrated.tsx; effort: M; impact: high)_
**85. Decompose 10.7k-line chat interface module** — The monolith defeats tree-shaking and makes fixes require whole-file context; split into Composer/Search/Upload/Voice/CodePreview feature modules behind a lean shell (no visual change). _(files: components/chat-interface-enhanced.tsx; effort: L; impact: medium)_
**86. Request-deduping query abstraction** — `useAsyncWithRetry` lacks dedup/cache/SWR, so concurrent identical fetches both fire; add `useQuery(key, fetcher)` with a pending-promise WeakMap, TTL cache, and `invalidate`. _(files: hooks/use-async-retry.ts, lib/api.ts; effort: M; impact: high)_
**87. Central Zod validation registry (frontend)** — Auto-generated API types use `[key:string]:unknown` and forms use ad-hoc checks, letting invalid data reach the backend; add `lib/validation.ts` schemas used in form resolvers and API parsing. _(files: lib/api-types.ts, lib/api.ts; effort: M; impact: medium)_
**88. Memoize heavy message/artifact components** — Markdown/charts/viewers re-render on every keystroke; wrap MermaidBlock/PlotlyChart/CodeBlock/ArtifactCard in `React.memo` with prop-scoped comparators (no visual change). _(files: components/message-component.tsx, components/chat-interface-enhanced.tsx; effort: M; impact: high)_
**89. Suspense + error boundaries for dynamic features** — Dynamically imported viewer/chart lack Suspense and per-feature boundaries, so failures silently break rendering; wrap each in a non-visual error boundary that degrades gracefully. _(files: components/chat-interface-enhanced.tsx, components/message-component.tsx; effort: M; impact: medium)_
**90. Document and centralize `dangerouslySetInnerHTML`** — 5 usages lack a documented sanitizer, risking XSS if sources change; add a `<SafeHTML sanitizer=...>` wrapper enforcing DOMPurify/rehype-sanitize and provenance comments. _(files: components/SearchPanel.tsx, components/figma-diagram-component.tsx, components/document-preview.tsx; effort: S; impact: low)_

## Documentation & Developer Experience

**91. Environment-setup runbook** — Required vars are scattered across `.env.example`, docs, and validators, blocking onboarding; add `docs/environment-setup.md` (per-env requirements, local validation) and log loaded env files on debug boot. _(files: backend/src/utils/config-validator.js, backend/src/config/load-env.js, docs/ENV_VARIABLES.md; effort: M; impact: high)_
**92. Auto-generated agent-tools reference** — 34 visual tools are documented only in inline comments; generate `docs/agent-tools-reference.md` from the tool-manifest registry (inputs/outputs/limits/errors) on `generate:openapi`. _(files: backend/src/services/agents/visual-media-tools.js, backend/src/services/agents/tool-manifest.js; effort: M; impact: high)_
**93. macOS coreutils/timeout dev guide** — `dev-up.sh` uses GNU `timeout`, silently failing on stock macOS; add `docs/macos-setup.md` and make scripts detect platform and degrade gracefully. _(files: scripts/dev-up.sh, SETUP.md, scripts/deploy-with-rollback.sh; effort: S; impact: medium)_
**94. ADR directory for architecture decisions** — Major choices (AsyncGuard, semantic caching, circuit breakers) have no searchable record of *why*; add `docs/decisions/` with a template and 3–5 retroactive ADRs. _(files: README.md, docs/architecture.md, backend/src/utils/async-guard.js; effort: M; impact: medium)_
**95. Health-endpoint contract docs** — Liveness/readiness/full-health have subtly different semantics monitoring teams can't see; document schemas, status codes, and polling intervals in `docs/health-endpoints.md`. _(files: backend/src/routes/health-routes.js, backend/src/services/observability/health-check.js, docs/deployment.md; effort: S; impact: high)_
**96. Error-code taxonomy + recovery docs** — Classifier codes (TIMEOUT, SAFETY_BLOCK, PROVIDER_RATE_LIMIT) have no client-facing recovery guidance; generate `docs/error-codes.md` from the classifier constants and add to OpenAPI as `x-error-codes`. _(files: backend/src/utils/task-error-classifier.js, backend/src/services/sira/tool-error-classifier.js, docs/openapi.json; effort: M; impact: medium)_
**97. CONTRIBUTING backend test runbook** — 300+ test files but no docs on running/debugging/adding tests; expand `CONTRIBUTING.md` with single-suite runs, watch/debug tips, fixture/mock patterns, and coverage. _(files: CONTRIBUTING.md, backend/package.json, README.md; effort: M; impact: medium)_
**98. Prisma migration safety docs** — Migration workflow, safety markers, and rollback steps are scattered; add `docs/database-migrations.md` covering local flow, destructive-op markers, the CI gate, and rollback. _(files: backend/prisma/schema.prisma, scripts/check-migration-safety.js, docs/deployment.md; effort: M; impact: medium)_
**99. Reliability-utilities instrumentation guide** — AsyncGuard/FetchInstrument/CircuitBreaker semantics and metrics are undocumented, so devs misuse them; add `docs/reliability-utilities.md` with when-to-use, common errors, and metrics to watch. _(files: backend/src/utils/async-guard.js, backend/src/utils/fetch-instrument.js, backend/src/utils/circuit-breaker.js; effort: M; impact: high)_
**100. Multi-provider fallback documentation** — The 9-provider matrix and Cerebras free-tier fallback logic is buried in orchestration code, hiding why responses switch providers; document the matrix, fallback hierarchy, response metadata, and env overrides. _(files: backend/src/orchestration/orchestration-gateway.js, backend/src/services/ai/provider-inference.js, docs/ENV_VARIABLES.md; effort: M; impact: high)_

---

### Backend-internal improvements

## Agent loop control & ReAct runtime
**1. Bound per-run tool-result cache** — The per-run tool-result cache in `agent-core.js` is a plain unbounded `Map` that accumulates stale entries across long autonomous runs (maxSteps 24–30); replace it with an LRU bounded to ~32 entries plus a trace `cleanup()` hook callable on run termination. _(files: backend/src/services/agents/agent-core.js, backend/src/services/react-agent.js; effort: M; impact: high)_
**2. Validate tool args before dispatch** — `dispatchTool()` validates arguments only after the handler runs, so malformed args still burn a tool-call slot; move `validateToolArgs()` ahead of the tool lookup and reject with a non-budget-counted `invalid_args` error. _(files: backend/src/services/react-agent.js; effort: S; impact: medium)_
**3. Classify tool errors vs. error budget** — All tool failures equally consume `MAX_TOOL_ERRORS=5`, so transient timeouts/429s exhaust tools as fast as deterministic bad-args; classify via `classifyTaskError` and only count terminal errors against the budget, resetting on next success. _(files: backend/src/services/react-agent.js, backend/src/services/agents/tool-call-retry.js; effort: M; impact: high)_
**4. Backoff/escape on exhausted-tool re-polling** — When the model repeatedly calls an already-exhausted tool the loop re-sends identical observations forever; track consecutive exhausted-tool calls and after 3 force a finalize with a one-time clear message instead of looping. _(files: backend/src/services/react-agent.js; effort: S; impact: medium)_
**5. Per-tool cache statistics in trace** — `toolCacheHits` is aggregated with no per-tool breakdown, hiding redundant-work patterns (e.g. reading the same file 10×); add a `toolCacheStats` map (`calls/hits/lastCalled`) to step records and emit it in `onStep` callbacks. _(files: backend/src/services/agents/agent-core.js, backend/src/services/react-agent.js; effort: M; impact: medium)_
**6. Global observation-size budgeting** — Observations are capped per-message (8000/4000 chars) but never globally, so a 30-step run resends ~120KB of accumulated observations each turn; add a cumulative `totalObservationChars` tracker that in-place summarizes older observations once over threshold. _(files: backend/src/services/react-agent.js, backend/src/services/agents/agent-core.js; effort: M; impact: high)_
**7. Timeout-guard parallel tool prefetch** — `prefetchParallelDispatch()` uses `Promise.all`, so one hung read-only tool stalls the whole step until the 60s budget burns; wrap each prefetched call in `Promise.race` with a 5s cap and return partial results instead of failing the batch. _(files: backend/src/services/react-agent.js; effort: M; impact: high)_
**8. Detect native tool-call parsing loops** — When a weak model emits the same malformed `<tool_call>` markup every turn, stripping leaves empty content and the loop no-ops without feedback; track repeated parse results and inject a one-time repair instruction, forcing finalize on a third repeat. _(files: backend/src/services/react-agent.js; effort: M; impact: medium)_
**9. Adaptive cumulative step-timeout budget** — Fixed per-step `REACT_STEP_TIMEOUT_MS=60s` lets a slow final step blow the wall clock ("dejó de responder"); compute `min(stepTimeoutMs, remainingMs − buffer)` per step and force `tool_choice=finalize` on step N−1 when the provider trends slow. _(files: backend/src/services/react-agent.js; effort: M; impact: high)_
**10. Per-tool global concurrency limits** — No per-tool concurrency cap means 4 parallel `web_search` calls × N sessions can fire 40 simultaneous external requests and trip provider rate limits; declare `usage_limits.concurrency_max` in the manifest and enforce it with a per-tool-name semaphore. _(files: backend/src/services/agents/tool-manifest.js, backend/src/services/react-agent.js, backend/src/services/agents/agent-tools.js; effort: L; impact: high)_

## RAG, embeddings & retrieval
**11. Token-aware embedding batching** — `_embedRaw()` hardcodes `BATCH=96` texts regardless of length, so a batch of long docs can exceed 30k tokens and waste quota near the OpenAI ceiling; accumulate by estimated tokens up to a ~100k-token cap per request. _(files: backend/src/services/rag-service.js; effort: M; impact: medium)_
**12. Cache query embeddings across retrieve()** — Identical/near-identical queries in quick succession re-embed every time since single-flight is ingest-only; extend the cache to retrieval by default with a 5-min TTL (`SIRAGPT_QUERY_EMBED_CACHE_TTL_MS`). _(files: backend/src/services/rag-service.js, backend/src/cache/single-flight.js; effort: S; impact: medium)_
**13. Retrieval latency budget enforcement** — `buildRuntimeContext()` runs hybrid+expansion+rerank+graph with no wall-clock tracking, sometimes exceeding 10s and starving the LLM's token budget; track latency and, past `SIRAGPT_RAG_MAX_LATENCY_MS` (~8s), skip reranking and reduce k, exporting context byte size. _(files: backend/src/services/rag/operational-runtime.js, backend/src/services/rag-service.js; effort: M; impact: high)_
**14. Batch triple-graph orphan cleanup** — `evictAndCleanOrphans` calls `tg.clearSource()` once per removed source (O(N) on large trims); add `batchClearSources(userId, collection, sources[])` and pass the whole removed array in one pass. _(files: backend/src/services/rag-service.js, backend/src/services/triple-graph.js; effort: S; impact: medium)_
**15. Dedup chunk embeddings within an ingest** — `ingest()` embeds every chunk even when boilerplate (headers/footers) repeats; build a content-hash→chunk map, embed only unique chunks, reuse vectors for duplicates, and report `deduplicatedChunks`. _(files: backend/src/services/rag-service.js; effort: M; impact: low)_
**16. Access-aware chunk eviction** — `trim()` uses oldest-wins eviction, dropping hot frequently-queried docs simply because they were uploaded first; track `access_count` per chunk and add a `least_accessed` eviction strategy that spares hot sources. _(files: backend/src/services/rag-store.js, backend/src/services/rag-service.js; effort: M; impact: medium)_
**17. Chunking quality telemetry** — Fixed-size chunking (1200 chars) has no telemetry on width variance or split efficiency, so over/under-chunking goes unnoticed; emit per-ingest metrics (mean/std/max chunk size, mid-sentence split %, coverage). _(files: backend/src/services/rag-service.js; effort: S; impact: low)_
**18. Configurable intent-gate min-relevance with fallback** — `rankSources()` hardcodes `minRelevance=0.35` and silently drops everything below it; expose `SIRAGPT_INTENT_GATE_MIN_RELEVANCE` and fall back to top-3 by relevance when the filter empties the set. _(files: backend/src/services/document-intent-rag-gate.js, backend/src/services/rag/operational-runtime.js; effort: S; impact: medium)_
**19. Tier/budget-aware collection cap** — `MAX_COLLECTION_CHUNKS=10000` is global regardless of plan, giving free-tier users the same cap as enterprise; resolve a per-user embedding budget and set the cap to `min(globalCap, budget/chunkTokens)`, throttling graph expansion near the limit. _(files: backend/src/services/rag-service.js, backend/src/services/rag/operational-runtime.js; effort: L; impact: high)_

## Document & file processing pipeline
**20. Per-stage extraction error context** — `processFile()` catches at the top level and returns a coarse error, losing which parser stage failed and whether it was transient; wrap each parser stage to collect `{stage, parser, code, retryable, duration}` and surface it for intelligent retries. _(files: backend/src/services/fileProcessor.js, backend/src/routes/files.js; effort: M; impact: high)_
**21. Runtime parser-availability audit** — The pipeline registry advertises 65 parsers but never verifies the providers exist, so `dispatchParse()` silently degrades; add async `initRegistry()` that probes availability at startup, warns on preference downgrades, and exposes `/admin/pipeline-status`. _(files: backend/src/services/sira/document-pipeline-registry.js, backend/src/services/fileProcessor.js; effort: M; impact: medium)_
**22. Structural validation for ZIP-based Office files** — DOCX/XLSX/PPTX pass MIME validation as `application/zip`, so a bare ZIP wastes extraction time before `assertReadableDocxZip` fails; do a ~10ms structural check for the Office marker (`[Content_Types].xml`) and reject early. _(files: backend/src/services/upload-security-policy.js, backend/src/services/fileProcessor.js; effort: M; impact: medium)_
**23. Complexity-aware batch concurrency** — `processFilesInParallel()` ignores the file-type mix, so 10 PDFs+images trigger concurrent OCR/tensor loads and memory spikes; classify by complexity class and cap concurrent high-complexity files to 2, queuing the rest. _(files: backend/src/routes/files.js, backend/src/services/xlsx-safe-workbook.js; effort: L; impact: high)_
**24. Per-parser timeout overrides** — A single `EXTRACT_TIMEOUT_MS=20s` times out legitimate 30–60s Docling/Marker OCR while letting trivial text extraction hang too long; add an optional `timeout` field per parser entry plus `SIRAGPT_PARSER_TIMEOUT_<id>` env overrides. _(files: backend/src/services/sira/document-pipeline-registry.js, backend/src/routes/files.js; effort: M; impact: medium)_
**25. Reservoir sampling for large XLSX** — `processExcel()` always shows header + first 5000 rows per sheet, biasing analysis and dropping tail data; replace linear sampling with reservoir sampling over a fixed budget and log the sampling method + coverage ratio in the header. _(files: backend/src/services/fileProcessor.js, backend/src/services/xlsx-safe-workbook.js; effort: M; impact: medium)_
**26. Extraction-quality gating for RAG indexing** — `scheduleDefaultRagIndex()` indexes any file with text, even low-confidence OCR/partial-PDF output, polluting embeddings; add `extractionQuality {confidence, method, partial, degraded}` and skip or route degraded text to a separate collection. _(files: backend/src/services/fileProcessor.js, backend/src/routes/files.js; effort: M; impact: medium)_
**27. Page-level recovery in streaming PDF** — `processPDFStreaming()` aborts the whole extraction if one page throws, discarding hundreds of clean pages; catch per-page errors, continue, record `failedPages[]`, and support `resumeFromPage`. _(files: backend/src/services/fileProcessor.js, backend/src/services/document/streaming-pdf.js; effort: M; impact: medium)_
**28. Structured extraction metrics** — File processing logs only to console, so silent low-quality extractions (10 chars from a 1MB PDF) show as "ready" with no signal; persist `extractionMetrics {bytesRead, charExtracted, ocrUsed, parserUsed, duration, estimatedQuality}` and expose `/admin/extraction-metrics`. _(files: backend/src/services/fileProcessor.js, backend/src/routes/files.js, backend/src/services/file-processing-status.js; effort: L; impact: high)_
**29. Circuit breaker for external parser chain** — The static `_externalParserLastFailed` cooldown causes a thundering herd when a flaky Docling recovers; replace it with a per-parser circuit breaker (healthy/degraded/open) backed by a guaranteed internal fallback. _(files: backend/src/services/fileProcessor.js, backend/src/services/document-pipeline/parser-orchestrator.js; effort: M; impact: high)_

## Billing, quota & provider routing
**30. Atomic idempotency for spendCredits** — `spendCredits` accepts an idempotency key without format validation, and two concurrent requests can both see "no existing transaction" and double-charge; add format validation and a `UNIQUE(userId, idempotencyKey)` constraint so PostgreSQL enforces single-execution atomically. _(files: backend/src/middleware/charge-credits.js, backend/prisma/schema.prisma; effort: M; impact: high)_
**31. Atomic org-quota check-and-increment** — `enforce-org-quota.js` reads `usedThisMonth`, checks, then increments separately, allowing concurrent requests to both pass even when the sum exceeds quota; collapse into one conditional `UPDATE ... WHERE usedThisMonth + cost <= monthlyQuota` and 429 when zero rows affected. _(files: backend/src/middleware/enforce-org-quota.js; effort: S; impact: high)_
**32. Pre-flight quota estimate for streaming** — Quota is checked point-in-time, so a user with 1 token can start a 100-token stream and refunds may never fire on client disconnect; accept a `quotaEstimate` upper bound, reject early if it exceeds remaining, and reconcile actual usage in a stream trailer. _(files: backend/src/middleware/enforce-plan-quota.js, backend/src/middleware/charge-credits.js; effort: M; impact: high)_
**33. Streaming-aware failover** — `resolveWithFallback()` can't tell "stream started" from "stream failed after 5 chunks", so a mid-stream OpenAI error never falls back to Anthropic; add a `streamStatusCallback` the attempt fn invokes so the policy can advance the chain on stream errors. _(files: backend/src/services/ai/failover-policy.js, backend/src/routes/ai.js; effort: L; impact: medium)_
**34. Provider-scoped rate-limit cooldown** — On 429s `rotateProfiles()` burns through all 20 keys before failover advances, even when the whole provider is rate-limited; integrate `provider-key-health.js` so a 429 records a provider-wide cooldown (opt-in flag for back-compat). _(files: backend/src/services/ai/auth-profile-rotation.js, backend/src/services/ai/provider-key-health.js; effort: S; impact: high)_
**35. Stream-resume timeout & buffer caps** — `stream-resume.js` caps chunk count but has no wall-clock timeout or byte cap, so a frozen upstream or one huge chunk consumes unbounded memory; add `STREAM_RESUME_TIMEOUT_MS` (~30min) and `STREAM_RESUME_MAX_BUFFER_BYTES` (~50MB) and end the stream with a typed error status. _(files: backend/src/services/ai/stream-resume.js; effort: S; impact: medium)_
**36. Centralized provider error classifier** — Error classification (401/402/429/5xx) is duplicated and inconsistent across `cerebras-client`, `failover-policy`, and `auth-profile-rotation`, so quota-exhaustion (402) is indistinguishable from transient 503; build a shared `error-classifier.js` returning `{code, retryable, severity, costImplications}` and emit a metric per category. _(files: backend/src/services/ai/failover-policy.js, backend/src/services/ai/auth-profile-rotation.js, backend/src/services/ai/provider-key-health.js; effort: M; impact: medium)_
**37. Per-provider timeout & retry budget** — `failover-policy` retries the whole chain with no per-provider timeout cap or retry budget, so a fast Cerebras request can wait through 5 slow models; add `PROVIDER_TIMEOUT_<NAME>_MS` and `FAILOVER_MAX_ATTEMPTS_PER_PROVIDER` (default 3) to skip a dead provider family fast. _(files: backend/src/services/ai/failover-policy.js; effort: M; impact: high)_
**38. Cost-estimation accuracy validation** — `feature-cost-estimator` pre-declares costs but never compares against actual consumption, allowing systematic under/over-charging to go invisible; add `validateActualCost(feature, estimate, actual)` that logs estimation error and feeds `cost-tracker` for drift reporting. _(files: backend/src/services/feature-cost-estimator.js, backend/src/services/ai/cost-tracker.js; effort: M; impact: medium)_

## Task store, sandbox & visual-tools safety
**39. Gzip snapshot compression** — `compressSnapshotBytes()` permanently drops event/checkpoint/artifact arrays when snapshots exceed 1MB, destroying trace history and artifact reference chains; gzip the original JSON instead and decompress transparently in `readTaskSnapshot()`. _(files: backend/src/services/agents/task-store.js; effort: M; impact: high)_
**40. Sanitize non-finite chart inputs** — Chart builders run `Math.max/min` on raw arrays, so `[NaN, Infinity]` produces invalid `d="NaN..."` SVG paths; add a `sanitizeNumericArray()` pass coercing non-finite values to 0 early in each builder. _(files: backend/src/services/agents/visual-media-tools.js; effort: S; impact: medium)_
**41. Clamp SVG dimensions** — `viewBox`/width/height are computed from data, so 1M labels yield giant viewBoxes that bloat client memory (potential DoS); clamp computed dimensions to `MAX_SVG_DIMENSION` (~8000) and flag truncation in the validation result. _(files: backend/src/services/agents/visual-media-tools.js; effort: S; impact: medium)_
**42. Escape SVG attribute values** — `xmlEscape()` covers text nodes but user-derived attribute values (colors, computed coords) are injected raw, so a crafted color string could break out of an attribute context; add `sanitizeAttributeValue()` applied to all dynamic attributes. _(files: backend/src/services/agents/visual-media-tools.js; effort: M; impact: high)_
**43. Lock + verify atomic snapshot writes** — `atomicWriteJson()` doesn't serialize concurrent same-taskId writes or verify `fsync`, risking partial snapshots on crash and lost updates on race; add lock-file serialization, explicit fsync checking, and integrity verification on read. _(files: backend/src/services/agents/task-store.js; effort: M; impact: high)_
**44. Validate sandbox config bounds** — `parseInt()` of `SANDBOX_*` env vars isn't bounds-checked, so `SANDBOX_MAX_SOURCE_BYTES=999999999` bypasses limits; add `validateSandboxConfig()` clamping each limit to a safe min/max at load time with a startup warning when set to max. _(files: backend/src/services/agents/code-sandbox.js; effort: S; impact: medium)_
**45. Size-aware priority event truncation** — `trimEvents()` truncates by count (1000) ignoring byte size, so verbose stdout dumps overflow the snapshot cap and high-value error/checkpoint events get dropped indiscriminately; implement `compactEventsBySize()` that preserves error/checkpoint/file_artifact events first. _(files: backend/src/services/agents/task-store.js; effort: M; impact: high)_
**46. Harden folder-code path-traversal defense** — `safeFolderCode()` can be fooled by Unicode normalization with no explicit `..`/separator assertion; after sanitizing, verify `path.relative(ARTIFACT_DIR, joined)` doesn't escape and reject with a security log, covered by adversarial tests. _(files: backend/src/services/agents/task-tools.js; effort: S; impact: medium)_

## HTTP API contracts & route hardening
**47. Standardize error response envelope** — Routes return 5+ inconsistent error shapes (`{error,message}`, `{error,details}`, `{error_code,message}`…), forcing clients to handle each; adopt one schema `{error, code?, message?, details?}` via normalizing middleware across all routes. _(files: backend/src/routes/ai.js, backend/src/routes/builder.js, backend/src/routes/files.js, backend/src/routes/scientific-search.js; effort: M; impact: high)_
**48. Validate streaming request bodies pre-stream** — Streaming POST handlers read `req.body.prompt/model/provider` without validation before opening the SSE stream, so malformed input reaches expensive logic and causes partial writes; add express-validator chains before stream creation, returning 400 on failure. _(files: backend/src/routes/ai.js, backend/src/routes/agent-batch.js, backend/src/routes/codex-runs.js, backend/src/routes/goals.js; effort: M; impact: high)_
**49. Idempotency keys for long-running routes** — Only credits/chats/paraphrase support idempotency, so retrying a timed-out `/generate`, agent batch, or video op risks duplicate work; add `Idempotency-Key` support backed by Redis with 409/200/201 semantics. _(files: backend/src/routes/ai.js, backend/src/routes/agent-batch.js, backend/src/routes/video.js, backend/src/routes/codex-runs.js; effort: L; impact: high)_
**50. SSE abort-signal cleanup** — `ai.js /generate` checks `writableEnded` but never listens for `req.on('aborted')`, so a client disconnect lets the request run to timeout wasting compute; attach an abort listener wiring an `AbortController` and check the signal in expensive loops. _(files: backend/src/routes/ai.js, backend/src/routes/answer.js; effort: M; impact: medium)_
**51. Normalize rate-limit HTTP semantics** — Rate-limit responses vary between 429/503/implicit-200 and many omit `Retry-After`, so client SDKs can't back off reliably; always return 429 with `Retry-After` and a `retryAfterMs` field across quota-enforcing routes. _(files: backend/src/routes/ai.js, backend/src/routes/agent-batch.js, backend/src/routes/scientific-search.js, backend/src/routes/github-search.js, backend/src/routes/orgs.js; effort: S; impact: medium)_
**52. Enforce request body-size limits** — No explicit `Content-Length`/body-size limits mean a 1GB payload to `/generate` or `/files` exhausts memory before validation runs; configure `express.json`/`urlencoded` limits and reject oversized `Content-Length` with 413 before parsing. _(files: backend/src/routes/ai.js, backend/src/routes/files.js, backend/src/routes/builder.js; effort: S; impact: medium)_
**53. Request-ID error-context propagation** — Catch blocks return generic messages with no requestId/userId/model correlation, so a reported failure can't be tied to a `console.error`; attach a `requestId` in auth middleware and include it in logs and every 4xx/5xx (plus SSE error events). _(files: backend/src/routes/ai.js, backend/src/routes/agent-batch.js, backend/src/routes/files.js; effort: M; impact: medium)_
**54. 202 Accepted for async operations** — Several enqueue-style endpoints return 200, so clients can't distinguish "queued" from "complete" and may block expecting a sync response; return 202 + `Location` when work is enqueued and document the convention. _(files: backend/src/routes/agent-batch.js, backend/src/routes/goals.js, backend/src/routes/video.js; effort: S; impact: low)_
**55. Standardize cursor-based pagination** — List endpoints mix offset/limit, cursor, and bare-limit styles with no `hasMore`/`totalCount`, breaking reliable pagination; standardize on `{items, hasMore, nextCursor, totalCount?}` (offset back-compat) across list/search routes. _(files: backend/src/routes/admin.js, backend/src/routes/users.js, backend/src/routes/scientific-search.js, backend/src/routes/github-search.js; effort: M; impact: low)_
**56. SSE backpressure handling** — `/generate` streams indefinitely without checking `write()` return value, so a stalled client fills the buffer silently and the connection hangs for hours holding a thread; track failed writes, emit a backpressure event, listen for `drain`, and close after a stall timeout. _(files: backend/src/routes/ai.js, backend/src/routes/agent-batch.js, backend/src/routes/answer.js; effort: L; impact: medium)_

## Reliability utilities wiring & correctness
**57. Clean up retry abort listeners** — `raceWithSignal` in `retry-with-backoff.js` never removes its abort listener on the happy path, so a reused external `AbortSignal` accumulates one dead listener per retry and triggers `MaxListenersExceededWarning`; register/remove via `Promise.race` with a `finally` cleanup. _(files: backend/src/utils/retry-with-backoff.js; effort: S; impact: medium)_
**58. Wire FetchInstrument.install() at boot** — `FetchInstrument` is exported but never installed, so 112+ raw `fetch()` calls bypass tracing, sanitization, and timeout guards; call `defaultInstrument.install()` early in app startup (or migrate call sites to `createFetch()`). _(files: backend/src/utils/fetch-instrument.js, backend/src/utils/async-handler.js; effort: M; impact: high)_
**59. Fix double-error-forward in guarded routes** — When `timeoutMs` is set, `async-handler.js` forwards the error via both the microtask path and the guard catch, causing duplicate logs and double error-handling downstream; make the guard the single source of truth and suppress the microtask forward. _(files: backend/src/utils/async-handler.js; effort: S; impact: low)_
**60. Circuit-breaker transition metrics** — The breaker emits `stateChange` but exposes only a state gauge, so ops can't tell "opened on 5 failures" from "opened on 1 timeout"; add `circuit_breaker_transitions_total{name,from,to}` counters plus failure/success gauges. _(files: backend/src/utils/circuit-breaker.js, backend/src/utils/metrics.js; effort: M; impact: medium)_
**61. Report guard cleanup-callback failures** — Cleanup callbacks that throw are swallowed with a `console.warn` and never reach telemetry, hiding failed rollbacks; route them through `error-telemetry.captureError()` with guard context and a dedicated GC-time metric counter. _(files: backend/src/utils/async-guard.js, backend/src/utils/error-telemetry.js; effort: M; impact: medium)_
**62. Fix broken reporter.capture() call** — `agent-collaboration.js` calls a nonexistent `reporter.capture()` (the module exports `captureError`), so errors are silently never recorded; add a `capture` alias delegating to `captureError` and fix the call site. _(files: backend/src/utils/error-telemetry.js, backend/src/services/agents/agent-collaboration.js; effort: S; impact: high)_
**63. Test & log progress-stream telemetry path** — `progress-stream.js` conditionally requires `error-telemetry` and swallows failures silently, so breakage in the `fail()` path goes undetected; add explicit import-failure logging and a test mocking telemetry to assert `captureError` is called with correct context. _(files: backend/src/services/progress-stream.js; effort: M; impact: low)_
**64. Unify already-aborted signal handling** — `run()` and `fetch()` in `async-guard.js` behave differently when passed an already-aborted signal (different error types/metadata); check `signal.aborted` before registering the guard token and reject immediately with a consistent `GuardError(reason='aborted', elapsedMs=0)`. _(files: backend/src/utils/async-guard.js; effort: S; impact: low)_
**65. Harden sanitizeFetchInit for SDK bodies** — `sanitizeFetchInit` strips Symbol keys but doesn't unwrap SDK metadata bodies (`{_internal, toString}`), so native fetch mis-serializes them and throws; detect/unwrap common SDK body wrappers and convert serialization errors into a typed `GuardError`. _(files: backend/src/utils/async-guard.js; effort: M; impact: low)_

## Attribution stack architecture & performance
**66. Consolidate overlapping attribution orchestrators** — Four orchestrators (`context-attribution-engine`, `context-intelligence-engine`, `attribution-suite`, `attribution-stack-runner`) duplicate identical normalization pipelines and coordinate the same ~20 modules; unify into one `AttributionOrchestrator` with pluggable lazy stages and a shared `ContextNormalizer`. _(files: backend/src/services/context-attribution-engine.js, backend/src/services/context-intelligence-engine.js, backend/src/services/attribution-suite.js, backend/src/services/attribution-stack-runner.js; effort: L; impact: high)_
**67. Per-stage memoization on the hot path** — Concept extraction runs multiple times per request across the circuit and intent-attribution pipelines, recomputing ~50 regex matches; extend `attribution-cache.js` to per-stage memoization via a shared `RequestCache` threaded through the orchestrator (opt-in flag). _(files: backend/src/services/attribution-cache.js, backend/src/services/concept-extractor.js, backend/src/services/attribution-suite.js, backend/src/routes/ai.js; effort: M; impact: medium)_
**68. Remove dead attribution modules & dedup feature extraction** — `attribution-replay-engine`, `attribution-prompt-fuzzer`, `attribution-ab-experiment-runner`, and `attribution-graph-comparator` are lazy-loaded but never invoked, and `intent-attribution-graph/index.js` shadows `concept-extractor`; audit/remove the unused modules and have the intent graph wrap the shared extractor. _(files: backend/src/services/attribution-replay-engine.js, backend/src/services/attribution-prompt-fuzzer.js, backend/src/services/attribution-ab-experiment-runner.js, backend/src/services/intent-attribution-graph/index.js; effort: M; impact: medium)_
**69. PromptBlockComposer for budget-bounded assembly** — `ai.js` concatenates ~24 prompt blocks each with its own char budget but no global dedup/priority, so totals can overflow the model budget and drop high-signal safety blocks; add a `PromptBlockComposer` that composes by priority within an overall char budget. _(files: backend/src/routes/ai.js, backend/src/services/context-attribution-engine.js, backend/src/services/context-intelligence-engine.js, backend/src/services/attribution-suite.js; effort: M; impact: high)_
**70. Centralize attribution input validation** — Input normalization/sanitization is scattered, letting escaped instruction-override patterns (`\<instruction>`) survive one layer and unescape in another, and attached files bypass sanitization entirely; add a single `ContextInputValidator` consumed once in the orchestrator. _(files: backend/src/services/context-attribution-engine.js, backend/src/services/conversation-understanding.js, backend/src/services/attribution-suite.js, backend/src/routes/circuit-attribution.js; effort: M; impact: high)_
**71. First-class attribution latency metrics** — The performance profiler exists but most orchestrators don't use it, so a slow turn can't be attributed to a stage; make every stage emit a `PerformanceEvent {stage, durationMs, inputSize, cached}`, warn on per-stage SLO breaches, and expose a read-only perf-stats endpoint. _(files: backend/src/services/attribution-performance-profiler.js, backend/src/services/attribution-suite.js, backend/src/routes/ai.js; effort: M; impact: medium)_
**72. Decouple entity tracking as opt-in stage** — `attribution-suite.run()` unconditionally runs entity register/unify, so a crash there aborts the whole suite; extract it into an optional, try/catch-wrapped stage configurable via `options.trackEntities` with per-stage error counts in telemetry. _(files: backend/src/services/attribution-suite.js, backend/src/services/cross-turn-entity-tracker.js, backend/src/services/cross-language-entity-unifier.js; effort: S; impact: medium)_
**73. Compute multi-hop detection once and share** — Multi-hop structure is recomputed independently by intent-planner, hidden-goal-extractor, and drift-monitor; lift it into a standalone `MultiHopAnalyzer` computed early and passed via the bundle, also exposing `/api/multi-hop`. _(files: backend/src/services/multi-hop-reasoner.js, backend/src/services/attribution-suite.js, backend/src/services/intent-planner.js; effort: M; impact: medium)_
**74. Deadline + circuit-breaker for attribution pipeline** — No per-stage timeout or overall deadline means a hung concept-extractor or 100-node graph blocks the whole turn and slows the queue; add a `CircuitBreakerPool` (per-stage timeout, 3-strike degrade) and a 500ms pipeline deadline falling back to a minimal prompt block. _(files: backend/src/services/attribution-stack-runner.js, backend/src/services/attribution-suite.js, backend/src/routes/ai.js; effort: L; impact: high)_

## Cowork memory, sessions & analysis
**75. Per-user index for active-memory scans** — `active-memory.js` filters the entire store on every recall/getStats/autoPromote, becoming O(n²) under concurrency; maintain a `Map<userId, Set<entryId>>` index updated on create/delete to drop recall to per-user cost. _(files: backend/src/services/active-memory.js; effort: M; impact: high)_
**76. Accurate session token accounting** — `compactSession` re-sums message tokens that callers usually pass as 0/undefined, producing wrong estimates that risk context-window overruns near `MAX_HISTORY_MESSAGES=200`; track `tokenCount` at `addMessage` time, use the running session total, and auto-compact on model-max breach. _(files: backend/src/services/session-manager.js; effort: M; impact: high)_
**77. Consistent memory dedup hashing** — `createMemoryEntry` normalizes to 240 chars for dedup but hashes the raw fact, so two long facts differing only after char 240 collide as duplicates; hash the normalized string instead and document the semantic limit. _(files: backend/src/services/active-memory.js; effort: S; impact: medium)_
**78. PII redaction before memory persistence** — Auto-extracted memory facts are written unencrypted to disk without PII detection, leaking emails/phones/SSNs that `deep-document-analyzer` would otherwise catch; add `redactPIIFromFact()` reusing `ENTITY_PATTERNS`, masking critical entities before storing `user_message`-sourced facts. _(files: backend/src/services/active-memory.js; effort: M; impact: high)_
**79. IDF-damped recall scoring** — Recall scoring lets recent, frequently-accessed junk outrank old, perfectly-relevant facts because relevance has no document-frequency penalty and recencyBoost can reach 1.0; add IDF damping per term and cap recencyBoost to ~0.5 so it breaks ties rather than dominating. _(files: backend/src/services/active-memory.js; effort: M; impact: medium)_
**80. Grace period for in-flight RAG indexing of auto-files** — `scheduleAutoFileRagIndex` marks files "ready" before async indexing completes, so immediate queries miss the content; set stage to `embedding` until `ensureIndexed` succeeds (with a timeout) and treat `embedding` files as not-yet-searchable with a warning. _(files: backend/src/services/auto-file-bridge.js; effort: M; impact: high)_
**81. Tier-respecting memory eviction** — `evictWeakest()` uses a blended score that can evict promoted long-term facts before short-term noise, breaking tier semantics under write pressure; evict short-term first and only touch long-term when short-term is empty, or enforce per-tier reserves. _(files: backend/src/services/active-memory.js; effort: M; impact: high)_
**82. Serialize session disk persistence** — `persistUserSessions` calls blocking `writeFileSync` synchronously, so 50 concurrent sessions block the event loop and fast successive writes can lose data; add a per-user persistence queue (`Map<userId, Promise>`) ensuring one ordered write at a time. _(files: backend/src/services/session-manager.js; effort: M; impact: high)_
**83. Pre-compile entity regexes in deep-document-analyzer** — `extractEntities` re-instantiates `ENTITY_PATTERNS` via `new RegExp()` on every call, churning ~70 allocations per document; pre-compile them at module load (like `COMPILED_DOMAIN_KEYWORDS`) and reset `lastIndex` per loop. _(files: backend/src/services/deep-document-analyzer.js; effort: S; impact: medium)_

## Search providers & external resilience
**84. Circuit breaker for scientific-search providers** — Per-provider retries lack a circuit breaker, so a continuously-503ing provider burns retries on every concurrent search; wrap each provider in a shared-state `CircuitBreaker` that fast-fails when OPEN and expose a `/circuit-status` endpoint. _(files: backend/src/services/scientific-search.js, backend/src/utils/retry-with-backoff.js, backend/src/routes/scientific-search.js; effort: M; impact: high)_
**85. Keep-alive connection pooling for upstream APIs** — Every fetch across 14 scientific providers + GitHub + X opens a fresh TCP/TLS connection, exhausting ephemeral ports under load (ECONNREFUSED); share `http(s).Agent` with `keepAlive` and bounded sockets via a fetch wrapper. _(files: backend/src/services/scientific-search.js, backend/src/services/github-search.js, backend/src/services/x-search.js; effort: M; impact: high)_
**86. Adaptive per-provider timeouts** — A single 8s `DEFAULT_TIMEOUT_MS` over-tunes fast providers (arXiv) and under-tunes slow ones (CORE), and one slow provider makes `search()` wait the full total timeout; track p95 latency, allow `SCIENTIFIC_SEARCH_TIMEOUT_<PROVIDER>` overrides, and compute adaptive caps. _(files: backend/src/services/scientific-search.js, backend/src/services/x-search-metrics.js; effort: M; impact: medium)_
**87. Lazy cache eviction off the read path** — Both search caches call `evictExpired()` on every `get()`, adding 5–50ms jitter to cache hits that should be sub-millisecond; move eviction to a 30s background interval plus lazy stale-skip on read. _(files: backend/src/services/scientific-search-cache.js, backend/src/services/github-search-cache.js; effort: S; impact: medium)_
**88. Surface rate-limit headers for GitHub/xAI** — GitHub 429s are retried without reading `Retry-After`/`X-RateLimit-Reset` and xAI exposes no quota signal, so callers can't tell they're near exhaustion; parse and expose remaining quota/reset in responses and via `GET /api/github-search/rate-limit`. _(files: backend/src/services/github-search.js, backend/src/services/x-search.js, backend/src/routes/github-search.js; effort: S; impact: medium)_
**89. Cross-source heterogeneous dedup** — Title-only dedup is fragile and there's no cross-source dedup across papers/repos/posts, so equivalent records appear multiple times; add a post-settle dedup layer with a three-tier key (exact ID → title+year+author → source-specific). _(files: backend/src/services/scientific-search.js, backend/src/services/research-agent.js, backend/src/services/search/source-confidence.js; effort: M; impact: medium)_
**90. Per-provider query sanitization** — PubMed/WoS build queries by string concatenation with loose paren escaping, so a crafted query can break the parser or pollute logs; add a per-provider sanitizer whitelisting alphanumerics + operators before URL construction. _(files: backend/src/services/scientific-search.js, backend/src/services/github-search.js, backend/src/services/x-search.js; effort: S; impact: low)_
**91. Request-ID tracing across provider fan-out** — Logs from 14 providers, retries, and failures interleave across concurrent users with no correlation ID; generate a per-`search()` UUID, thread it through all provider/retry calls and error objects, and return it in response metadata. _(files: backend/src/services/scientific-search.js, backend/src/services/github-search.js, backend/src/services/research-agent.js; effort: S; impact: high)_

## Database, migrations & vector store
**92. Resolve P3009 failed migration state** — `20250919203029_init` sits in a FAILED P3009 state blocking the entire migration queue; inspect `_prisma_migrations`, classify partial vs. complete failure, resolve via roll-forward or `migrate resolve --rolled-back`, and add a pre-deploy check guarding CI. _(files: backend/prisma/migrations/20250919203029_init, scripts/check-migration-safety.js, backend/prisma/schema.prisma; effort: M; impact: high)_
**93. Declarative tuned HNSW index for pgvector** — The `user_memories` vector(1024) index isn't defined in the Drizzle schema and lacks tuned `m`/`ef`, degrading recall/latency at scale; add an explicit HNSW migration (m=16, ef=100) with an EXPLAIN test confirming index usage. _(files: db/migrations/002_user_memories_pgvector.sql, db/schema/user-memories.ts, backend/src/services/user-memory-vector.ts; effort: M; impact: high)_
**94. Pool monitoring & graceful shutdown** — `db/index.ts` creates a pool with hardcoded defaults, no error listener, no drain on exit, and background jobs spawn their own `PrismaClient` risking leaks; add pool error handling, idle/total gauges on `/health`, a `gracefulShutdown()` on SIGTERM, and consolidate jobs onto the shared pool. _(files: db/index.ts, backend/src/services/memory-consolidation-job.ts, backend/src/services/user-memory-vector.ts; effort: M; impact: high)_
**95. Parameterize raw SQL in memory services** — `forgetStaleMemories` interpolates the day interval into SQL and `rawExecWrite` builds vector literals manually, fragile to refactor errors; convert to parameterized queries / Drizzle `sql` tags, bind vectors as driver params, and add a lint rule rejecting SQL string interpolation. _(files: backend/src/services/memory-consolidation-job.ts, backend/src/services/user-memory-vector.ts; effort: M; impact: medium)_
**96. Composite index for consolidation queries** — Consolidation queries filter on `(user_id, importance_score, access_count, last_accessed_at)` which existing single-column indexes can't serve, causing full scans and an O(n²) self-join; add a covering composite index plus a `(user_id, embedding)` HNSW index and verify with EXPLAIN. _(files: db/migrations/002_user_memories_pgvector.sql, backend/src/services/user-memory-vector.ts; effort: S; impact: high)_
**97. Transactional batch memory operations** — `storeMemoriesBatch()` inserts row-by-row and `consolidateMemories()` runs three independent UPDATEs, so a mid-batch failure leaves partial/inconsistent state; use a single multi-row INSERT ON CONFLICT and wrap the promote/merge/demote passes in one transaction with serialization-failure retry. _(files: backend/src/services/user-memory-vector.ts, backend/src/services/memory-consolidation-job.ts; effort: M; impact: medium)_
**98. Resilient memory-consolidation job** — The 30-min job fails an entire run on one DB blip with no retry/backoff or per-user isolation; add exponential backoff on client init, a circuit breaker after N consecutive failures, per-user try/catch so one user's failure doesn't abort the run, and job metrics. _(files: backend/src/services/memory-consolidation-job.ts; effort: M; impact: medium)_

## Security, auth & secrets
**99. CORS credential + wildcard hardening** — Reflecting `*` into `Access-Control-Allow-Origin` alongside credentialed requests lets any attacker domain make authenticated cross-origin reads; reject the wildcard when credentials are present and validate the incoming Origin against the allowlist before reflecting. _(files: lib/next-api-cors.ts, backend/src/middleware/cors-policy.js; effort: S; impact: high)_
**100. JWT revocation list + log forged claims & redact secrets** — Logout/fingerprint-mismatch tokens stay usable during the 50ms DB-dedup window, forged `isSuperAdmin` claims fall back silently to IP bucketing, and Authorization headers leak when body logging is on; add a short-TTL in-memory revocation list checked before DB lookup, log suspicious JWT-claim attempts, and redact token-shaped header values in the request logger. _(files: backend/src/middleware/auth.js, backend/src/middleware/rate-limit-policy.js, backend/src/middleware/request-logger.js; effort: M; impact: high)_

---

## Como usar este plan
- Cada item lleva archivos, esfuerzo (S/M/L) e impacto. Orden sugerido: impacto **high** + esfuerzo **S/M** primero.
- Quick-wins: backend #1-#10 (control del loop ReAct), profesional Testing/CI-CD (estabilizar el `npm ci` flaky de Docker + la migracion fallida P3009), y el gap **cross-doc** del E2E.
- Puedo implementar un subconjunto priorizado cuando lo indiques (UI intacta, CI verde).
