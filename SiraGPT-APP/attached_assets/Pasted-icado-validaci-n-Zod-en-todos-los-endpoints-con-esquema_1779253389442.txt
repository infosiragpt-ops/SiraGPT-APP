icado, validación Zod en todos los endpoints con esquemas en server/schemas/, sanitización de inputs contra XSS y prompt injection con detección heurística, y logging estructurado con pino enviando a Better Stack o Sentry con sampling configurable; integra Cloudflare R2 vía SDK @aws-sdk/client-s3 apuntando al endpoint R2 para mover el almacenamiento de artifacts pesados (Word, Excel, PowerPoint, PDFs de tesis, anexos académicos) fuera del servidor Express, generando presigned URLs con expiración corta que el frontend ya existente consumirá sin cambios mediante los mismos endpoints que ya devuelven URLs (solo cambia internamente el origen del archivo); crea suite de tests con Vitest para unitarios e integración del nuevo gateway y orquestación, Playwright para E2E sobre los flujos críticos (login, chat, upload, generación de documento, download), y @playwright/test visual regression comparando snapshots pixel-perfect de las rutas clave (/, /chat, /chat/[id], /settings, /projects, /projects/[id]) antes y después del cambio debiendo arrojar diff visual cero, fallando el build si detecta cualquier alteración; configura GitHub Actions en .github/workflows/ci.yml con jobs paralelos para lint (eslint + prettier check), typecheck (tsc –noEmit), test-unit (vitest run), test-integration (vitest con DB de test desplegada con testcontainers), test-e2e-playwright, visual-regression-check, build (next build + tsc backend), drizzle-migrations-dry-run, secret-scan (gitleaks), dependency-audit (npm audit –production), y un job final ui-lock-verify que ejecuta scripts/verify-ui-lock.sh comparando hashes contra docs/UI_LOCK_HASHES.txt y falla si detecta cualquier modificación en archivos de UI, asegurando que el pipeline termine en CI green check verificable con gh run list --limit 1 --json status,conclusion mostrando {"status": "completed", "conclusion": "success"}; documenta toda la arquitectura interna en docs/INTERNAL_ARCHITECTURE.md con diagramas Mermaid del flujo de orquestación multi-LLM, capas de fallback, pipeline RAG, ciclo de vida de memoria Mem0 y trazas Langfuse, más un docs/ENV_VARIABLES.md con cada variable, su propósito, formato esperado y proveedor de origen; finalmente ejecuta npm install, aplica las migraciones Drizzle con npm run db:migrate, ejecuta npm run build && npm run start localmente, valida manualmente que cada botón, ruta, animación y flujo de usuario funciona idéntico al estado previo, ejecuta git diff --stat client/ && bash scripts/verify-ui-lock.sh para confirmar cero cambios en frontend con exit code 0, haz commit semántico con mensaje exacto feat(orchestration): integrate LangGraph, Mem0, LlamaIndex, Voyage, Tavily, Langfuse, Upstash, R2 and multi-provider paid LLM gateway preserving UI lock y push a la rama, abre Pull Request titulado [SirAGPT] Internal orchestration upgrade — zero UI changes con descripción detallada del cambio, checklist de verificación visual marcado, listado completo de archivos modificados (todos en server/, db/, docs/, scripts/, .