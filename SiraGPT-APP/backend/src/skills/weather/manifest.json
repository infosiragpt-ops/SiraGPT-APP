{
  "id": "weather",
  "name": "Weather and forecast",
  "version": "1.0.0",
  "description": "Return structured current weather and a short forecast for a city, region, airport code, or coordinates using a bounded public weather request.",
  "capabilities": ["net:outbound"],
  "timeoutMs": 15000,
  "params": {
    "type": "object",
    "properties": {
      "location": { "type": "string", "description": "City, region, airport code, or latitude/longitude." },
      "days": { "type": "integer", "minimum": 1, "maximum": 5, "description": "Forecast days to return. Default 3." },
      "units": { "type": "string", "enum": ["metric", "imperial"], "description": "Temperature and wind units. Default metric." }
    },
    "required": ["location"],
    "additionalProperties": false
  }
}
