# Server Orchestration

This repository's Express backend lives under `backend/src`, so the active orchestration implementation is in:

- `backend/src/orchestration/`

This directory exists as the stable architecture marker requested by the internal upgrade plan. Do not place UI code here.
