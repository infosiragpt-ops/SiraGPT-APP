Aquí tienes el prompt, listo para pegar directo en Claude Code:

---

Investiga y corrige de raíz el problema de "could not renew lock for job" y el subsiguiente "Missing lock for job ... moveToFinished" que afecta al `agent-task-worker` del backend de IliaGPT.io, donde un job de larga duración (taskId `fd46df8c`, ~1134 segundos, 80 steps, terminado por `max_steps`) pierde el lock de BullMQ de forma repetida durante toda su ejecución porque el `lockDuration` y la frecuencia de renovación del lock no están dimensionados para tareas de agente que corren minutos sin ceder el event loop; localiza la configuración del Worker de BullMQ en el backend (probablemente `src/workers/agent-task-worker.ts` o equivalente) y aumenta `lockDuration` a un valor holgado acorde a la duración real de las tareas de agente (por ejemplo 60000–120000 ms) junto con un `stalledInterval` y `maxStalledCount` coherentes, asegurándote de que el ratio entre `lockRenewTime` (por defecto `lockDuration/2`) y la latencia real entre steps del agente garantice la renovación antes de la expiración; identifica además el bloqueo del event loop como causa raíz —los pasos del agente (llamadas LLM, generación de artifacts) que corren de forma síncrona o sin yield impiden que BullMQ ejecute el timer de renovación de lock— y refactoriza el procesador para que las operaciones largas usen `await` real, no bloqueen el hilo, y llamen periódicamente a `job.extendLock()` o emitan progreso vía `job.updateProgress()` durante las fases largas; implementa un manejo robusto del cierre de job que verifique que el lock siga vigente antes de `moveToFinished` y capture el `Missing lock` como condición esperada en jobs que excedieron su tiempo, evitando que se registre como `worker error` ruidoso; añade logging estructurado que distinga entre renovación fallida transitoria (warn) y pérdida definitiva de lock (error) e incluya `taskId`, `attemptsMade` y tiempo transcurrido; introduce un límite de `max_steps` configurable por env var con un fallback más bajo y un mecanismo de checkpoint/persistencia parcial de artifacts para que una tarea cortada por `max_steps` se reporte como `completed` controlado y no deje el worker en estado inconsistente; revisa la concurrencia del Worker y la conexión a Redis (`maxRetriesPerRequest: null`, `enableReadyCheck`) para descartar contención, escribe pruebas que simulen un job que excede `lockDuration` y verifiquen que ahora renueva correctamente sin emitir el error, ejecuta el linter, los tests y el typecheck en local, confirma cero errores, haz commit con un mensaje descriptivo del fix de renovación de lock y abre/actualiza el PR esperando el check verde de CI antes de dar por cerrada la tarea.