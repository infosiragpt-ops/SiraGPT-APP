# apps — SiraGPT product surfaces

Paridad con carpetas `apps/` de plataformas agente (OpenClaw/Hermes layout).

| Upstream `apps/` | SiraGPT |
|---|---|
| Web app | `app/` (Next.js 14) |
| Android | `android/` |
| iOS | `ios/` |
| Browser extension | `extension/` |

No duplicamos UI aquí. Esta carpeta documenta el mapeo. Ver `npm run agent:platform:map`.
