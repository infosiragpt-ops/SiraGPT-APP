Actúa como ingeniero full-stack senior con autonomía total dentro del repositorio del módulo Codex de la plataforma (stack React 19 + Vite en el frontend, Express.js en el backend, PostgreSQL con Drizzle ORM, colas BullMQ, streaming SSE, orquestación multi-modelo vía OpenRouter y sandbox de ejecución por proyecto en sandbox.chatagic.com) y, dado que el sistema ya está funcionando en producción, comienza con una fase de investigación obligatoria en la que exploras el código actual del chat, del agente y del microservicio sandbox, documentas en docs/codex-agent-ux.md el nuevo estándar de experiencia agéntica inspirado en Replit Agent que vas a implementar, y solo después desarrollas todo detrás de un feature flag CODEX_AGENT_V2 sin romper ningún flujo existente. El flujo objetivo completo es el siguiente: el usuario ingresa al módulo Codex, crea un nuevo proyecto que provisiona un directorio de trabajo aislado en la nube dentro del sandbox (workspace por usuario con repositorio git inicializado, instalación de dependencias, servidor de desarrollo y URL de preview accesible), se habilita un chat donde el usuario describe el contexto de lo que desea construir (landing page, página web o sistema web completo), el agente responde primero con el modelo propuesto —un plan estructurado de arquitectura, páginas, componentes y tareas que el usuario aprueba o ajusta, respetando además un toggle "Plan" en el composer que restringe la corrida a solo planificación sin ejecutar nada— y, al ejecutar el build, el chat narra en tiempo real, en primera persona y en español, lo que el agente va haciendo y programando, intercalando párrafos de narrativa con filas de chips de acciones agrupadas (íconos por tipo de herramienta: terminal, lectura de archivos, razonamiento, web) con contador "N actions" expandible al detalle de cada comando y su salida resumida, bloques de razonamiento colapsables con etiqueta y duración en segundos al estilo "Planning database migration verification (47 seconds)", y una píldora flotante "Scroll to latest" cuando el usuario se desplaza hacia arriba durante el streaming. Para soportarlo define un protocolo SSE tipado y persistido (eventos reasoning_start/delta/end, action_start/end, narrative_delta, checkpoint_created, run_summary y action_required) que permita reconstruir el timeline completo al recargar la página, crea las migraciones Drizzle para codex_projects, codex_runs, codex_events, codex_actions, codex_checkpoints y codex_run_metrics, y orquesta cada corrida como job de BullMQ que cronometra la duración real, cuenta las acciones ejecutadas, acumula las líneas leídas, calcula el diffstat con git y registra el usage exacto de tokens y costo que devuelve OpenRouter en cada llamada (campo usage con include true y, cuando aplique, consulta a /api/v1/generation para el costo nativo), de modo que al finalizar cada corrida se rendericen las dos tarjetas colapsables señaladas con flechas en las capturas de referencia: la tarjeta "Checkpoint made X ago", que materializa un commit git real en el workspace con título descriptivo estilo commit, fecha y tres acciones funcionales ("Rollback here" con hard reset confirmado del workspace a ese commit, "Changes" abriendo un visor de diff y "View preview" abriendo la URL viva del sandbox), y la tarjeta "Worked for N minutes" con las métricas reales de la corrida (Time worked, Work done en número de acciones, Items read en líneas, Code changed con +adiciones −eliminaciones y Agent Usage con el costo total mostrando el precio original tachado y el precio final aplicado), e implementa también tarjetas de error accionable encabezadas con "Acción requerida de su parte 🔴" que detecten patrones bloqueantes en los logs (por ejemplo 402 Insufficient credits de OpenRouter, renderizando el error crudo en un bloque de código copiable, la lista de capacidades bloqueadas y el enlace de remediación) junto a una tabla de diagnósticos benignos (como ECONNREFUSED :5050 durante el boot, explicado como arranque normal del frontend antes que el backend). Replica con fidelidad mobile-first el composer de la referencia (placeholder "Make, test, iterate...", botón +, toggle Plan, selector de modo "Power" mapeado a tiers de modelos de OpenRouter, micrófono para dictado y botón de envío) y la barra inferior de pestañas (Preview, Agent, Web, Conexiones, Checklist, Archivos), agrega las claves i18n en español, escribe pruebas unitarias y de integración que cubran el flujo completo crear proyecto → describir contexto → plan propuesto → build con streaming → checkpoint → rollback → preview, además del renderizado de cada tipo de evento SSE y del cálculo correcto de métricas y costos, ejecuta lint, typecheck, build y la suite completa corrigiendo cualquier fallo encontrado, realiza commits convencionales con push al remoto y finaliza únicamente cuando el CI esté en verde.